# v0.30 revision-1 planning — archived

Frozen artifacts of the rebuild's *research phase* (2026-08-09), moved here
when PLAN revision 2 landed. Reference-only; relative links inside these
files may point at paths as they were when written.

- **v0.30.0-m0-m1-gate-{handoff,prompt}.md** — executed → ADR-0001,
  ADR-0002 (P1 INCONCLUSIVE).
- **v0.30.0-m1-rerun-{handoff,prompt}.md** — executed → ADR-0003
  (P1 **FAIL**; option E accepted: full postings).
- **architecture-components.svg / architecture-index-and-refer.svg** —
  the revision-1 design diagrams (MST keyspace, BIC wire, ~250 MB size
  model). Superseded by `docs/compare/index-format.compare.md` (tiered
  JSONL); kept because they document what the measurements changed.

The living plan is `docs/PLAN.md` (revision 2); the live handoff pair is
in `docs/handoff/`.
