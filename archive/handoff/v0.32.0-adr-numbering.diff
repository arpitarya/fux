--- a/CLAUDE.md
+++ b/CLAUDE.md
@@ -157,8 +157,10 @@
 
 4. **One feature → one ADR** in [`docs/adr/`](docs/adr/) (see
    [`TEMPLATE.md`](docs/adr/TEMPLATE.md)): decision, context, alternatives,
-   consequences. **Numbering continues at 0016**; ADRs 0001–0015 belong to the
-   archived engine and are cited by their archive path.
+   consequences. **[`docs/adr/README.md`](docs/adr/README.md) owns the
+   numbering policy** — read it and take the next free number from its table.
+   Do not restate the policy here; a convention written in two places is how
+   the two came to disagree.
 
 **Every rule, ADR, and material decision must carry a reference** — a paper, a
 blog post, or a concrete example link. A rule or ADR with no reference is
@@ -305,7 +307,7 @@
   compare/          closed decisions — debate, matrix, verdict, reopen-trigger
   proposals/        parked ideas — same rigor, status: proposed
   handoff/          handoff + prompt pairs (live work only)
-  adr/              one ADR per feature, numbering continues at 0016 (+ TEMPLATE.md)
+  adr/              one ADR per feature; adr/README.md owns the numbering policy (+ TEMPLATE.md)
   archive/          v0.26-docs/ (frozen), PLAN-v0.26.md, implemented handoffs
   conformance/      measurement evidence & analysis (one folder per run)
 tools/
--- a/docs/adr/README.md
+++ b/docs/adr/README.md
@@ -19,13 +19,16 @@
 | [0011](0011_fux-dir-layout.md) | The `.fux/` directory — declared committed vs derived planes, `.fux/sources/urls`, `.fux/middleware/cdp.py`, opaque `[sources.url.config]`, doctor checks | ⏳ proposed (Arpit ratifies) |
 | [0012](0012_playground-sibling-repo.md) | Demo corpus leaves the engine repo — `examples/` deleted, graded `fux-playground` sibling, file-only committed index, 50 ranked goldens with named `xfail` gaps | accepted |
 
-**Unresolved for Arpit (2026-08-12):** the live `CLAUDE.md` §"How work
-happens here" still says *"Numbering continues at 0016"*, which contradicts
-this file's restart-at-0001 policy and the numbers actually on disk. ADR-0012
-took the next free number under *this* file's policy. The two documents need
-reconciling by whoever owns the convention; `CLAUDE.md` was deliberately not
-edited to match a choice made here.
+**This file is the single source of truth for ADR numbering** (Arpit,
+2026-08-12, closing W-33). `CLAUDE.md` points here rather than restating the
+policy — restating it in two places is how the two came to disagree.
 
+**Taking a number.** Use the next free one in the table above. Note the shape
+of the sequence: **0005–0009 are reserved** for M2–M6 and are already cited by
+name in `PLAN.md` and the `open/` DoDs, while 0010–0012 are taken. So M2's ADR
+is **0005**, filling a hole rather than appending — that is intentional, not a
+mistake to "fix".
+
 Renumbering note (2026-08-09): these files previously carried numbers
 0016–0018 continuing the v0.26 sequence; all live references were rewritten
 in the same change. Frozen artifacts (`PRE-REGISTRATION*.md`, conformance
