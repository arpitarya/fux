> **2026-08-18 — the artefacts this item cited are archived.** The prepared
> `CLAUDE.md` diffs moved to `archive/handoff/` with the whole handoff
> directory, and an archived doc may not ground a live claim. **The decision is
> stated in full below and in [`../OPEN-WORK.md`](../OPEN-WORK.md) Lane B**;
> the diffs may be read and named, not cited, and the live `CLAUDE.md` has moved
> on since they were written.

> **2026-08-18 — the records holding these decisions changed.**
> ADR-URL-MIDDLEWARE and ADR-FUX-DIR were archived with the rest of the v0.30
> set. The same decisions now live in
> [ADR-URL-INGEST](../../docs/adr/0008_url-ingest.md),
> [ADR-DOTFUX](../../docs/adr/0003_fux-directory.md) and
> [ADR-CONFIG](../../docs/adr/0014_config.md), all **accepted**. The questions
> below are unchanged and still yours — only where they are recorded moved.

# W-31 — Ratify ADR-URL-INGEST and ADR-DOTFUX

**Status:** OPEN · **human** (Arpit)
**Blocked by:** —
**Artifacts:** [`../adr/0010_url-source-consumer-fetcher.md`](../../archive/adr/0010_url-source-consumer-fetcher.md)
· [`../adr/0011_fux-dir-layout.md`](../../archive/adr/0011_fux-dir-layout.md)

Both are **built and shipped in the tree** but still `⏳ proposed`. The code
is live; only the ratification is outstanding.

## ADR-URL-INGEST — URL source via consumer-owned fetcher

`fux.toml [sources.url]` names a consumer-editable Python file; `fux ingest
--refresh-urls` is the only networked ingest path. Hashed meta by default;
plain ingest stays offline and carries `url:` records forward
byte-identically. **Core gains no adapter, no network code, no dependency.**
Amended in place by ADR-DOTFUX (paths moved into `.fux/`).

## ADR-DOTFUX — the `.fux/` directory as a declared layout

Every child declared committed (`index/`, `sources/`, `fetchers/`) or
derived (`runtime/`, `cache/`, `CACHEDIR.TAG`-tagged). Self-describing
`.fux/README.md`; a narrow `.fux/.gitignore` that **never uses `*`**; two
`fux doctor` checks.

**Two things needing Arpit specifically:**

1. ADR-DOTFUX §Alternatives closes the original handoff's open question —
   `.fux/README.md` is generated at **ingest** time, not by `doctor --fix`.
   Reversible; flagged because it was decided by the builder, not by him.
2. ~~The one-line CLAUDE.md layout addition is **proposed, not applied**.~~
   **Resolved 2026-08-19.** §Layout was rewritten on 2026-08-18 to describe the
   real `work/` + `docs/` tree, and W-32 closed the same day when Arpit adopted
   the rewrite. There is nothing left to apply; the prepared diff is history.

## Definition of done

*Two of the three were satisfied without Arpit, by other changes. Verified
against the repo 2026-08-19; what is left is smaller than this list looked.*

- [x] ~~Both ADR statuses → **accepted**~~ — **already done, 2026-08-18**, when
      the v0.30 set was archived and its successors took the engine. ADR-DOTFUX,
      ADR-URL-INGEST and ADR-CONFIG all read `status: accepted` on disk. **This
      is the awkward part of the item**: the records were accepted *because a
      record cannot own the engine and be a proposal at once*, not because Arpit
      ratified them. His call arrives after the fact. If he requests a change it
      becomes a new W-item; the built code is not silently left contradicting an
      unratified ADR.
- [x] ~~`adr/README.md` status column updated~~ — **done 2026-08-19.** It had
      been printing ⏳ proposed for eight records whose frontmatter said
      accepted, and contradicting its own prose two paragraphs below.
- [ ] **The `CHANGELOG` item was wrong about where.** The "⏳ proposed"
      qualifiers are **not** in `[Unreleased]` — that section is empty. They are
      in **`[0.32.0] - 2026-08-12`**, a shipped release entry (lines ~108 and
      ~117), and both cite `archive/adr/…` paths — two archived docs backing a
      live claim in a user-facing file. So this is a small decision, not a
      chore: **amend a released entry with a dated note, or leave it as history
      and let the register carry the status?** Amending shipped changelog text
      is the same class as editing a past WORKLOG entry.
