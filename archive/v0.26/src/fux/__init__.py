"""Fux — a portable, agent-aware knowledge engine.

Rules bound to code, read by agents before they touch anything, checked
deterministically (never by a model). $0, stdlib-only runtime. See CLAUDE.md.
"""

__version__ = "0.26.0"
