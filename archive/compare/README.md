# `archive/compare/` — forks that are closed AND cannot reopen

**How to use this file.** A compare doc lives in
[`work/compare/`](../../work/compare/README.md) while its fork is live — and a
*decided* fork is still live there, because the doc carries the verdict **and
the reopen-trigger** that would bring it back. A doc moves here only when
**both** are spent: the fork is closed, and the trigger can no longer fire.

**Archive is not evidence.** These docs may be *named* — "the wire format was
decided in `wire-format.compare.md`" — but never cited as backing a live claim.
Repoint any live citation at the record that carries the decision now. See
[`../README.md`](../README.md) §Archive is not evidence.

## Moving a doc here

In one change: `git mv`, add a row below saying **why the trigger cannot fire**,
delete its row from `work/compare/README.md`, and bump the DOC-REGISTRY. A doc
moved without that sentence is indistinguishable from one filed by mistake.

🔴 **There is a SECOND permitted form of that sentence, added 2026-09-20**
(Arpit's default under [W-206](../../work/open/W-206-compare-and-proposals-sweep.md)):
**"the trigger lives in `<record>` `<decision>`."** A fork whose verdict *and*
reopen-trigger have both moved into the owning record has nothing left in the
compare doc that a reader needs — the trigger is still checkable, it is simply
checkable somewhere better. ⚠ **Without this form, nine decided forks would stay
live forever** precisely because their triggers are checkable, which is not what
*live fork* means.

⚠ **The two forms are not interchangeable.** *Why it cannot fire* closes the
question; *the trigger lives in `<record>`* moves it. A row using the second form
**must name the record and the decision**, and the port must already be there —
[SR-WORK-ARCHIVE](../../records/0062_WORK-archive.md) decision 5 forbids an
archived doc backing a live claim, so a trigger left only here would be a rule
nobody may cite.

| doc | closed | why the trigger can no longer fire |
|---|---|---|
| [`cache-policy.compare.md`](cache-policy.compare.md) | 2026-09-20 | **The trigger lives in [SR-CACHE](../../records/0131_cache.md) veto 6** — *a measured hit-rate on real Fux workloads shows ARC no better than LRU*, with the post-hoc synthetic result explicitly not counting. The verdict (ARC over LRU) and that condition are both in the record, **verbatim**; nothing was ported in the move |
| [`refer-fetch-cache.compare.md`](refer-fetch-cache.compare.md) | 2026-09-20 | **The trigger lives in [SR-CACHE](../../records/0131_cache.md) veto 7** — *a real workflow shows the ACL-staleness window produced a wrong-access answer*, at which point `no_cache` stops being advisory and becomes mandatory at ingest. ⚠ **Its own status line read *"record-freshness ⏳ awaiting Arpit"*** and had been stale since `record-freshness`'s premise died on 2026-08-25; that doc archived in the same change |
| [`copilot-skill-surface.compare.md`](copilot-skill-surface.compare.md) | 2026-09-20 | **The trigger lives in [SR-AGENT-POLICY](../../records/0132_agent-policy.md) decision 16** — an **observed** duplicate-name error, which is what the fork turned on and what remains unmeasured. Decisions 14–16 carry the verdict: Copilot gets `.github/skills/`, option A rather than C, and Codex and Copilot share `.agents/skills/`. ⚠ **Arpit overruled the doc's own recommendation** to defer — the `install = ["copilot"]` hole outweighed an unmeasured risk whose worst case is a hard error, not divergent instructions |
| [`cross-encoder-reopen.compare.md`](cross-encoder-reopen.compare.md) | 2026-09-20 | **The trigger lives in [SR-RERANK](../../records/0138_rerank.md) veto 1**, and it is two measurements rather than an argument: score-level drift below the target corpus's **adjacent-gap floor**, *and* value measured on that corpus's own goldens. 🔴 **The bar moved once, in the strict direction, and the record carries the correction** — a ranking changes when drift exceeds the gap to the next document, not when it exceeds `round(score, 9)`; measured median adjacent top-5 gap `0.27` against observed drift `1.907e-06`. ⚠ **The doc's §6 recommendation was refused**, and the record holds no position on the cross-encoder's value: whoever reopens supplies the number |
| [`file-type-filter.compare.md`](file-type-filter.compare.md) | 2026-09-20 | **The trigger lives in [SR-TYPES](../../records/0128_types-list.md) reopen 1** — *a consumer needs different types for different roots*, whose migration is additive (a `types=` attribute on a `dirs` line). ⚠ **The mechanism this doc argued over was superseded anyway**: the types list left the line grammar for `.fux/formats.toml` on 2026-09-11 (decision 12), so the fork is closed twice over |
| [`index-lock.compare.md`](index-lock.compare.md) | 2026-09-20 | **The trigger lives in [SR-LOCKS](../../records/0140_locks.md) veto 4** — *a consumer runs `fux` against a repo on a network filesystem*, where `O_EXCL` is documented as racy and the pid-file mutex stops being sound. 🔴 **It was ABSENT from the record until 2026-09-20 and was ported in the change that moved this file** (W-206 A2): archiving first would have deleted the only written statement of it. It is checkable by asking where a pilot's repo lives, not by grepping `src/`, which is why the record carries no capture for it |
| [`maintenance-trigger.compare.md`](maintenance-trigger.compare.md) | 2026-09-20 | **The trigger lives in [SR-MAINTENANCE](../../records/0129_hooks.md) veto 10** — *a failed re-index leaves a half-written committed shard*. 🔴 **Ported in the change that moved this file** (W-206 A2); it was the one of the doc's three conditions the record did not already carry. **R5's half is SPENT** (fired 2026-08-20, answered by the deferring hook) and R6's half is the merge harness. ⚠ **The doc cites the deferral as "decision 1d"; the record numbers it 1a**, and 1a is the one it means. Its remaining half is absorbed by [`hook-at-scale`](../../work/compare/hook-at-scale.compare.md), which stays live |
| [`path-hops-bound.compare.md`](path-hops-bound.compare.md) | 2026-09-20 | **The trigger lives in [SR-GRAPH](../../records/0126_graph.md) decision 17**, which carries all four conditions alongside the verdict — option (c), Arpit 2026-09-14: `routes()` takes an expansion `budget` and returns `(routes, truncated)`, so a cut-short search **says so**. ⚠ **The doc still reads *"not yet implemented"* and it has been implemented since 2026-09-14** — that sentence is why a reader should go to the record rather than here |
| [`table-tokens-in-flen.compare.md`](table-tokens-in-flen.compare.md) | 2026-09-20 | **The trigger lives in [SR-RANKING](../../records/0111_ranking.md) decision 3** and [SR-TUNE](../../records/0135_tuning.md)'s `b` row. Ruled by measurement, not by the doc's own recommendation: **W-144 PASS on 2026-09-16 at `b = 0.15`** — the first value in the frozen descending order that nets positive on **both** benefit families with all four controls holding. 🔴 **The two benefit families cross at different values**, which is why a rule asking for *either* would have shipped `0.3` and left half the mechanism unmeasured behind a passing verdict. ⚠ **The measured result is in the record, never here**; this doc holds the three options as they were put |
| [`graph-plane-format.compare.md`](graph-plane-format.compare.md) | 2026-09-20 | **Neither half can fire.** Half 1 is a **50 000-document** target, and [SR-WORK-SCALE](../../records/0057_WORK-scale.md) makes 10 000 a ceiling on what may be measured *or promised* — so the condition cannot be evaluated without breaking a law. Half 2 is *a real 10k corpus takes > 1 s*, and **there is no such corpus**: the golden ladder's top rung is generated, and the one real corpus is this repository at ~400 documents. Live successor: [SR-GRAPH](../../records/0126_graph.md) decision 8 |
| [`index-format.compare.md`](index-format.compare.md) | 2026-09-20 | **All three halves are spent.** T2 was **declined on measurement** — [R9](../../work/regression/2026-08-22-r9-t2-at-10k/VERDICT.md) answered worst-case queries at **12.46 ms against a 150 ms bar**, 12× inside it — and nothing was ever built. The 200 000-document half is above the measurement ceiling. The Python-version half is [SR-LAW-3](../../records/0005_LAW-3-deterministic.md)'s subject, not a format fork's. Live successors: [SR-RECORD](../../records/0109_index-record.md) · [SR-POSTINGS](../../records/0112_postings.md) · SR-LAW-3. ⚠ **`docs/GLOSSARY.md` cited this doc for tier T1 and was repointed at SR-POSTINGS in the same change** |
| [`record-freshness.compare.md`](record-freshness.compare.md) | 2026-09-20 | **The premise died on 2026-08-25.** The fork rested on *the committed record carries no ingest time*, and **`mtime` is a committed field** ([SR-RECORD](../../records/0109_index-record.md)) that `[ranking] recency_half_life_days` already reads. A trigger whose precondition is false cannot fire. 🔴 **The dead premise had propagated** — `refer/freshness.py`'s docstring and SR-URL-FRESHNESS's §Alternatives were both still reciting it in 2026-09, although [SR-REFER](../../records/0127_refer-plane.md) decision 4 had vacated it and said it may not be cited; **both were corrected in the change that moved this file**. Live successors: SR-REFER decision 4, which carries its own reopen, and [SR-URL-FRESHNESS](../../records/0147_url-freshness.md) |
| [`record-shape-migration.compare.md`](record-shape-migration.compare.md) | 2026-09-20 | **It has no reopen-trigger at all**, and it could not have a usable one: a **landed migration cannot be re-done**, and the shape it argued over has moved twice since — `fux.index` is at **v4** (W-194, 2026-09-20), which refuses a v3 index by name and has no in-place migration. Live successors: [SR-RECORD](../../records/0109_index-record.md) and [SR-TUNE](../../records/0135_tuning.md) decision 6 |
| [`source-exclusion.compare.md`](source-exclusion.compare.md) | 2026-09-20 | **The verdict lost to the option it beat.** Verdict E — a `!` entry in the source list — is the **deprecated spelling**; option B won in practice through [SR-FUXIGNORE](../../records/0144_fuxignore.md), whose grammar scopes by path and whose `.fuxignore` is where exclusions live. `fux doctor`'s `dirs exclusions migrated` row reports a repo that still has `!` lines. A trigger that would restore the deprecated spelling is not a trigger. Live successors: SR-FUXIGNORE · [SR-DIR-LIST](../../records/0120_dir-list.md) decision 2b |
| [`storage-architecture.compare.md`](storage-architecture.compare.md) | 2026-09-20 | **Trigger 1 fired and was ABSORBED** — the pruning question it named was answered by [P1-RERUN](../../work/regression/2026-08-09-pruning-rerun/VERDICT.md), a 35.9-point recall loss, and full postings shipped permanently. **Trigger 2 says *"even hashed"*** and hashed display meta was **deleted** on 2026-09-20 with law L5 retired (W-194), so its precondition no longer exists. **Trigger 3 is unmeasurable** on any corpus this project may promise against. Live successors: [SR-REFER](../../records/0127_refer-plane.md) · [SR-LAW-2](../../records/0004_LAW-2-content-never-durable.md) · [SR-POSTINGS](../../records/0112_postings.md). ⚠ **`docs/GLOSSARY.md` cited this doc twice and was repointed in the same change** |
| [`meta-privacy.compare.md`](meta-privacy.compare.md) | 2026-09-20 | **Every trigger presupposes something that was deleted.** Each one is about hashed meta or the display cache, and **W-194 deleted both on 2026-09-20** — `meta`, `title_h`, `--plain`/`--hashed` and `store/displaycache.py` are gone, and law **L5 is retired**. ⚠ **The doc's 2026-08-25 note *"both halves are built"* is FALSE and is left standing as history** rather than edited: the second half was never built, and the first was removed. 🔴 **The exposure this fork was about is now an ACCEPTED, DOCUMENTED one, not a solved problem** — every URL record commits a readable `title` and `phrases`. Live successor: [SR-LAW-5](../../records/0007_LAW-5-hashed-meta.md), kept at `status: superseded` with the argument, the AOL-2006 citation and the reopen trigger |
| [`ingest-mode-naming.compare.md`](ingest-mode-naming.compare.md) | 2026-09-12 | Verdict `extracted` (no model) / `enriched` (model) was ratified by Arpit 2026-08-19, closing W-30, and the doc's own **Reopen-trigger section reads *"None — a naming call, closed permanently"***. The one residual condition in its verdict block — *a `mode` value other than these two in a committed record* — cannot fire **from this fork**: `src/fux/ingest/run.py` writes `mode="extracted"` and nothing else, `inferred` is retired repo-wide, and a third mode value would be a new decision needing its own record, not a re-run of this comparison. ⚠ It also survived internally contradictory — a verdict block reading **DECIDED** above a stale *"Status: ⏳ proposed"* line, and a pointer to `../adr/0001_ingest-mode-naming.md`, a path that has not existed since 2026-08-18. **Its matrix was folded verbatim into [ADR-EXTRACTED](../../docs/adr/0115_extracted-mode.md) §2 before the move**, because that record grounded a decision in it; the live decision is there now |
| [`wire-format.compare.md`](wire-format.compare.md) | 2026-08-25 | Decided 2026-08-09 and then **superseded the same day** by `index-format` for the committed plane. Its chosen artifact — the BIC wire format — **was never built**, and the doc survived only on "survives inside tier T2". **T2 was measured and declined**: [R9-T2-AT-10K](../../work/regression/2026-08-22-r9-t2-at-10k/VERDICT.md) PASS at 12.46 ms against a 150 ms bar, 12x inside it. The compact encoding it describes lives on as a live proposal inside [ADR-POSTINGS](../../docs/adr/0013_postings.md), which is where a reader should go |
| [`keyspace-unification.compare.md`](keyspace-unification.compare.md) | 2026-08-25 | Verdict "one MST keyspace", **superseded the same day (2026-08-09)** — git itself provides the Merkle tree. No MST exists anywhere in `src/`. Its reopen-trigger is *"T2 needs a content-defined chunk store"*, and **T2 was declined on measurement**, so the trigger's precondition can never arrive |
| [`pruning-criterion.compare.md`](pruning-criterion.compare.md) | 2026-08-25 | `status: rejected`. The prediction was **falsified** by P1-RERUN, and option E — full postings, permanently — shipped. **Reopening is not merely unlikely, it is forbidden**: [ADR-POSTINGS](../../docs/adr/0013_postings.md) decision 8 states *"Postings are never pruned"* and bars pruning work outside a dedicated item; that item was W-38, **dropped on Arpit's instruction** 2026-08-22. `CLAUDE.md`: *"the pruned-index design is dead, not deferred"* |
| [`r7-size-budget.compare.md`](r7-size-budget.compare.md) | 2026-08-25 | `status: retired`. It forked over what **shape** R7's committed-size budget should have — and **R7 itself was retired with no successor** by Arpit on 2026-08-22, so there is no budget to shape. Recorded in [ADR-POSTINGS](../../docs/adr/0013_postings.md) and [ADR-INDEX-LIFECYCLE](../../docs/adr/0009_index-lifecycle.md). The size checks survive as prints, not gates — the ruled outcome. ⚠ Its README row still read *"⏳ awaiting Arpit"* for three days after the retirement |
