"""The derived query accelerator — M2. Empty stub in M1: no term-major files,
no `mx` blocks, no offset tables. `fux ask` in M1 is a scan over `store/`
(see `../query/`); nothing under `.fux/derive/` exists yet, and nothing here
should be built ahead of that milestone (scope fence, M1 handoff §3).
"""

from __future__ import annotations
