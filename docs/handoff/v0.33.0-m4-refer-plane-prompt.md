---
type: Prompt
title: "v0.33.0 — Claude Code prompt for M4, the refer plane"
description: Paste-ready prompt executing docs/handoff/v0.33.0-m4-refer-plane-handoff.md. Opus for the API shape and the ARC differential; a named Sonnet window for the adapters and the bench.
status: proposed
timestamp: 2026-08-12T00:00:00Z
---

# Prompt — v0.33.0: M4, the refer plane

**Model: Opus**, with one named Sonnet window (the adapters + the mock bench,
§C). State the model when handing this over — the wrong model here does not
error, it returns confident, plausible, wrong work.

**Do not paste this until ADR-0005 is ratified** (or Arpit explicitly says to
proceed without it). M2's shipped code sits under an unratified ADR, and M4
builds directly on its differential law.

---

```
You are working in the fux repo (~/my_programs/fux), the v0.30 index-and-refer rebuild.

READ FIRST, IN THIS ORDER — do not write anything until you have:
  1. CLAUDE.md — INCLUDING the "Triage first" rule at the top. If every open item
     is blocked on Arpit, your FIRST output is the blocked list in <=3 lines, then stop.
  2. docs/INTERVIEW.md — the reset block and BOTH 2026-08-12 entries. The second one
     tells you that a green safety test can be measuring the corpus instead of the code.
  3. docs/handoff/v0.33.0-m4-refer-plane-handoff.md   <- YOUR CONTRACT
  4. docs/adr/0005-derived-accelerator.md — the differential law is DEFINED there
     and M4 inherits it wholesale.
  5. docs/OPEN-WORK.md + docs/open/W-24-m4-refer-plane.md
  6. The two graduating proposals, in full:
       docs/proposals/caller-set-freshness-policy.md
       docs/proposals/token-budget-retrieval.md
     ...and docs/proposals/agent-search-landscape.md — the "Benchmark caution"
     section specifically, BEFORE you import any framing or metric from it.

Then EXPLORE before planning. Report back, in your own words and before any edit:
  - what M2 actually shipped, and what "the differential law" concretely requires of you
  - why `answer` today returns index structure rather than passages, and what changes
  - which of the two proposals' open questions you think are genuinely Arpit's
  - anything in the handoff you believe is mistaken. Say so now, not later.

THIS IS THE MILESTONE THAT CAN BREAK THE ARCHITECTURE'S CENTRAL LAW.
Content is never durable outside its source system. The ARC cache holds fetched
bytes in a DERIVED, gitignored, bounded plane — it is a cache, not a copy. If you
find yourself persisting fetched content anywhere but .fux/cache/, STOP and ask.

BINDING CONSTRAINTS (handoff §2 — read it, this is the summary):
  - $0, stdlib only, deterministic, offline by default. No new dependency, ever.
  - Network ONLY in fenced, opt-in paths. The import-fence test must stay green.
    `fux ask` must still answer with the network unplugged.
  - The adapter cap is HTTP + Confluence. MCP is a proposal, not a backlog item.
  - Age is measured against RECORDED PROVENANCE, never wall clock at query time.
  - Bytes, not tokens, in the engine. A per-model tokenizer is a dependency.
  - R4 is pre-registered and MAY NOT MOVE. A knob that fails to move the
    index-vs-fetch mix is a knob that gets DROPPED — that is a successful
    outcome, not a failed task. Same for a flat quality-per-byte curve.
  - Docs move with the work IN THE SAME CHANGE. A closed item's OPEN-WORK row
    and its docs/open/ file are deleted TOGETHER.
  - Conformance evidence goes in docs/conformance/<date>-<run>/ with the raw
    dumps DOT-PREFIXED (.evidence/) — filing them un-prefixed contaminates the
    corpus fux indexes. See W-45 and the 2026-08-12 R2 run's Finding 3.

WRITE THE ARC DIFFERENTIAL HARNESS BEFORE THE CACHE. Then — and this is the part
M2 learned the hard way — PROVE THE HARNESS CAN FAIL. Return a deliberately wrong
cached body, or disable eviction, and confirm it goes red. M2's differential was
green while the block bound was replaced by a CONSTANT ZERO, because the default
`top` never exercised it. A harness that cannot fail is not a test, and you will
not discover that by looking at it.

PLAN before you build. Show me the plan and wait: the fetch interface both
adapters sit behind, the cache key and its bounds, how the freshness policy is
recorded in the answer bundle, how the assembler's greedy selection breaks ties
deterministically, and how the ARC differential gets its cases. I want to see
the design before the code.

DEFINITION OF DONE: handoff §5, in full. The headline items:
  [ ] R4: cold k=10 <= 3s, warm <= 300ms on a MOCK server (never the real internet)
  [ ] R4 + freshness sweep: three max_age settings must move the fetch mix
  [ ] R4 + budget sweep: report answer quality PER BYTE, not just latency
  [ ] ARC differential green, with the harness proven able to fail
  [ ] offline degradation honest — declared staleness, never fabricated freshness
  [ ] ADR-0007 written with real references, accepted
  [ ] BOTH proposals moved to docs/archive/ stamped implemented + ADR link
  [ ] OPEN-WORK row + docs/open/W-24-*.md DELETED; WORKLOG; DOC-REGISTRY; CHANGELOG

TWO QUESTIONS ARE ARPIT'S, NOT YOURS. Present both, recommend, do not decide:
  1. Does a fetch that times out FAIL the query or DEGRADE it? (degrade for
     agents, fail for --audit — so the answer is "policy, per caller")
  2. Does the budget bound the CITATIONS or the WHOLE RENDERED ANSWER?

THEN STOP. Report R4 with its sweeps, the ARC differential result (including
the proof it can fail), and both open questions. Then WRITE — do not build —
the M3 handoff + prompt pair.
```

---

## §C — the Sonnet window

Once the design in the plan above is fixed and reviewed, the **HTTP and
Confluence adapters plus the mock-server bench** are implementation against a
written definition of done and can be handed to Sonnet:

```
Implement docs/handoff/v0.33.0-m4-refer-plane-handoff.md §3.1 and the R4 mock
bench ONLY, against the design already agreed in this session's plan.

  - Conditional GET (ETag / If-Modified-Since); 304 is a first-class result.
  - Redirect and timeout bounds; no unbounded waits anywhere.
  - Confluence token from the ENVIRONMENT, never from config, never logged.
  - Both adapters behind the single internal fetch interface — the assembler
    must not be able to tell which system answered.
  - Every test uses a mock server. A test that reaches the real internet is
    non-deterministic and will be deleted.
  - The import-fence test must stay green.

Do NOT touch the ARC cache, the assembler, or the answer contract — those are
the Opus window's, already designed. If the adapters seem to need a change
there, stop and say so rather than making it.
```

---

## §D — after M4

Per the v0.32.0 handoff §5, the chain continues **M3 → M5 → M6 → M7**, each
entered only through its own handoff + prompt pair, written at the end of the
preceding phase. M3 is a port against an existing eval and has not got harder
for waiting.

W-38 (M8) stays parked: one ADR + Arpit's sign-off, each.
