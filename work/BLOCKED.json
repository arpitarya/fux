{"decision":"ASK","reason":"main is RED: 48 tests fail. Merged on Arpit's explicit repeated instruction with the regression surfaced first and named in the commit, the records and OPEN-WORK. Nothing is hidden and nothing is pushed.","questions":["RULE THE OUTPUT.TOML FORK -- this is the urgent one. ADR-OUTPUT decision 19 makes a missing `.fux/output.toml` a HARD ERROR; the file is write-if-missing (ADR-DOTFUX decision 6) so it reaches NEW repos only; therefore every pre-existing repo hard-fails on `fux ask` / `fux find` with exit 1 after upgrading. Two options: (1) a MISSING file falls back to engine defaults, keeping the hard error only for a file that exists and is incomplete -- decision 19's own wording, 'once it is in effect', arguably already means this and it is a one-line change; (2) decision 19 stands and `fux setup` gains a migration path reaching existing repos, which decision 6 forbids doing by rewrite.","THE ENGINE ABSTAINED 0 TIMES OUT OF 20 on blind-authored unanswerable questions. ADR-QUALITY decision 5 puts that class INSIDE the gate, so the recall@k headline describes the ANSWERABLE HALF ONLY. No threshold proposed and R10 untouched, deliberately.","THE 7 `partial` GOLDENS are the two blind annotators' exact-set disagreements, held out of recall@k's denominator. They need a human or a THIRD blind reader -- any session that has seen the scores is the wrong party."],"safe_alternative":"`git reset --hard 7743649` restores the last green state on main; nothing is pushed, so the red never left this machine. All of the 2026-08-28 quality-contract work (recall@k, decision 12, the four measured runs) sits INSIDE that green range and would survive the reset.","surfaced":true,"filed":"2026-08-28","rederived":"2026-08-28 (Claude Code, local macOS shell: main at 7a72ff9, working tree clean, 48 failed / 2365 passed / 2 skipped. Verified the pre-merge tree green at 7743649 in a separate worktree: 2391 passed / 3 skipped.)"}
