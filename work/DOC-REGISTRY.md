---
type: Registry
description: "Per-doc freshness table for live docs: update triggers and last-verified dates."
---

# DOC-REGISTRY — the freshness tracker for **live** documents

**How to use this file.** One row per maintained doc: what triggers an update
to it, and when it was last verified true. **Bump the row in the same change
that touches the doc** — the row's date is the only thing that makes staleness
visible at a glance. A row older than ~30 days is a review prompt, not a
failure.

### The rule: this registry lists live documents only

**An archived document loses its row in the same change that archives it.**
Not struck through, not annotated "retired" — **deleted**, the same discipline
[`OPEN-WORK.md`](OPEN-WORK.md) applies to closed items, and for the same
reason: the file's length should mean something. A registry padded with
tombstones stops answering *"what do I have to keep true?"*, which is the only
question it exists to answer.

Three consequences, all mechanical:

1. **No row may point into [`../archive/`](../archive/README.md).** An archived
   doc is frozen and never updates, so a trigger and a last-verified date are
   meaningless on it. Where it went is the archive map's job, not this file's.
2. **Every row's target must exist.** A row for a deleted file is the same
   defect wearing different clothes.
3. **One row per document.** Two rows means two last-verified dates for one
   file, which is the confusion this file exists to prevent.

Enforced by [`../tests/test_doc_registry.py`](../tests/test_doc_registry.py).

**The archive map is deliberately not listed here.** It lives under `archive/`,
so rule 1 excludes it — and it does not need a row: the archive law in
`CLAUDE.md` and `tests/test_archive_law.py` already require it to exist and to
name a successor for everything in it.

**Contract:** every maintained doc has an update **trigger** and a
**last-verified** date. A task that fires a trigger updates the doc *and* its
row, in the same change. Docs large enough to carry §humans + §agents sections
update both.

| Document | Update trigger | Last verified | Notes |
|----------|---------------|---------------|-------|
| [`../CLAUDE.md`](../CLAUDE.md) | Scope, constraints, lifecycle, layout changes | 2026-09-15 | **L11 — the sealed answer key is a law.** **2026-09-15 (L11):** the sealed-key prohibition became law `L11` ([SR-LAW-11](../records/0012_LAW-11-sealed-answer-key.md)); SR-WORK-GOLDEN now carries only the process, and every law count, range and `L0..L11` diagram moved with it. · **2026-09-15 (W-146 row 17 — CLOSED):** §Golden answer key became the file's **second generated block** — [SR-WORK-GOLDEN](../records/0066_WORK-golden.md) states the prohibition, [`scripts/gen-golden.py`](../scripts/gen-golden.py) renders it and [`tests/test_claude_md_golden.py`](../tests/test_claude_md_golden.py) binds it. 🔴 **Both options row 17 offered were wrong** — moving the paragraph deletes Cowork's only cover, leaving it keeps two copies that can disagree — so Arpit ruled SR-LAW-0 decision 5's third shape. §Where the state of play gains a row and the WORK range reads `0051`–`0066`. **2026-09-14 (W-173 — the extraction FINISHED):** eight more sections folded into the records that state them — §Conformance runs + §A pre-registered threshold into **SR-RS 10a/10b**, §Law zero into **SR-LAW-0 1a** + **SR-WORK-OWNERSHIP 2a**, §The SR standing rules into the register, §Layout into `docs/index.md`, §Hard-won into the new [`work/LESSONS.md`](LESSONS.md) (a LOG, because a record carries no history), §Build & test's gotchas into [`MACHINE.md`](MACHINE.md), §Error contract into **SR-CLI 4/5**, §Triage first into **SR-WORK-OPEN-QUEUE 32-34/39-45**. **668 to 437 lines.** ⚠ **437, not the item's ~330** — the estimate did not count the eight pointer stubs it created. 🔴 **Three rules had NO record home and folding is what found them**: the two-strikes rule (now SR-WORK-SESSION 13, four citations repointed), SR-RS's explicit deferral to this file, and two of the register's own six standing rules. 🔴 **And §Error contract CONTRADICTED SR-CLI** about exit `2`. **2026-09-14 (the extraction):** eight sections became pointers — §Litmus, §How work happens, §Follow the OKF pattern, §Documentation (style + discipline + keep-in-sync merged), §Session continuity (three-file + hazards + answer length + markers merged), §Archive is not evidence, §Package identity and release (+ merge wall), §Blockers — each now naming the WORK record that states the rule and the decisions to read. **1059 lines to 668.** §Where the state of play lives gains nine index rows. Nothing was deleted that is not stated in `0057`–`0064`.  **2026-09-13 (2.0.0):** §Build & test and §Package identity name **`2.0.0`**, and the version lineage gains the promotion of the alpha line.  **2026-09-13 (the renumber's other half):** the numbering paragraph corrected — it claimed `0012`–`0100` reserved and empty; there are **three** ranges and the WORK range holds `0051`–`0056`.  **2026-09-13 (SR-WORK-BENCHMARK):** what a benchmark run always captures is stated once in [`records/0053_WORK-benchmark.md`](../records/0053_WORK-benchmark.md) — CAP-1…CAP-7, enforced by `tests/test_benchmark_capture.py` from 2026-09-13. `SR-WORK-ENVIRONMENTS` no longer states what a benchmark measures; its no-answer-key clause is reversed. **2026-09-13:** L9 retired — the environment rule is now [SR-WORK-ENVIRONMENTS](../records/0052_WORK-environments.md) (`0052`, `kind: process`), not a law. The handle and `records/0011` are never reused.  **2026-09-13 (SR-WORK-OPEN-QUEUE):** §OPEN-WORK is a POINTER — the queue's rules are stated only in [`records/0051_WORK-open-queue.md`](../records/0051_WORK-open-queue.md).  **2026-09-13 (SR rename):** `ADR-<NAME>` became `SR-<NAME>` and `docs/adr/` moved to `records/` at the repo root — one mechanical change, every reference repointed. Archived `ADR-NNNN` citations and `archive/v0.26-docs/adr/` are unchanged.  **2026-09-13:** §OPEN-WORK became a pointer — the ball legend and every other queue rule are stated only in [SR-WORK-OPEN-QUEUE](../records/0051_WORK-open-queue.md).  **2026-09-12 (W-149):** §Build & test gains the bundler and the `node --test node/test/*.test.mjs` invocation (the bare directory form is `MODULE_NOT_FOUND`, not a failure); §Package identity says the bundle is a DERIVATION checked by `--with-bundle`, not a fifth version site. **2026-09-12 (npm publication):** §Package identity's *"single source"* claim CORRECTED — three more version strings live in `node/`, now gated by `scripts/check-version-parity.py`; §Build & test now says released on npm as well as PyPI, and that npm *stages* rather than publishing automatically. **2026-09-12 (W-122 — §Non-negotiable constraints is now GENERATED):** the ten law texts moved into the `SR-LAW-n` records; the section is rendered from them by `scripts/gen-laws.py` and held byte-equal by `tests/test_claude_md_laws.py`. ⚠ **Editing that block by hand is now a test failure** — amend the record and regenerate. §SR standing rules repointed: each law's home is its own record. ⚠ Said out loud: a change to this file, on Arpit's ruling of 2026-09-06. **2026-09-12 (positioning):** §OKF — **the ALL-CAPS `type` exemption is retired** (the spec has no such rule; 94 of 314 files were failing a bar claimed in prose), tracker `type` names listed, and the section now names `tests/test_okf_bundle.py` as what enforces it. **2026-09-11 (balls primary):** §OPEN-WORK point 8. **2026-09-11 (marks final):** §OPEN-WORK point 8 + triage line → 🧨/🔺. **2026-09-11 (blocks):** inbox bullet names the `↳ blocks:` sub-row. **2026-09-11 (balls):** §OPEN-WORK point 8 + triage line name the balls and ⏫. **2026-09-11 (marks):** §OPEN-WORK point 8 names the row marks. **2026-09-11 (rule 10):** §OPEN-WORK point 8 — one or two lines per item; `filed` lives in the item file. **2026-09-11 (SR-WORK-ENVIRONMENTS):** §Non-negotiable constraints gains **SR-WORK-ENVIRONMENTS** — each sibling environment has one job; SR-LAWS' table in the same change. ⚠ A law change, said out loud, on Arpit's ruling. **2026-09-11 (W-137):** §lifecycle point 4 states the **two live ranges** — `0001`–`0100` laws (`0011`–`0100` reserved, empty) and `0101`– everything else, today `0101` SR-CLI to `0154` SR-DOCTOR. ⚠ Said out loud: a fact edit, recording Arpit's ruling. **2026-09-11 (W-136):** new §Golden answer key — Claude never reads `work/golden/golden-answer/`; Layout gains `golden/`. ⚠ Said out loud: records Arpit's ruling. **2026-09-11 (rule 9):** §OPEN-WORK gains item 7 — no git housekeeping in the queue, pointing at OPEN-WORK rule 9. ⚠ Said out loud: records Arpit's ruling. **2026-09-11 (W-129):** *Build & test* gains one line — a hand-built test repo writes `.fux/pii.toml` (SR-PII decision 17). **2026-09-06 (L0): §Non-negotiable constraints gains `L0` and the header goes `L1–L8` → `L1–L0`.** SRs are the only source of truth and the Law records outrank every other record; **this section becomes NON-NORMATIVE** once W-122 lands, replaced by a block generated from the nine records and bound by a test. The bullet names the describes/implements/enforces test, and says out loud that precedence is judgment and that a record self-contradicting inside one file stays ungated. ⚠ Said out loud per §Documentation discipline: this edit records Arpit's ruling, it does not grant the session latitude. **2026-09-01: version → `2.0.0-alpha.5`** in both the build section and the package-identity history line. **2026-08-27 (later): SR-ENRICHED's citation repointed to SR-ENRICH** in §What we are building — the record was superseded and `git rm`'d the same day, and the W-38/M8 gate reference went with it. **The mode is still NOT authorized to be built**; `fux enrich` is a different feature. **2026-08-27 (W-91): `L8` REWRITTEN in §Non-negotiable constraints**, together with SR-LAWS' table per SR-LAWS decision 4. Arpit reverted the law the day it was written; the section now carries the ⚠ pointer to decision 8 for what the reversal trades away. **2026-08-27 (Arpit, W-89): §Non-negotiable constraints gains `L8` — the first law about *use* rather than about the corpus**, and the section header goes L1–L7 → **L1–L8**. *What fux retains about use is hashed, bounded, and local.* Landed in the same change as [SR-LAWS](../records/0001_LAWS.md)'s table row and decision 8, as SR-LAWS decision 4 requires. ⚠ **Named out loud, as §Documentation discipline demands** — the session edited the file it is judged by, on Arpit's explicit instruction, to record his ruling. The gap: L2 governs *corpus content* and a query is not content; the trigger: a durable use record **already exists** (`maintain/lastcited.py`) and [`proposals/ranking-tuning.md`](proposals/ranking-tuning.md) §8 calls a per-repo query log *"an asset fux gets for free"*. **L8 forbids nothing fux does today** — verified against the code before the text was written. **2026-08-25 (Arpit, W-78 ruling 2): §Conformance runs gains the RUN-CLASSIFICATION rule.** Every *measured* run is `blind` or `informed` and says which; an informed run is **reclassified, never banned** and never supplies a delta (TREC's manual/automatic split, since 1994); the run names who authored each artifact and what evaluation material they could reach; a delta below the set's resolution is **no detected change** (provisionally ±2 queries on 50). Step 6 added to the per-run checklist. CLAUDE.md stays the normative home; [SR-RS](../records/0133_predictions.md) decisions 11-15 explain and guard it. ⚠ Two of the six accepted parts — the sealed set and the decoy/placebo controls — are **NOT built** (W-81) and nothing may cite them as in force. **2026-08-22 (Arpit): §Litmus gains the distinction that protects the architecture docs** — the ceiling is on **measurement and promises, not on the design's reach**. Prose describing behaviour at 10⁵–10⁶ describes the architecture and is not stale; the per-sentence test is *does it commit at that size, or describe how the design works there* — commitments go, descriptions stay. **2026-08-22 (Arpit, later): §Litmus's ceiling extended from MEASUREMENT to COMMITMENT** — above 10 000 documents, no threshold, budget, bound, veto condition or pre-registration may be written either. **R7 and R8 withdrawn under it**, ids retired and not reused. The rule that keeps this safe is stated there: a *promise* above the ceiling is removed, a *measurement already taken* above it is untouched. **2026-08-22 (Arpit): §Litmus gains a MEASUREMENT CEILING.** 10 000 documents was the design point; it is now also the largest size any measurement may use — *"no testing should go beyond ten thousand"* until the build is done. Three non-consequences written in explicitly (un-measures nothing, deletes no feature, does not forbid reasoning about scale), because a scope ruling misread is how filed measurements get destroyed. **2026-08-21: §Litmus rewritten — the design point is 10 000 documents (Arpit); 50 000 then 100 000 are staged later targets, not the filter.** What changed is the scale filter; the enterprise deployment filter (Windows fleets, proxies/SSO, air-gap, audit) is explicitly unchanged, because a 10k corpus inside a corporation is still inside that corporation. A new note states the consequence for gates: a verdict measured at a deferred size **stands as measured** and is re-judged by a *new* pre-registration, never by editing a frozen one. **2026-08-21 (release): version facts bumped to `0.35.0`** — the source verbs and the progress plane. Earlier the same day, to `0.34.0` — a statement of fact, fixed on contact, not a change to what the file instructs. **2026-08-21 (PRIORITY P7): the mandatory `Cost:` line dropped from §The three-file session discipline** — Arpit's direct verdict, [`archive/proposals/process-diet.md`](../archive/proposals/process-diet.md). **2026-08-19 (later): version facts bumped to `0.33.0`** — a statement of fact, fixed on contact, not a change to what the file instructs. **2026-08-19: ADOPTED — the PROPOSED header is deleted; the M0a rewrite is in force** (W-32, Arpit). Five factual passages fixed in the same change: "no package on `main` yet" → **0.32.0 released**; version `0.30.0.dev0` → **0.32.0**; "§Error contract (applies once `src/` exists)"; an archived-ADR citation for the ingest modes → [SR-EXTRACTED](../records/0115_extracted-mode.md)/SR-ENRICHED; and §Scope's "**No M2+ work while P1 is unmeasured or failed**" — P1 closed FAIL and M2 *shipped*, so the file forbade a released milestone. §Documentation discipline gains the preserved rule the header used to carry — **agent-steering files are proposed, never auto-applied** — with an explicit carve-out that **statements of fact about the repo are fixed on contact**, which is why five of them had rotted. **2026-08-18: §Documentation discipline added** (three-file session discipline · OPEN-WORK rules · archive-is-not-evidence · concurrent-session + ground-truth hazards · SR standing rules), §Layout rewritten to the `work/` + `docs/` tree, the laws named **L1–L7**, and two stale passages fixed ("numbering continues at 0016"; "`src/fux/` does not exist yet"). Still carries its **PROPOSED** header — W-32 is Arpit's call. Earlier: **Still proposed, not adopted** — the file's own header says so. **Correction (2026-08-12): there is no `CLAUDE.md.proposed`.** The rewrite was written straight into `CLAUDE.md` at `3892c55` and every session since 2026-08-09 has followed it, so "adopt" = delete the header and "reject" = revert ~800 lines. Three passages are stale (`src/fux/` "does not exist", §Layout's `archive/v0.26-docs/`, the dead `OPEN-WORK §2` pointer) and are **deliberately unfixed** under law 7. Packaged for decision in [the ratification package](../archive/handoff/v0.32.0-ratification-package.md) §4. **2026-08-12: new *Triage first* section added on Arpit's explicit instruction** (a human-blocked queue stops the session, blocked-list first) — a deliberate edit to the live file, separate from the W-32 adopt/reject call. **2026-08-12: Triage-first section extended on Arpit's explicit instruction** — the inbox rule (5-day naming), two-strikes-→-gate, and the worklog `Cost:` line. |
| [`../README.md`](../README.md) | Status, guarantees, reading order change | 2026-09-15 | **2026-09-15 (W-178, Claude Code):** the URL bullet says `fetch=<name>` resolves to any `.py` in `.fux/fetchers/`, the same way `.fux/decoders/` already works. **2026-09-15 (W-177, Claude Code):** the verb table's *re-read sources* row is `fux ingest` — `fux update` is deleted and `--no-fetch` is the offline form. **L11 — the sealed answer key is a law.** **2026-09-15 (L11):** the sealed-key prohibition became law `L11` ([SR-LAW-11](../records/0012_LAW-11-sealed-answer-key.md)); SR-WORK-GOLDEN now carries only the process, and every law count, range and `L0..L11` diagram moved with it. · **2026-09-13 (2.0.0):** the Status block is no longer a pre-release — `2.0.0`, released on PyPI and npm, with a break held for the next major.  **2026-09-13:** L9 retired — the environment rule is now [SR-WORK-ENVIRONMENTS](../records/0052_WORK-environments.md) (`0052`, `kind: process`), not a law. The handle and `records/0011` are never reused.  **2026-09-13 (SR rename):** `ADR-<NAME>` became `SR-<NAME>` and `docs/adr/` moved to `records/` at the repo root — one mechanical change, every reference repointed. Archived `ADR-NNNN` citations and `archive/v0.26-docs/adr/` are unchanged.  **2026-09-12 (W-107 Phase 4):** §Install gains the Node reader (`node .fux/node/fux.mjs`, `npx fux-engine`) and `from fux import open`.  **2026-09-12 (W-138):** reading-order item 5 was *Playground setup — a graded corpus to try fux on*; it is now *Sibling environments*, pointing at `work/setup/README.md`, because the playground grades nothing (SR-WORK-ENVIRONMENTS). **2026-09-12 (positioning):** tagline reframed to *"A search index for your written knowledge"* on Arpit's ruling — no *code*, *codebase* or *organization*; lede drops *"committed next to your code"*; new last *Why fux* bullet **It indexes documents, not code**. **2026-09-11: rewritten from scratch as a front door, not a status log.** The old file was one 90-line status blockquote that restated rules owned by SRs (the `.fux/` table, `doctor`'s contract, the laws) and carried a dead reading-order entry (`archive/adr-old/`) and an unresolvable `../fux-playground/` link. Now: what fux is, install (`--pre`), a quickstart transcript captured from a real run, a task → command table, agents, formats/URLs, measured evidence, the nine laws as links only, reading order. Every rule is linked to its record, never restated (L0). **2026-09-05 (cleanup): the Status block said `2.0.0-alpha.0` and was dated 2026-08-24 — SEVEN releases stale on the public front door.** Corrected to `2.0.0-alpha.7` with a one-row-per-release table (alpha.1 dense lane removed … alpha.7). **And `fux doctor`'s one-sentence description was a two-check summary of an eighteen-check command** — rewritten to say what it actually reads, that it is offline and read-only by contract, that only two things FAIL, and why everything else is a warning. **2026-09-01 (alpha.5): the `.fux/` table was UNTRUE** — it said *"every child is declared as committed or derived"* while omitting six committed files and the whole third category. Rewritten from `fuxdir`'s own declarations, with `acquired/` and why not-rebuildable is the point. **2026-09-01 (W-98 Phase 1): the two-fetcher paragraph corrected** — it described `cdp.py` only as *"Chrome DevTools Protocol, also pure stdlib"*, which said nothing about what it returns. It now says the fetcher **borrows the session your signed-in Chrome already holds** and that **neither shipped fetcher renders a page** — both return bytes plus a content type. **2026-08-25: two stale passages fixed on contact for the model removal.** The `ask --hybrid` paragraph claimed a dense lane that *"closes three known gaps and breaks nine working queries"* — that lane is deleted, and the numbers were the DOC-level lane's anyway, superseded by DENSE-CHUNK's 0 fixed / 2 broken. And the index description listed *"dense binary codes"* among the committed plane's contents; they are gone. The wheel line now reads 6.84 MB -> 233 KB. **2026-08-24 (v2.0.0-alpha.0): status block rewritten** — record shape moves to `fux.index.v2`; names the five-field BM25F, `.fux/tune.toml`, reranking, `fux enrich` and `fux mcp` additions. **2026-08-22 (first major release): version bumped to `1.0.0`** — statement-of-fact fix on contact, no SR affected; marks the milestone `v0.37.1` already reached, no behaviour changed. **2026-08-22 (patch release): version bumped to `0.37.1`** — statement-of-fact fix on contact, no SR affected. **2026-08-22 (this pass): version bumped to `0.37.0`; W-57's "two named gaps still open" replaced with the closure** — both the 24/24 graph-acceptance run and the two-machine determinism check landed the same day, so the line claiming them unmeasured was already stale. **+the archived-content paragraph** — `archived: true`, the `[archived]` marker and the stderr disclaimer, with the byte-identical-at-default guarantee named alongside `[ranking] archived_weight` (SR-ARCHIVED-CONTENT). **2026-08-21 (W-63): the URL paragraph rewritten** — `fux url` + `--refresh-urls` replaced by the `add`/`remove`/`update` paragraph, and the "only networked path" claim corrected to **two**, both named. **2026-08-21 (Cowork): the status block's `--scan` line rewritten** — `ask`/`find`/`answer` scan by default now (no build step needed); `--fast` opts into the accelerator; `--scan` still forces the reference path explicitly. **2026-08-21 (PRIORITY P2 reconciliation):** "No graph query lane yet (M3)" was stale — M3 landed 2026-08-20 (unreleased); replaced with what shipped and its two open acceptance gaps (W-57). Reading-order item 4's link text said `work/adr/0004_index-format.md` while pointing at `archive/adr/0004_index-format.md` — text corrected to match the target, and a note added that the archive may be named for orientation but never backs a live claim. **2026-08-19 (W-54, the sources rewrite, released as `0.33.0`):** the status block gains `fux setup` and `fux url`, the two committed source lists are named as the corpus, and the **two scaffolding moments** are stated where a reader would otherwise assume ingest writes code. **Status rewritten for M2**: the accelerator, the measured R3 number, the differential law, the five verbs, and hybrid-off. Previously: "T0 slice: ingest + ask work on this repo"; gained a **`.fux/` layout table** and the new URL paths (SR-DOTFUX). **Reading-order item 5 repointed at the **`fux-playground` sibling repo** (SETUP-PLAYGROUND); `examples/` deleted from this tree.**  **2026-08-22 (W-62's README half): two false statements of fact fixed on contact.** The status line read *"M2 shipped"* on a repo that has since shipped M3, M4 and M5 across five releases, and the graph lane was labelled *unreleased* when it went out in `0.34.0`. **Checked against what GitHub actually serves, not against the local file** — `git ls-remote` puts `refs/heads/main` at `fa3ba30` and the raw README at `main` matches this tree, so origin is current and the staleness was in the content, not the push. ⚠ The rendered HTML page fetched at the same moment showed **v0.25.x and 134 commits** — a cached rendering, not the repo's state; recorded because a future check that reads only the HTML page will see it too and should not conclude the push is missing. |
| [`../.fux/README.md`](../.fux/README.md) | The `.fux/` layout table changes (a plane is added, renamed, or flips committed/derived), **or the verb groups in SR-CLI §1 change** | 2026-09-15 | **2026-09-15 (W-177, Claude Code):** `update` left the **sources** group and the **write** row says `ingest` re-fetches; `--json` list names `ingest --check`. ⚠ **The template in `store/fuxdir.py` moved in the same edit** — `test_verb_table_agreement` holds the two together, and this file being write-if-missing is how they drift. **2026-09-12 (W-107 Phase 4):** *"the CLI is the contract; the modules are not"* is **retired** — the CLI and `fux.api` are the contract. §Calling fux from a script gains the in-process Python form and the Node reader's three invocations. ⚠ The generated declaration table is write-if-missing, so this repo's copy shows the old table; the new `node/` and `fux` rows reach new repos only.  **2026-09-12 (Arpit):** rewritten and greatly extended — the declaration table, fetcher note and rules are unchanged, and four sections now follow them: *What fux is*, *The commands* (the SR-CLI group table), *Calling fux from a script, in any language* (shell/Python/Node/Go + the `--json`/exit-code contract), and *For an AI agent*. ⚠ **Write-if-missing still holds**, so an existing repo never sees this until its `.fux/README.md` is deleted. ⚠ **The verb table duplicates SR-CLI §1 and nothing checks it.** **2026-09-11 (W-130 rename):** `.fux/types.toml` -> `.fux/formats.toml`. **2026-09-11 (W-130):** + `types.toml` row, matching `fuxdir.COMMITTED_FILES`. **2026-08-21 (W-63): the generated header no longer says fetching happens only under `--refresh-urls`** — it names `fux add <URL>` and `fux update`. Fixed in `store/fuxdir.py`'s template and in this repo's own copy. **2026-08-19 (W-54, the sources rewrite, released as `0.33.0`):** `sources/` holds two lists; the fetcher section names `fux setup` as the writer and `http.py` as the second file. New row. **Generated** by `store/fuxdir.py` at ingest, write-if-missing — edit the generator, not the file; a consumer's copy is never overwritten (SR-DOTFUX) |
| [`../CHANGELOG.md`](../CHANGELOG.md) | A release-worthy change lands | 2026-09-15 | **2026-09-15 (W-178, Claude Code):** `[Unreleased]` §Added — `fetch=` takes any fetcher name; a typo now parses and `fux doctor`'s new `fetcher bindings` row is what catches it. **2026-09-15 (W-177, Claude Code):** `[Unreleased]` opened a **Removed — BREAKING** section: `fux update` is deleted with no alias, `--all` is `--refetch-all`, a bare `fux ingest` goes to the network and `--no-fetch` is the offline form, and `fux hooks` must be re-run or `post-merge` opens sockets. **2026-09-14 (W-165 + W-166, Claude Code):** `[Unreleased]` opened with the first BREAKING change of the 3.0 line — `No confident matches.` on stderr — plus `fux remove` writing `.fux/.fuxignore`, the deletions clause on the ingest summary, and the three carry-forward digests with their one-off full re-extraction stated as the price. **2026-09-13:** **`2.0.0` cut** — the alpha line promoted plus everything that had accumulated unreleased since `2.0.0-alpha.7`; `Unreleased` is empty again.  **2026-09-13 (W-147 SHIPPED):** the journal's two consent surfaces are BOUND by a test (all four states) and the specimen line now opens `⚠ NOT a rendering key`. Found in the build: `output.toml` is a COMPLETE declaration, and `provenance.py` named the REJECTED `tune.toml` home. **2026-09-13 (W-153 SHIPPED):** `mtime` is on every `--json` hit — five readers, `required: always`, `null` a CLAIM. First amendment SR-API's frozen surface has taken. Prose output unchanged; sorting by it is NOT authorised. **2026-09-13 (W-152 SHIPPED):** `archived_weight` and `recency_half_life_days` are REMOVED, with `recency_multiplier` and `stats.json`'s `newest_mtime` — all three DOCUMENT priors are now gone. No ranking change: every one shipped as a no-op. SR-ARCHIVED-CONTENT decision 6 REVERSED; the W-73 bound test re-aimed at `[priority]`. **2026-09-13 (W-151 SHIPPED):** `[ranking] superseded_weight` is REMOVED — the knob, its key, its Node twin and its four records. A committed key is refused BY NAME. No ranking change: it shipped at `1.0`. The `superseded` FACT and the declared tie-break are untouched. **2026-09-13 (SR rename):** `ADR-<NAME>` became `SR-<NAME>` and `docs/adr/` moved to `records/` at the repo root — one mechanical change, every reference repointed. Archived `ADR-NNNN` citations and `archive/v0.26-docs/adr/` are unchanged.  **2026-09-12 (W-107 Phase 4):** Added — the Node read plane and `from fux import open`; Fixed — `fux.open()`'s 50 ms decoder import, and the MCP `k` default Node emitted as a string against its own `"type": "integer"`.  **2026-09-12 (W-141):** Unreleased/Changed — Codex + Copilot skills move to `.agents/skills/`. **2026-09-11 (decision 15): the operating guides and scoped pointers, under Unreleased/Added.** **2026-09-11 (W-130 rename):** `.fux/types.toml` -> `.fux/formats.toml`. **2026-09-11 (W-130):** BREAKING - the types list is `.fux/types.toml`; `[sources] types_file` refused. **2026-09-11 (`[index]`):** `[index]` move + `max_phrases` 12 -> 32 + the delta-ingest digest fix, under Unreleased/Changed. **2026-09-05 (W-101):** `fux doctor`'s four new checks and the redaction counts. **2026-09-05 (W-111):** the declared tie-break and `find`'s filters. **2026-09-05 (W-110):** doc2query, and the enrichment-reuse fix. **2026-09-05 (W-109):** `--expand`, `-q` RRF fusion, and the accelerator-bound fix. **2026-09-05 (W-108):** an `[Unreleased]` block — `answer` refers the top 3, the additive per-passage `id`/`loc`/`sha`, the per-URL fetcher dispatcher, and the three measured costs (bytes, band demotions, `ask`/`answer` disagreement). **2026-09-01 (W-98 Phase 1): Unreleased gains Changed + Removed** — `cdp.py` returns the intercepted resource rather than `outerHTML`, `validate()` added, `LAUNCH_CHROME` defaults `False`, and ⚠ **`settle_ms` removed, which is BREAKING for a repo that sets it** (it raises a message naming the retirement rather than reading as a typo). **2026-08-28: SR-OUTPUT's three-root migration and the no-fallback rule filed under Unreleased/Changed** — `.fux/output.toml` is now the sole source of every key it is asked for (an unset key errors, naming the fix), `--no-output-config` reaches six more verbs, and `[mcp] top` no longer drifts from what `tools/list` advertises. **2026-08-25: the model removal filed under Unreleased/Removed**, with the A/B table, the reason (DENSE-CHUNK, 0 fixed / 2 broken) and a migration paragraph — `[dense]` out of both config files, `--hybrid` out of scripts, `RUNTIME_SCHEMA` v4 -> v5. Marked **breaking**: `--hybrid` shipped in `v2.0.0-alpha.1`. **2026-08-24 (later): `[2.0.0-alpha.1]`** — SR-TUNE built. `.fux/tune.toml`, per-source priority, `--no-tune`, `fux tune`; `[ranking]`/`[dense]` retired from `fux.toml`; `RUNTIME_SCHEMA` -> `fux.runtime.v4`; the `fux doctor` undeclared-file warning fixed |
| [`../docs/index.md`](../docs/index.md) | Bundle contents change | 2026-09-15 | **L11 — the sealed answer key is a law.** **2026-09-15 (L11):** the sealed-key prohibition became law `L11` ([SR-LAW-11](../records/0012_LAW-11-sealed-answer-key.md)); SR-WORK-GOLDEN now carries only the process, and every law count, range and `L0..L11` diagram moved with it. · **2026-09-13:** L9 retired — the environment rule is now [SR-WORK-ENVIRONMENTS](../records/0052_WORK-environments.md) (`0052`, `kind: process`), not a law. The handle and `records/0011` are never reused.  **2026-09-13 (SR rename):** `ADR-<NAME>` became `SR-<NAME>` and `docs/adr/` moved to `records/` at the repo root — one mechanical change, every reference repointed. Archived `ADR-NNNN` citations and `archive/v0.26-docs/adr/` are unchanged.  **2026-09-12 (positioning §6b):** new §*What is in the bundle, and what conforms* — ALL-CAPS exemption retired, three exclusions named with why each is a boundary, the pre-2026-08-25 regression freeze baselined, and the gate linked. OKF bundle root |
| [`architecture-high-level.svg`](architecture-high-level.svg) + [`architecture-detailed.svg`](architecture-detailed.svg) + [`architecture-decoders.svg`](architecture-decoders.svg) + [`architecture-ask.svg`](architecture-ask.svg) + [`architecture-answer.svg`](architecture-answer.svg) + [`architecture-two-readers.svg`](architecture-two-readers.svg) | The plane, verb or record shape each one draws changes. **high-level** only on a change to *what fux is* rather than how it works | 2026-09-14 | **2026-09-12 (W-149):** two diagrams corrected on contact and their PNGs re-rendered at the established 1.4 scale — `two-readers`'s arm block goes five surfaces to **six** (the BUNDLE vs the module tree) and its *the arm does not compare the bundle* box, now false, is replaced by `verify`/`--why`/`--receipt`, which genuinely have no Node twin; `detailed`'s `.fux/node` box goes *37 files, 196 KB* to **one bundled `fux.mjs` + manifest, 244 KB, no build step FOR YOU**. **2026-09-12 (a SIXTH diagram, `two-readers`):** the set gains `architecture-two-readers.svg` — search in Python versus Node, drawn per component: the ten shared twins on the spine, the Python-only modules in the left gutter (`derive/accel.py`, `query/provenance.py`, `ingest/pii.py`, `decode/`, `refer/arc.py`, `refer/fetchcache.py`, the fetchers), Node's behaviour in the right gutter, the three reimplemented Python semantics, the asymmetric `url:` verdict table, the graph-plane asymmetry, the arm's five surfaces, and what is in NEITHER runtime. Authority is [SR-NODE-SEARCH](../records/0153_node-search.md) decisions 1-12; **trigger:** any of those decisions changes, or a Node twin is added or removed. **2026-09-12 (the Node plane lands, second pass):** all five redrawn again the same day, because the set was drawn hours before `.fux/node/` and `fux.api` were committed and it said so wrongly — `detailed` listed **The Node reader** under *Not built*, which is the one class of error that band exists to prevent. Now: a **THE TWO READERS** band (Python writes and is the only writer; the vendored Node reader is **committed**, 37 files / 196 KB, no dependencies, no build step, rewritten at the head of every ingest on a version difference), the differential law's **third arm** `python == node`, the three deliberate asymmetries (no fetch -> `url:` is never `current` or `cached`; the graph plane rebuilt in memory, so no `fux build`; no decoders, so it declines what it cannot decode), and `from fux import open` ([SR-API](../records/0154_api.md)). `also committed:` gains `node/` and `fux`. `ask`, `answer` and `decoders` each gain the matching Node band; `high-level` moved by one line only — *Read it with Python, or with Node.* PNGs in `docs/` re-rendered from these SVGs at 2x. | **2026-09-14 (W-112 closed, Cowork):** `proposal-search-v3-target.svg` moved to `archive/diagrams/` with its map row — the condition the 2026-09-12 review wrote down; its two `search-v3.md` links repointed, `README.md`/`governance.md`/`docs/index.md` recounted. **2026-09-12 (Arpit): all four retired and FIVE drawn from the code** — high-level - detailed - decoders - ask - answer. The old set was read against `src/` first and was stale in eight ways: the 2026-08-28 *PROPOSED - NOTHING IN THIS BAND IS BUILT* footer listed `--expand`, `-q` RRF, answer-top-3 and the declared tie-break, **all since shipped**; `sources/types` was renamed `formats.toml` on 2026-09-11; the refer box listed **four** verdicts and `as-ingested` has shipped since 2026-09-01; the sort was the pre-W-111 two-key form; decoders read *16 modules* against 17. The new set states what is **not** built in its own band, including that the paper's `L/ P/ D/ V/ E/ M/` keyspace does not exist. ⚠ `architecture-search-v3.svg` was **renamed** [`proposal-search-v3-target.svg`](../archive/diagrams/proposal-search-v3-target.svg) — it draws a proposal's target, not the architecture of record, and the three live citations were repointed. |
| [`../docs/handbook.html`](../docs/handbook.html) | A verb, a law, a decision, a pending fork, the confidence block, the quality contract, or the merge/fetcher/decoder contracts change | 2026-09-15 | **2026-09-15 (W-178, Claude Code):** the fetcher section gains the `fetch=<name>` bullet — a name, not a menu, resolving to `<fetchers dir>/<name>.py`. **2026-09-15 (W-177, Claude Code):** the verb table's `fux update [entry]` row is folded into `fux ingest [entry]` with its full flag list; the L4 card and the two-fenced-paths bullet name `fux ingest`. **2026-09-13 (SR rename):** `ADR-<NAME>` became `SR-<NAME>` and `docs/adr/` moved to `records/` at the repo root — one mechanical change, every reference repointed. Archived `ADR-NNNN` citations and `archive/v0.26-docs/adr/` are unchanged.  **2026-09-12 (positioning):** cover lede and *in plain terms* reframed to *written knowledge*; *"a codebase's own documents"* → *"a project's"*. **2026-09-11 (W-130 rename):** `.fux/types.toml` -> `.fux/formats.toml`. **2026-09-11 (W-130):** decoder section's `.fux/sources/types` -> `.fux/types.toml`. **2026-08-29: decoder count 16 → 19** — three text spots (`#s-decode`'s SVG, the ingest slide's bullet, the decoder-plane slide's SVG) and the "sixteen built-ins" module list, all naming `jsonldoc`/`svgdoc`/`imagedoc` alongside the existing thirteen (SR-DECODE decision 10a). — carried forward: **2026-08-28: `[confidence]` in `tune.toml` (SR-CONFIDENCE decision 13).** Two new slides — **The formulas** (`#s-conf-formula`: the shared `idf`, all four signals plus `doc_coverage` as expressions, the five-clause ladder, and NQC/clarity-score grounding for `separation`) and **Tuning the floors** (`#s-conf-tune`: both keys, the measured 19-of-50 cost of `doc_coverage_floor = 1.0`, and the reversal's price stated rather than softened). ⚠ **Three STALE claims fixed on contact:** *The five fields* said five and there were six (`doc_coverage` shipped 2026-08-28); the `--json` sample omitted `doc_coverage` and both floors; and *Fact vs guess* said **SR-CONFIDENCE is proposed, not accepted** — it has been `accepted` since 2026-08-27, and the same bullet still claimed the floor was *not a `tune.toml` key, deliberately*. A page that tells a reader a knob does not exist is worse than one that omits it. — carried forward: **2026-08-27 (later): RESTRUCTURED into 32 slides on Arpit's five guidelines** — (1) text is POINTERS, not prose: every section leads with bullets and the paragraphs that survived are the ones that carry an argument; (2) **nine more diagrams** (ingest pipeline · analyzer chain · field weights · the two planes · verb granularity · citation anatomy · freshness verdicts · four test layers · the invocation ladder), joining the five that existed — anything a table could only list is now drawn; (3) **long text is collapsed by default** in `<details class="more">` blocks, so the spine reads in one screen and the argument is one click away; (4) a **collapsible left sidebar** with per-group toggles, scrollspy and a persisted collapsed state; (5) **presentation mode** — `▶ Present` or `p`, one `<section class="slide">` at a time, `→`/`←`/`Home`/`End`, `f` fullscreen, `?` for keys, `Esc` to leave, a progress bar, and every `<details>` forced open so nothing is hidden on a projected slide. Presenting resumes from the section you were reading (the scrollspy keeps the index). ⚠ **The JS is inline, dependency-free and degrades**: with scripting off the page is still a complete scrolling document, because every slide is a plain `<section>` and only `body.present` hides anything. A `@media print` rule expands the collapsibles. **The theme button cycles system → light → dark** and persists; all fourteen diagrams still paint only from `--d-*` variables. **2026-08-27 (W-89): `L8` added to the law strip** in the refer-loop figure, and the nav label goes *The seven laws* → **The eight laws**. ⚠ **A pre-existing defect found while in there and NOT fixed:** the nav links to `#s-laws` and **no element carries that id** — the laws section the nav promises does not exist on the page, so both the old label and the new one point at nothing. Recorded rather than silently patched, because writing the section is a content task, not a rename. **2026-08-27: SUPERSEDES `docs/guide.html`** — one page, renamed and widened on Arpit's instruction. Everything the guide carried is folded in; four sections are NEW: **how fux works** (ingest in five steps, the two planes, the refer loop), **the confidence block** (four signals, the band ladder, ⚠ `SEPARATION_FLOOR` provisional/R10 and `separation` ordinal where Chow's rule wants a calibrated probability), **how it is tested** (four layers, the guards that test the project rather than the engine, SR-WORK-QUALITY's four-gate funnel and its published `t = 0.75 → c = 2`, and *what is not verified*), and a **reference** for merge · fetcher · decoder · CLI. ⚠ **Counts were RE-DERIVED, not carried:** the guide said *41 records, 26 accepted, 15 proposed*; the truth at write time is **44 live records, 42 accepted, 2 proposed**, with `0141_confidence.md` named as ruled-for-deletion-and-still-on-disk. **All four diagrams are theme-aware** — they paint from `--d-*` variables defined on bare `:root`, under `@media (prefers-color-scheme: dark)` guarded by `:not([data-theme="light"])`, and under `:root[data-theme="dark"]`; the guide's single SVG hard-coded light hexes and was unreadable in dark mode. ⚠ **`docs/guide.html` was NOT deleted in this change** — the Cowork shell was down (`no space left on device`), so no `rm` could run; one `git rm docs/guide.html` locally closes it, and until then two files describe the same thing. **The rest of this row is the guide's own history, kept because the triggers did not change:** **2026-08-26 (gap found by Arpit): the page never documented the CITATION FORMAT** — heading-delimited passages, paragraph splits above 4 KB, and `path:L12-L40` line ranges. All shipped in W-76 Phase 5; the omission was the guide's, not the engine's. Added, with output CAPTURED from a real run rather than typed. **2026-08-26: written.** One self-contained page — how to use fux, the invocation ladder, the architecture (diagram inlined), the seven laws, decisions made, decisions pending, and an explicit *what is not verified* section. ⚠ Its counts (41 records, 26 accepted, 27 forks) are facts about a moment and go stale silently; re-derive before quoting them. |
| [the SR register](../records/README.md) | Any design/scope/status change | 2026-09-15 | **L11 — the sealed answer key is a law.** **2026-09-15 (L11):** the sealed-key prohibition became law `L11` ([SR-LAW-11](../records/0012_LAW-11-sealed-answer-key.md)); SR-WORK-GOLDEN now carries only the process, and every law count, range and `L0..L11` diagram moved with it. · **2026-09-13 (the renumber's other half):** ~300 dead `NNNN_slug.md` citations repointed across `src/`, `tests/`, `.fux/decoders/`, the agent skills, `tools/` and live `work/` docs; **`work/regression/`, `archive/`, `work/golden/`, WORKLOG, CHANGELOG and every PRE-REGISTRATION are the stated frozen set** and keep the number that was true when written. New gate `tests/test_record_paths_resolve.py`; SR-WORK-OWNERSHIP decision 15. The register's own *"two ranges"* corrected to three.  **2026-09-13 (SR-WORK-BACKLOG):** the backlog is the second work file — `work/BACKLOG.md`, 241 named-but-unclaimed items in five classes, governed by [SR-WORK-BACKLOG](../records/0055_WORK-backlog.md) at `0055`. **2026-09-12 (W-149):** ownership gains `src/fux/store/nodebundle.py` (carved out of `store/` on `acquired.py`'s precedent) and three `describes` rows; the `node/` row's *37 files* corrected to 44 authored / one shipped. **2026-09-11 (W-140): ten records amended** — SR-URL-FRESHNESS 6a, SR-PII 19/20, SR-DOCTOR 10, SR-REFUSAL, SR-GRAPH, SR-REFER, SR-URL-LIST, SR-MAINTENANCE, SR-MERGE-DRIVER, SR-INDEX-LIFECYCLE, SR-DOTFUX, SR-OUTPUT, SR-CLI. **2026-09-11 (W-137): THE NUMBER LINE WAS SPLIT IN TWO.** §The convention's *Path* is now a table — `0001`–`0100` laws (`0011`–`0100` reserved and **empty, no placeholder files**), `0101`– everything else; a new record takes the next free number **in its own range**. A new blockquote above the 2026-09-06 note carries why, what was rewritten (1 772 path tokens in 136 files; unresolved links 437 before and after), and ⚠ **the first new law takes `0011`, SR-CLI's ordinal until today** — the vacated-ordinal hazard, accepted a second time on purpose. **2026-09-11 (W-130 rename):** `.fux/types.toml` -> `.fux/formats.toml`. **2026-09-11 (W-130):** SR-TYPES and SR-URL-LIST rows; `ingest/typesfile.py` row owned by SR-TYPES; `sources.py`, `sourcelist.py` and the doctor/SR-DECODE rows reworded. **2026-09-11 (`[index]`):** SR-CONFIG / SR-TUNE / SR-TABULAR rows reworded for `[index]`; csv.py ownership row repointed. **2026-09-05 (W-101): six `describes` rows** — `doctor.py` under SR-REFUSAL / SR-DECODE / SR-URL-FRESHNESS / SR-INGEST, `urlstate.py` under SR-REFUSAL, `urlsrc.py` under SR-URL-FRESHNESS; `tests/test_sr_ownership.py` needed no edit (the describes table is parsed, not enumerated). **2026-09-05 (W-109): [SR-EXPAND](../records/0149_expand.md) is NEW and accepted** — it owns `query/expand.py` and `query/fuse.py`; RRF's revival, which SR-PORT-LIST rule 1 said would need a record. **2026-09-05 (W-106): `tools/vector-gate/` claimed by SR-RS** under decision 10's ownership fallback — `SR-VECTORS` does not exist and a proposal is not a valid owner. **2026-09-05 (W-108): three `describes` rows** — SR-ANSWER on `query/__init__.py` and `query/refer_answer.py` (**it owned nothing**, so the freshness gate could never open the record for the `answer` verb — W-108 opened five records and not that one), and SR-RERANK on `refer/_rescore.py`. **2026-09-05 (W-108): seven records amended** — SR-ANSWER (4, 5, 11–13, §1 + both diagram twins, veto), SR-REFER (21–22), SR-RERANK (9), SR-CONFIDENCE (14), SR-ASK, SR-OUTPUT, SR-URL-FRESHNESS. **2026-09-02 (W-103 closed): SR-LOCKS' veto capture RE-RUN** and the ⚠ owing it deleted — all three checks not fired, two line numbers moved and the set of call sites did not, which is what the check is. **2026-09-01 (W-103): SR-LOCKS `⏳ proposed` → `accepted`** — the last proposed record in the register; Arpit ruled. It introduces no mechanism, so acceptance is a ruling rather than a build. ⚠ **Its veto capture is still the 2026-08-27 one and is owed a re-run** — the session that accepted it had no shell. **2026-09-01 (cleanup): SR-ACQUIRED's *"Owed"* paragraph named two gaps Phase 3 had ALREADY CLOSED** — `doctor` reports the store's size and `fux remove <url>` drops the blob. A record describing behaviour the code no longer has reads as authority; Law zero's third obligation, and the unenforced one. Four records' Reference blocks repointed at the archived W-98 file, SR-CDP-FETCHER's spike citation re-grounded in its own measured table. **2026-09-01 (red-suite sweep): the `fuxdir.py` → SR-PII *describes* row gained a real reason** — it was 36 characters and tripped `test_every_describes_row_states_a_reason` (veto condition 4): a bare pair is unauditable. **2026-09-01 (W-98 Phase 1): SR-CDP-FETCHER's register row rewritten** — *"drives your existing Chrome"* → **borrows your signed-in Chrome and intercepts the response, returning the server's bytes rather than a rendering**. The row had described the retired behaviour. **2026-09-01 (W-98): +SR-ACQUIRED (`0050`) registered**, `⏳ proposed`, and the "`0001`–`0049`" contiguity note bumped to `0050`. The record shipped staged-but-unregistered and was failing `test_sr_ownership.py`; its `owns:` also claimed `src/fux/store/acquired.py`, which does not exist — corrected to `owns: []` with the reason stated in §2, per SR-WORK-OWNERSHIP's no-conditional-claim rule. **2026-08-28 (Cowork): `src/fux/output_config.py`'s ownership row rewritten for the three-root design (`CLI_VERBS`/`MCP_KEYS`, the new sole-source-of-truth resolution) and a `src/fux/mcp.py` → SR-OUTPUT describes-row added** (decisions 11/16/17 reach into it: `[mcp]`'s key set, the resolved-not-literal `top` in `tools/list`, the load-once-at-`serve()`-start rule). **2026-08-28: SR-URL-INGEST decision 9** carries the recall gap rescued from W-82's queue row. **2026-08-27 (third pass): `tools/quality-controls/` claimed by SR-RS**, with its `owns:` list updated in the same change. **2026-08-27 (later): SR-ENRICHED's row DELETED** (superseded by SR-ENRICH, file `git rm`'d) and the §"number line is contiguous" note CORRECTED — it described a renumber that never happened and must not, and pointed at two files that have never existed. `0017` and `0025` are **burned ordinals**, per W-82 ruling 7. **2026-08-27 (W-91): SR-PROVENANCE row added, and `src/fux/query/provenance.py` carved out of SR-ASK's `query/` directory claim** — a different *decision*, not merely a different concern: it is the one module in the tree that may write a plaintext use record. ⚠ **The pending renumber does not know about `0046`** — see the `../records/` row. **2026-08-27 (W-91): SR-PROVENANCE (`0046`) filed, and SR-CLI · SR-ASK · SR-LAWS amended in the same change.** ⚠ **SR-LAWS decision 8 REVERSES `L8`** on Arpit's ruling — plaintext question *and* answer are legal, the bound became a design default, stdout is permitted, git and the network stay forbidden — and its **veto check 3 was rewritten with it**, because the old one asserted the hashing and the `MAX_QUESTIONS` bound and would now pass on a repo that had broken the surviving half. SR-CLI gains decision **9b** (`verify` is a verb because it takes a FILE; `answer`'s three new flags are three because **only `--journal` writes**); SR-ASK's decision 11 is corrected — **it claimed every `--json` branch was validated and only one was** — and gains decision **12** (`--why` is a second query, so `rank.py`/`scan.py`/`accel.py` are outside its blast radius). ⚠ **`0046` IS NOT IN `scripts/renumber-adrs.sh` (a 2026-08-27 one-shot, never run and **deleted 2026-09-11** with W-137)**: the pending renumber takes `0026`–`0045` down by one to end at `0044`, so unless it is extended to carry `0046` → `0045`, running it leaves a hole and a record above the end of the line. **2026-08-27 (latest, Arpit): THE NUMBER LINE IS RENUMBERED — `0025`'s hole closed, `0026`–`0045` down by one**, end state `0001`–`0044` contiguous. The duplicate row and the whole ⚠ collision block are **deleted** (both files are gone), replaced by a short statement of what the line is and the single thing it costs: **a bare number in any document older than today may name a different record now**, and `WORKLOG.md` is append-only so some of those sentences stand uncorrected. ⚠ **The renames did NOT run** — this surface cannot rename a file; `scripts/renumber-adrs.sh` (a 2026-08-27 one-shot, never run and **deleted 2026-09-11** with W-137) does the 20 `git mv`s, the repo-wide link rewrite, the `title:` frontmatter and the register's display column, and Arpit runs it. ⚠ **The rewrite matches `NNNN_slug.md` and never a bare number** — deliberate, because rewriting prose numbers means guessing which of them denote a record. **2026-08-27: the SR-CONFIDENCE duplicate RULED — keep `0045`.** The register's ⚠ block previously justified leaving `0141_confidence.md` on disk because it belonged to a **concurrent session**; the owner has ruled, so that justification is discharged and the block now says so, names the *substantive* reason `0045` wins (its decision 6 binds `SEPARATION_FLOOR` to SR-WORK-QUALITY's frozen `t = 0.75`, where `0043` still presents the floor as its own number to pick), and instructs that **the block and the duplicate row are deleted in the change that lands the `git rm`** — a warning about a file that no longer exists is the next session's confusion. ⚠ **The deletion itself did NOT land**: the bash sandbox was unavailable for the whole session (`useradd` failure on every call), so `records/0141_confidence.md` and `work/open/W-89-the-confidence-plane.md` are still on disk and the three red tests are still red. **2026-08-27 (the record-set rewrite): the register was REWRITTEN, not amended.** It was the last document in `records/` still carrying renumbering notes, W-nn narrative and per-row history — the thing every record was just cleaned of. It now carries the ten-key frontmatter convention, the *a record states what is true now, it carries no history* rule, an enforcement table naming all five checks and the `no SR affected` escape hatch, **one line per record** (`# · name · title · status · built`), and the ownership table. ⚠ **`status` and `built` are stated as two different questions**, because conflating them is a mistake this project has already paid for: `accepted` means the decision is ratified, `built` means the engine does it, and a reader should not have to open the record to learn which. **Six ownership rows added** — `src/fux/__main__.py` → SR-CLI (the invocation ladder's fourth rung, which W-82 §3.6 recorded as *not existing*; it exists), and SR-RANKING's five-file carve-out out of SR-ASK's `query/` claim (`rank.py`, `bm25f.py`, `analyzer.py`, `stem.py`, `tokenize.py`) — the last of which is what makes ingest and query agree **by construction** rather than by review. ⚠ **`0025_codes-table.md` is GONE from the register and from `records/`** — moved to `archive/adr/` with **no successor**, which is the gap the 2026-08-25 entry below flagged as *"Arpit's to close"*, closed: the record whose *subject* ceased to exist is not a fourth status, it is an archival with the archive map saying plainly that nothing replaced it. ⚠ **Three tests are red and the register says so in a block of its own** — `0141_confidence.md` is a stale duplicate of `0141_confidence.md` (same `name:`, colliding number) belonging to a concurrent session; it was **deliberately not deleted**, because removing another session's asset to turn a suite green is how a collision becomes a silent data loss. **2026-08-27 (later): +the SR-CONFIDENCE row (`0045`, ⏳ proposed, `built: yes`) and +the `src/fux/query/confidence.py` ownership row**, carved out of SR-ASK's directory claim — most specific wins, the same shape as `rerank.py`'s carve-out and for the inverted reason: it is the one thing under `query/` that reads the ranking's **output** and never its inputs. ⚠ **The record was written as `0043` and had to be renumbered to `0045` mid-session** — a concurrent session took `0043` (SR-LOCKS) and `0044` (SR-WORK-QUALITY) while this one was building. **`records/0141_confidence.md` is a STRAY FILE that must be deleted**; two records at one number is exactly what this register's cite-by-name rule exists to survive, but `test_sr_ownership.py` will still fail on it. **2026-08-27: +the SR-WORK-QUALITY row (`0044`, ✅ accepted, `built: no`) and +the `tools/quality/` ownership row.** The `built: no` cell is claimed by W-87 phases P1–P5, as the register's own rule requires — a decision nobody is going to act on is a wish. **2026-08-25 (previous): SR-RERANK RULED** (veto 1 condition 1 **vacated**, condition 2 **restated**), plus amendments to **SR-RANKING** (`superseded_weight` measured and failed) and **SR-RERANK** again (the default stays `0.0` **against** a `+4`, because the constants were tuned on the evaluation). **2026-08-25 (later): SR-RERANK's veto 1 condition 2 amended** — its `5e-10` bar is derived from `round(score, 9)`, which is not the binding constraint, and demands ~200 000x more precision than the corpus can detect. Restated as falsifiable (score-level drift vs the adjacent-gap floor). ⚠ **Condition 2 is not reopened.** **2026-08-25: the `src/fux/embed/` OWNERSHIP ROW IS DELETED** — the package no longer exists, and `test_sr_ownership.py` requires every listed path to. That row had carried its own ⚠ since W-76 Phase 7 (a component writing the COMMITTED index, claimed by the derived plane's record); the contradiction is resolved by the component ceasing to exist. SR-CODES-TABLE's row rewritten as dead-but-kept. **2026-08-25: SR-RS's row rewritten** — the run-classification rule (W-78 ruling 2, Arpit), decisions 11-14 checked by `tests/test_regression_runs.py` from 2026-08-25, decision 15 marked `NOT BUILT`. **2026-08-24 (later): SR-TUNE's `built` cell -> yes** and `src/fux/tune.py` claimed in the ownership table. `status` stays `proposed`: built is not ratified. Earlier the same day: display numbers fixed on 16 rows and the table sorted `0001`-`0041` contiguous **2026-09-15 (W-168 step 1, Claude Code):** thirteen records amended for the anchor field — SR-INGEST 17, SR-EXTRACTED 10, SR-INDEX-LIFECYCLE 14 (`_format` v3), SR-RANKING 12, SR-T1-ACCELERATOR 15, SR-TUNE 17, SR-GRAPH 18, SR-ASK 13, SR-EXPAND 15, SR-CONFIDENCE 16, SR-PII 21, SR-ARCHIVED-CONTENT 9, SR-NODE-SEARCH 19. |
| [`open/`](open/README.md) | **An item opens or closes** — its file is created with its index row and **archived, never deleted**, when the row goes | 2026-09-15 | **2026-09-15 (golden unblock, Cowork):** `W-191` (the ladder carries no links — 0 `ref` edges, a data defect under SR-RS d23) and `W-192` (the blind paraphrases) filed; W-176's void *Codex's key* wording corrected against SR-LAW-11. **2026-09-15 (arms settle, 16a):** W-188 trimmed to one Node arm. · **2026-09-15 (Node column BUILT, W-188):** W-188 updated — harness and report built, the run outstanding. · **2026-09-15 (Node as a column, W-188):** `W-188-node-column-every-capture.md` filed — ratified, not built. · **2026-09-15 (W-187, the CAP slide spine):** W-187 filed and archived the same day — built, not handed off. · **2026-09-15 (CAP slide spine, Cowork):** `W-187-report-cap-slide-spine.md` filed — ratified, not built. · **2026-09-15:** W-148 · W-179 archived too — eleven items closed this session. · **2026-09-15:** W-140 archived too — seven items closed this session. · **2026-09-15:** W-170 · W-181 · W-182 · W-183 · W-184 · W-185 archived; W-186 filed. · **2026-09-15 (🟡 needs an item, Cowork):** five items filed — `W-179` Node latency column, `W-180` the frozen `b`-sweep run, `W-181` the observer latency capture, `W-182` make the runner race reproducible, `W-183` design a non-circular quality endpoint — one per 🟡 row, under the new SR-WORK-OPEN-QUEUE 23a/23b. **2026-09-15 (W-168 step 1 RULED (c), Cowork):** the ruling written into `W-168-search-improvements.md` in the W-151 shape; option (a) refused, not deferred. **2026-09-14 (blocker walk, Cowork):** `W-156` and `W-112` moved to `archive/open/` with map rows; `W-176-abstention-gates.md` added; rulings written into W-144, W-146, W-148, W-161, W-168, W-170, W-175. **2026-09-14 (W-174 CLOSED, Cowork):** `W-174-offline-url-answers.md` moved to `archive/open/` with its map row. **2026-09-14 (W-174, Cowork):** `open/W-174-offline-url-answers.md` added — the `fetch_at_answer` ratification. **2026-09-14 (gap check):** every 3.0 doc and W-160–W-166 gained a *verification and the keep/remove call* section. **2026-09-14 (backlog sweep, Cowork):** 21 rows left `BACKLOG.md` — 18 promoted into W-163/W-164/W-165/W-166, 3 resolved by correcting their stale source sentences (B-032 SR-T1-ACCELERATOR, B-239 SR-REFUSAL, B-240 SR-CONFIG); B-242/B-243 added for the two 3.0 proposals; B-129/B-133/B-140/B-175 repointed at the 3.0 docs. **2026-09-13 (3.0.0-alpha.0 branch, Cowork):** five work documents filed from the session's rulings — compare docs `ask-graph-expansion` (accepted), `fux-correct` (accepted), `abstention-gates` (proposed; two rulings recorded); proposals `search-improvements-v3`, `fux-inspect`; items W-160 / W-161 / W-162. Nothing built. **2026-09-13 (W-147 SHIPPED):** the journal's two consent surfaces are BOUND by a test (all four states) and the specimen line now opens `⚠ NOT a rendering key`. Found in the build: `output.toml` is a COMPLETE declaration, and `provenance.py` named the REJECTED `tune.toml` home. **2026-09-13 (W-147):** rewritten as a ratified-not-built build item — the ruling, the two rejected options, the definition of done and what is out of scope.  **2026-09-13 (W-154 Part A):** proximity reranking PRICED — +15 to +19 ms p50, **flat in corpus size**, **same on `ask` and `answer`**, 0/124 queries faster. Surface capture, no verdict. W-154 stays open on Part B. **2026-09-13 (W-155 SHIPPED):** the table-is-the-answer gap is MEASURED and the answer is YES — `dump` 30/30 → 0/30, `content` 0/30 → 30/30, both controls hold. (b) is right for two document shapes and wrong for a third. Pre-registration committed alone at `a414012`. **2026-09-13 (archive, never delete — W-157):** this row's trigger read *"its file is created with its index row and **deleted with it**"* and it was WRONG — `work/open/README.md` contract 2 has said *archive* since 2026-08-19. 35 closed items' files were deleted on the strength of it; the rule is now [SR-WORK-OPEN-QUEUE](../records/0051_WORK-open-queue.md) rules 54–58 and the recovery is W-157.  **2026-09-13 (W-150 SHIPPED):** the benchmark harness files all SEVEN captures — planted key (no corpus byte moved), CAP-2 rows, CAP-3 hit@k, CAP-4 answers, CAP-5 size, CAP-6 phases, CAP-7 HTML. First run filed; 10/10 planted unanswerables ANSWERED by both versions. **2026-09-13 (W-153 SHIPPED):** `mtime` is on every `--json` hit — five readers, `required: always`, `null` a CLAIM. First amendment SR-API's frozen surface has taken. Prose output unchanged; sorting by it is NOT authorised. **2026-09-13 (W-152 SHIPPED):** `archived_weight` and `recency_half_life_days` are REMOVED, with `recency_multiplier` and `stats.json`'s `newest_mtime` — all three DOCUMENT priors are now gone. No ranking change: every one shipped as a no-op. SR-ARCHIVED-CONTENT decision 6 REVERSED; the W-73 bound test re-aimed at `[priority]`. **2026-09-13:** table-in-`flen` gained option **(d) lower `b`**; W-155 closes the *table IS the answer* gap; W-156 carries the environments-vs-single-corpus conflict.  **2026-09-13 (W-151 SHIPPED):** `[ranking] superseded_weight` is REMOVED — the knob, its key, its Node twin and its four records. A committed key is refused BY NAME. No ranking change: it shipped at `1.0`. The `superseded` FACT and the declared tie-break are untouched. **2026-09-13:** W-143 CLOSED — all four priors ruled; W-151/W-152 close three, W-153 covers the date gap, W-154 restates `rerank_weight` as a cost question. Ratified, not built.  **2026-09-13:** W-97 CLOSED (descoped) — both rows deleted, file archived, residue moved to W-143.  **2026-09-13:** `superseded_weight` ruled closed (W-143 → W-151); **ratified, not built** — the knob goes, the `superseded` fact stays.  **2026-09-13 (SR-WORK-OPEN-QUEUE):** the queue's 53 rules are stated once in [`records/0051_WORK-open-queue.md`](../records/0051_WORK-open-queue.md); this file is the LIST plus the declared ball legend, and states no other rule.  **2026-09-13 (SR rename):** `ADR-<NAME>` became `SR-<NAME>` and `docs/adr/` moved to `records/` at the repo root — one mechanical change, every reference repointed. Archived `ADR-NNNN` citations and `archive/v0.26-docs/adr/` are unchanged.  **2026-09-13:** W-145 and W-136 gained their 🟣 2026-09-30 gate sections, carrying the `↳ blocks:` text out of the inbox rows before those rows went (rule 3); W-87 gained the gate note.  **2026-09-12 (W-149 CLOSED):** the item file is DELETED with its row — outcome in `IMPLEMENTATION.md`, and every live link to it repointed (SR-LAW-10 three times). **2026-09-12 (W-149 measured): hazard 3 ANSWERED and it was wrong** — `.fux/node` links as a workspace in npm, pnpm, yarn 1 and bun; the glob control is what justifies the wiring, and the bin's location differs by manager. §4 hazard 3 rewritten, DoD rows 9 and 10 marked done, +row 10 on the records now being AHEAD of the code. **2026-09-12 (W-149 FULLY RULED): the item is 🟢 `agent`.** Third ruling — a monorepo is **auto-detected and wired up**, not declared. ⚠ **This file's own *declared, never detected* proposal was wrong on the precedent and says so**: SR-FETCHER decision 5 and W-86 fork E govern INGEST, where detection makes the *index* environment-dependent under L3, and `fux setup`'s scaffolding is not the index. §4 rewritten as the detection table plus five hazards — the first write to a file fux does not own, `ensure_layout` running on every ingest, and the **unmeasured** question of whether a dot-prefixed workspace path links at all in npm/pnpm/yarn/bun. §7 carries the execution prompt. **2026-09-12 (W-149, second ruling): the item NARROWED to one call.** Arpit ruled the reader is **bundled at publish time and shipped in both registries**, never bundled on the consumer — so the *no build step* line, the offline promise and SR-NODE-SEARCH decision 6 all survive, and §3 is now seven build obligations rather than a fork. What is left is declared-vs-detected for the workspace shape. **2026-09-12 (W-149 OPENED):** `W-149-the-consumer-gets-no-source.md` created with its row — Arpit ruled the consumer's `.fux/node/` carries no `src/` tree; the file holds the three candidate shapes and the two records (SR-NODE-SEARCH decision 6, the *no build step* line) that the ruling moves. **2026-09-12 (W-107 CLOSED):** its two files **deleted with its row** and retired to [`archive/open/`](../archive/README.md) — outcome in [`IMPLEMENTATION.md`](IMPLEMENTATION.md), evidence under [`regression/2026-09-12-node-tune-and-surfaces/`](regression/2026-09-12-node-tune-and-surfaces/report.md). New `W-148-what-the-two-readers-still-owe.md` carries the four obligations W-107 could not close; **it is `lane: arpit` and two of its rows are his calls**. **2026-09-12 (W-107):** four stale claims re-derived and corrected in W-107's file — Phase 0's last box, O1, O2, and the R4 twin test. **2026-09-12 (W-142, W-115 CLOSED):** both detail files **deleted with their rows**, outcomes in [`IMPLEMENTATION.md`](IMPLEMENTATION.md) and evidence under [`regression/2026-09-12-reaim-and-instruments/`](regression/2026-09-12-reaim-and-instruments/report.md) — rule 2, no tombstone. `W-115`'s successor is [VERDICT-W115](regression/2026-09-12-reaim-and-instruments/VERDICT-W115.md); `W-142`'s is [VERDICT-W142](regression/2026-09-12-reaim-and-instruments/VERDICT-W142.md). **`W-144` re-scoped to `lane: arpit`** — measured, and what is left is a ruling on [the compare doc](compare/table-tokens-in-flen.compare.md). **2026-09-12 (W-122):** phases 1, 4, 5 and 6 marked done; **§3a is Phase 1's delivered inventory** (26 rows, one per `CLAUDE.md` section) and §3's sketch is marked superseded rather than deleted; the two rows that need Arpit are named at the end of §3a. **2026-09-12 (positioning §6b):** `open/README.md` +`type: Index` frontmatter. **2026-09-12 (W-141):** `W-141-codex-skill-directory.md` deleted with its row. **2026-09-11 (balls primary):** README contract 1. **2026-09-11 (marks final):** README contract 1 → 🧨/🔺. **2026-09-11 (balls):** README contract 1 — a row opens with its ball. **2026-09-11 (rule 10):** every file gains *From OPEN-WORK (moved 2026-09-11)*; new `W-142-heading-negative-control.md`, `W-143-four-no-op-priors.md`; README contract 1 amended. **2026-09-11:** `W-115-chunking-quality-unmeasured.md` created — the row had existed with no detail file, breaking contract rule 1. ⚠ **Two open rows still have none** — the `heading` negative control and the four no-op priors. **2026-09-11 (W-130 closed):** its detail file moved to `archive/open/` with `status: implemented`. **2026-09-11: W-130's detail file** — the types file as TOML, blocked on its compare verdict. **2026-09-05: W-97's detail file** — the `--tune` blocker cleared, the playground-rows blocker restated with why it is Arpit's. **2026-09-05: W-111 closed**; three items remain, all on Arpit. **2026-09-05: W-110 closed** too; four items remain. **2026-09-05: W-109 closed** and retired to `archive/open/`; five items remain. **2026-09-05: W-108 closed** and its file retired to `archive/open/`; six items remain (W-106, W-107, W-109–W-112). **2026-09-02: −4 files.** `W-102`/`W-103`/`W-104`/`W-105` deleted with their rows: all four verified on a working shell and recorded in `IMPLEMENTATION.md`. **2026-09-01: +4 files** — `W-102-enrich-pii.md`, `W-103-accept-locks.md`, `W-104-enrich-skill-one-target.md`, `W-105-cdp-parallel.md`. All four **stay open**: the session that wrote them had no shell, so nothing was executed. **2026-09-01 (Phase 2 blocked):** W-98 §2 and §11 said `.fux/refusals.py`, a **code predicate**; corrected to the declarative `.fux/refusals.toml`, and §11's Phase 2 paragraph + socket constraint synced to the current prompt. **2026-09-01 (W-98 Phase 1):** the detail file's §5.1 amended to CDP interception, Risk 1 closed, the measured spike tables **bannered as history rather than rewritten** (they record what was actually run), and both executable spike sections replaced. **2026-09-01 (W-98): +[`W-98-acquired-plane.md`](../archive/open/W-98-acquired-plane.md)** — the acquired-plane spec, moved out of a `handoff/` directory it should never have been written into (retired 2026-08-18) and folded into one file per Arpit's ruling: §1–§10 the spec, §11 the execution prompt, a `**Model: Opus**` line added because the handoff carried none. **Then re-folded** when a concurrent session rewrote the retired directory's files underneath this one — merged, never clobbered. **§9 gains the spike's measured results (steps 1–3)**: `credentials:'include'` cannot be answered with `ACAO:*`, so cross-origin + cookies is structurally impossible; and the spec's own suggested spike host is CSP-blocked for the very tests it was named for. **2026-08-28: +[`W-97-tuner-knob-sweep.md`](../archive/open/W-97-tuner-knob-sweep.md)** with its row. **2026-08-28: down to ONE item.** W-82 (+ its rulings ledger), W-90 and W-91 archived; only `W-87-what-good-means.md` remains. ⚠ **A claim W-82's row was the only home of moved out first** — answer-time verification cannot fix recall, now [SR-URL-INGEST](../records/0107_url-ingest.md) decision 9. **2026-08-27 (W-91 OPENED): `open/W-91-the-provenance-plane.md` created with its index row**, per this directory's contract. It leads with the **law reversal**, because that is the part a later session must not misread as evidence the AOL risk was disproved. **2026-08-27 (W-89 RULED): [`open/W-89-does-l2-reach-a-query-log.md`](../archive/open/W-89-does-l2-reach-a-query-log.md) stamped `status: ruled`** with the verdict at the top and the original question kept beneath it — the reasoning that produced a call is worth keeping. ⚠ **Its `git mv` into `archive/open/` did NOT run** (no shell), so this row's trigger is half-served: the item is closed, the file has not moved, and the move is filed in OPEN-WORK rather than left implicit. **2026-08-27 (later): +[`open/W-90-the-confidence-plane.md`](../archive/open/W-90-the-confidence-plane.md)** — the confidence plane, built. ⚠ **Filed as W-89 first and renamed**: the concurrent session took that id the same day, so `open/W-89-the-confidence-plane.md` is a **STRAY FILE that must be deleted**. Two sessions minting ids from the same queue is the concurrency hazard `CLAUDE.md` §Two hazards names, and re-reading OPEN-WORK before writing is what caught it — one Edit earlier and it would have landed as a duplicate row. **2026-08-27: +[`open/W-89-does-l2-reach-a-query-log.md`](../archive/open/W-89-does-l2-reach-a-query-log.md)**, and W-87's file reconciled to Phase 0 **ruled**. ⚠ **W-87 is the counter-example to this row's own trigger** — its file was created without its index row, which is the half of *"created with its index row"* that no test enforces. Four files now: W-82, W-82's rulings, W-87, W-89, plus the README. **2026-08-26 (previous): W-86 CLOSED and archived.** Nine phases, ten forks. Row deleted from OPEN-WORK, detail file moved to `archive/open/`, six files repointed - plus one link fixed in a **concurrent session's** file that this move broke. ⚠ Three forks (F, G, I) moved with it and are **named in the archive row** rather than lost. **2026-08-26 (W-86 P2-P5): P2-P5 marked built; §6b's tree corrected** - it listed three OpenDocument modules and there is **one**, the single exception to one-module-per-format, with the reason recorded beside the tree. **2026-08-26 (W-86 §13): four follow-ups answered, +P8, +forks H/I/J.** ⚠ **A live L3 defect found in the fetcher contract:** `fetch()` returns *markdown*, so `http.py:43`'s *"both fetchers must produce the same markdown … or which fetcher retrieved a URL becomes visible in the index"* is **same sources → same index demoted to a comment**. `fetch() -> bytes` retires it and makes a PDF-serving URL indexable. **Two refusals recorded** — `fux decoder` may never install (L4), and tables are extraction's job not decoding's. **2026-08-26 (W-86 fork E RULED): +§12, +P7.** Consumer-owned decoders at `.fux/decoders/`, declared and committed; a missing dependency **fails ingest** rather than shrinking the index. ⚠ **`CLAUDE.md` and SR-LAWS are NOT amended and that is the finding** — the ruling is SR-ENRICH decision 1's table gaining a third row, not a new hole in L1. ⚠ The binding objection was **L3** (detection makes the index a function of the environment), not L1. §2/§5/§6/§4-DoD/§10 reconciled in the same edit. **2026-08-26 (W-86 filed): +`open/W-86-the-decoder-plane.md`** — the decoder plane, seven phases, seven forks. ⚠ Its starting finding is that the plane **already exists in `.fux/fetchers/`, twice**, and its P0 is a **live defect**: `.rst`/`.adoc`/`.org` are allowed types whose headings match nothing in `extract.py`'s `^#{1,6}` regex. ⚠ **A concurrent-session race was observed and then disproved inside one session** — `open/W-85…` read as an open detail file with no OPEN-WORK row; by the time it was checked against `archive/open/` and `git status` it had been **moved, correctly**, by the session that owned it. **The claim was a stale listing, not a defect, and re-deriving is what caught it.** `open/` holds W-86 and the README. **2026-08-26 (W-84): opened and closed in one session** — `open/W-84-heading-level-ask.md` was written, built and retired into `archive/open/` the same day (the map is the archive README). ⚠ **No OPEN-WORK row was ever added**: a row deleted in the change that created it is a tombstone. **One open item remains** (W-82) — one file, plus the directory README. **2026-08-26: W-85 created and archived the same day** — `archive/open/W-85-max-parallel-is-required.md`. **2026-08-26: W-83 created and archived the same day** — `archive/open/W-83-the-unconfigured-fetch-ceiling.md`, the unconfigured fetch ceiling. **2026-08-26 (W-82 built): five phases landed** — the URL health report, the detector, parallel fetch and the cap, the changed/unchanged line, and the agent surface. Nine records amended in the same change; 1 433 unit tests green. ⚠ `tests_e2e/` unverified in the build environment (fails identically on a clean tree). ⚠ 3.0 and 3.5 did not land — neither is a code task. **2026-08-26 (later): W-82 gains §3.6 — the agent surface and the venv invocation ladder** (Arpit). It fixes a **live** defect: `fux.agent.md` tells an agent to fall back to ordinary search when `fux` is not on `PATH`, so an unactivated `.venv` reads as *not installed* and the agent silently uses grep. Four-rung ladder, probed with `--version`, never activating anything; ⚠ rung 4 (`python -m fux`) **does not exist** — no `__main__.py`. Four new forks. **2026-08-26 (the consolidation): W-74, W-75, W-77 and W-81 merged into [`open/W-82-the-consolidated-build.md`](../archive/open/W-82-the-consolidated-build.md)** — a merge, not a close; every fork stays open. Nine documents archived (4 open detail files, 3 proposals, 2 compare docs), each with a successor row in [`archive/README.md`](../archive/README.md). ⚠ **Both compare verdicts were folded into W-82 §4 VERBATIM before the move**, because archiving a compare doc makes its verdict uncitable. Four calls made the same day; `update --warm` and `answer --memo` **withdrawn**. **2026-08-24: +`open/W-80-the-bundled-model-has-no-live-recipe.md`**; W-79 closed and retired. Five files, one per open item **2026-09-13 (W-157 CLOSED):** the archive-never-delete rule is executed and gated — **20 files recovered for 19 ids** into `archive/open/` (nine from committed history, eleven from `git fsck --unreachable` loose objects), **20 ids lost for good** and 12 that were never files, each written down. W-157's own file archived. New gate: `tests/test_no_work_item_is_lost.py`. **2026-09-15 (W-168 step 1 BUILT, Claude Code):** the build written into `W-168-search-improvements.md`; `W-184-differential-harness-utf8.md` filed — the differential harness cannot run on this repository and has not been able to. |
| [`OPEN-WORK.md`](OPEN-WORK.md) | **Any work item or prediction changes state** | 2026-09-15 | **2026-09-15 (W-178, Claude Code):** W-178 closed and its row deleted. **2026-09-15 (W-177, Claude Code):** W-177 closed and its row deleted; **W-193 filed 🔴** (argparse exits 2, SR-CLI decision 5 says it never does) and it is the only inbox row. W-144's row shortened under rule 10. **2026-09-15 (golden unblock, Cowork):** every 2026-09-30 gate void — W-168 🟢, W-176 🟡 on W-136, W-161 🟡 on W-191, W-175 🟡 on W-192; three concurrent-session rows trimmed to rule 10. **2026-09-15 (Node column BUILT, W-188):** W-188's row rewritten — built, awaiting a run. · **2026-09-15 (Node as a column, W-188):** W-188 added under `testing`, 🟢 `agent`. · **2026-09-15 (W-187, the CAP slide spine):** W-187's row added and removed the same day; the queue is unchanged otherwise. · **2026-09-15 (CAP slide spine, Cowork):** W-187 added under `testing`, 🟢 `agent`. · **2026-09-15:** the inbox has its first row since W-168 step 1 — W-144's three options. · **2026-09-15:** W-185 filed and closed the same day; W-140 re-balled 🟢. · **2026-09-15:** three rows deleted (W-182 · W-183 · W-184), W-185 added, W-154 and W-140 re-balled. · **2026-09-15 (🟡 needs an item, Cowork):** every 🟡 row repointed from a prose wait to `waiting on W-nn`; five 🟢 rows added (W-179–W-183). **2026-09-15 (W-168 step 1 RULED (c), Cowork):** inbox row and `↳ blocks:` sub-row deleted, inbox declared `*Empty since 2026-09-15*`, W-168 re-balled 🔴 → 🟢. **2026-09-15 (rule 23a):** every 🟡 row now names the item it waits on, or says **`not a queue item`** in those words — and all five said the second, which is the finding: **no 🟡 row in the queue is blocked by anything anyone has filed**. W-146's row and its inbox row left with the close; §adr update is empty and gone. **2026-09-14 (W-174 CLOSED, Cowork):** the row deleted — `fetch_at_answer` shipped; reconciled against the new `IMPLEMENTATION.md` entry. **2026-09-14 (W-174 filed, Cowork):** `[sources.url] fetch_at_answer` ratified, not built — answer-time URL citations pin to `.fux/acquired/`, refer stays on, no socket opens. Filed because the mode already exists (`Policy(mode=NEVER)` + `_obtain`'s never-branch) and `refer_answer.py` hard-codes `ALWAYS`, so nothing could select it. **2026-09-14 (W-165 + W-166 CLOSED, Claude Code):** both rows deleted and both detail files removed, reconciled against the new `IMPLEMENTATION.md` row. Nothing else touched — a concurrent Cowork session was mid-renumber on W-167–W-173 throughout. **2026-09-14 (backlog sweep, Cowork):** 21 rows left `BACKLOG.md` — 18 promoted into W-163/W-164/W-165/W-166, 3 resolved by correcting their stale source sentences (B-032 SR-T1-ACCELERATOR, B-239 SR-REFUSAL, B-240 SR-CONFIG); B-242/B-243 added for the two 3.0 proposals; B-129/B-133/B-140/B-175 repointed at the 3.0 docs. **2026-09-13 (3.0.0-alpha.0 branch, Cowork):** five work documents filed from the session's rulings — compare docs `ask-graph-expansion` (accepted), `fux-correct` (accepted), `abstention-gates` (proposed; two rulings recorded); proposals `search-improvements-v3`, `fux-inspect`; items W-160 / W-161 / W-162. Nothing built. **2026-09-13 (W-147 SHIPPED):** the journal's two consent surfaces are BOUND by a test (all four states) and the specimen line now opens `⚠ NOT a rendering key`. Found in the build: `output.toml` is a COMPLETE declaration, and `provenance.py` named the REJECTED `tune.toml` home. **2026-09-13 (W-147 RULED):** the answer journal has **two** consent surfaces — `fux answer --journal` and a committed `[cli.answer] journal = true` — both explicit, ruled (a) by Arpit. Its inbox row and `↳ blocks:` sub-row DELETED, the item re-balled 🔴 → 🟢 and re-laned `arpit` → `agent` under *fux build*. **Ratified, not built:** a test binding both surfaces and a specimen line that says the key writes are owed.  **2026-09-13 (W-154 Part A):** proximity reranking PRICED — +15 to +19 ms p50, **flat in corpus size**, **same on `ask` and `answer`**, 0/124 queries faster. Surface capture, no verdict. W-154 stays open on Part B. **2026-09-13 (W-155 SHIPPED):** the table-is-the-answer gap is MEASURED and the answer is YES — `dump` 30/30 → 0/30, `content` 0/30 → 30/30, both controls hold. (b) is right for two document shapes and wrong for a third. Pre-registration committed alone at `a414012`. **2026-09-13 (archive, never delete — W-157):** closing a row now MOVES its detail file to `archive/open/` with an `archive/README.md` map row — [SR-WORK-OPEN-QUEUE](../records/0051_WORK-open-queue.md) rules 54–58. W-157 filed to recover the 35 files deleted under the old, wrong trigger.  **2026-09-13 (W-150 SHIPPED):** the benchmark harness files all SEVEN captures — planted key (no corpus byte moved), CAP-2 rows, CAP-3 hit@k, CAP-4 answers, CAP-5 size, CAP-6 phases, CAP-7 HTML. First run filed; 10/10 planted unanswerables ANSWERED by both versions. **2026-09-13 (W-153 SHIPPED):** `mtime` is on every `--json` hit — five readers, `required: always`, `null` a CLAIM. First amendment SR-API's frozen surface has taken. Prose output unchanged; sorting by it is NOT authorised. **2026-09-13 (W-152 SHIPPED):** `archived_weight` and `recency_half_life_days` are REMOVED, with `recency_multiplier` and `stats.json`'s `newest_mtime` — all three DOCUMENT priors are now gone. No ranking change: every one shipped as a no-op. SR-ARCHIVED-CONTENT decision 6 REVERSED; the W-73 bound test re-aimed at `[priority]`. **2026-09-13:** table-in-`flen` gained option **(d) lower `b`**; W-155 closes the *table IS the answer* gap; W-156 carries the environments-vs-single-corpus conflict.  **2026-09-13 (W-151 SHIPPED):** `[ranking] superseded_weight` is REMOVED — the knob, its key, its Node twin and its four records. A committed key is refused BY NAME. No ranking change: it shipped at `1.0`. The `superseded` FACT and the declared tie-break are untouched. **2026-09-13:** W-143 CLOSED — all four priors ruled; W-151/W-152 close three, W-153 covers the date gap, W-154 restates `rerank_weight` as a cost question. Ratified, not built.  **2026-09-13:** W-97 CLOSED (descoped) — both rows deleted, file archived, residue moved to W-143.  **2026-09-13:** `superseded_weight` ruled closed (W-143 → W-151); **ratified, not built** — the knob goes, the `superseded` fact stays.  **2026-09-13 (SR-WORK-OPEN-QUEUE):** the queue's 53 rules are stated once in [`records/0051_WORK-open-queue.md`](../records/0051_WORK-open-queue.md); this file is the LIST plus the declared ball legend, and states no other rule.  **2026-09-13 (`kind`):** every record declares `kind: law **2026-09-13 (W-157 CLOSED):** its row deleted and the file archived — the recovery ran, and every `W-nn` id in the queue now has a file a test can find. | component | **2026-09-14 (blocker walk, Cowork):** inbox reduced to W-140's `--hops` row; W-156 and W-112 closed; W-176 opened; seven rows moved to the `agent` lane. process` — the gate it answers to. 12 · 52 · 4. SR-WORK-OWNERSHIP decision 11; register §The three kinds.  **2026-09-13 (SR rename):** `ADR-<NAME>` became `SR-<NAME>` and `docs/adr/` moved to `records/` at the repo root — one mechanical change, every reference repointed. Archived `ADR-NNNN` citations and `archive/v0.26-docs/adr/` are unchanged.  **2026-09-13:** rule 6 gains the 🟣 ball, its precedence, the `YYYY-MM-DD` requirement and *a date-gated item is not in the inbox*; W-145 and W-136 left *Blocked on Arpit* (nine rows → seven), W-87 re-balled 🟣; W-147's `↳ blocks:` sub-row reworded to `nothing else in the queue`.  **2026-09-12 (W-149 CLOSED):** the row is removed, rule 2 — L10 satisfied, decisions 13-16 built. **2026-09-12 (W-149 measured):** its row now names the probe. **2026-09-12 (W-149 ruled): its inbox row and `↳ blocks:` sub-row DELETED and the item re-balled 🔴 → 🟢, lane `arpit` → `agent`** — nothing in it waits on Arpit now. **2026-09-12 (W-149 narrowed):** its inbox row and `fux build` row rewritten — the three-shape fork is ruled (A + C, bundled at publish); one call left. **2026-09-12 (W-149):** +inbox row, +`↳ blocks:` sub-row, +one `fux build` row — 🔴 `arpit`, the shape of what `fux setup` vendors. **2026-09-12 (W-107 CLOSED):** its row **deleted**, replaced by `W-148` in the `arpit` lane and in the inbox. **2026-09-12 (W-107):** npm publication landed — the row now names the `fux doctor` PATH row the R1a amendment owes. **2026-09-12 (cleanup, re-derived not read):** W-97 and W-146 moved to `adr update` — both now need only a ruling. 🔴 **W-87 re-balled 🟡 → 🔴**: its Part B waits on W-145, so W-145's `↳ blocks:` no longer says *nothing else in the queue*. **W-106's two recorded blockers are STALE** — search-v3 §8 was ratified 2026-09-05 and the x86-64 arm is reachable via Rosetta; row says so. W-144 stays 🟢 with its unblocked path named; W-115 loses 🧨 (static damage, not accruing); W-138 carries the re-derived count (42 files name the playground). Rule 6 gains the four verbs the ball gate actually reads. **2026-09-12 (positioning §6b):** +`type: Queue` frontmatter. **2026-09-12 (W-145): the inbox row turned over and six rows went 🔴 → 🟡.** W-136 phase 1's *did Codex write the questions* row is gone — it is answered — replaced by **W-145**, Codex regenerating the Claude-authored stopgap key, whose `↳ blocks:` sub-row says **nothing**: it governs what a phase-5 number may claim, not whether work runs. W-136 itself is **🟢 `agent`** with phase 2 runnable now; W-106, W-115, W-97, W-142, W-87, W-144 and W-143 were red *through* phase 1 and are now 🟡 waiting on phase 2. W-112 stays 🔴 — it is Arpit's ruling, not a chain. **2026-09-12 (W-141):** closed — inbox row, blocks sub-row and lane row removed. **2026-09-11 (balls primary):** ball always (🔴 transitive/🟡/🟢) + optional 🧨/🔺; rule 6 rewritten. **2026-09-11 (marks final):** 🟣→🧨, ⏫→🔺; "ball"→"status mark". **2026-09-11 (blocks):** each inbox row gains a `↳ blocks:` sub-row; rule 10. **2026-09-11 (balls):** 🔴/🟣/🟡/🟢 one per row + ⏫ Arpit's, legend in header + rule 6; supersedes 🔥/🟠. **2026-09-11 (W-140): twelve rows closed, one moved to W-122; the row now reads 6 code rows + 13 record/code disagreements.** **2026-09-11 (marks):** legend 🔥/🟠/none in header + rule 6; 🔴 retired as a row mark. **2026-09-11 (rule 10): one or two lines per item**; detail moved into `open/`; W-142/W-143 opened for the two id-less rows; gated by `tests/test_open_work_rows_are_short.py`. **2026-09-11: four priors ruled (b); inbox row out; W-136 row asks for prompt 1b.** **2026-09-11 (decision 15): W-140 and W-141 filed; W-141 inbox row.** **2026-09-11: abstention inbox row → proposals/abstention-gate.md.** **2026-09-11 (SR-WORK-ENVIRONMENTS ratification): W-115, W-97, heading control, W-106, W-112, W-107 re-laned behind W-136; four priors down to (b)/(c).** **2026-09-11 (SR-WORK-ENVIRONMENTS): W-138/W-139 filed; R-11 row deleted (void); four-priors (a) void; W-87 re-laned.** **2026-09-11 (cleanup): rule 2 enforced against the file itself — 369 → 336 lines.** Six tombstone blocks deleted (five struck-through search-v3 items with landing notes, W-130's removal note, W-116's outcome inside W-115's row, two ✅ *blocker CLEARED* blocks in W-97, the four-priors *disclosure SHIPPED* line and its *merged from four rows* note). 🔴 **Two stale claims corrected, both of which would have misdirected a session**: the search-v3 block said W-110's gate was *AMBIGUOUS and in the inbox above* when Arpit **VOIDED** it on 2026-09-06 and the row was deleted, and W-87 said the `judged` series waited on W-134, which landed the same day — that half of W-87 is `agent` work and runnable now, so its lane is split. The four-priors list headed *none of which exists today* had a first bullet saying it landed. **2026-09-11 (W-137): the renumber LANDED and the row is deleted** — 54 records +90, both suites green. **2026-09-11 (W-137): SR renumber filed — laws 0001–0100, others 0101+.** **2026-09-11 (W-136): the sealed golden benchmark filed; inbox row for phase 1 (Codex).** **2026-09-11 (reconcile): W-116 re-laned `agent` (stale `fux-lab` blocker); W-87 inbox row states the R-11 ruling; inbox 1.** **2026-09-11 (partial goldens): resolved by a third blind annotator; row folded into W-134; inbox 2.** **2026-09-11 (W-135): headroom obligation ratified; inbox row → agent item; inbox 3.** **2026-09-11 (W-130 closed):** W-130's inbox row, item and the `types_file` defect deleted - outcome in IMPLEMENTATION; W-122's two `types_file` sentences now say it was removed. **2026-09-11 (rule 9): no git housekeeping in the queue; push row and every commit/staged status line removed; W-134 filed; enrichment row closed; inbox 4.** **2026-09-11 (rulings): W-132, W-133 filed; W-118 re-filed `agent`; W-121 closed; inbox 11 → 8.** **2026-09-11 (CLEANUP): 502 -> 413 lines.** Four rows about the four no-op ranking priors merged into ONE (two inbox lines, W-94, an *adr update* row — all blocked on one action). W-117's ✅ tombstone deleted, legal only because W-115/W-117/W-119/W-120/W-121's outcomes were written to IMPLEMENTATION first — none of the five was in it. Four permanent facts moved to [SR-RS](../records/0133_predictions.md) decision 21 before their rows went; the `validate()` row deleted as a restatement of SR-DOTFUX decision 6; the one-item *Measurement plumbing* section folded into *testing*. W-122's row re-derived — it claimed its rename was uncommitted and it has been committed since `5cf30bc`. **2026-09-11 (W-126/W-127 closed):** both rows DELETED, their outcomes in IMPLEMENTATION; three references repointed at [SR-ARCHIVED-CONTENT](../records/0134_archived-content.md) decision 1a rather than left dangling at a deleted row (rule 3). **2026-09-11 (W-129):** W-129's two rows deleted (landed); the W-126 row notes the suite is green again. **2026-09-11 (the shell session):** the 🔴 HANDS inbox row rewritten to what is actually left — `_to_delete/` gone, suite green on the Mac, `ruff` **is not a gate**; what remains is the `test_sr_freshness` red on `94231b2bf`, diagnosed as the `describes` relation over-firing. **W-115 and W-119 rows DELETED** (outcomes in IMPLEMENTATION). Both `[index]` rows deleted. **+1 row**: `fux doctor` reports `[OK]` for a `fux.toml` that will not load. The rule-4 preamble rewritten — the environment-blocker failure has now fired three times. **2026-09-11 (`[index]`):** +2 rows under *fux build*: commit the `[index]` move; `pii-digest` written before extraction. **2026-09-06 (W-110):** **the `k` inbox row DELETED** — Arpit voided the gate rather than naming a `k` after the numbers; the verdict is filed and the feature's claim demoted. **2026-09-06 (R-3):** **the `log()` inbox row DELETED** — Arpit ruled option (b); W-107's item row and the search-v3 group both restated, and *"every remaining search-v3 item is on Arpit"* corrected to W-112 alone. **2026-09-06 (later):** **W-114 deleted** (outcome in IMPLEMENTATION, rule 2); **W-118 filed** - do `fux-decoder`/`fux-usage` get `.github/skills/` too? Doing nothing is legitimate. **2026-09-06:** W-114 filed (`arpit`, *adr update*) plus its inbox row - one verdict owed on the Copilot skill surface; the code half landed the same day. **2026-09-05 (W-101): W-101's row DELETED, and with it the recency-prior plumbing row the same pass closed.** The two rows found stale are deleted too — `tests_e2e/` runs on `windows-latest` py3.11/3.14 on every push (green on `80ee187`), and the clean-corpus `recall@k` is the doc2query `none` arm, whose numbers moved into SR-WORK-QUALITY before the row went. **2026-09-05 (later): the inbox gained a pointer to [`proposals/unblock-2026-09-05.md`](../archive/proposals/unblock-2026-09-05.md)** — a proposed ruling per row; two rows named as stale on re-derivation (Windows e2e runs in CI; clean recall = the doc2query `none` arm). **2026-09-05: W-111's row deleted**; a new *Measurement plumbing* row — a corpus copied out of git loses the entire recency prior and nothing reports it. **2026-09-05: W-110's row deleted**; two new `Blocked on Arpit` rows (its gate's `k`, and whether the prior enrichment measurements need re-running). **2026-09-05: W-109's row deleted on landing**; W-97 gains a third knob (`expand_weight`) and the note that `rerank_weight` now moves two mechanisms. **2026-09-05: search v3 RATIFIED** — seven rows flipped `arpit` → `agent`; **W-108's row deleted** on landing; the `Blocked on Arpit` ages recomputed (they were written 2026-09-01 and read `0d` on 2026-09-05) and W-107 Phase 0's unstruck `log()` choice added. **2026-09-02 (W-102–W-105 closed): −4 rows.** Verified against a real shell, two of them against measured runs; nothing left tombstoned. **2026-09-01 (W-102–W-105): +4 rows**, and the unnumbered *`.fux/enrich/` is committed and unredacted* row became W-102 — widened, because the leak reaches `.fux/index/` and not only the committed file. **2026-09-01 (cleanup + release): 442 → 331 lines.** W-98 (four phases landed) and the PII item CLOSED and their rows deleted — outcomes in [`IMPLEMENTATION.md`](IMPLEMENTATION.md), detail file archived — with their residue folded into ONE item, W-101's `doctor` pass. ⚠ **The `Blocked on Arpit` block CLAUDE.md requires DID NOT EXIST in this file**; written, with `filed` + age per row and two new rows (the ETag criterion; whether the twice-recorded W-83 shape gets a gate). ⚠ **An id collision reconciled**: the PII work was filed as W-99, already taken by the decoder map — recorded as **W-103**. **2026-09-01 (red-suite sweep): W-100 and W-102 rows DELETED** — both closed, outcomes in [`IMPLEMENTATION.md`](IMPLEMENTATION.md); W-101's *"pairs with W-102"* line corrected, and W-98's row records the two live defects the sweep found (a `NameError` on every retaining fetch; `refer` raising on an unconfigured repo). **2026-09-01: W-98 Phase 2 marked 🔴 BLOCKED** — `refusals.toml`'s `status`/`final_url_*` conditions and the off-origin floor trip [SR-FETCHER](../records/0117_fetcher.md) decision 13's own veto check. **2026-09-01: W-98 🔴 → 🟡, Phase 1 landed.** The row's *"`cdp.py` returns a DOM snapshot with the type hardcoded"* was corrected in place; the blocked-on-spike paragraph replaced with what the spike measured. ⚠ Two items carried forward as **Arpit's calls**: an acceptance criterion that interception cannot meet as written, and the stray `handoff/` directory under `work/` — **the second was closed 2026-09-01 and was never his**: the law says *move to the one archive*, not delete, so the executed pair is now in [`archive/handoff/`](../archive/README.md) with a successor row. **2026-09-01 (W-98): +W-98** (`agent`, under *fux build*) — browser-session resource fetching + the acquired plane, 🔴 **blocked on a spike** nobody has run — narrowed the same day once steps 1–3 were measured: the 302 was never the problem, the credentialed wildcard is, and step 4 now asks one question. **2026-08-28 (W-97): +W-97** (`agent`, the knob sweep) under *testing*, pointing at `benchmark/PRE-REGISTRATION-TUNER.md` and its runbook. **2026-08-28 (W-93): W-93's row DELETED — executed, not descoped** — and three items filed from what came out of it: **W-94** (`arpit`, `superseded_weight` ships as a no-op — Arpit's call, and doing nothing is also a choice), **W-95** (`agent`, the marker suite is saturated and must never be a primary endpoint again), **W-96** (`agent`, a `blind` benchmark needs two sessions). The per-query-rows row is **partly closed**: a harness emits them and a gate checks them, but `fux-lab/shared/regress/run.py` still emits totals only. **2026-08-28: CUT FROM 209 LINES TO 150, and most of what went was tombstones** — struck-through table rows, ✅ notes, and a section whose whole content was *"Empty. All seven closed."* Rule 2 forbids all of it: **[SR-WORK-OPEN-QUEUE rule 3](../records/0051_WORK-open-queue.md) held, and the tombstones went. W-82, W-90 and W-91 closed and archived; what is left is two decisions, two authoring tasks that need someone who has not read the goldens, and two test-surface gaps. Rules 2, 3 and 4 gained the specific failures that produced them. **2026-08-27 (third pass): W-87 P1's blocker was FALSE too** — two of its three controls built within the hour of looking. Only the sealed subset is left and it is a judgement, not a build. **2026-08-27 (later): the whole §"Blocked on an environment that does not exist on the build machine" was FALSE** — both `fux-lab` and `fux-playground` were on the machine, and one of the six items it held was R10, which then ran. Section corrected and now leads with *re-derive before believing a blocker*. Hands is down to ONE item (L8). **2026-08-27 (queue-clearing session): FIVE hands items CLOSED and removed** — the five git operations ran on a machine with a shell. Two remain (one real external URL for the daemon; the L8 sanity-check). Three test-surface claims reconciled against measurement, and **one of them was WRONG**: *"four hook tests go green-by-vacuity"* is one, measured. **2026-08-27 (W-91 OPENED): an inbox line and an item row added** under a new SR-PROVENANCE · SR-LAWS · SR-CLI · SR-ASK group. The inbox line leads with **⚠ a law was reverted — read L8 first**. **2026-08-27 (W-89 CLOSED): both W-89 rows deleted** — the inbox line and the item row — under rule 3, its outcome now in [`IMPLEMENTATION.md`](IMPLEMENTATION.md). W-87's *"fork 6 was NOT settled"* sentence corrected to the ruling, and the stray-files hands item goes **two `git rm` → three operations**, gaining W-89's detail-file `git mv` into `archive/open/`. ⚠ **The `git mv` did not run** — no shell this session (the sandbox `useradd` failure), so the file is stamped `status: ruled` and left in place rather than duplicated. **2026-08-27 (later): +W-90, the confidence plane**, under a new record grouping naming SR-CONFIDENCE, plus an inbox line: the cutoff policy was **asked and not answered**, so the item names the assumption made rather than burying it. ⚠ **R10 is registered and unmeasured, and it is not a threshold sweep** — [SR-WORK-QUALITY](../records/0056_WORK-quality.md) had frozen `t = 0.75` hours earlier, so the floor is a **proxy calibrated to that**, not a second abstention threshold; and `separation` is **ordinal where Chow's rule assumes a probability**. **2026-08-27: +W-87 and +W-89, and the inbox is no longer `Empty`.** ⚠ **W-87's row is LATE, not new** — the item was filed 2026-08-27 as a detail file under `open/` with **no index row at all**, so the queue read as one item while it was two, and the *Blocked on Arpit* block said `Empty` while three things sat on him. That is rule 3's understating direction, and nothing mechanical catches it. W-87's row records Phase 0 **ruled** (all six forks, SR-WORK-QUALITY) and that its remaining phases are blocked on **environments, not decisions**; W-89 is the L2/query-log law question fork 6 deliberately did not settle. **2026-08-26 (previous): W-86 removed on closing.** **2026-08-26 (W-86 filed): the queue is two items again** — the decoder plane, under a **new record grouping naming `SR-DECODE`, which does not exist yet**. That is Law zero said out loud at filing time rather than discovered at commit time: the item knows which record it will have to write. **2026-08-26 (W-85 filed and closed the same day): `max_parallel` is now REQUIRED and `[sources.url]` ships live** — W-83's "exposed" property was a commented line inside a commented table, and `setup`'s write-if-missing meant it reached no existing `fux.toml`. **2026-08-26 (W-83 filed and closed the same day): the queue returned to one item.** The parallel-fetch ceiling — filed, built, closed; row deleted and the detail file archived. **2026-08-26 (W-82 built): five phases landed** — the URL health report, the detector, parallel fetch and the cap, the changed/unchanged line, and the agent surface. Nine records amended in the same change; 1 433 unit tests green. ⚠ `tests_e2e/` unverified in the build environment (fails identically on a clean tree). ⚠ 3.0 and 3.5 did not land — neither is a code task. **2026-08-26 (later): W-82 gains §3.6 — the agent surface and the venv invocation ladder** (Arpit). It fixes a **live** defect: `fux.agent.md` tells an agent to fall back to ordinary search when `fux` is not on `PATH`, so an unactivated `.venv` reads as *not installed* and the agent silently uses grep. Four-rung ladder, probed with `--version`, never activating anything; ⚠ rung 4 (`python -m fux`) **does not exist** — no `__main__.py`. Four new forks. **2026-08-26 (the consolidation): W-74, W-75, W-77 and W-81 merged into [`open/W-82-the-consolidated-build.md`](../archive/open/W-82-the-consolidated-build.md)** — a merge, not a close; every fork stays open. Nine documents archived (4 open detail files, 3 proposals, 2 compare docs), each with a successor row in [`archive/README.md`](../archive/README.md). ⚠ **Both compare verdicts were folded into W-82 §4 VERBATIM before the move**, because archiving a compare doc makes its verdict uncitable. Four calls made the same day; `update --warm` and `answer --memo` **withdrawn**. **2026-08-25 (latest): W-78 CLOSED** — both rulings made, neither built anything. Queue is four. **2026-08-25 (later): W-78's determinism leg weakened by measurement** — not removed. The ruling is still Arpit's and still ruling 1 only. **2026-08-25 (cleanup): the queue was REVIEWED, not trimmed — all five items are live and four are `arpit`-lane rulings.** W-77 grew two governance items found by the sweep (the SR status vocabulary has no value for a record whose subject ceased to exist; a frozen report's pre-registration link points at a path that never held it). **2026-08-25 (later): W-80 CLOSED by dissolution** — the bundle it was about was deleted, so there is no provenance left to be missing; neither fork it offered was taken. **W-81 grew two items**: a scope defect in SR-RS decision 12 (an informed run may not state a delta, which as written forbids reporting a file size) and an unbuilt orphaned-module check. **2026-08-25: W-78 ruling 2 RULED (accepted, rewritten) and +W-81** (`agent`) — the accepted rule's unbuilt half, the sealed query subset and the decoy/placebo control arms. The inbox lead-in is corrected: the *zero broken* diagnostic is p ≈ 0.49 and the statistic that carries is the **concordance**; `+1`/`-1` are below the new resolution floor. W-78 stays open on ruling 1 alone. **2026-08-24 (latest): +W-80** (`agent`) — the bundled model's recipe is archived while live code points at the live path. Queue is five |
| [`BACKLOG.md`](BACKLOG.md) | **A record, proposal or compare doc names something outstanding**, or a `B-nnn` is promoted into the queue — same change, always | 2026-09-15 | **2026-09-15:** B-249 — the quality-endpoint proposal. · **2026-09-14 (blocker walk, Cowork):** `B-175` promoted to W-176; `B-245` added for the parked vector plane. **2026-09-14 (cage-search-leg):** proposal for fux's half of cage's search leg filed; B-244. **2026-09-14 (backlog sweep, Cowork):** 21 rows left `BACKLOG.md` — 18 promoted into W-163/W-164/W-165/W-166, 3 resolved by correcting their stale source sentences (B-032 SR-T1-ACCELERATOR, B-239 SR-REFUSAL, B-240 SR-CONFIG); B-242/B-243 added for the two 3.0 proposals; B-129/B-133/B-140/B-175 repointed at the 3.0 docs. **2026-09-13 (new):** the first sweep — all seventy records read, **241 rows** in five classes (`unbuilt` 49, `ungated` 41, `unmeasured` 50, `unruled` 50 incl. 13 parked proposals, `cost` 51). ⚠ Its length is not a signal, and the sweep is a claim rather than a proof. Rules in [SR-WORK-BACKLOG](../records/0055_WORK-backlog.md); the thirteen items the queue holds carry no row. |
| [`../docs/paper/the-fux-index-paper.md`](../docs/paper/the-fux-index-paper.md) | Architecture changes; a P-prediction gets measured (M7 updates §5–6 to measured) |  2026-09-12 | **2026-09-12 (positioning §4.7):** §2 *Adjacent tools* — the non-existent *"symbol edges"* removed; fux joins a code tool only where a document names a file path. **2026-08-25 (cleanup): a status banner, and deliberately NO rewrite.** Three of the paper's committed-index components were never built or were built and deleted — **pruned** term statistics (falsified by P1-RERUN), **dense binary codes** (deleted 08-25), and the **six-prefix MST keyspace** (superseded 2026-08-09). §5's size model and §6's latency model quote figures resting on them. **The models are not re-derived** — that would mean inventing numbers nobody measured. ⚠ §4 stays untouched by Arpit's standing ruling in `BLOCKED.json`. **2026-08-22 (W-26): §5 and §6 rewritten from projection to MEASURED at 10 000 documents** — 2.3 MB packed and 12.46 ms worst-case warm p95, with the 10⁶ figures **relabelled as deferred-target projections, not deleted**. Also corrected: the abstract, §1.3's `~250 B/doc` and its P1 claim (P1 FAILed), and §8's P2/P3 rows, both retired with plan revision 1 and still presented as live gates. ⚠ **§4 is untouched** — its staleness is architectural (it describes the MST keyspace `index-format.compare.md` superseded), which is a rewrite, not a measurement, and is Arpit's. **Moved `docs/paper/` → `work/paper/` 2026-08-18** (it is mid-rewrite: §4–§6 are rewritten to measured at M6). Prior: Architecture of record + figures |
| [`../records/BIBLIOGRAPHY.md`](../records/BIBLIOGRAPHY.md) | **A source is added to any record's *Papers and specifications* block, a compare doc's *References*, a proposal or an environment note — or a listed source changes status** (parked graduates, refused reopens, not-built gets built, a measurement falsifies it) | 2026-09-13 | **2026-09-14 (correction):** the RRF row said *"and the fusion inside `--expand`"* — **false**. `--expand` does no fusion; it is a weighted term multiplier inside a single `rank()` call. Corrected in place, with the old claim named rather than silently swapped. **2026-09-13 (second sweep, same day):** re-swept against the five documents filed after the first pass — **+16 sources, 3 rows moved**. New: §1b, the ten parked ranking ideas with a paper each (anchor text, RM3's actual lineage, SDM, MMR, the cluster hypothesis, INEX); §1c, the `fux inspect` diagnostics four (Zipf, Heaps, Broder minhash, Azzopardi retrievability); Haveliwala's Topic-Sensitive PageRank under the accepted graph-expanded `ask`; FALCON answer-type matching as the one abstention gate that reaches the u017 class; Rocchio and doc2query-proper under the accepted `fux correct`; Solr elevate as the `--pin` prior art. ⚠ **Method defect found and fixed**: the first pass read only each document's final `References` block, and these five use `## Reference` singular or cite inline. **2026-09-13 (moved to `records/` the same day, Arpit):** it sits beside the register it indexes, not in `work/` — it is a **support file** of `records/`, like `README.md` and `TEMPLATE.md`, and deliberately **not a numbered Standing Record**: it owns no `src/` component, rules nothing, and has no veto condition. **2026-09-13 (new):** first sweep — every external source in `records/`, `work/compare/`, `work/proposals/`, `work/setup/`, `work/paper/` and the cited `tools/` harnesses, in one table per area, each row ending in what DEPENDS on it or what was NOT BUILT because of it. ⚠ It is an index of the repo's sourcing, **not** an authority: where it and a record disagree the record wins. `work/regression/**` is deliberately not swept — runs cite records, not the reverse. |
| [`INTERVIEW.md`](INTERVIEW.md) | Direction/strategy/major decision changes — **kept current during the session** | 2026-09-15 | **2026-09-15:** the W-146 link repointed into `archive/open/` with the close. **2026-09-14 (fux.toml audit, Cowork):** the four rulings and the broken-config-table finding. **2026-09-14 (three fux.toml rulings, Cowork):** the W-174 block gains the live-keys ruling, the closed-domain rule it establishes, and the template move. **2026-09-14 (W-174 SHIPPED, Cowork):** the ratification block replaced by what landed, incl. the branch-with-a-test-and-no-selector lesson and the concurrent-tree note. **2026-09-14 (W-174, Cowork):** a §1 block on the never-fetch mode being built and unselectable, and why `fetch_at_answer` is neither `--no-refer` nor `update=never`. **2026-09-14 (backlog sweep, Cowork):** 21 rows left `BACKLOG.md` — 18 promoted into W-163/W-164/W-165/W-166, 3 resolved by correcting their stale source sentences (B-032 SR-T1-ACCELERATOR, B-239 SR-REFUSAL, B-240 SR-CONFIG); B-242/B-243 added for the two 3.0 proposals; B-129/B-133/B-140/B-175 repointed at the 3.0 docs. **2026-09-13 (3.0.0-alpha.0 branch, Cowork):** five work documents filed from the session's rulings — compare docs `ask-graph-expansion` (accepted), `fux-correct` (accepted), `abstention-gates` (proposed; two rulings recorded); proposals `search-improvements-v3`, `fux-inspect`; items W-160 / W-161 / W-162. Nothing built. **2026-09-13 (W-154 Part A):** proximity reranking PRICED — +15 to +19 ms p50, **flat in corpus size**, **same on `ask` and `answer`**, 0/124 queries faster. Surface capture, no verdict. W-154 stays open on Part B. **2026-09-13:** table-in-`flen` gained option **(d) lower `b`**; W-155 closes the *table IS the answer* gap; W-156 carries the environments-vs-single-corpus conflict.  **2026-09-13:** W-143 CLOSED — all four priors ruled; W-151/W-152 close three, W-153 covers the date gap, W-154 restates `rerank_weight` as a cost question. Ratified, not built.  **2026-09-13:** W-97 CLOSED (descoped) — both rows deleted, file archived, residue moved to W-143.  **2026-09-13:** `superseded_weight` ruled closed (W-143 → W-151); **ratified, not built** — the knob goes, the `superseded` fact stays.  **2026-09-13:** SR-OWNERSHIP became **SR-WORK-OWNERSHIP** (`0146` → `0054`, the WORK range); the hole at `0146` closed by shifting `0147`–`0156` down one — a file number is an ordinal.  **2026-09-13:** L9 retired — the environment rule is now [SR-WORK-ENVIRONMENTS](../records/0052_WORK-environments.md) (`0052`, `kind: process`), not a law. The handle and `records/0011` are never reused.  **2026-09-13 (SR-WORK-OPEN-QUEUE):** the queue's 53 rules are stated once in [`records/0051_WORK-open-queue.md`](../records/0051_WORK-open-queue.md); this file is the LIST plus the declared ball legend, and states no other rule.  **2026-09-13 (`kind`):** every record declares `kind: law | component | process` — the gate it answers to. 12 · 52 · 4. SR-WORK-OWNERSHIP decision 11; register §The three kinds.  **2026-09-13 (SR rename):** `ADR-<NAME>` became `SR-<NAME>` and `docs/adr/` moved to `records/` at the repo root — one mechanical change, every reference repointed. Archived `ADR-NNNN` citations and `archive/v0.26-docs/adr/` are unchanged.  **2026-09-13:** §1 gains the 🟣 ruling, what left the inbox, and the sub-row check that had been reading one row.  **2026-09-12 (W-149 CLOSED):** §1's W-149 section rewritten from *ruled, not built* to built, with the two record defects that only appeared by implementing the table; the Berry *unmeasured* warning replaced by the measured linker split. **2026-09-12 (W-142/W-115/W-144/W-136/W-140):** three instruments, three verdicts; W-142 retired because its endpoint sits at the corpus base rate, W-115 measured at last, W-144 answered and sent to a compare doc, the golden ladder completed to 10 000, and W-140 row 22 (a `⚠` that crashed a Windows console from inside a `FuxError`) fixed while row 21 was deliberately left alone after 11 failed reproductions. **2026-09-12 (positioning):** state of play — the proposal graduated, what Arpit overruled, and what is still on his hands. +`type: Handoff` frontmatter. **2026-09-12 (W-141):** `.agents/skills/` shared; 71 agent files. **2026-09-11 (balls primary):** constraint. **2026-09-11 (marks final):** constraint → 🧨/🔺. **2026-09-11 (blocks):** standing constraint for the sub-row. **2026-09-11 (balls):** standing constraint for OPEN-WORK row balls. **2026-09-11 (W-130 rename):** `.fux/types.toml` -> `.fux/formats.toml`. **2026-09-11 (W-130):** section rewritten from *filed* to *landed*. **2026-09-11 (W-128/129):** state of play — `pii.toml` required; checksum validators. **2026-09-11 (`[index]`):** state of play: `[index]` in tune.toml, uncommitted, and the veto-check lesson. **2026-09-05 (W-101): state of play.** **2026-09-05 (later): §1 gains the unblock-proposal section** — where the proposed rulings live, the three rows that were facts, and the playground's staged half-state. **2026-09-05:** search v3 ratified; W-108 landed; W-107 Phase 0 + W-106 next. **2026-09-01 (cleanup + release): §1 led with a STALE blocker** — *"🔴 Blocked right now: W-98 Phase 2"* while Phase 2 had landed and Arpit had ruled. Replaced with the nothing-is-blocked state, the two genuinely-pending calls, and W-101 as the one open thing. **2026-09-01: owed-item 2 CLOSED and it was never Arpit's** — the stray `handoff/` directory under `work/` was a *move*, not a deletion; suite line re-stated at 2 648/73. **2026-09-01: a blocked-right-now section added at the top** — the Phase 2 boundary fork, stated as a two-column table so it can be answered in one line, with an explicit *do not build the pure half meanwhile*. **2026-09-01: the in-flight section rewritten for W-98 Phase 1** — 🔴 blocked → landed, with the generalisable lesson stated (*a spec can foreclose a decision it got wrong, and the marker that says "settled" is what stops the next reader checking*). **2026-09-01 (W-98): state of play updated** — W-98 in flight and blocked on the CORS spike, plus the two traps already sprung (the `extract_links` "dead code" that is not, and SR-ACQUIRED's unregistered claim on a module that does not exist). **2026-08-28 (W-93): state of play updated** — the benchmark ran and its two findings (a saturated endpoint, a prior that ships off) are the lessons a next session needs. **2026-08-27 (later): rule 4 promoted to the lead.** Two consecutive sessions found the queue's own blockers stale or false the moment a shell existed. **2026-08-27 (latest): §1 leads with `L8` — THE LAW COUNT CHANGED**, which is the one fact a session arriving cold will otherwise get wrong; the record-set rewrite demoted to *before that*. The entry states what L8 is, that it forbids nothing fux does today, the hashed-key-is-not-anonymity limit, and why it was a law rather than one more SR decision. **2026-08-27: the two stray-file notes reconciled to Arpit's ruling** — §1's *"three tests are red"* item and §2's stray-file item both read as *left in place because a concurrent session owns it*, which stopped being true the moment he ruled **keep `0045`**. Both now say the removal is two `git rm`s nobody has run, not an open question. ⚠ **Neither `git rm` ran** — the bash sandbox was down all session. **2026-08-27: new §1 head — the quality contract is declared**, with the four things a cold session gets wrong (the cost is frozen and re-setting it voids the decision; `nDCG`'s demotion is structural and reverses if either condition stops holding; the declared headline `recall@k` is **not computed**; fork 6's law question is open as W-89 and may not be closed by inference). ⚠ **§2's lede was two merges and a close out of date** — it read *"the queue is five"* and named W-74/W-75/W-77/W-81 as separate items after they merged into W-82 on 2026-08-26, and W-78 as open after it closed 2026-08-25. Replaced with a three-row table; the stale lede is kept below it, labelled superseded, because the judgment under it is still worth reading. **2026-08-26 (previous, W-86): new §1 head — the decoder plane**, with the three findings and the two refusals (`.json` needs a measurement not an argument; full YAML is refused on correctness because expanding anchors inflates `tf`). W-84 demoted to *the change before it*. **2026-08-26 (W-84): `ask` cites at HEADING level and refuses to cite at line level** — a line range computable only at ingest would lie after one edit, and costs a positional index; `answer` cites lines because it fetches. `query/headings.py` is new; `ask` text, `ask/find --json` and MCP `fux_search` render it; `find`'s piped stdout is untouched. SR-ASK d10 + SR-MCP d9. 1 500 tests. ⚠ Not committed — a concurrent session was mid-rename. **2026-08-26 (the consolidation): W-74, W-75, W-77 and W-81 merged into [`open/W-82-the-consolidated-build.md`](../archive/open/W-82-the-consolidated-build.md)** — a merge, not a close; every fork stays open. Nine documents archived (4 open detail files, 3 proposals, 2 compare docs), each with a successor row in [`archive/README.md`](../archive/README.md). ⚠ **Both compare verdicts were folded into W-82 §4 VERBATIM before the move**, because archiving a compare doc makes its verdict uncitable. Four calls made the same day; `update --warm` and `answer --memo` **withdrawn**. **2026-08-25 (cleanup): the head was two releases and a deletion out of date.** *"Most recent change: `v2.0.0-alpha.0`"* -> the model removal; *"hybrid fusion is built and ships default-off"* -> the lane is deleted; *"the queue is three items, all `arpit`"* -> five, one of them `agent`; *"the immediate next step: build SR-TUNE"* -> built 08-24. ⚠ The queue count was wrong **twice** before it was right. **2026-08-24: brought current** — it had said `HEAD` was still `9bb870e` with nothing committed, two releases ago. New §1 entry for `v2.0.0-alpha.0` and the audit; §2 rewritten to a three-item `arpit`-only queue with SR-TUNE named as the next step |
| [`WORKLOG.md`](WORKLOG.md) | **Every substantive exchange** (append) | 2026-09-15 | **2026-09-15 (golden unblock, Cowork):** appended — the questions unblocked one row of four, and the read-declaration for `questions/`. **2026-09-15 (arms settle, 16a):** the tier-off arm leaves the benchmark. · **2026-09-15 (Node column BUILT, W-188):** the build, the three pair kinds, and the banned filename. · **2026-09-15 (Node as a column, W-188):** the third ask of the session, appended to its entry. · **2026-09-15 (every report on the spine):** the ruling, the five rebuilds, and the fabricated-zero defect. · **2026-09-15 (W-187, the CAP slide spine):** the ruling and the build, in one entry. · **2026-09-15 (CAP slide spine, Cowork):** one entry — the ruling, decision 14, and W-187. · **2026-09-15:** the green queue, worked continuously. · **2026-09-15 (🟡 needs an item, Cowork):** the rule and the five items it produced appended to this session's entry. **2026-09-15 (W-168 step 1 RULED (c), Cowork):** the ruling appended to this session's entry — anchor text on the edge, folded at read time; option (a) refused not deferred. **L11 — the sealed answer key is a law.** **2026-09-15 (L11):** the sealed-key prohibition became law `L11` ([SR-LAW-11](../records/0012_LAW-11-sealed-answer-key.md)); SR-WORK-GOLDEN now carries only the process, and every law count, range and `L0..L11` diagram moved with it. · **2026-09-15:** session entry — W-146 closed, SR-WORK-GOLDEN written, queue rule 23a. **2026-09-14 (fux.toml audit, Cowork):** the audit entry. **2026-09-14 (three fux.toml rulings, Cowork):** live `update`/`fetch_at_answer`, and the starter moved to `templates/fux.toml.txt`. **2026-09-14 (W-174 SHIPPED, Cowork):** the build entry. **2026-09-14 (W-159/163/164, Claude Code):** entry appended — four process failures of mine recorded, including an amend that produced a commit importing a module it did not contain, and two blocked actions I stopped at rather than worked around. **2026-09-14 (W-174, Cowork):** the ratification entry. **2026-09-14 (W-165 + W-166, Claude Code):** entry appended — what shipped, the two process failures (an amend that produced a commit importing a module it did not contain; hashes restamped across a peer session's records), and the SEARCH-SKILL correction held back. **2026-09-14 (cage-search-leg):** proposal for fux's half of cage's search leg filed; B-244. **2026-09-14 (gap check):** every 3.0 doc and W-160–W-166 gained a *verification and the keep/remove call* section. **2026-09-14 (backlog sweep, Cowork):** 21 rows left `BACKLOG.md` — 18 promoted into W-163/W-164/W-165/W-166, 3 resolved by correcting their stale source sentences (B-032 SR-T1-ACCELERATOR, B-239 SR-REFUSAL, B-240 SR-CONFIG); B-242/B-243 added for the two 3.0 proposals; B-129/B-133/B-140/B-175 repointed at the 3.0 docs. **2026-09-13 (3.0.0-alpha.0 branch, Cowork):** five work documents filed from the session's rulings — compare docs `ask-graph-expansion` (accepted), `fux-correct` (accepted), `abstention-gates` (proposed; two rulings recorded); proposals `search-improvements-v3`, `fux-inspect`; items W-160 / W-161 / W-162. Nothing built. **2026-09-13 (W-154 Part A):** proximity reranking PRICED — +15 to +19 ms p50, **flat in corpus size**, **same on `ask` and `answer`**, 0/124 queries faster. Surface capture, no verdict. W-154 stays open on Part B. **2026-09-13 (SR-WORK-BACKLOG):** the backlog is the second work file — `work/BACKLOG.md`, 241 named-but-unclaimed items in five classes, governed by [SR-WORK-BACKLOG](../records/0055_WORK-backlog.md) at `0055`. **2026-09-13:** table-in-`flen` gained option **(d) lower `b`**; W-155 closes the *table IS the answer* gap; W-156 carries the environments-vs-single-corpus conflict.  **2026-09-13:** W-143 CLOSED — all four priors ruled; W-151/W-152 close three, W-153 covers the date gap, W-154 restates `rerank_weight` as a cost question. Ratified, not built.  **2026-09-13:** W-97 CLOSED (descoped) — both rows deleted, file archived, residue moved to W-143.  **2026-09-13:** `superseded_weight` ruled closed (W-143 → W-151); **ratified, not built** — the knob goes, the `superseded` fact stays.  **2026-09-13:** SR-OWNERSHIP became **SR-WORK-OWNERSHIP** (`0146` → `0054`, the WORK range); the hole at `0146` closed by shifting `0147`–`0156` down one — a file number is an ordinal.  **2026-09-13:** L9 retired — the environment rule is now [SR-WORK-ENVIRONMENTS](../records/0052_WORK-environments.md) (`0052`, `kind: process`), not a law. The handle and `records/0011` are never reused.  **2026-09-13 (SR-WORK-OPEN-QUEUE):** the queue's 53 rules are stated once in [`records/0051_WORK-open-queue.md`](../records/0051_WORK-open-queue.md); this file is the LIST plus the declared ball legend, and states no other rule.  **2026-09-13 (`kind`):** every record declares `kind: law | component | process` — the gate it answers to. 12 · 52 · 4. SR-WORK-OWNERSHIP decision 11; register §The three kinds.  **2026-09-13 (SR rename):** `ADR-<NAME>` became `SR-<NAME>` and `docs/adr/` moved to `records/` at the repo root — one mechanical change, every reference repointed. Archived `ADR-NNNN` citations and `archive/v0.26-docs/adr/` are unchanged.  **2026-09-13:** entry for the 🟣 ball, the two date gates, the `blocks_subrows()` key defect and W-140's unexplained 🟡.  **2026-09-12 (W-149):** entry for the build — bundler, prune, monorepo wiring, publish, the arm's sixth surface, and the Berry probe. **2026-09-12:** entry for W-107's close. **2026-09-12 (W-142/W-115/W-144/W-136/W-140):** three instruments, three verdicts; W-142 retired because its endpoint sits at the corpus base rate, W-115 measured at last, W-144 answered and sent to a compare doc, the golden ladder completed to 10 000, and W-140 row 22 (a `⚠` that crashed a Windows console from inside a `FuxError`) fixed while row 21 was deliberately left alone after 11 failed reproductions. **2026-09-12:** session entry for W-138, W-139, W-106 and W-140. **2026-09-12:** one entry — positioning graduated. +`type: Log` frontmatter. **2026-09-12 (W-141):** two entries. **2026-09-11 (balls primary):** entry. **2026-09-11 (marks final):** entry. **2026-09-11 (blocks):** entry. **2026-09-11 (balls):** entry. **2026-09-11 (rule 10):** entry for the OPEN-WORK cut. **2026-09-11 (W-137):** the SR renumber — and the entry invokes this file's *repo-wide mechanical rename* exception, the only case where past entries' SR paths are repointed. **2026-09-11 (Claude Code):** the OPEN-WORK cleanup, and what each deletion was traded for. **2026-09-11 (Claude Code):** W-126 + W-127 land; the gate could not demand the record they amended. **2026-09-11 (Claude Code):** the shell arrives — four rows that said *blocked on an environment* closed in an hour. **2026-09-11 (`[index]`):** entry for the `phrases` / `[index]` session. **2026-09-06 (later):** entry for the enrich extension and the ruling. **2026-09-06:** entry for the fourth-vendor change. **2026-09-05 (W-101): an entry.** **2026-09-05 (later): +1 entry** — the unblock proposal, nothing implemented. **2026-09-05:** the ratification + W-108 entry. **2026-09-02: +1 entry** — `alpha.6` shipped with red Windows CI; `alpha.7` fixes it. **2026-09-02: +1 entry** — the punch-list discharged, two runs filed, four items closed, a release cut. **2026-09-01: +1 entry** — W-102/103/104/105 written, nothing run. **2026-09-01: +1 entry** — Phase 2 stopped before its first line. **2026-09-01: +1 entry** — W-98 Phase 1. **2026-09-01 (W-98): +the spike-blocker entry** — nothing built, and why that is the correct outcome. **2026-08-28 (W-93): entry appended** — the benchmark executed end to end. **2026-08-27 (latest): entry appended — W-91 built, `L8` reverted the day it was written.** **2026-08-27 (latest): entry appended — W-89 ruled, `L8` written.** **2026-08-27: entry appended** — the SR-CONFIDENCE duplicate ruled (keep `0045`), and it leads with the fact that **nothing was deleted** rather than with the ruling, because a docs-only change that reads like a completed cleanup is how a red suite gets inherited as a mystery. **2026-08-27 (W-90): entry appended** — the confidence plane, and it leads with the two collisions rather than the feature, because a session that mints an id and an SR number from a queue another session is editing will do it again. **2026-08-27 (W-87 Phase 0): entry appended** — six forks ruled, the cost model frozen **before** any score, and the recommendation that was overruled recorded rather than quietly dropped (holding fork 6 back was advised; Arpit ruled all six, and fork 6's law question survives as W-89 regardless). **2026-08-26 (W-86 closed): entry appended** - P0's shipped defect, P8's un-re-costed break, and the race P6 reproduced before fixing. **2026-08-27 (W-88): entry appended** — the skip notice, its four judgement calls, and the two things left open (tests unverified under `pytest`; nothing committed, a concurrent session holds ~60 staged paths). **2026-08-26 (four forks ruled): entry appended** - and it leads with the rule I cited that did not apply, because a false blocker costs as much as a missed one. **2026-08-26 (the `fux-decoder` skill): entry appended** - the docs gap the question exposed: the protocol lived only in a module docstring and in SR-DECODE **§2, the agent-facing half**, and nowhere a consumer looks. **2026-08-26 (W-86 P7): entry appended** - why fetchers have `.py.txt` and decoders do not, and the ruling that overturned §13.4. **2026-08-26 (W-86 P2-P5): entry appended** - sixteen decoders; the entry leads with the four silent defects rather than the feature list, because that is the part a future session needs. **2026-08-26 (W-86 §13): entry appended** — four follow-ups, two refusals, and the fetcher-contract L3 defect. **2026-08-26 (W-86 fork E): entry appended** — the consumer-owned decoder, and the finding that L1 did **not** need amending. **2026-08-26 (W-86 filed): the decoder plane** — three findings recorded, all of them corrections to what the session expected to find: the HTML decoder already exists in both fetchers, `.rst`/`.adoc`/`.org` headings have never reached the heading field, and the index lock has one caller. Entry also records a **race observed and then disproved** inside the same session. **2026-08-26 (W-84): `ask` is heading-level** — the refusal of line level is half the entry; a live `fux_search` tool-description defect (claimed *"line-range citations"*) found and fixed. ⚠ Not committed. **2026-08-26 (W-85): the property W-83 said it exposed was a comment, and had reached nobody** — required key, live table, and the rule that a write-if-missing template change is for new repos only. **2026-08-26 (W-83): the unconfigured fetch ceiling** — the record already specified `default 4` and the code implemented the contradicting sentence four paragraphs above it. **2026-08-26 (W-82 built): five phases landed** — the URL health report, the detector, parallel fetch and the cap, the changed/unchanged line, and the agent surface. Nine records amended in the same change; 1 433 unit tests green. ⚠ `tests_e2e/` unverified in the build environment (fails identically on a clean tree). ⚠ 3.0 and 3.5 did not land — neither is a code task. **2026-08-26: entry appended** — W-79 finished, `query/fuse.py` + `tests/query/test_fuse.py` deleted, four records amended; the entry explicitly corrects the earlier 2026-08-26 entry that recorded the keep (append-only, so a new entry is the correction). **2026-08-24 (Claude Code): entry appended** — v2.0.0-alpha.0 committed and published; inserted above the reconciled 2026-08-22 Cowork entries per newest-on-top. **2026-08-22 (fourth Cowork pass): entry appended** — URL freshness proposed, two compare docs, W-75 filed. **2026-08-22 (third Claude Code pass): entry appended** — W-68 built; W-26's last box filed as a fork. **2026-08-22 (second Claude Code pass): entry appended** — W-66 finished, W-67 re-measured, W-65 reconciled, W-26's tier question answered; three items to Arpit. **2026-08-22 (this pass, Claude Code): entry appended** — W-66 Phases 1+3 and W-44's demotion weight landed; 1040 tests passing. **2026-08-22 (Cowork): W-61 ruled — the hook defers, R6 adjudicated.** **2026-08-21 (PRIORITY P7): the `Cost:` line dropped from the entry template** — 58/58 prior entries had said `unmeasured`. **2026-08-21: two entries appended** — P5 and P6, plus the session-lock hook fix, plus the pre-existing SR-CACHE entry from a prior Cowork session, reconciled across four commits. **2026-08-20 (later): second entry appended** — W-45+W-55, W-56, M5 and W-60 closed in five commits; the hold on prediction runs honoured and named as the reason two records stay ⏳ proposed. **2026-08-20 (latest): entry appended** — the refer-plane fetch-cache idea researched and filed as W-60, compare doc written, proposed verdict F. Earlier **2026-08-20: entry appended** — W-45 and W-55 asked for again and found **already closed** in `9ba02b1`; verified by re-derivation (archive listing, IMPLEMENTATION row, 719 passing) rather than rebuilt, and the entry says so plainly. Earlier **2026-08-20: entry appended** — W-46, W-48, M3 and M4 core closed in four commits; W-55/56/57/58/59 filed; the three things deliberately refused, and the two deviations from a written DoD, both stated in the entry. **2026-08-18: header rewritten** — chat-only sessions count, the `Cost:` line is mandatory, past entries are never edited (one stated exception: a repo-wide mechanical rename). Earlier: Rolling session handoff |
| [`../docs/GLOSSARY.md`](../docs/GLOSSARY.md) | New recurring term, or a term changes meaning | 2026-09-15 | **2026-09-15 (W-178, Claude Code):** *Fetcher (URL)* gains **the set is open** — why the grammar validates shape and never imports the module to check it. **2026-09-15 (W-177, Claude Code):** *URL source* and *fetcher* name `fux ingest`, and the offline carry-forward is `fux ingest --no-fetch`. **2026-09-14: +`Agent surface`** — the repo had shipped five kinds of them and had no word for the set, calling them *agent-steering files* in `CLAUDE.md` and *vendor surfaces* in SR-AGENT-POLICY. The entry defines the word once, gives the four classes (instructing · acting · protocol · emitted) and states that both older names defer to it. ⚠ It also says **do not call the category "steering"** — that is one of its members. Owned by [SR-AGENT-SURFACES](../records/0155_agent-surfaces.md). **2026-09-13:** L9 retired — the environment rule is now [SR-WORK-ENVIRONMENTS](../records/0052_WORK-environments.md) (`0052`, `kind: process`), not a law. The handle and `records/0011` are never reused.  **2026-09-13 (SR rename):** `ADR-<NAME>` became `SR-<NAME>` and `docs/adr/` moved to `records/` at the repo root — one mechanical change, every reference repointed. Archived `ADR-NNNN` citations and `archive/v0.26-docs/adr/` are unchanged.  **2026-09-12 (W-138):** *Playground* rewritten from *the graded corpus* to Arpit's sandbox with its former job as history; *Golden query* no longer defined in terms of the playground and now names the schema module; *Fux-lab* points at the golden ladder; **+`Fux-benchmark`**, which had no entry at all. **2026-09-12 (W-122):** *use record* repointed — L8 is stated by SR-LAW-8, not by `CLAUDE.md`. **2026-09-12 (positioning §4.5, §5): +`Documents, not code`** — the three facts (no AST/symbols/call graph; no source extension on `DEFAULT_TYPES`; the `code` edge is a path), so `fux ask` has a live answer on fux's own repo. **+`` `code` (edge kind)``** — Arpit took §5's cheap alternative over the `code`→`path` rename. **2026-08-27: +`Use record (the law, L8)`** — the new recurring term, defined against `Content-never-durable` rather than beside it, because the whole point of L8 is that **a query is not content**. Carries the hashed-key-is-not-anonymity warning at the point of definition, so a reader who never opens SR-LAWS still meets it. **2026-08-27: +`write.lock` (the index mutex)** — live in `maintain/runner.py`, in SR-MAINTENANCE, in [`compare/index-lock.compare.md`](compare/index-lock.compare.md) and in a `.gitignore` line, and defined nowhere. The entry carries the two things a reader gets wrong: **read verbs hold nothing**, and the stop files beside it are **not** locks. It also separates `write.lock` from the archived `fux.lock`, which the glossary's own header already flags as not-current. **2026-08-21 (W-63): `--refresh-urls` references repointed to `fux update`.** **2026-08-21 (SR-CACHE): +`TTL fetch cache` and +`` `cached` (the fourth verdict)`` — two v0.30 recurring terms that had NO entry**, a month after W-60 shipped them: `TTL` was live in a policy field, a verdict label, a compare doc and an SR without ever being defined. The `ARC` entry, which predated SR-CACHE and cited only the compare doc, now explains the ghost lists and the scan-resistance argument, says it is in-memory and per-process as against the TTL store's on-disk, and cites [SR-CACHE](../records/0131_cache.md). **2026-08-19: ingest-mode entries re-grounded** — three citations pointed at `archive/adr/0001_ingest-mode-naming.md`, an archived doc backing a live claim; repointed to [SR-EXTRACTED](../records/0115_extracted-mode.md)/SR-ENRICHED, and *extracted mode*'s "⏳ name proposed" is now ratified. **+6 M2 terms** (`accelerator (T1)`, `block / block line`, `differential law`, `mx (block max)`, `offset table`, `skipping`). **+4 terms** (`.fux directory`, `derived plane`, `middleware (URL)`, `url source`) — paying SR-URL-INGEST's recorded debt; fixed a stale "numbering continues at 0016" line. Rewritten for v0.30 (index vs db, ledger, keyspace, wire/runtime, refer/snapshot, inferred/enriched, pruning, ARC); archived v0.26 terms marked not-current. **+3 terms** (`playground`, `golden query`, `known failure / xfail`) from SETUP-PLAYGROUND. |
| [`compare/README.md`](compare/README.md) | A compare doc opens, closes, changes status | 2026-09-13 | **2026-09-13 (3.0.0-alpha.0 branch, Cowork):** five work documents filed from the session's rulings — compare docs `ask-graph-expansion` (accepted), `fux-correct` (accepted), `abstention-gates` (proposed; two rulings recorded); proposals `search-improvements-v3`, `fux-inspect`; items W-160 / W-161 / W-162. Nothing built. **2026-09-13 (W-155):** the table-in-`flen` doc's §3 and §4 amended — the *not measured* gap it named in its own recommendation is MEASURED and the answer is YES; its recommendation still stands and is Arpit's. **2026-09-13:** table-in-`flen` gained option **(d) lower `b`**; W-155 closes the *table IS the answer* gap; W-156 carries the environments-vs-single-corpus conflict.  **2026-09-12 (archive pass): -`ingest-mode-naming`** — the only one of the 22 whose fork is closed **and** whose trigger is spent (its own Reopen-trigger section reads *None — a naming call, closed permanently*). Moved to [`archive/compare/`](../archive/compare/README.md) with the required why-it-cannot-fire sentence; its matrix was folded into [SR-EXTRACTED](../records/0115_extracted-mode.md) §2 first, because that record's Reference block grounded a decision in it. **The other 21 stay**: each carries a reopen-trigger that can still fire, which is the bar `archive/compare/README.md` sets. | **2026-09-12 (W-144): +`table-tokens-in-flen`** — measured, `hit@1` 0/30 -> 30/30 at `p ≈ 0` with both controls holding, and the threshold located between a table share of 0.26 and 0.29 against a ladder median of 0.344. Recommends **(b) exclude table-row tokens from `flen[body]` only**; `proposed`, Arpit's call, and the gap is named — no probe has a table that IS the answer. **2026-09-12 (W-140 row 12): +`path-hops-bound`** — `fux path --hops` is unbounded; measured 0.65 s at `--hops 2` and **84.6 s at `--hops 6`** on a 738-node / 4 446-edge index, ~11× per hop above 4. Proposed **(c) bound the walk's work**, with a `truncated` flag in every rendering; 🔴 UNRULED. **2026-09-12 (positioning §6b):** +`type: Index` frontmatter. **2026-09-12 (review): `maintenance-trigger`'s row records that R5's half of its reopen-trigger FIRED on the day it was accepted; `record-shape-migration` is `accepted`, its four owed items discharged.** **2026-09-12 (W-141):** `copilot-skill-surface` row — directory moved to B. **2026-09-11 (W-130 rename):** `.fux/types.toml` -> `.fux/formats.toml`. **2026-09-11 (W-130 built):** `types-toml` row -> ✅ accepted and built. **2026-09-11:** row for `types-toml` (W-130), ⏳ proposed. **2026-09-06 (later):** `copilot-skill-surface` row rewritten to the ruling. **2026-09-06:** row for `copilot-skill-surface`. **2026-08-27: `what-good-means` moves `proposed` → ✅ `accepted`** (Arpit, all six forks; [SR-WORK-QUALITY](../records/0056_WORK-quality.md)). It stays in `compare/` rather than moving to the archive, because the doc carries its own **reopen-trigger** and four of its five conditions can still fire. **2026-08-26 (W-86): +the `index-lock` row** — the table had 16 rows and gains a 17th, the first `⏳ proposed` one on it since the consolidation. **2026-08-26 (the consolidation): W-74, W-75, W-77 and W-81 merged into [`open/W-82-the-consolidated-build.md`](../archive/open/W-82-the-consolidated-build.md)** — a merge, not a close; every fork stays open. Nine documents archived (4 open detail files, 3 proposals, 2 compare docs), each with a successor row in [`archive/README.md`](../archive/README.md). ⚠ **Both compare verdicts were folded into W-82 §4 VERBATIM before the move**, because archiving a compare doc makes its verdict uncitable. Four calls made the same day; `update --warm` and `answer --memo` **withdrawn**. **2026-08-25 (later): [cross-encoder-reopen](compare/cross-encoder-reopen.compare.md) RULED** — ✅ accepted (Arpit, on delegation). ⚠ **Its own §6 recommendation was NOT taken**: condition 1 was **vacated** rather than rewritten. **2026-08-25 (cleanup): FOUR docs archived and the table reconciled.** `wire-format`, `keyspace-unification`, `pruning-criterion` and `r7-size-budget` moved to `archive/compare/` — a decided fork stays here because the doc carries its **reopen-trigger**, and these four can no longer fire. ⚠ **Four rows were STALE** — `ingest-mode-naming`, `source-exclusion` and `record-freshness` still read *"awaiting Arpit"* for forks ruled between 08-19 and 08-20. ⚠ **And four docs had NO ROW AT ALL** (`df-over-the-union`, `file-type-filter`, `graph-plane-format`, `record-shape-migration`); all four added. The table is now 18 rows for 18 docs. **2026-08-25: [blind-authorship-rule](compare/blind-authorship-rule.compare.md) RULED** — ✅ accepted (Arpit), in the rewritten form of its §5; the wording drafted 2026-08-24 was refused. Row rewritten with the verdict and the two unbuilt parts. **2026-08-24: two docs opened**, both `proposed`, both awaiting Arpit|
| [`proposals/README.md`](proposals/README.md) | A proposal is filed, graduates, is superseded, or leaves the directory | 2026-09-15 | **2026-09-15 (W-178, Claude Code):** `glassbox-sessions.md` §2's blocker is **gone** — `fetch=` is typed and shipped; §6's *do sessions belong in the URL list at all* is what remains open. **2026-09-14 (proposal review, Cowork):** six moved to `archive/proposals/` with map rows — `search-v3` (every item closed), `fux-inspect` (shipped, SR-INSPECT), `cage-search-leg` (superseded by SR-OBSERVE), `abstention-gate` (all three options graduated → W-176), the 2026-09-05 unblock pair (overtaken; inbox empty); thirteen stay, each with its reason in the archive map. **2026-09-14 (cage-search-leg):** proposal for fux's half of cage's search leg filed; B-244. **2026-09-14 (gap check):** every 3.0 doc and W-160–W-166 gained a *verification and the keep/remove call* section. **2026-09-13 (3.0.0-alpha.0 branch, Cowork):** five work documents filed from the session's rulings — compare docs `ask-graph-expansion` (accepted), `fux-correct` (accepted), `abstention-gates` (proposed; two rulings recorded); proposals `search-improvements-v3`, `fux-inspect`; items W-160 / W-161 / W-162. Nothing built. **2026-09-13 (SR-WORK-BACKLOG):** the backlog is the second work file — `work/BACKLOG.md`, 241 named-but-unclaimed items in five classes, governed by [SR-WORK-BACKLOG](../records/0055_WORK-backlog.md) at `0055`. **2026-09-12 (W-138):** `unblock-2026-09-05.md` and its prompt gained void-in-part banners (R-8, R-11 and §2.5 name a corpus SR-WORK-ENVIRONMENTS closed); `search-v3.md`'s W-106 and Phase 0 DoDs de-defaulted; `ranking-tuning.md`'s asset table struck its playground row. **2026-09-12:** `positioning-documents-not-code` **graduated** — ruled and applied; its own framing overruled. Row rewritten. **2026-09-12 (review): `search-v3-claude-code-prompt.md` ARCHIVED** (executed in full, cited by nothing live); **`structure-aware-extraction` graduated into W-144** — its OOXML trigger had fired; **`ranking-tuning`'s row corrected** — its trigger was misread as unfireable and is alive. **2026-09-11: +abstention-gate.md.** **2026-09-11:** row added for `positioning-documents-not-code.md`. **NEW ROW 2026-09-05 — this maintained index had none**, while `compare/README.md` did. **2026-09-05 (cleanup): rewritten.** It carried a duplicated intro, a stale ⚠ housekeeping block naming two files that had already moved on 2026-08-27, an orphaned *Filed 2026-08-10* section, an empty *Filed 2026-08-26* heading kept with a note asking someone else to resolve it, and **six of nineteen files on disk had no row at all**. Now ordered newest-first, every live file indexed, with a *Left this directory* table naming each departure and its live successor.|
| [`compare/*.compare.md`](compare/) | New evidence, verdict change, reopen-trigger fires | 2026-09-13 | **2026-09-14 (gap check):** every 3.0 doc and W-160–W-166 gained a *verification and the keep/remove call* section. **2026-09-13 (3.0.0-alpha.0 branch, Cowork):** five work documents filed from the session's rulings — compare docs `ask-graph-expansion` (accepted), `fux-correct` (accepted), `abstention-gates` (proposed; two rulings recorded); proposals `search-improvements-v3`, `fux-inspect`; items W-160 / W-161 / W-162. Nothing built. **2026-09-13 (W-155):** the table-in-`flen` doc's §3 and §4 amended — the *not measured* gap it named in its own recommendation is MEASURED and the answer is YES; its recommendation still stands and is Arpit's. **2026-09-13:** table-in-`flen` gained option **(d) lower `b`**; W-155 closes the *table IS the answer* gap; W-156 carries the environments-vs-single-corpus conflict.  **2026-09-13 (SR rename):** `ADR-<NAME>` became `SR-<NAME>` and `docs/adr/` moved to `records/` at the repo root — one mechanical change, every reference repointed. Archived `ADR-NNNN` citations and `archive/v0.26-docs/adr/` are unchanged.  **2026-09-12 (W-144): +`table-tokens-in-flen`** — measured, `hit@1` 0/30 -> 30/30 at `p ≈ 0` with both controls holding, and the threshold located between a table share of 0.26 and 0.29 against a ladder median of 0.344. Recommends **(b) exclude table-row tokens from `flen[body]` only**; `proposed`, Arpit's call, and the gap is named — no probe has a table that IS the answer. **2026-09-12 (W-138, SR-WORK-ENVIRONMENTS):** `cross-encoder-reopen`'s *cheapest thing that would settle leg 1* and `what-good-means`'s `unanswerable` bullet both assumed the playground's 50 goldens, which SR-WORK-ENVIRONMENTS retired — both now say the cheapest thing is more expensive than it reads. **2026-09-12 (W-141):** `copilot-skill-surface` verdict amended — directory moved to B, `.agents/skills/`. **2026-09-11 (W-130 rename):** `.fux/types.toml` -> `.fux/formats.toml`. **2026-09-11 (W-130 built):** `types-toml` ACCEPTED - D and F1-F6 as proposed; verdict block records the two refinements the build found (exact-case *stated twice*; upper-case bound pattern refused by the conversion). **2026-09-11:** new fork - [`types-toml`](compare/types-toml.compare.md), proposed verdict **D**, awaiting Arpit. It reverses SR-TYPES' recorded TOML rejection and says so up front, and corrects the agent's own false claim that `!` lines are order-sensitive. **2026-09-06 (later):** `copilot-skill-surface` **RULED A** by Arpit, overruling its own proposed C - with a correction to its own crux: the duplicate-name risk it called *unknown* is bounded, because the copies are byte-identical by construction. **2026-09-06:** new fork - [`copilot-skill-surface`](compare/copilot-skill-surface.compare.md), proposed verdict **C (write nothing new)**, awaiting Arpit. Its crux is unmeasured on purpose. **2026-08-27: `what-good-means` ACCEPTED (Arpit) — all six forks**, with a verdict block at the head and a mechanism §4 did not specify: the cost of an error is a **confidence target**, `t = 0.75` → `c = t/(1-t) = 2`, because **only the ratio is identifiable** (Chow) and `t` is arguable where a bare weight is not. Ruled in [SR-WORK-QUALITY](../records/0056_WORK-quality.md). ⚠ **Fork 6 is ruled `no query log` WITHOUT ruling the law question** — that is W-89, exactly as fork 6's own verdict asked. **2026-08-26 (previous): `index-lock` ACCEPTED** (verdict B + the `write.lock` rename), and **`file-type-filter`'s verdict G amended** - its structure untouched, its default's contents widened. G was rulable precisely because of its own confidence line calling it a defaults judgment. **2026-08-26 (W-86 fork E): `index-lock` §4 reconciled the same day it was written.** Arpit's consumer-dependency ruling does **not** reach the mutex — a decoder is consumer code, the lock is `src/fux/` runtime code — so `filelock`/`portalocker` stay refused, and rejections (a) and (b) never rested on L1 anyway. **Noted explicitly because a section that merely LOOKS stale gets "fixed" by someone who then reverses a correct call.** **2026-08-26 (W-86): +[`index-lock.compare.md`](compare/index-lock.compare.md)** — one file or two, for the mutex and the enrichment queue. **Proposed verdict B: two, and neither is new.** ⚠ The doc's value is mostly what it *found* rather than what it argues: `maintain/runner.py::acquire` has **one caller**, so a foreground `fux ingest` writes the index holding no lock at all; and `.fux/enrich/<sha>.md` is already the committed pin, so what was missing was never the manifest but the **queue**. Rejects a merged file on one line — a mutex must be gitignored, a queue must be committed, and no `.gitignore` can express half a file. **2026-08-26 (the consolidation): W-74, W-75, W-77 and W-81 merged into [`open/W-82-the-consolidated-build.md`](../archive/open/W-82-the-consolidated-build.md)** — a merge, not a close; every fork stays open. Nine documents archived (4 open detail files, 3 proposals, 2 compare docs), each with a successor row in [`archive/README.md`](../archive/README.md). ⚠ **Both compare verdicts were folded into W-82 §4 VERBATIM before the move**, because archiving a compare doc makes its verdict uncitable. Four calls made the same day; `update --warm` and `answer --memo` **withdrawn**. **2026-08-24: +2 for W-78's rulings** — [`cross-encoder-reopen`](compare/cross-encoder-reopen.compare.md) (recommend **CONFIRM**, rewritten reason) and [`blind-authorship-rule`](compare/blind-authorship-rule.compare.md) (recommend **ACCEPT**, rewritten). Both carry the literature and three corrections to fux's own filed evidence |
| [`../records/`](../records/) | A feature completes (one SR per feature); **any behaviour change, in the same change** | 2026-09-15 | **2026-09-15 (rule 45a hooks):** SR-WORK-OPEN-QUEUE rule 45a amended to name two new PostToolUse guards for OPEN-WORK.md's empty-inbox declaration — `scripts/check-open-work-inbox.py`, `.claude/hooks/guard-open-work-inbox.sh` (blocks) and `.codex/hooks/guard-open-work-inbox.sh` (advisory). · **2026-09-15 (arms settle, 16a):** **decision 16a** — the arms are `A` · `B` · `B-node`; the tier's value returns to W-161. · **2026-09-15 (Node column BUILT, W-188):** decision 16 marked BUILT with the `version`/`reader`/`tier` table; decision 7's `answer-layer.jsonl` name enforced in the harness. · **2026-09-15 (Node as a column, W-188):** SR-WORK-BENCHMARK **decision 16** — `B-node` and `B-node-nograph` join CAP-1/2/3/4; parity reads the opposite way from an A/B pair; reported, never gated; no Node column on CAP-5 or ingest/build. · **2026-09-15 (every report on the spine):** SR-WORK-BENCHMARK **decision 15** — every filed report is rebuilt to the spine; a number that exists goes on it, one that does not says so and names the next run. Supersedes 11a for shape only. · **2026-09-15 (W-187, the CAP slide spine):** SR-WORK-BENCHMARK decision 14 — one slide per capture, titled by its CAP, comparing the two arms; CAP-7 none. **Built the same day.** · **2026-09-15 (CAP slide spine):** SR-WORK-BENCHMARK gained **decision 14** — one slide per capture, titled by its CAP id, comparing the two arms; CAP-7 none. Ratified, not built (W-187). · **L11 — the sealed answer key is a law.** **2026-09-15 (L11):** the sealed-key prohibition became law `L11` ([SR-LAW-11](../records/0012_LAW-11-sealed-answer-key.md)); SR-WORK-GOLDEN now carries only the process, and every law count, range and `L0..L11` diagram moved with it. · **2026-09-14 (W-159 + W-163 + W-164, Claude Code):** eleven records moved from *stated* to *fixed*; SR-WORK-BACKLOG reads `built: yes` and owns its test; SR-CLI §1's verb table gained the `fux output` it had been missing; SR-DOCTOR's register gained nine rows and is now held complete against the code. **2026-09-14 (W-165 + W-166, Claude Code):** sixteen records amended, five of them from *stated* to *fixed* — SR-FUXIGNORE 5a–5b (veto 3 **discharged**), SR-DECODE 11a, SR-INGEST Consequences ×2, SR-PII (the 🔴 before 18), SR-FIND 6 (**veto replaced**: it waited on an event and never fired). SR-DOCTOR gained two rows; SR-CLI's `fux remove` transcript was re-captured from the shipped CLI rather than hand-written. **2026-09-14:** **eight WORK records added, `0057`–`0064`** — SR-WORK-SCALE, SR-WORK-LIFECYCLE, SR-WORK-DOCS, SR-WORK-SESSION, SR-WORK-OKF, SR-WORK-ARCHIVE, SR-WORK-RELEASE, SR-WORK-BLOCKERS, all `kind: process`, all ratified from `CLAUDE.md`'s own prose. Five previously **unowned** enforcement components gained an owner (`test_doc_registry.py`, `test_doc_links.py`, `test_okf_bundle.py`, `test_archive_law.py`, `check-version-parity.py` + `test_version_parity.py`), plus the three `.claude/hooks/` scripts. The WORK range's next free number is `0065`.  **2026-09-13 (the renumber's other half):** ~300 dead `NNNN_slug.md` citations repointed across `src/`, `tests/`, `.fux/decoders/`, the agent skills, `tools/` and live `work/` docs; **`work/regression/`, `archive/`, `work/golden/`, WORKLOG, CHANGELOG and every PRE-REGISTRATION are the stated frozen set** and keep the number that was true when written. New gate `tests/test_record_paths_resolve.py`; SR-WORK-OWNERSHIP decision 15. The register's own *"two ranges"* corrected to three.  **2026-09-13 (W-147 SHIPPED):** the journal's two consent surfaces are BOUND by a test (all four states) and the specimen line now opens `⚠ NOT a rendering key`. Found in the build: `output.toml` is a COMPLETE declaration, and `provenance.py` named the REJECTED `tune.toml` home. **2026-09-13 (W-147 RULED):** SR-PROVENANCE decision 10's false *"only `--journal` WRITES"* replaced by the two-surface ruling (`tune.toml` refused as the home; always-on-by-default still refused), and SR-OUTPUT decision 22 ratifies `journal` as a **declared exception to decision 2's boundary rule** — it leaves the result set identical and still writes bytes.  **2026-09-13 (SR-WORK-BACKLOG):** the backlog is the second work file — `work/BACKLOG.md`, 241 named-but-unclaimed items in five classes, governed by [SR-WORK-BACKLOG](../records/0055_WORK-backlog.md) at `0055`. **2026-09-13 (W-155 SHIPPED):** the table-is-the-answer gap is MEASURED and the answer is YES — `dump` 30/30 → 0/30, `content` 0/30 → 30/30, both controls hold. (b) is right for two document shapes and wrong for a third. Pre-registration committed alone at `a414012`. **2026-09-13 (W-150 SHIPPED):** the benchmark harness files all SEVEN captures — planted key (no corpus byte moved), CAP-2 rows, CAP-3 hit@k, CAP-4 answers, CAP-5 size, CAP-6 phases, CAP-7 HTML. First run filed; 10/10 planted unanswerables ANSWERED by both versions. **2026-09-13 (W-153 SHIPPED):** `mtime` is on every `--json` hit — five readers, `required: always`, `null` a CLAIM. First amendment SR-API's frozen surface has taken. Prose output unchanged; sorting by it is NOT authorised. **2026-09-13 (W-152 SHIPPED):** `archived_weight` and `recency_half_life_days` are REMOVED, with `recency_multiplier` and `stats.json`'s `newest_mtime` — all three DOCUMENT priors are now gone. No ranking change: every one shipped as a no-op. SR-ARCHIVED-CONTENT decision 6 REVERSED; the W-73 bound test re-aimed at `[priority]`. **2026-09-13 (W-151 SHIPPED):** `[ranking] superseded_weight` is REMOVED — the knob, its key, its Node twin and its four records. A committed key is refused BY NAME. No ranking change: it shipped at `1.0`. The `superseded` FACT and the declared tie-break are untouched. **2026-09-13:** SR-OWNERSHIP became **SR-WORK-OWNERSHIP** (`0146` → `0054`, the WORK range); the hole at `0146` closed by shifting `0147`–`0156` down one — a file number is an ordinal.  **2026-09-13 (SR-WORK-BENCHMARK):** what a benchmark run always captures is stated once in [`records/0053_WORK-benchmark.md`](../records/0053_WORK-benchmark.md) — CAP-1…CAP-7, enforced by `tests/test_benchmark_capture.py` from 2026-09-13. `SR-WORK-ENVIRONMENTS` no longer states what a benchmark measures; its no-answer-key clause is reversed. **2026-09-13:** L9 retired — the environment rule is now [SR-WORK-ENVIRONMENTS](../records/0052_WORK-environments.md) (`0052`, `kind: process`), not a law. The handle and `records/0011` are never reused.  **2026-09-13 (SR-WORK-OPEN-QUEUE):** the queue's 53 rules are stated once in [`records/0051_WORK-open-queue.md`](../records/0051_WORK-open-queue.md); this file is the LIST plus the declared ball legend, and states no other rule.  **2026-09-13 (`kind`):** every record declares `kind: law | component | process` — the gate it answers to. 12 · 52 · 4. SR-WORK-OWNERSHIP decision 11; register §The three kinds.  **2026-09-13 (SR rename):** `ADR-<NAME>` became `SR-<NAME>` and `docs/adr/` moved to `records/` at the repo root — one mechanical change, every reference repointed. Archived `ADR-NNNN` citations and `archive/v0.26-docs/adr/` are unchanged.  **2026-09-12 (W-149):** seven records amended in one change — SR-NODE-SEARCH (13-16 RULED→BUILT, decision 15's self-contradicting table corrected), SR-LAW-10 (decision 8 → satisfied), SR-DOTFUX (`node/`, `fux`, `ensure_layout`), SR-DOCTOR, SR-T1-ACCELERATOR (five surfaces → six), SR-ACQUIRED, SR-PII. **2026-09-12 (compare archive pass):** [SR-EXTRACTED](../records/0115_extracted-mode.md) §2 now carries the ingest-mode-naming **matrix in full** instead of a link to it, and its Reference block no longer points at a compare doc — the doc was retired to `archive/compare/` the same change, and *archive is not evidence*, so a record may not ground a decision in one. No behaviour change; no owned path touched. | **2026-09-12 (the held-back code lands):** five records that had described behaviour the engine did not yet have — SR-CONFIG, SR-DOTFUX, SR-PII, SR-ACQUIRED, SR-URL-FRESHNESS — each gains a dated line saying so. ⚠ **The general form is in SR-CONFIG after decision 15: a record AHEAD of its code reads as authority exactly as one behind it does, and `test_sr_freshness.py` sees neither.** **2026-09-12 (W-107 CLOSED):** **SR-NODE-SEARCH `partial -> yes`** — decision 8's gap closed (the reader reads `.fux/tune.toml` and `.fux/output.toml`) and **three new decisions name where Node is deliberately a SUBSET of Python**: 9 the derived graph plane, 10 the `ranked_by` label, 11 the decoder boundary; 12 records the MCP handlers reading the advertised argument names, which they did not, in a package already on npm. **SR-API gains decision 6** — every method through `run_query`, because `fux.api` was ranking without its own tune file; its `under` divergence from the CLI is STATED rather than changed, because the surface is frozen. **SR-T1-ACCELERATOR gains 14** (the arm compares five surfaces, and compared one), **SR-MCP gains 12**, **SR-OUTPUT gains 22**, **SR-TUNE decision 11 becomes two-runtime**. **2026-09-12 (W-107, npm):** **SR-NODE-SEARCH amended** — the *"No global bin ships in the first npm release"* consequence is superseded by Arpit's ruling; the published alpha.7 carries the bin and the deferred `doctor` PATH row is now owed. **2026-09-12 (W-107 Phase 4):** two records accepted — **SR-NODE-SEARCH** (0155, owns `node/`) and **SR-API** (0156, owns `src/fux/api.py`) — with register rows, ownership rows and a `describes` row. **SR-DOTFUX gains decision 6a**, the fourth `.fux/` shape (committed, engine-owned, OVERWRITTEN on a version difference); **SR-DOCTOR gains a `node reader` check row**; **SR-MCP gains decision 11** (the tool descriptions are authored once and held equal by a test, NOT read from one file — saying they were read from one file was the first version of that decision and it was false); **SR-T1-ACCELERATOR** records that `tools/differential/` now holds three arms.  **2026-09-12 (W-138, SR-WORK-ENVIRONMENTS):** SR-RS, SR-WORK-QUALITY, SR-ACCELERATOR and SR-CONFIDENCE gained `SR-WORK-ENVIRONMENTS` in `laws:` (SR-CONFIDENCE's list normalised from `[1, 3, 4]` to L-form); **SR-ANSWER got a provenance note instead of a law**, because its mention cites a filed run rather than naming a procedure the law governs. Each of the five now says, where it cites a playground number, that the corpus is retired and the run is not re-runnable. SR-ACCELERATOR also carries the `playground_grade.py` → `goldens_grade.py` rename and the fact that **the differential law now has no graded instrument at all**. **2026-09-12 (W-122 — the laws come home):** all ten `SR-LAW-n` records now **state** their law in a `LAW-TEXT` block in §2 and stop calling themselves rationale-only; SR-LAWS is the router (decision 1 struck through, 3/4 amended, its §1 diagram and veto rewritten — **the veto FIRED and its remedy was wrong**); SR-LAW-0 gains decision 7 and corrects its own Consequences about `runtime.schema.json`; SR-CONFIG gains decisions 13–15 (the declared key block, unknown keys refused, `config.schema.json` deleted); SR-TUNE gains decision 14 (its own key block); SR-ACQUIRED and SR-URL-FRESHNESS note their keys are now gate-bound; SR-PROVENANCE repointed off `CLAUDE.md` for L8. **2026-09-12 (positioning):** SR-AGENT-POLICY amended (usage renderings drop *"codebase"*; why `policy-version` stays at 1); SR-LAWS notes the `frontmatter.py` docstring still claiming a guarantee L1 withdrew on 2026-09-06, corrected on contact. **2026-09-12 (W-141): SR-AGENT-POLICY decision 16 (Codex + Copilot share `.agents/skills/`, vendor docs cited), with 9a/11/13/14/14a/15a, veto 3 and the references amended; SR-DOTFUX, SR-ENRICH, SR-DECODE touched on contact.** **2026-09-11: SR-RS decision 23 — test data is built so the feature under test can move it (Arpit).** **2026-09-11 (SR-WORK-ENVIRONMENTS): +[SR-WORK-ENVIRONMENTS](../records/0052_WORK-environments.md)** — the first law in the reserved range; SR-LAWS table + frontmatter, register row and range table. **2026-09-11 (W-130 rename):** `.fux/types.toml` -> `.fux/formats.toml`. **2026-09-11 (W-130's describers):** five records amended for the types-list move — **SR-ARCHIVED-CONTENT** (decision 1a's *"both source lists"* now means EVERY committed source list, since `types` left the line grammar), **SR-URL-FRESHNESS** (`ttl` is now the only `validate=` attribute on a LIVE committed list; `decoder`'s spec went vestigial, so the typed-attribute seam has one user), **SR-ACQUIRED** (`keep`'s THREE-layer resolution is what a TOML `urls` would have to answer for — the comparison's reopen trigger 1), **SR-PII** and **SR-REFUSAL** (`pii.toml` and `refusals.toml` were cited as precedent for the move, so their form is now load-bearing outside their own records). **2026-09-11 (W-130):** SR-TYPES decision 12 (+2, 2a, 3, 11, 11a, consequences, alternatives, references, veto 6); amendments in SR-FUXIGNORE 5, SR-DECODE 13, SR-CLI 1a, SR-DOTFUX 6 (third instance) and layout, SR-CONFIG, SR-URL-LIST, SR-DIR-LIST; path mentions in SR-INGEST, SR-TUNE, SR-OUTPUT. **2026-09-11 (W-121 ruled):** SR-TABULAR's veto section records Arpit's ruling — the `informed` evidence stands, no `blind` run owed. **2026-09-11 (W-131):** SR-PII decision 12a — US/Canadian starter rules; decision 12's list. **2026-09-11 (W-128/129):** SR-PII decisions 16–17 (+4/5/6/10/12, veto, §1 diagram + twin, references); SR-DOTFUX decision 6; SR-CLI decision 4; the register's SR-PII row, the template ownership row, and three describes rows (`cli.py`, `setup.py`, `doctor.py`). **2026-09-11 (`[index]`):** SR-TUNE decision 13 (vetoes 1 and 4 fired by ruling), SR-EXTRACTED 9, SR-INGEST 15b, SR-TABULAR 4 reversed + 4a, SR-CONFIG `[decode]` refused, SR-DOTFUX, SR-ASK, SR-PII, SR-CHUNKING. **2026-09-06 (W-110 voided): SR-RS gains a FIFTH outcome, `VOID`** — for a bar that could not be applied, and the record states why it is **not** `INCONCLUSIVE` (the instrument decided; the bar did not) and **not** a soft FAIL (only the adjudication is withdrawn, never the controls). Only Arpit may void one. `tests/test_regression_runs.py`'s closed vocabulary widened with it — ⚠ the same closed-vocabulary gap SR-CODES-TABLE hit, this time closed by adding the missing value rather than mislabelling the record. **2026-09-06 (W-110 voided): SR-WORK-QUALITY gains decision 2a and SR-ENRICH decision 15 is DEMOTED IN PLACE.** 2a: **a bar on `recall@k` names its `k`, or it is void** — an omitted `k` is four bars, and whoever supplies it afterwards picks the verdict, which is a moving threshold wearing an omission instead of an edit; such a bar has not failed and has not passed. A `k` justified by a ceiling must be justified **in the pre-registration**, checkable against the clean-arm curve the record already publishes. SR-ENRICH decision 15 now states **BUILT AND UNPROVEN** as its whole evidential standing, with *do not cite `net +7 at recall@1` as a pass* written into the record, and decision 16's filter marked unproven at a **2 %** treatment. ⚠ **The demotion is written in place of the sentence it corrects, not appended** (the template forbids `Amended` sections). **2026-09-06 (R-3): SR-RANKING gains decision 8a** — `round(score, 9)` is named as the **cross-runtime contract for the score**, with the ORDER byte-equal regardless, on Arpit's option-(b) ruling. The rule lives in SR-RANKING and nowhere else (L0); SR-NODE-SEARCH will **link** to it when that record is created, never restate it. ⚠ Its evidence is `work/regression/2026-09-05-node-log-divergence/` (darwin + glibc, `idf`'s argument domain enumerated exhaustively), and a divergence above `~1e-9` relative on any platform pair voids it. **2026-09-06 (later): `0001_laws.md` → `0001_LAWS.md`** — the index in caps to match the `LAW-n` records that hang from it; **70 files repointed**, and `tests/test_sr_ownership.py`'s `endswith("_LAWS.md")` guard with them. **2026-09-06: +[SR-LAW-0](../records/0002_LAW-0-authority.md) — L0**, the constitutional record (source + precedence + entrenchment), superseding SR-LAWS decision 1 the same day it was written. **The eight law records renamed to `*_LAW-n-*.md` and cited `SR-LAW-n`**; SR-LAWS gains the L0 row and a supersession note above its own decision 1; the register gains the row. ⚠ `0061` was taken by SR-TABULAR between plan and build — the `LAW-` prefix made that harmless. **2026-09-06 (later, W-114 ruled):** **SR-ENRICH decision 10 amended** - `fux-enrich` now ships to all four skill surfaces; the rule was always *never ambient*, never *claude only*, and the record had **flagged the gap in its own text** while it persisted. **SR-AGENT-POLICY 9a amended** (its *"the two skill surfaces - Claude and Kiro"* was a count doing a rule's job), **decision 14** records the ruling, **veto 5c** added. **SR-DOTFUX d9**: `.github/skills/` joins the outside set. **2026-09-06 (W-114):** SR-AGENT-POLICY gains decisions 11-13 and veto 5a/5b - **Codex is the fourth vendor**, decision 3's *"a template plus a rendering plus a row"* held with one correction (it also needed the root-file gate), and 🔴 **decision 13 records that Copilot reads `.claude/skills`**, so `fux-enrich`'s claude-only confinement describes what fux WRITES, not what other agents READ. §1's ASCII twin and console example updated with it. **2026-09-05: SR-FIND's §1 rewritten by a concurrent session** — `find`'s three post-filters described where the record previously described only the projection. Committed here rather than left in the working tree. **2026-09-05 (W-101): SR-PII decision 15** (the redaction counter) and **SR-REFUSAL decision 11** (the refusal counter) are new; SR-ACQUIRED's and SR-URL-FRESHNESS's `as-ingested` debts are CLOSED and both vetoes carry a captured output block; SR-DOTFUX gains its fourth worked instance; SR-INGEST, SR-FETCHER, SR-MAINTENANCE and SR-DECODE record the components that moved under them; SR-WORK-QUALITY gains the clean-corpus `recall@k` and the paired-metric rule. **2026-09-05 (W-111): SR-RANKING, SR-FIND, SR-ASK, SR-CLI, SR-ANSWER, SR-CONFIDENCE, SR-OUTPUT, SR-RERANK and SR-TUNE amended** — the declared tie-break, `tie`, and `find`'s three filters. **2026-09-05 (W-110): SR-ENRICH 15-18, SR-INGEST, SR-AGENT-POLICY, SR-PII amended** — questions instead of prose, the self-retrieval filter, `superseded_by:`, and the reuse defect that made enrichment a no-op. **2026-09-05 (W-109): SR-EXPAND added; eight records amended** — SR-ASK, SR-CLI, SR-ANSWER, SR-TUNE, SR-MCP, SR-CONFIDENCE, SR-OUTPUT, SR-PROVENANCE, SR-RANKING, SR-T1-ACCELERATOR, SR-AGENT-POLICY. **2026-09-02 (alpha.7): SR-ENRICH +14** — one path spelling in the enrichment report on every platform; SR-PII notes it because `refused:` is its surface. **2026-09-02 (verification pass): four more records.** SR-CDP-FETCHER **+7c** — the live Chrome run, with the table showing pre-fix 0/12 at parallel 2 and HEAD 12/12 at 1/2/4/6. SR-INGEST **+15a** — enrichment's own redaction pass and the three traps around it. SR-CLI **+2a** — the `TARGET` positional. SR-OUTPUT — the 2026-09-02 note saying a `nargs="?"` positional is structurally outside decision 10. SR-MAINTENANCE **+8a** — the docstrings that named a retired lock file for five days, found by a pasted capture rather than by any check. SR-PII decision 1 and SR-INGEST 15a now cite measured runs. **2026-09-01 (W-102/104/105): five records.** SR-ENRICH +12 (the enrichment body is inside the redaction boundary) and +13 (`TARGET`, and the skill plans internally). SR-PII decision 1 widened — *the index* means **two** sources of committed vocabulary, and enrichment sat outside the phase while the record read as though it were inside it. SR-FETCHER +9a, SR-CDP-FETCHER +7a/7b, SR-HTTP-FETCHER +7a: 🔴 **all three gave a FALSE reason for `MAX_PARALLEL = 1`** (one shared WebSocket — there had been a socket per fetch for some time), so a reader who fixed the socket would have raised the number over a live hazard. **2026-09-01 (W-99a): SR-TYPES gains decision 11a and SR-DECODE decision 13 is amended — the binding check shipped too tight and refused its own main use case within hours.** `*.geojson decoder=jsondoc` — a NEW extension on an EXISTING decoder — raised, because `_bind` required the extension to be in the named module's `EXTENSIONS`. ⚠ **The defect was a misreading of what `EXTENSIONS` is**: a decoder's DEFAULT CLAIM, not a declaration of what it is capable of reading. A `.geojson` is JSON and a `.cnf` is an INI file; making a consumer copy `jsondoc.py` and edit one tuple to say so would leave the map **able to describe dispatch but not to change it**, which is most of the point of committing it. **Extending is now separated from redirecting**: an extension NO decoder claims may be given to any decoder (additive — nothing to be stale against); an extension another decoder DOES claim may not be handed to a module that does not claim it, and the error now names the claimant. `registry()` snapshots the claimed set before applying any binding so iteration order cannot change an answer (L3). ⚠ **Cost stated rather than hidden:** fux can no longer tell a deliberate extending binding from a typo'd one — accepted because a typo there binds a decoder to a suffix no file has and indexes nothing, while the still-refused direction silently re-reads real documents with the wrong reader. **Same-sized answers for different-sized mistakes was the bug.** Header prose in `setup.py` and `.fux/sources/types` rewritten to say so; 4 new tests (24 in `test_binding.py`); suite **749 / 5 pre-existing**, e2e **74 / 0**. **2026-09-01 (W-99): SR-TYPES gains decision 11, SR-DECODE gains decision 13, SR-URL-LIST decision 12 is NARROWED, and SR-CLI + SR-DOTFUX are annotated — four owning records for four changed components, exactly what the freshness gate demands.** `.fux/sources/types` gains `decoder=<module stem>`: the file now BINDS an extension to the decoder that reads it, and `decode._bind` CHECKS the binding rather than trusting it. ⚠ **This reverses SR-TYPES' "no attributes at all for `types`"**, a rule so settled that `sourcelist.parse`'s own error text says *"Adding one is a change to the record, not a config addition"* — this is that change, made deliberately on Arpit's ruling rather than by accretion. The test that distinguishes it from the rejected proposals is stated in decision 11 and is not *"is this useful"*: **whose property is it?** A `types=` per `dirs` line belongs to the directory and stays refused (veto 1, untouched); `decoder` belongs to the **extension**, which is what a line in this file names — which is also why a binding on `docs/api/*.json` is refused rather than silently widened to every `.json`. ⚠ **Both failure modes are HARD ERRORS, never fallbacks**, and that is the load-bearing half: a binding naming a missing module, or one whose `EXTENSIONS` does not claim the extension, stops the run — because **the wrong decoder does not fail visibly**, it emits a plausible document and a plausible index, which is decision 7's argument one layer up. ⚠ **SR-URL-LIST decision 12 (*"a fux-written line carries every attribute"*) is narrowed, not waived**: an attribute at an EMPTY default is omitted, because a bare `decoder=` states no policy and cannot be diffed into one; all six existing attributes have non-empty defaults and are unaffected, which is checkable rather than asserted. ⚠ **A stale line was found and dropped**: the setup template listed `#*.svg` as a commented opt-in under a heading reading *"nothing here has a built-in decoder"* while `*.svg` had been an **active** default line since `svgdoc` shipped 2026-08-29 — the file contradicted itself for two days, and `tests_e2e` was pinning the wrong half. ⚠ **Verified in a Linux container, NOT on the device** (`device_bash` wedged all session): `tests/` 400 passed / **5 pre-existing failures recorded as W-100**, `tests_e2e/` 74 passed / 0 failed. **2026-09-01 (W-98 Phase 1): SR-CDP-FETCHER rewritten in place; SR-FETCHER amended in the same change.** The browser fetcher stopped rendering: §1 (prose, Mermaid **and its ASCII twin**), decision 5, four new decisions 10–13, consequences, four new rejected alternatives, two new veto checks and the reference block. ⚠ **The technique changed on a MEASUREMENT, not a preference** — an in-page `fetch()` via `Runtime.evaluate` returned `TypeError` on a cross-origin URL sending no `Access-Control-Allow-Origin` where interception read **8557 bytes**, and a cross-origin in-page fetch cannot see `ETag` at all, so `validate()` could never have been built on it (decision 10's table). ⚠ **Decision 12 states a limit rather than letting it be assumed**: interception is at `requestStage: "Response"`, so a matching `ETag` saves the decode and the shard comparison, **NOT the download** — the word *validate* invites the opposite reading. ⚠ **Decision 11's invariant has no mechanical guard inside Chrome**: an unresolved paused request wedges the page until the timeout and **does not raise**, so it reads as a slow site; `tests/ingest/test_cdp_fetcher.py` asserting each is resolved exactly once is the only thing standing between it and a plausible regression. **SR-FETCHER was NOT demanded by the freshness gate for its own sake but owns `src/fux/templates/`** — it gains the reason the two shipped fetchers must stay on one axis: decision 5 forbids escalation between them, which is only coherent while both produce the same kind of thing, and a renderer beside a downloader would make *which fetcher ran* a fact about the committed index. **2026-08-29: SR-DECODE gains decision 10a + SR-TYPES decision 5 amended** — `jsonldoc`, `svgdoc` and `imagedoc` shipped as built-ins (sixteen → nineteen), and because SR-TYPES decision 1 unions every built-in's extensions into `DEFAULT_TYPES` automatically, this reverses the SVG half of decision 5 (images and `.jsonl` were never named by it, so those are additions, not reversals) — the same move `.json` already made on 2026-08-26. Both records cite each other rather than restate. `docs/handbook.html`'s decoder count and module list, and `work/architecture-decoders.svg`'s count and header, updated in the same change; `work/architecture-high-level.svg` deliberately left alone — its format list is illustrative, not exhaustive, and this diagram is ruled to move rarely, at the *what fux is* altitude, not per decoder. **2026-08-29: SR-CLI decision 11 + SR-CONFIDENCE decision 14** — cross-reference notes only, no new ruling. The working tree's `sections` flag pair (`cli.py`) and `headings` gate (`query/__init__.py`) were ratified under SR-OUTPUT decision 21 on 2026-08-28 but had not touched their own owning records, which `test_sr_freshness.py::test_working_tree_is_not_mid_violation` caught as a genuine device-only failure (the mirror run that verified decision 21 never saw it — the gate diffs the working tree against `HEAD`, and a mirror has no `HEAD` to diff against). `pytest -q tests tests_e2e` now **2425 passed, 3 skipped, 0 failed** on the device, superseding the mirror's 2201/21. **2026-08-28: SR-CONFIDENCE decision 13 + SR-TUNE decision 5d** — the two band floors become `[confidence]` keys in `.fux/tune.toml`, **reversing SR-CONFIDENCE decision 7**, which had refused them. Decision 7 is kept in place, quoted, with the half that survives named. ⚠ **SR-TUNE decision 4 was STALE and is fixed in the same change:** it said *keys ship COMMENTED* and the specimen has shipped **live lines** since Arpit's 2026-08-27 ruling — the record's own specimen block was a commented copy of a file that is no longer commented. That is the W-83 class (a record describing behaviour the code does not have), found by re-reading the record the change landed under rather than by any check. — carried forward: **2026-08-27 (W-93, second pass, Arpit's correction): SR-FUXIGNORE gains decision 11 + 11a-e, and SR-INGEST decisions 4 and 15 are REWRITTEN IN PLACE.** `fux ingest` now writes the skip list into the **committed** `.fux/.fuxignore` as two delimited blocks, and **deletes `.fux/runtime/skipped` on every run** - the list lived in a derived, gitignored file nobody could review. Five properties carry it and each closes one failure: the blocks are written **FIRST** (last match wins here, so a block written last would silently beat a human's `!`); a block line is a **literal path**, never a glob; **which block a line sits in IS its class**, so decision 15's split survives with nothing parsing note text; **the note is the reason that PUT the line there**, so run 2 does not answer *why* with *"because run 1 said so"*; and **a hand-written pattern suppresses the generated lines it covers** (`__pycache__/` + `*.py[cod]` collapse 257 of this repo's 599, leaving 342). ⚠ **The accepted cost: a generated line DECIDES, so it FREEZES its verdict** - widen `sources/types` and the listed `.py` files stay out; write content into a file listed as `empty` and it stays out. **Arpit's explicit call, stated before he made it, not undone** - made loud by a stderr staleness warning (`gitdir.would_index`, applied in the walk's own order so it cannot drift, reading bytes only for a path that passed both lists). ⚠ **Two losses recorded rather than worked around**: a URL has no repo-relative path, so **W-88's report-once promise now covers files only** and a URL skip prints every networked run (`url-state.json` remains the home for repeat failure); and **ingest now writes one of its own inputs**, so a new skip dirties the working tree on the hook path (an unchanged run writes nothing, so steady state is quiet). **Rejected: inferred patterns** - six lines instead of 342, but an inferred pattern can over-reach onto a file the corpus does not have yet, and that failure is a document silently missing. ⚠ **SR-DOTFUX was checked and NOT touched**: neither the record nor `fuxdir.py`'s layout table ever named `runtime/skipped`, so there was nothing to correct - verified by reading both, not assumed. **2026-08-27 (W-93, first pass): SR-INGEST gains decision 15** — a skip carries its CLASS (`POLICY` — a committed list said no · `UNREADABLE` — fux opened it and could not read it), **set where the skip is made and never parsed back out of the reason string**, and `fux ingest` counts the two separately: `598 not indexed, 1 skipped` where it used to say `599 skipped`. Decision 4's *"what this does not touch"* line is rewritten in place (the summary count is the one thing that moved), §1's prose, Mermaid **and its ASCII twin** carry the split, and the veto gains two checks — one that no code derives a class from a reason string (**one known hit, `sources.py`, named in the check so it is green today rather than shipped red**), one that `--list-skipped` still matches `.fux/runtime/skipped` byte for byte. ⚠ **Nothing machine-readable moved**: `--list-skipped` and the notice file stay `path: reason`, sorted and unprefixed, because things pipe them. ⚠ **The finding behind it**: Arpit read `599 skipped` as 599 files that should be in `.fuxignore`; the walker records an *ignored* file as skipped too, so that remedy would have changed the reason string and left the count at 599 — and per-file lines would have frozen a *derived* allowlist verdict into a file that **outranks** the allowlist. **No other record was touched and no committed byte moved.** **2026-08-27 (W-89, Arpit): SR-LAWS gains `L8`** — `laws:` key, description, feature line, *seven rules* → eight, the handle-table row, **decision 8**, a third veto check, and the AOL-2006 grounding (a de-identified query log that still identified a named individual). ⚠ **Decision 8 records what L8 does NOT fix**: a hashed key is not anonymity while the cited locators sit in the value — the file adds *frequency*, not new exposure, and L8 bounds it without making it uninteresting. **SR-WORK-QUALITY** decision 11 and its debt line repointed from *"not settled, filed as W-89"* to the ruling; its `laws:` key gains `L8`. **No code changed and no other record was touched.** **2026-08-27 (latest, Arpit): EVERY RECORD REWRITTEN — the metadata is stated once, and no record carries history.** Three instructions, all structural rather than cosmetic. ① **Frontmatter was defined twice** — a YAML block and, immediately below it, a `- **Name:** / **Status:** / **Date:** / **Feature:** / **Owns:** / **Laws:**` bullet list restating it. The bullet block is deleted everywhere; the frontmatter is now ten keys in a fixed order (`type · name · title · description · status · date · feature · owns · laws · timestamp`, plus optional `supersedes`/`ratifies`) and the body opens at §1. ② **`Amended` sections are abolished** — the current truth is written **in place** of the sentence it corrects. ⚠ **This is the W-83 class attacked at the source**: an amendment leaves the false sentence standing above its own correction, an agent reads top-down and acts on the first answer, and no mechanical check has ever been able to see it. ③ **Historical prose removed** — W-nn narrative, dates, renumbering notes and superseded text are gone; **what survives is every argument that still binds** (rejected alternatives, the ⚠ warnings naming silent failure modes, measured evidence), because the failure is the argument and the date it happened is not. **Depth: verified against code, not merely tidied** — the components each record owns were read, and stale claims were corrected rather than reformatted: SR-DOTFUX's *"veto condition 1 has FIRED"* (fixed by `COMMITTED_FILES`), SR-ARCHIVED-CONTENT's *"`archived_weight` lives in `fux.toml`"* (it is `.fux/tune.toml`), SR-TUNE's `_weight`-suffixed `[bm25f]` keys (they are bare field names), SR-DECODE's veto 3 contradicting its own superseded decision 9, SR-AGENT-POLICY's reference to a *"decision 10 AGENTS.md"* that does not exist, and SR-MCP's `## 1 · Examples` shape (converted to §1/§2). **Status flipped where the code exists** (Arpit's ruling): the end state is **43 `accepted`, 2 `proposed`** — SR-LOCKS and SR-CONFIDENCE, both unratified — and the exact before/after list is `git diff records/| grep '^[-+]status:'`, which is where a per-record history belongs now. **`0025_codes-table.md` ARCHIVED** with no successor: its subject (`codes.jsonl`, the dense lane) was deleted, and the closed status vocabulary's missing value for *a record whose subject ceased to exist* is answered by moving it, not by inventing a fourth status. ⚠ **Two records had no `# ADR-…` H1 at all** — fixed on `0141_confidence.md`; `0141_confidence.md` is a stale duplicate that must be `git rm`'d, and is left in place because it belongs to a concurrent session. **2026-08-27 (later): +[SR-CONFIDENCE](../records/0141_confidence.md), and THREE records amended in the same change** — SR-ASK (`rank()`'s `stats_out` out-parameter and the analyzer's second view), SR-T1-ACCELERATOR (the differential law now covers the block; and the block bound is *why* `support` cannot be a corpus-wide count), SR-MCP (`fux_search` carries the block and its tool description names the action per band). ⚠ **The record's own floor was rewritten on contact with a concurrent session's work**: SR-WORK-QUALITY decision 6 had frozen `t = 0.75` the same day, so `SEPARATION_FLOOR` is demoted from a threshold to a **proxy calibrated against it** — two abstention thresholds governing one decision is drift with extra steps. ⚠ **`separation` is ordinal and Chow's rule assumes a calibrated probability**; the record states the gap rather than closing it, and R10 must close it or say it is a heuristic. **2026-08-27: +[SR-WORK-QUALITY](../records/0056_WORK-quality.md)** — the quantity SR-RS was always silent about. Four gates, `recall@k` as the headline, `nDCG` demoted to a diagnostic on structural grounds, the query prior declared and versioned, `unanswerable` **inside** the gate, and the `judged` series pinned and never fused. ⚠ **The load-bearing part is an ORDERING, not a number:** the cost of an error (`t = 0.75` → `c = 2`) is frozen in [`tools/quality/mix.toml`](../tools/quality/mix.toml) **while `recall@k` is still uncomputed** — after a score exists, any weight is tuning and a metric chosen to flatter is undetectable. Veto condition 3 is the check. ⚠ **It carries no output block, deliberately** — nothing has been measured under the contract and an invented transcript is worse than none. ⚠ **Decision 11 declines a query log and explicitly declines to settle whether L2 reaches one** (W-89) — do not close that by inference. **New owned component `tools/quality/`**; `tests/test_sr_ownership.py` needed no edit, because it parses the table rather than hard-coding it. **2026-08-27: +[SR-LOCKS](../records/0140_locks.md)** — the index write lock stated in one place: `.fux/runtime/write.lock`, a pid, one `O_CREAT+O_EXCL` syscall, gitignored, held by **every** index writer and by **no** read verb. **Scoped by Arpit to fux-as-a-package** — a first draft also mapped `.claude/.locks/` (agent-session), `uv.lock` (a dependency pin) and `.git/index.lock` (git's); all three were **cut**, because this record is about how fux works, not about every file in the tree with `lock` in its name. ⚠ **Three sibling files that are NOT locks** — `runner.stop`/`daemon.stop`/`daemon.pid` — are named as such, because reading a stop file as a lock is the available mistake. **It owns nothing on purpose** (every component it describes is already SR-MAINTENANCE's) and the register's ownership prose now carries a seventh own-nothing paragraph — ⚠ **with the cost stated: the freshness gate cannot demand this record, so nothing mechanical catches it going stale.** Also **+`write.lock`** in the GLOSSARY, a term live in code, records and a compare doc with no definition. ⚠ **One debt named and deliberately NOT fixed**: `runner.lock` survives in three places describing a file that no longer exists (`maintain/runner.py` and `maintain/daemon.py` docstrings, SR-MAINTENANCE decision 11a, where the false sentence stands **above** its own correcting amendment — the W-83 class). All three belong to one change against SR-MAINTENANCE, which a concurrent session holds; taking it would be a second writer on one asset, which is what the record is about. **2026-08-27 (W-88): three records touched for one behaviour change** — **SR-INGEST** decision 4 amended (a skip is reported ONCE, not every run; the rule is *defended*, not weakened, because a wall nobody reads makes a dropped file as invisible as silence would) plus a second-run capture; **SR-DOTFUX** amended (`runtime/` gains a third derived file, `skipped`); **SR-CLI** annotated as **describing but not owning** — its `fux ingest` captures show the un-suppressed shape and stay verbatim. Only SR-INGEST was demanded by the freshness gate; the other two are W-77's governance gap met deliberately. **2026-08-26 (four forks ruled): SR-TYPES, SR-FETCHER and SR-DECODE amended.** `DEFAULT_TYPES` widened from six globs to thirty-six. ⚠ **SR-DECODE decision 9 is SUPERSEDED and its wrong text kept visible** - it had claimed reversing verdict G needed a new pre-registration at 10 000 documents; the rule governs frozen thresholds, and G's contents were *a defaults judgment rather than a measurement*. The error is recorded, not edited away. **2026-08-26 (the `fux-decoder` skill): SR-DECODE decision 12 + SR-AGENT-POLICY amended.** A fourth agent rendering, exempt from the verbatim policy block and the third name on an escape hatch a test pins. Vendor choice follows SR-ENRICH decision 10's **reasoning** rather than its vendor list - a Kiro skill is progressive-disclosure, only Kiro steering is ambient. **2026-08-26 (W-86 P7): SR-DECODE decision 11 + SR-DOTFUX amended.** `.fux/decoders/` is a committed child unlike any other: setup writes all sixteen and **the copies are what run**. ⚠ Recorded explicitly as **Arpit overruling the item's own recommendation**, with the declined alternatives named, so a later session does not 'restore' the old behaviour from §13.4's prose. **2026-08-26 (W-86 P2-P5): SR-DECODE gains decision 10** - the per-format judgements, each recorded because it is a place a later session would otherwise 'fix' in the wrong direction (DOCTYPE refused outright; zip members sorted, slides numeric; runs joined with NO separator; Title->H1 above Heading 1->H2; JSON keys sorted; YAML aliases never expanded; notebook outputs dropped; PDF scanned not xref-parsed, with the merged-CMap cost stated). **2026-08-26 (W-86 P1): +[SR-DECODE](../records/0139_decode.md), and FOUR records amended in the same change** — SR-INGEST (the walker's skip rules and the `parse_document` seam), SR-FETCHER (the shipped templates carried the converter too), SR-HTTP-FETCHER and SR-CDP-FETCHER. ⚠ **SR-HTTP-FETCHER decision 7 asserted a test that does not exist** — *"a test asserts the two agree on the same input"*, where the cited test asserted determinism and heading handling and never agreement. **The freshness gate cannot see this class of defect**: the record was touched, current, and wrong about its own evidence. Second instance after W-83's self-contradicting amendment. **2026-08-26 (W-84): two records amended for one feature** — [SR-ASK](../records/0103_ask.md) gains **decision 10** (heading level, and the argument for why line level is refused: a range computed at ingest lies after one edit, and costs 2–4× the postings) plus a fourth veto check that the selection stays display-only; [SR-MCP](../records/0136_mcp.md) gains **decision 9** and records that its own `fux_search` **tool description** claimed line-range citations it has never returned — documentation compiled into the package that **no gate reads**. **2026-08-26 (W-85): SR-CONFIG and SR-DOTFUX amended again** — the key is required, the table ships live, and SR-DOTFUX gains the general rule that a write-if-missing template change reaches new repos only. **2026-08-26 (W-83): SR-CONFIG, SR-FETCHER and SR-DOTFUX amended in one change.** SR-CONFIG's W-82 amendment was **internally contradictory** — *"`None` means whatever the fetcher declares"* four paragraphs above *"default `4` when a fetcher declares more"* — and the code had implemented the wrong one; the first sentence is repaired. ⚠ **The gap it exposes is unfixed**: the freshness gate checks that a record was *touched*, never that it is *coherent*. **2026-08-26: the committed record's shape moved into ONE template** — `store/index-record.json`, loaded by `store/recordshape.py`. It had lived in four places that agreed only by habit. No committed byte moved (asserted by comparing canonical encodings); template key order is presentational because `canonical_dumps` sorts keys, and that is asserted too. **2026-08-26 (post-build): the register states what SHIPPED** — ten rows annotated with their W-82 phase; the detailed diagram redrawn (it still showed `vectors[]`, sign codes and dense fusion, all deleted 2026-08-25); `docs/guide.html` written. **2026-08-26 (W-82 built): five phases landed** — the URL health report, the detector, parallel fetch and the cap, the changed/unchanged line, and the agent surface. Nine records amended in the same change; 1 433 unit tests green. ⚠ `tests_e2e/` unverified in the build environment (fails identically on a clean tree). ⚠ 3.0 and 3.5 did not land — neither is a code task. **2026-08-26 (the consolidation): W-74, W-75, W-77 and W-81 merged into [`open/W-82-the-consolidated-build.md`](../archive/open/W-82-the-consolidated-build.md)** — a merge, not a close; every fork stays open. Nine documents archived (4 open detail files, 3 proposals, 2 compare docs), each with a successor row in [`archive/README.md`](../archive/README.md). ⚠ **Both compare verdicts were folded into W-82 §4 VERBATIM before the move**, because archiving a compare doc makes its verdict uncitable. Four calls made the same day; `update --warm` and `answer --memo` **withdrawn**. **2026-08-25 (later): THIRTEEN records amended for the model removal** — SR-T1-ACCELERATOR, SR-ASK, SR-CLI, SR-TUNE, SR-EXTRACTED, SR-INGEST, SR-RECORD, SR-DOCS-TABLE, SR-ENRICHED, SR-CONFIG, SR-PORT-LIST, SR-CODES-TABLE (dead — its whole subject was deleted) and SR-RS (decision 16, the mirrored pre-registration). ⚠ **SR-CODES-TABLE exposed a gap in the closed status vocabulary**: `accepted | proposed | **2026-09-14 (blocker walk, Cowork):** `0157` SR-OBSERVE filed `proposed`; SR-LAW-0 decisions 2a and 4a; SR-LAW-10 decision 2 (third exemption); SR-RS, SR-TUNING, SR-GRAPH, SR-ENRICH lose the single-corpus routing. **2026-09-12 (W-149): SR-NODE-SEARCH +decisions 13-16, each marked RULED / NOT BUILT** — the consumer gets a published artefact not source; the bundle is built at publish time into both registries; a monorepo is auto-detected and wired up ([measured](regression/2026-09-12-workspace-dotpath-probe/report.md)); the `.fux/fux` shim resolves the bin in three rungs. **Decision 6 gains its reach clause** — what is authored is no longer what ships. Two consequence bullets: the 47-file paragraph is KEPT as the measurement that produced the ruling, and **a sixth arm surface, the bundle, is owed**. ⚠ **The record is now AHEAD of its code** — `643305b`'s lesson, same day — so the markers flip in the change that builds it. **SR-DOTFUX: three rows amended** — `node/` (47 files / 223 KB, superseded when W-149 lands), `ensure_layout` (**the manifest edit is `setup`'s alone**, or a no-op ingest rewrites the consumer's `package.json`), and `fux` (the shim becomes a resolver: pnpm and bun do not hoist the bin). **2026-09-01 (red-suite sweep): SIX owning records amended** — SR-CONFIG d12 (`[sources.url]`'s four new keys; `acquired_max_bytes` was documented before it was parsed), SR-INGEST d15–16, SR-ENRICH d11 (+ the 🔴 `.fux/enrich/` hole stated in the record), SR-REFER d19–20 with veto 4 widened and 4a added, SR-ASK (six-state enum) and SR-OUTPUT (three new flags that decision 10 deliberately does not reach). superseded` has no value for a record whose SUBJECT ceased to exist. It stays `proposed`, which is literally true, rather than inventing a fourth status or repeating the SR-T2-SEGMENTS deletion that put two records on number `0022`. **That gap is Arpit's to close.** **2026-08-26 (W-79 finished): four records amended for one deletion** — [SR-ASK](../records/0103_ask.md) (decision 9 extended; the `--hybrid` table row names `query/dense.py::fuse`, not RRF; the §2 transcript annotated as a capture of the deleted lane), [SR-TUNE](../records/0135_tuning.md) (the *"is **not** deleted"* note reversed, the twelve-constants context dated to ten, a dead reference link removed), [SR-CLI](../records/0101_cli-surface.md) (*"via RRF"* struck from decision 6), [SR-PORT-LIST](../records/0114_port-list.md) (the RRF row struck with a retirement note; rule 1 still governs revival). **2026-08-25: two records touched for W-78 ruling 2** — [SR-RS](../records/0133_predictions.md) gains decisions 11-15, four veto conditions, a §1 human section and the literature (TREC, CONSORT 2025, ARRIVE 2.0, Kaufman & Rosset, Kriegeskorte, KDD 2024 source bias, doc2query, BIG-bench canary); [SR-ENRICH](../records/0137_enrich.md) gains a pointer saying why its silence on authorship is deliberate and where the rule now lives. ⚠ SR-RS decision 15 is filed `NOT BUILT` rather than written as in force. **2026-08-24 (later): nine records amended for the SR-TUNE build** — six that OWN the changed components (SR-ASK, SR-CLI, SR-CONFIG, SR-DOTFUX, SR-GRAPH, SR-REFER) and **three that only DESCRIBE them** (SR-RANKING, SR-T1-ACCELERATOR, SR-RUNTIME-STATS). The freshness gate demanded only the six; the other three are W-77's governance gap met deliberately **2026-09-12 (the progress bar):** **SR-CLI gains decision 12** — the bar's line budget is the terminal's width less one column, not the hard-coded 80 that was eliding paths on a window with room to spare. 80 is kept as the fallback for a pipe, a Windows console reporting 0, or a test's fake stream; the display-width-vs-`len()` gap for CJK/emoji paths is named unguarded. |
| [`golden/questions/README.md`](golden/questions/README.md) | **The questions are re-released, or the key is regenerated** | 2026-09-12 | **2026-09-12: created.** `questions/questions.jsonl` — 124 lines, `{"id","question"}` and nothing else — exists **before the ladder is frozen**, on Arpit's instruction, so a chat agent reads the questions instead of asking him. 🔴 The README states the two things that are easy to get wrong: the file omits every other key field, and **ids carry no type signal** — rows were permuted with a fixed seed and renumbered, because `g107`–`g118` had been a contiguous `unanswerable` band a runner could abstain on by arithmetic. It also states the cost: the release ordering in [`../README.md`](golden/README.md) is broken on purpose, phase 2 is on its honour, and when Codex regenerates the key the ids change and any prediction file written against today's ids is worthless. |
| [`golden/README.md`](golden/README.md) | **The benchmark process changes, or a W-136 phase runs** | 2026-09-15 | **2026-09-15 (W-136 prompt 4 — the ladder was STALE):** the eight rungs were re-frozen from the current seed after `0aa4bbcf` moved 7 of the 20 seed documents under them; §*Re-frozen on 2026-09-15* added, the build heading re-dated, and the determinism claim marked **measured** (0 of 18 800 generated documents drifted on re-derivation). · **L11 — the sealed answer key is a law.** **2026-09-15 (L11):** the sealed-key prohibition became law `L11` ([SR-LAW-11](../records/0012_LAW-11-sealed-answer-key.md)); SR-WORK-GOLDEN now carries only the process, and every law count, range and `L0..L11` diagram moved with it. · **2026-09-15 (W-146 row 17):** §The one rule **stopped restating the prohibition** and now links [SR-WORK-GOLDEN](../records/0066_WORK-golden.md). It was the second copy, and nothing had noticed because no record owned either. The guard table stays — what each guard *stops* is this file's, not the record's. **2026-09-13 (SR rename):** `ADR-<NAME>` became `SR-<NAME>` and `docs/adr/` moved to `records/` at the repo root — one mechanical change, every reference repointed. Archived `ADR-NNNN` citations and `archive/v0.26-docs/adr/` are unchanged.  **2026-09-12 (W-136 phase 2 COMPLETE):** the cap is lifted and the ladder is **all eight rungs to 10 000** — `rung-02000`/`05000`/`10000` built from the same committed generator and seed, 10 000/10 000 carrying an `mtime`, 1 003 archived and 1 002 superseded declared at the top rung, **nesting verified byte-for-byte across all eight**. ⚠ The three new rungs were built **after** `questions/` was opened, so what protects them is the generator's determinism rather than the commit ordering that covers the first five — stated, not glossed. **10 000 is the ceiling**: no rung above it may be built. **2026-09-12 (W-136 phase 2 run):** §Feature coverage — the `heading` negative control row is FILLED (32 `ext/sibling/` heading-matched distractors at rung 100, 392 at rung 1 000), so it is no longer *not measurable yet*; new §*Built on 2026-09-12* carries the five rungs, their archived/superseded/`mtime` counts, and that rungs 2 000–10 000 are **not built**. **2026-09-12 (questions released early): the layout gains `questions/`, phase 3 step 3 moves to phase 1, and the release-ordering paragraph gains its ⚠.** 🔴 A rung built after the questions are released is `informed` for good, and the questions now exist first — so the phase-2 bullet is red, prompt 2 gains a prohibition, prompt 4 is told to read the file rather than ask, and **nothing mechanical enforces any of it**. Step 3 also gains the ids-carry-no-type-signal rule. **2026-09-12 (positioning §6b):** +`type: Index` frontmatter; `seed/` and `golden-answer/` declared outside the OKF bundle so no docs rule can ever justify editing the sealed data. **2026-09-12 (W-145): phase 1 closed PROVISIONALLY and the file says so at the top** — Arpit's Codex quota ran out mid-phase-1, so he ruled that Claude write the feature-coverage documents and the 124-question key. 🔴 The new §*The key in use today is Claude-authored and provisional* states the defect plainly: **nothing leaked**, but the same model family now authors the questions and grows the corpus, so the file's own *"Claude wrote the brief but no facts"* property is **false for the key and for documents 11–15 and `seed/archive/`**, and **every run scored against it is `informed` with no delta stateable**. Prompt **1b merged into prompt 1 and deleted** (Arpit), so the *who does what* table and the phase-1 bullet lose the 1b link and `~100 questions` becomes `~120–125`. §*Feature coverage* is **filled in**: 4 superseding pairs and 5 archived documents named by file, counts 12 / 9 / 7 — so no row still reads *(filled by …)* and all three priors are measurable for the first time. **2026-09-11 (d23): §Feature coverage, prompt 1b, phase-2 declare-and-date mechanics, `rung-seed`.** **2026-09-11 (SR-WORK-ENVIRONMENTS):** the ladder moves to fux-lab; the test data is fux-lab's only. **2026-09-11 (later): §Where the key lives — no key file by default; Codex asks file-or-chat in phases 1/3/5.** **2026-09-11 (later): phase 1 points at the Quillfern brief in prompt 1; every seed extension must be indexed.** **2026-09-11 (later): one directory and one index per rung.** **2026-09-11 (W-136): created** — the sealed golden benchmark: Codex seeds 10 docs + ~100 Q&A in `golden-answer/` (never read by Claude), Claude grows 10 → 10 000 blind, Codex freezes and releases questions, Claude runs, Codex scores. Five prompts under `golden/prompts/`. |
| [`setup/`](setup/README.md) | **An external dependency is set up differently, or a new one appears** | 2026-09-13 | **2026-09-13 (W-150 SHIPPED):** the benchmark harness files all SEVEN captures — planted key (no corpus byte moved), CAP-2 rows, CAP-3 hit@k, CAP-4 answers, CAP-5 size, CAP-6 phases, CAP-7 HTML. First run filed; 10/10 planted unanswerables ANSWERED by both versions. **2026-09-12 (W-107 O3):** SETUP-BENCHMARK **inherits the Node latency fence** (p95 ≤ 150 ms at 10 000) moved out of PRE-REG-NODE-2 by Arpit — and states that nothing measures it until fux-benchmark is built. **2026-09-12 (W-139): SETUP-BENCHMARK gains standing rule 0a — ONE PROCESS PER `runs/<id>/`, measured the hard way.** Two sessions chose the run id `2026-09-12-l9` three hours apart; `bench.py latency` opens its CSV with `"w"` and **silently truncated the first sweep's 10 000-document pass**, while for the overlap the two were timing each other's CPU. ⚠ **The null control cannot see this class** — it compares one arm to itself inside one process. The harness now takes an owner lock; the shape block also names `bench_v1_mcnemar.py` as the retired harness. **2026-09-12 (W-138, SR-WORK-ENVIRONMENTS): ALL THREE REWRITTEN.** SETUP-PLAYGROUND stops saying *grades* — its graded contract is a collapsed history block and decision part 4 is marked retired; SETUP-LAB names the golden ladder as its corpus and **closes** the 2026-08-22 five-repo redesign that had sat unexecuted for three weeks; **SETUP-BENCHMARK is rewritten as the spec W-139 builds against** — seven corpora it generates itself, a query set with no answer key, two arms every run, latency + ranked lists, and a `## The two-session blind protocol — retired here` section explaining why the protocol moved to the lab rather than vanishing. `README.md`'s *which is which* is a pointer table now, not a restatement of SR-WORK-ENVIRONMENTS. **2026-09-12 (positioning §6b):** `setup/README.md` +`type: Index` frontmatter. **2026-09-05:** W-108 → W-107 Phase 0. **2026-08-28 (W-97): `SETUP-BENCHMARK` gained the two-session blind protocol** (the paragraph W-96 said it owed), standing rule 1 corrected — the null control is the **same-corpus repeat**, a cross-seed pairing is a rate check — and rules 6 (a knob sweep never re-ingests, and hashes the index per pass) and 7 (procedure in a runbook, bars in a pre-registration, neither restates the other). `bench.py`'s shape notes the sweep verbs W-97 owes. **2026-08-28 (W-93): `SETUP-BENCHMARK` is now a THIRD sibling and the index says so** — `README.md`'s *"two working directories"* became three, with the row and the *which-is-which* paragraph. ⚠ **`tests/test_setup_docs.py` was RED before this**: the document was added on 2026-08-27 without its index row, and the prior session's in-flight line claimed a green suite. **SETUP-BENCHMARK was also CORRECTED to what was actually built** — it described seven `bin/*.sh` scripts that were never written (the real harness is `bench.py` + `latency.py`), it said `ARMS.toml` lives at the harness root (it is generated into the run's evidence, because a manifest beside the harness describes whatever the harness is today), and it now carries the three things the first run taught: a saturated suite is indistinguishable from two equal engines, one session cannot produce a `blind` run, and `fux answer` writes its repeat-query note to stderr. ⚠ **2026-08-25 (later): a CORRECTION to my own claim.** The block saying fux-playground's `tune.toml` still had `[dense]` and that every command there errors was **wrong** — inferred from fux's own config and asserted three times unchecked. What is actually owed is real and different: its index is `fux.index.v1` with a **two-field analyzer**, which current fux refuses outright. **2026-08-25 (cleanup): the playground chore filed where it SURVIVES.** `fux-playground/.fux/tune.toml` still carries `[dense]` and now errors on every command; that instruction existed only in `NOW.md`, which is overwritten every session. Also annotated: the nine xfails were pre-registered targets for the dense lane (half of them now unreachable) and two `fux-lab` findings are about a deleted lane on corpora that no longer exist. **2026-08-22 (later): both docs gain a "⚠ Planned redesign — not yet executed" section** — `SETUP-PLAYGROUND`'s goldens/grading contract is slated for retirement (personal sandbox only, Arpit's call); `SETUP-LAB`'s single-repo shape is slated to split into five independent per-tier repos (10/100/1000/5000/10000). Neither external repo was actually touched — both sections describe a plan, not a completed change, and each names open questions (where W-57/W-59's graded phenomena go; how `shared/` is reused across five independent repos) that gate execution. **2026-08-20 (later): both docs now describe environments that exist**, are under git, and name what could not be restored. **2026-08-20 (W-56): both setup docs gained a `Rebuilt` section, and both describe environments that now exist again.** SETUP-LAB's environment table is relabelled *existed before 2026-08-20* — every baseline and corpus in it is gone, including `rfc`, the 8 872-document corpus **R3's 27.2 ms was measured on**. SETUP-PLAYGROUND records that the corpus and harness were rebuilt and the **~50 goldens deliberately were not**, and that this invalidates W-57's named query ids. **New row.** `fux-playground` (`~/my_programs/fux-playground`, grades) and `fux-lab` (`~/my_programs/fux-lab`, measures) — neither is in this repo and neither is reconstructible from it. `SETUP-PLAYGROUND` was SR-PLAYGROUND until 2026-08-18; `SETUP-LAB` was written the same day, the lab having gone weeks without being written up anywhere|
| [`../tests/test_setup_docs.py`](../tests/test_setup_docs.py) | **The setup-doc rules change** | 2026-08-28 | **2026-08-28 (W-93): found RED, not changed** — `fux-benchmark.md` had been added without its `README.md` row. The test did its job; the index was fixed. **New row.** Every doc in `work/setup/` declares `type: Setup`, a name, and a **`location` that resolves outside this repository** — if it were inside, it would not need a setup document |
| [`../tests/test_okf_bundle.py`](../tests/test_okf_bundle.py) | **The OKF bundle's scope or its `type` bar changes** — a directory leaves the bundle, or the pre-2026-08-25 regression freeze baseline moves | 2026-09-13 | **2026-09-13 (SR rename):** `ADR-<NAME>` became `SR-<NAME>` and `docs/adr/` moved to `records/` at the repo root — one mechanical change, every reference repointed. Archived `ADR-NNNN` citations and `archive/v0.26-docs/adr/` are unchanged.  **2026-09-12 (positioning §6b): created.** The conformance bar was prose in `CLAUDE.md` and `docs/index.md` and checked nowhere for a month; 94 of 314 files failed it. Now 237 documents checked, 0 failures. **Its docstring is where the three exclusions are justified** — keep that and [`../docs/index.md`](../docs/index.md) saying the same thing. |
| [`regression/`](regression/README.md) | **2026-09-12 (W-107): +[`2026-09-12-node-arm-rungs`](regression/2026-09-12-node-arm-rungs/report.md)** — the Node differential arm's **first run on the golden ladder**: both ends, each verified against its committed manifest before a byte was read, **750 comparisons per rung, 0 discordant, graph digests identical**. 🔴 **And why it had been green — it compared Node against a Python path nobody runs.** `node_arm.py` called `scan.ask`; `fux find` calls `run_query`, which applies `.fux/tune.toml`. Aimed at the real path, fux's own repo goes **90 of 174 discordant** and **0 of 174** with `--no-tune`: **Node reads no `tune.toml` at all**, R5's tune row is unbuilt and `rerank.mjs` is owed. Reproduced in isolation at 20 of 58 vs 0 of 58. ⚠ Every golden rung's tune is all-defaults, which is why the ladder runs are unaffected. **No verdict and nothing claims `N5 passes`** — the questions are unreleased, the matrix is one machine on Node 24, and §2 says a green arm is filed, not announced. `blind`, 1 500 per-query rows. **2026-09-12 (W-142/W-115/W-144): +[`2026-09-12-reaim-and-instruments`](regression/2026-09-12-reaim-and-instruments/report.md)** — 🔴 **W-142 RETIRED**: the endpoint is pinned at the corpus base rate (1.96 expected, 2.06-2.14 observed) under every setting of **both** fields, `p = 1.0000`; ✅ **W-115 MEASURED**, `hit@1` 0/30 -> 30/30 at `p ≈ 0` with headroom proven under 22c(b), and the previous run's *wrong formats* diagnosis corrected to *wrong content*; ✅ **W-144 ANSWERED**, the counterfactual ranks better above a table share of ~0.29 and goes to [a compare doc](compare/table-tokens-in-flen.compare.md) rather than shipping. Three verdicts, 766 rows, instruments committed at `aff3c82` **before** the first number. **2026-09-12 (W-143/W-97/W-142/W-144/W-115): +[`2026-09-12-priors-and-tables`](regression/2026-09-12-priors-and-tables/report.md)** — 🔴 **the four ranking priors answer NO** on every rung (verdict filed; the knob works, and every value that perfects current-seeking destroys history-seeking one probe for one). 🔴 **C4's heading premise is unsupported** — net 5, below the floor; the distractors win on body similarity. 🔴 **Tables inflate `flen` and move ranking**, 41 of 44 top-1 changes in the predicted direction, but the key-free quality endpoint saturates → Inconclusive. 🔴 **W-115 moves 0 of 994** on the ladder — the third corpus that cannot see it, reason now named. `informed`, no delta, 1 950 per-probe rows. **2026-09-12 (W-136): +[`2026-09-12-golden-ladder`](regression/2026-09-12-golden-ladder/report.md)** — the golden ladder built and frozen to rung 1 000 **before the questions were opened**, then 124 questions run on each of five rungs. 🔴 Three key-free findings: **0 abstentions of 124 on every rung** (third occurrence; the two-strikes gate is owed and unwritten), **a declared supersession lands at a coin flip in the document plane while `fux answer` recovers it** — the endpoint decides whether the defect is visible — and **`hit@5 ≤ 94/124` at rung 1 000 by structure**. ✅ W-143's missing corpus now exists, with a key-free endpoint. `informed`, **no delta stated**, 620 per-query rows, no verdict. **2026-09-11 (W-116): +[`2026-09-11-w116-chunking`](regression/2026-09-11-w116-chunking/report.md)** — 🔴 **W-115 moved 304 of 954 documents and cost 96 of them their title** (`decoys.jsonl` → `Record 1`); the shipped decoder violated the contract its own skill documents, and it is **fixed in the same change**. De-confounded with a third arm — 76 documents belong to `max_phrases`, not W-115. On the playground the arms are **byte-identical**: zero headroom, so Inconclusive, and **the queue row named the wrong corpus**. W-115 stays **unmeasured for quality**. **2026-09-11 (four priors): +[`2026-09-11-four-priors-headroom`](regression/2026-09-11-four-priors-headroom/report.md)** — 🔴 **the remeasure Arpit ordered CANNOT RUN on the corpus it was told to use.** Three of the four priors give byte-identical results at every value **including `0.0`** (0 of 50 goldens move) because the playground declares no `supersedes:` key on any branch in its history, no `archived=true`, and has no `mtime` spread. The `0 broken` bar is met **vacuously**. ⚠ Caught by SR-RS decision 22d hours after ratification — the `heading` control's failure exactly. `rerank_weight` is the one with headroom and its net `+4` stays below the floor. 650 rows, no verdict, handed back. **2026-09-11 (W-133): +[`2026-09-11-blind-unanswerable-rerun`](regression/2026-09-11-blind-unanswerable-rerun/report.md)** — Arpit asked whether the abstention finding still holds. **It does: 20/20 `answerable: true`, 0 ids flipped**, fourteen days and five ranking changes later. ✅ **SR-RS decision 22's first adjudicating use** — improvement headroom 20 (proven) → a real null; **regression headroom 0 → INCONCLUSIVE, not *no detected change***. Bands moved and verdicts did not (net 2, below the floor). ⚠ **The 2026-08-28 report's *median 0.448* is not reproducible from its own `per-query.csv` (0.3982)** — the frozen file is NOT edited; the evidence wins. Pre-registration frozen in its own commit first; no threshold, no `VERDICT.md`, R10 untouched. **2026-09-11: +`2026-09-11-third-annotator`** (the 7 `partial` goldens resolved). **2026-08-22: two runs filed** — [`2026-08-22-r6-rerun`](regression/2026-08-22-r6-rerun/report.md) (R6 **PASS** against a repaired instrument) and [`2026-08-22-r9-t2-at-10k`](regression/2026-08-22-r9-t2-at-10k/report.md) (R9 **PASS**, T2 not built). Every measurement run — report + ANALYSIS + evidence, **plus `VERDICT.md` when it rules on a pre-registered threshold** | 2026-09-15 | **2026-09-15 (W-178): +[`2026-09-15-consumer-fetchers`](regression/2026-09-15-consumer-fetchers/report.md)** — a consumer's own `glassbox.py` answering a query, and the one-letter typo that now parses. **2026-09-15 (W-177): +[`2026-09-15-ingest-absorbs-update`](regression/2026-09-15-ingest-absorbs-update/report.md)** — a surface capture whose instrument is the fetcher's own call log, not the stderr announcement. **2026-09-15 (W-136 prompt 4): +[`2026-09-15-ladder-seed-refresh`](regression/2026-09-15-ladder-seed-refresh/report.md)** — all eight golden rungs were frozen against a **superseded seed** (7 of 20 documents, 131 lines, `0aa4bbcf`) while every check in the repo passed, because they all compare the ladder to itself. Rebuilt and re-frozen; **`ext/` byte-identical on all eight, 0 of 18 800 generated documents drifted**. Two strikes → a gate. A surface capture. · **2026-09-15 (W-179): +[`2026-09-15-node-column`](regression/2026-09-15-node-column/report.md)** — Node's latency measured at last: **the reader is FLAT** (26.3 → 26.4 ms across a 10× corpus) and W-161's graph rebuild is all of the growth. · **2026-09-15 (W-180):** [`2026-09-15-b-sweep`](regression/2026-09-15-b-sweep/VERDICT.md) went from pre-registered to **measured** — `FAIL`, no value clears, and the lever reaches the endpoint below the frozen range. · **2026-09-15 (W-186): +[`2026-09-15-ladder-reingest`](regression/2026-09-15-ladder-reingest/report.md)** — all eight rungs were unreadable by HEAD; re-ingested, re-stamped, and the stamp gained `engine_commit:` because a version match was a false match. · **2026-09-15 (W-181): +[`2026-09-15-observer-latency`](regression/2026-09-15-observer-latency/report.md)** — the observer seam is free (`+0.7 ms` / `−0.3 ms`) and a 2 000 ms observer adds **+63 ms**, not 2 000. · **2026-09-15 (W-182 · W-185): +[`2026-09-15-runner-race-soak`](regression/2026-09-15-runner-race-soak/report.md)** — the stranding unreproduced in 104 trials and a **different** race captured; **+[`2026-09-15-index-temp-ignore`](regression/2026-09-15-index-temp-ignore/report.md)** — the fix, with a control: **0 `git add -A` failures in 3 871 runs against 2 335 of 3 933**. · **2026-09-15 (W-183): +[`2026-09-15-quality-endpoint-screen`](regression/2026-09-15-quality-endpoint-screen/report.md)** — the circularity SCREEN, `agreement` **0.4141** over 524 cited-decision contests against a chance rate of **0.0748**, inside a band frozen one commit earlier. W-154's Part B has an instrument at last. **2026-09-15 (W-184): +[`2026-09-15-differential-repaired`](regression/2026-09-15-differential-repaired/report.md)** — 22 144 byte-identical comparisons through `run.py` itself, the first that ever reached one on this repository. · **2026-09-14 (W-161): +[`2026-09-14-graph-ask`](regression/2026-09-14-graph-ask/PRE-REGISTRATION.md)** — **pre-registered, not yet measured**, and legally half-empty: the two arms of the graph-composed `ask` are frozen (A, Tier A’s RRF boost against `fux lexical`, with an **absolute** no-harm direction — one flip the wrong way on a non-link question removes the boost; B, Tier B’s `related` at ≥ 0.50 recall on the link-dependent subset, median length ≤ 5). 🔴 **The question set does not exist and no Claude session may author it** — link-dependent questions are Codex’s under SR-RS decision 23, available **2026-09-30**. Committed alone, ahead of the composition: the commit carrying this file holds no `compose.py`, no `[graph] ask_*` key and no `related` tier. **2026-09-13 (W-154 Part A):** proximity reranking PRICED — +15 to +19 ms p50, **flat in corpus size**, **same on `ask` and `answer`**, 0/124 queries faster. Surface capture, no verdict. W-154 stays open on Part B. **2026-09-13 (W-155 SHIPPED):** the table-is-the-answer gap is MEASURED and the answer is YES — `dump` 30/30 → 0/30, `content` 0/30 → 30/30, both controls hold. (b) is right for two document shapes and wrong for a third. Pre-registration committed alone at `a414012`. **2026-09-13 (W-150 SHIPPED):** the benchmark harness files all SEVEN captures — planted key (no corpus byte moved), CAP-2 rows, CAP-3 hit@k, CAP-4 answers, CAP-5 size, CAP-6 phases, CAP-7 HTML. First run filed; 10/10 planted unanswerables ANSWERED by both versions. **2026-09-12 (W-149):** the Yarn Berry probe listed, and the first probe's *Berry is UNMEASURED* clause repointed at it. **2026-09-12:** +`2026-09-12-workspace-dotpath-probe` — the four-package-manager workspace probe, a surface capture. **2026-09-12:** [`2026-09-12-node-tune-and-surfaces`](regression/2026-09-12-node-tune-and-surfaces/report.md) — `blind`, 1 748 per-query rows, four runs. **2026-09-12 (W-139): +`2026-09-12-benchmark-l9`** — the first two-version run under SR-WORK-ENVIRONMENTS. **The latency gap between the majors is a FIXED ~28 ms, not a slower search** (B − A 35.7 → 18.9 ms while the ratio collapses 1.32 → 1.02); ingest **~2× faster** in B with a ~5 % smaller index; **31–54 of 60 ranked lists differ and 6 of 60 at rank 1**. 🔴 Two corpora excluded, both the running session's fault, not findings. **2026-09-12 (W-106): +`2026-09-12-vector-gate-crossarch`** — the two-architecture arm. One embedder build on arm64 and x86-64 (Rosetta): cosine **1.000000**, **119/119** and **124/124** int8 vectors byte-identical, **0/124** orderings discordant. **It inverts 2026-09-05** (two implementations, one architecture: 0 of 125 identical, 41/50 discordant) — **the variable is the implementation, not the machine**. ⚠ Rosetta answers *does the x86-64 BUILD differ*, and only stands in for *does native x86-64 HARDWARE differ*. No delta stated against 2026-09-05. **2026-09-12 (positioning §6b):** `regression/README.md` +`type: Index`; `2026-09-06-csv-chunk-granularity/ANALYSIS.md` +`type: Analysis` (a post-baseline miss). **Runs before 2026-08-25 are exempt by freeze, not fixed** — same baseline as `test_regression_runs.py`. **2026-09-06 (W-110 VOIDED): the doc2query run gains its `VERDICT.md`, and the verdict is `VOID`** — [`W110-DOC2QUERY`](regression/2026-09-05-doc2query/VERDICT.md), Arpit's ruling. 🔴 **Not PASS and not FAIL**: the bar said *net ≥ 6 on `recall@k`* and never named `k`, so it was four bars whose verdicts disagree (+7 at `k=1`, +3/+2/+1 at 3/5/10) and it ruled neither way. ✅ The placebo control is untouched and is **not** voided with it — 0 discordant at every `k`, so the gain is the questions' content. ⚠ The doc2query−− filter stays **unproven, not disproven** (2 of 98, a 2 % treatment). **2026-09-06 (W-107 Phase 0 CLOSED): the node-log-divergence run gained a SECOND addendum** — [`ADDENDUM-GLIBC`](regression/2026-09-05-node-log-divergence/ADDENDUM-GLIBC.md), the last blank the run and its first addendum both named. Measured on **glibc 2.39 / x86-64 / Node 22**: 722/100 000 wide doubles differ, 4.44 % of BM25F scores differ bit-for-bit, **0 at `round(9)`, 0 top-5 moves**. 🔴 **And it stopped sampling the `idf` population altogether** — `idf`'s argument is `(n - df + 0.5)/(df + 0.5) + 1` with `df ∈ 1..n`, so at a given corpus size the domain is finite and enumerable; **all 10 939 arguments at `n ∈ {101, 838, 10 000}`** were checked, 841 (7.69 %) differ, **0 at `round(9)`**. ⚠ The glibc number came from a Linux container, **not** from `log-probe.yml`, which is still unrun; musl, Windows and Node 20 stay unmeasured. **2026-09-05 (W-107 Phase 0 addendum): the node-log-divergence row gained the widened-`idf` result** — 13 → 182 arguments, 7.69 % of them and 8.98 % of scores diverge, nothing survives `round(9)`, glibc still the only blank. **2026-09-05: a SIXTH run** — [`2026-09-05-declared-ties`](regression/2026-09-05-declared-ties/report.md) (W-111; the tie-break is correct, differential-green and **unexercised by either corpus**). **2026-09-05: a FIFTH run** — [`2026-09-05-doc2query`](regression/2026-09-05-doc2query/report.md) (W-110's gate, **ambiguous, no `VERDICT.md`**, handed to Arpit). **2026-09-05: a FOURTH run** — [`2026-09-05-expand`](regression/2026-09-05-expand/report.md) (W-109's gate, **16 fixed / 0 broken**, expansions written by a session blind to the judgments). **2026-09-05: THREE runs filed** — [`2026-09-05-answer-top3`](regression/2026-09-05-answer-top3/report.md) (W-108, `informed`) and [`2026-09-05-node-log-divergence`](regression/2026-09-05-node-log-divergence/report.md) (W-107 Phase 0, `blind`) and [`2026-09-05-vector-gate`](regression/2026-09-05-vector-gate/report.md) (W-106, `informed`, **no `VERDICT.md` — Arpit's ruling**, because the corpus behind DENSE-CHUNK's bar is gone). **2026-09-02: +2 runs** — [`2026-09-02-cdp-parallel`](regression/2026-09-02-cdp-parallel/report.md) (real Chrome, the W-105 corruption reproduced and closed) and [`2026-09-02-enrich-pii-leak`](regression/2026-09-02-enrich-pii-leak/report.md) (the W-102 leak reproduced through `fux find` and closed). Both `informed`; both file per-query rows; neither carries a `VERDICT.md`, because neither had a prediction to rule on. **2026-08-28 (latest): +[`2026-08-28-benchmark-contested`](regression/2026-08-28-benchmark-contested/report.md)** — **W-95 built, run and filed.** The first suite here that COULD detect a ranking change: 4 candidates sharing the query's terms at **equal tf, equal field and equal length**, one target, and a `--selftest` that **asserts** the headroom. 🔴 **C1 (primary): 0 discordant of 120 with 94 queries of headroom** — both arms 21.7 % against a 25 % chance level, so the null is about the ENGINES, not the corpus. **C3 (path): 0 % → 100 %**, the first version delta this project has shown — ⚠ a **capability** demonstration, not a ranking win. **C2 (ablation): `rerank_weight` 0.0 → 0.5 takes proximity 22 % → 100 %** — the machinery works and ships off; 🔴 **NOT an argument for the default** (the suite rewards exactly what the reranker does; hand-graded it is worth +4). 🔴 **C4, the negative control, SATURATED and is Inconclusive, not Pass** — the run's own headroom column caught it. **Six VERDICT files (C1–C6), 2 660 per-query rows.** `informed`, so no delta stated. ⚠ **Method correction: a cross-seed pairing is a rate check, not a determinism check — the v1-vs-HEAD B9 carries the same weakness.** **2026-08-28 (latest): [`2026-08-28-first-recall`](regression/2026-08-28-first-recall/report.md)** — 🔴 **`recall@k` computed for the FIRST time**, the headline SR-WORK-QUALITY declared and could not compute until decision 12 split the rank contract from the relevance set. Over the 43/50 declared `complete`: **`@1` 0.5969 · `@3` 0.8566 · `@5` 0.9535 · `@10` 0.9884** as a curve against context bytes; **19 of 43 are multi-document**, where recall genuinely is not `hit@k`. ⚠ **NOT a generalisation estimate** — every installed enrichment file was authored by someone who had read these queries. ✅ Back-compat **verified**: `hit@k` unmoved at 41/9 in both harnesses after the swap. **2026-08-28 (W-93): +[`2026-08-28-benchmark-v1-vs-head`](regression/2026-08-28-benchmark-v1-vs-head/report.md)** — the version benchmark, `1.0.0` vs `HEAD`, **and the first run in the store to file PER-QUERY ROWS** (10 row files, one per arm per tier, every suite). Seven verdicts in one directory, which is new: `VERDICT.md` (B1) plus `VERDICT-B2/B3/B5/B6/B7/B9.md`, because one `verdict:` key cannot carry PASS, FAIL and INCONCLUSIVE at once. 🔴 **Every pre-registered paired test returned a discordant count of ZERO.** **B1 INCONCLUSIVE — the primary endpoint was SATURATED** (`hit@5` 240/240 in *both* arms at every tier), so a correctly-sized set measured a null that the corpus, not the engines, determined. **B2 FAIL** — both arms invert identically because `superseded_weight` ships at `1.0`; post-hoc at `0.5`, 21/40 → 0/40. B3/B5/B6 PASS (bytes 1.002 ×, wheel 7.11 MB → 259 KB, p95 1.32 ×, ingest 1.04 ×). B7 INCONCLUSIVE, B9 PASS. ⚠ **`informed`** by the pre-registration's own rule. **2026-08-28 (later): two more runs** — [`2026-08-28-annotator-agreement`](regression/2026-08-28-annotator-agreement/report.md), where a SECOND blind annotator confirms the first at **Cohen's κ = 0.960** (both name the same **25 of 50** goldens as multi-document, against one asserted) — **`recall@k` ≠ `hit@k` is now measured, and W-87 P2 is blocked on a schema SR rather than on inputs**; and [`2026-08-28-placebo-and-seal`](regression/2026-08-28-placebo-and-seal/report.md), the placebo's first adjudicating use — content-free matched-length prose moves **one** query (`p = 1.0000`), so **source bias does NOT explain enrichment's lift** (it clears source bias, never contamination). 🔴 That run also **refutes a queue item's impossibility claim** (the `+9` was re-run in one command: `n_d = 9`, `p = 0.0039`, clears the floor) and finds the **seal EXERCISED but unable to adjudicate**, because it postdates the artifact it was applied to. **2026-08-28: [`2026-08-28-blind-unanswerable`](regression/2026-08-28-blind-unanswerable/report.md) filed — the FIRST run to use a quality control to adjudicate anything** (SR-RS decision 15's `BUILT IS NOT PROVEN`, discharged for one of four). 🔴 **The engine abstained 0 times out of 20** blind-authored near-miss questions, ruled unanswerable by a SECOND blind session; 17/20 sat above the `separation_floor`. 🔴 **And a blind annotator judged 25 of 50 goldens to have MORE than one relevant document**, withdrawing the same-day *"`recall@k` IS `hit@k`"* inference — the field count was right about the file's shape and wrong about the corpus. ⚠ **Proposes no threshold; R10 untouched.** Also corrected the brief's own validation loop, which graded questions by the engine's `answerable` — the system under test arbitrating its own test. **2026-08-27 (third pass): two more** — [`p3-sha-stability`](regression/2026-08-27-p3-sha-stability/VERDICT.md) (**PASS**, 19/19, fork 3's gate clears) and [`decoy-control`](regression/2026-08-27-decoy-control/report.md), whose FIRST run found **one of fifteen unanswerable questions reported `grounded`** — corpus-wide coverage, and no R10 ruling reaches it. **2026-08-27 (later): two more runs** — [`2026-08-27-daemon-real-url`](regression/2026-08-27-daemon-real-url/report.md), the daemon over the real internet (TLS/DNS/CDN, a real 404, the **first ever** exercise of the rate-limit path against a real 429, and the URL tail closing unassisted on Wikipedia's `Special:Random`), and **R10 MEASURED** — [`INCONCLUSIVE`](regression/2026-08-27-r10-separation-floor/VERDICT.md), because the pre-registration froze two rules that disagree on this curve. **2026-08-27: two directories filed** — [`2026-08-27-daemon-lifecycle`](regression/2026-08-27-daemon-lifecycle/report.md), a **surface capture** of the daemon's whole lifecycle against a local HTTP server, and [`2026-08-27-r10-separation-floor`](regression/2026-08-27-r10-separation-floor/evidence/PRE-REGISTRATION.md), which is **pre-registered and not yet measured** — a state the per-run contract used to make illegal (SR-RS decision 17). **2026-08-25 (latest): [`2026-08-25-supersession-and-reranker-default`](regression/2026-08-25-supersession-and-reranker-default/report.md) filed with a pre-registration frozen in its OWN commit ahead of the run.** **P-SUPERSEDE: FAIL** — but the prior **fired for the first time since it shipped**, fixed `q015`, and broke four queries **whose correct answer is the superseded document**. **Supersession belongs to the query's intent, not the document.** Reranker reproduces `28 -> 32` exactly; **default still does not flip** — its constants were swept on these goldens, making the `+4` `informed`. **2026-08-25 (later): [`2026-08-25-rank-flip-susceptibility`](regression/2026-08-25-rank-flip-susceptibility/report.md) filed** — SR-RERANK veto 1 condition 2 measured *element* drift where **ranking** drift decides. At `1.907e-06`: **0.00 % flips, 0.00 % at-risk**; the knee is ~52x higher. ⚠ A **lower bound** — the score-level drift has never been measured. ⚠ Found in passing: **4.38 % of top-5 orderings are decided by `docidx`, not relevance**. **2026-08-25: [`2026-08-25-model-removal`](regression/2026-08-25-model-removal/report.md) filed** — the A/B for deleting the embedding model. Wheel **30.1x** smaller, index **-22.6 %**, ingest **6.8x** faster, differential law intact. **The first run filed under the classification rule**, and `informed`. Plus the frozen DENSE-CHUNK pre-registration mirrored into its own run, because the module carrying it was deleted. **2026-08-25: the per-run contract gains row 7** — `classification: blind |informed` plus an `## Authorship` section, for *measured* runs filed from 2026-08-25; a surface capture is exempt. Baselined on the run directory's own date, because a filed report is frozen and editing evidence to satisfy a later rule is the act the rule prevents. **2026-08-24 (latest): cross-arch drift + declared supersession filed** — [`2026-08-24-crossarch-drift-and-declared-supersession`](regression/2026-08-24-crossarch-drift-and-declared-supersession/report.md). Veto 1 condition 2 measured (82.9% of elements differ, max 1.9e-06); `q015` recovers in both blind arms via a committed `supersedes:` declaration **2026-09-15 (W-168 step 1, Claude Code):** `2026-09-15-anchor-text/PRE-REGISTRATION.md` filed and indexed — frozen the day the mechanism shipped, before any claim about ranking quality. **2026-09-15 (W-168/W-161/W-176): +[`2026-09-15-anchor-mechanism`](regression/2026-09-15-anchor-mechanism/report.md)** — 🔴 **the golden ladder has NO LINKS**: **0 anchor-bearing edges of 1 002 on all eight rungs**, every edge `supersedes`, not one `ref`, and no link syntax anywhere in `work/golden/seed/`. Measured **0 of 124 flips at `anchor ∈ {0.5, 1.0, 2.0, 3.0}`** on four rungs including `rung-10000`, **with a positive control** that moves the target from non-candidate to top-1 — so the zero is the corpus, not the harness. 🔴 **Three queued items name the wrong blocker**: W-168 step 8, **both** W-161 arms (`ask_kinds` follows `ref` alone, so the walk reaches nothing) and W-176 step 10. They wait on **link-bearing documents**, not questions. A **data defect (23b), not a null**: no `VERDICT.md`, both frozen pre-registrations annotated and **unmoved**, and the corpus choice filed to the inbox for Arpit. Surface capture; 1 488 rows; no answer key read. |
| [`benchmark/`](benchmark/README.md) | **A version comparison is planned, frozen, or executed** — a pre-registration is added, or a run rules against one | 2026-09-13 | **2026-09-13:** W-97 CLOSED (descoped) — both rows deleted, file archived, residue moved to W-143.  **2026-09-13 (SR-WORK-BENCHMARK):** what a benchmark run always captures is stated once in [`records/0053_WORK-benchmark.md`](../records/0053_WORK-benchmark.md) — CAP-1…CAP-7, enforced by `tests/test_benchmark_capture.py` from 2026-09-13. `SR-WORK-ENVIRONMENTS` no longer states what a benchmark measures; its no-answer-key clause is reversed. **2026-09-13:** L9 retired — the environment rule is now [SR-WORK-ENVIRONMENTS](../records/0052_WORK-environments.md) (`0052`, `kind: process`), not a law. The handle and `records/0011` are never reused.  **2026-09-12 (W-107 O3): PRE-REGISTRATION-NODE-2 FROZEN**, sha `2ea40303…`. Standing arm (every change, both readers, fux-lab, golden ladder); ends-per-push + all-eight nightly; ids N5-N8; latency fence moved out to fux-benchmark. **2026-09-12 (W-138/W-139): `RUNBOOK-TUNER` is VOID IN PART** — its veto leg graded `fux-playground` and has **no instrument at all** now (the golden ladder's questions carry no rank contract, and authoring one inside the run that uses it is the contamination the scheme exists to stop). `README.md`'s two rows say so, and the frozen `PRE-REGISTRATION-TUNER` was **superseded, never edited**. **2026-09-12 (positioning §6b):** `benchmark/README.md` +`type: Index` frontmatter. **2026-09-05 (W-97, and PRE-REGISTRATION-TUNER is NOT yet frozen — §7 step 1 has not run):** §8's `--tune` item ticked; §1's defaults table gains `expand_weight`, which shipped with W-109 and was missing from a table that claims to be read from source; §6 gains its out-of-scope row (no suite here passes `--expand`, so no query can move it); **T1 gains a stated limit** — `rerank_weight` moves two mechanisms since W-108, so T1.d's veto is the exposed leg. **No bar moved.** **2026-09-06: `PRE-REGISTRATION-NODE.md` is FROZEN IN FULL** — Arpit ruled the one deliberately-unfrozen cell **(b), scores equal after `round(9)`**; sha `0e3b4c80bf9e6a3ad122cb4e0db4f81adf04693fd47047fa24dd9edc7cb037a7`. §0 rewritten to record the ruling, §2's score row filled, §6's `idf`-population clause **struck rather than deleted** and marked discharged — by enumeration, not by a wider sample, and **its bar was never lowered to discharge it**. ▶ Phase 1 starts. **2026-09-05 (W-107 Phase 0): [`PRE-REGISTRATION-NODE.md`](benchmark/PRE-REGISTRATION-NODE.md)** — the Node read plane's bar, ids `N0`–`N4`, a fourth id space. 🔴 **One cell is deliberately unfrozen** (§2's score comparison — Arpit's `log()` pick), so the document is **not frozen yet** and Phase 1 does not start. **2026-09-05: one run filed** — [`2026-09-05-answer-top3`](regression/2026-09-05-answer-top3/report.md), `informed`, 172 per-query rows, four arms. **2026-08-28 (W-97): +[`PRE-REGISTRATION-TUNER.md`](benchmark/PRE-REGISTRATION-TUNER.md)** — the frozen thresholds for the **knob sweep** over `.fux/tune.toml`, ids **`T0`–`T5`**, a **third id space**; one engine, three legs per knob (generated suite selects · hand-graded playground vetoes · latency fence prices), output a candidate table with **no recommendation**. **+[`RUNBOOK-BENCHMARK.md`](benchmark/RUNBOOK-BENCHMARK.md)** and **+[`RUNBOOK-TUNER.md`](benchmark/RUNBOOK-TUNER.md)** — the agent-executable procedures; **a runbook restates no bar** and is corrected as the harness teaches, unlike a pre-registration. README rewritten: three document kinds and which may change, the id-space rule (`B` · `C` · `T`), rules 5–7 added (headroom beside power — practised, ratification Arpit's; the same-corpus repeat is the null control; harness picks no threshold, runbook restates none), and the v1-vs-HEAD row now says its §7 is discharged and its **B-full ceiling table was never produced**. **2026-08-28 (later still): +[`PRE-REGISTRATION-CONTESTED.md`](benchmark/PRE-REGISTRATION-CONTESTED.md)** — the frozen thresholds for the contested-answer suite, `sha256 e8417b33…`, written and delivered to disk **before the first corpus byte existed**. ⚠ It defines **`C1`–`C6`**, a **new id space**: the `B` ids belong to the frozen v1-vs-HEAD document and may never be reused or renumbered. Its **arms are deliberately identical** to that run's, so only the instrument differs. Executed the same day as [`regression/2026-08-28-benchmark-contested`](regression/2026-08-28-benchmark-contested/report.md). **2026-08-28 (later): +[`benchmark-v1-vs-head.html`](benchmark/reports/2026-08-28-benchmark-v1-vs-head.html)** — an 18-slide presentation of the executed run, self-contained (no network beyond Google Fonts) and theme-aware. ⚠ **This is a NAMED EXCEPTION to this directory's own *results do not live here* rule**, and the README now states it: a presentation is read by people rather than cited by documents, it duplicates no evidence, and **anything in it that disagrees with [`regression/`](regression/README.md) is the presentation being wrong.** The same file is published as an artifact; **the repo copy is the source** and the artifact is republished from it. **New row, owed since the directory was created on 2026-08-27 and filed now (W-93).** Holds the *plans* for version-to-version comparisons; **results never live here** — they go to [`regression/`](regression/README.md) like every other measured run. **2026-08-28: [`PRE-REGISTRATION-V1-VS-HEAD.md`](benchmark/PRE-REGISTRATION-V1-VS-HEAD.md) had its `HEAD` sha written in — `75ade57`, committed before the first command ran** — and was then executed. ⚠ **The frozen document defines B1, B2, B3, B5, B6, B7 and B9 only**; the item and the prose both said *"thresholds B1–B9"* and **B4 and B8 were never written**. Nothing was skipped — two ids never existed. ⚠ **Nothing else in it may ever be edited**: a later `HEAD` is a new run citing this pre-registration, not an amendment to it. **2026-09-13 (presentation):** `benchmark-captures.html` added — the 2026-09-13 capture run as a 17-slide deck, and **slides 13–16 carry the LINEAGE of all four benchmark runs**: what each instrument could detect, what changed between them, the abstention shape that repeats, and the three cross-run pairs that may never be compared. Every cross-run figure is attributed to its own filed run; the presentation carries no number a run does not. 🔴 **2026-09-13 (SR-WORK-BENCHMARK decisions 7–10, Arpit):** every benchmark HTML report moved to [`benchmark/reports/`](benchmark/reports/README.md), one per run, dated `<yyyy-mm-dd>-<run>.html` — the run directory's own name with `.html` on the end. The three `benchmark-*.html` decks are gone from this directory; their rebuilds are in `reports/` and the originals in `archive/benchmark-reports/`. `reports/TEMPLATE.html` is the skeleton, and every number on a report now carries its direction of goodness. |
| [`benchmark/reports/`](benchmark/reports/README.md) | **A benchmark run is filed** — its report is added here, or the template changes | 2026-09-15 | **2026-09-15 (every report on the spine):** all five rebuilt; `2026-09-15-node-column.html` **created** — that run had no report and CAP-7 has been mandatory since 2026-09-13. Every filed benchmark run now has one. · **2026-09-15 (W-187, the CAP slide spine):** the report's spine is now one slide per capture — decision 14. · **2026-09-13:** created. CAP-7's only home, per [SR-WORK-BENCHMARK](../records/0053_WORK-benchmark.md) decision 7. Holds `TEMPLATE.html` + three reports rebuilt by hand from filed rows; ⚠ **the harness emits neither this path nor this shape yet — W-158**, and [`2026-09-12-benchmark-l9`](regression/2026-09-12-benchmark-l9/report.md) is the one filed benchmark run with no report. |
| [`../node/`](../node/README.md) | A verb, a flag, the `_format` version policy, or a `compat/`/`hash/` module changes; every `src/fux/` module that gains a Node twin | 2026-09-13 | **2026-09-13 (SR rename):** `ADR-<NAME>` became `SR-<NAME>` and `docs/adr/` moved to `records/` at the repo root — one mechanical change, every reference repointed. Archived `ADR-NNNN` citations and `archive/v0.26-docs/adr/` are unchanged.  **2026-09-12 (W-149):** REWRITTEN for both audiences (npm page + the vendored `.fux/node/README.md`) — `.fux/fux` is the only documented entry point, the two shapes are a table, `fux.mjs` is marked GENERATED with the command that regenerates it, and *no build step* now says **for you**. The stale *W-107 Phase 1* status block is gone. **2026-09-12 (W-107 Phase 4):** the Node read plane, wired in — packaged into the wheel by `force-include` (file by file, so `node/test/` is not vendored into a consumer's repo), vendored into `.fux/node/` by `ensure_layout`, and gated on `.fux/pii.toml` exactly as Python is (**O1, ruled by Arpit**). Two defects fixed on contact: `mcp-tools.json` was missing from `package.json`'s `files`, so `fux mcp` would have broken in the published npm package, and the `{{TOP}}` substitution produced a **string** default on a property declared `"type": "integer"`. ⚠ **Not published to npm**, and no global bin in the first release (R1a). |
| [`../tools/differential/`](../tools/differential/) | An arm is added, or what an arm compares changes | 2026-09-12 | **2026-09-12 (W-149):** `node_arm.py` gains the SIXTH surface — `--bundle-cap` builds the published bundle and compares it against the module tree on `find`/`ask`/`answer`, one MCP session and the library export. 0 discordant on this repo. **2026-09-12 (W-107 Phase 4):** three arms now, not one — `node_arm.py` (Python's reader vs Node's, on **parsed values, never stdout bytes**), `graph_arm.py` (the plane digest) and `adversarial_corpus.py` (ids straddling U+FFFF; **it MUTATES the index it is pointed at**, so it runs in a copy). Run by [`node-arm.yml`](../.github/workflows/node-arm.yml) across 3 OSes × Node 20/22. ⚠ **No test imports any of them**, which is SR-T1-ACCELERATOR's standing warning, unchanged. |
| [`../tools/quality-controls/`](../tools/quality-controls/README.md) | A control is added, **used in a run for the first time**, or its procedure is corrected | 2026-09-12 | **2026-09-12 (W-142/W-144/W-115):** four new files. `verdict.py` replaces every hard-coded *net >= 6* with the **exact two-sided binomial** (SR-RS decision 19a — a net of 8 on 30 discordant pairs clears the floor of all floors and fails decision 19's own table). `body_control.py` is the `heading` control re-aimed at `bm25f.body` and **prints its endpoint's corpus base rate before it runs an arm** — the mechanical half of W-142's retire. `w144_graded.py` and `w115_instrument.py` each carry a `gen`/`run`/`sweep` and, for W-115, a `selftest` that is decision 22c(b)'s headroom proof. **2026-09-12 (W-138):** the decoys are unanswerable **relative to a corpus** that no longer exists, so the set may not simply be pointed at the golden ladder — re-authoring is authoring. `BLIND-AUTHOR-BRIEF`'s destination path (`<playground>/goldens/`) retired. **2026-08-28: the `unanswerable` class RAN** — [the run](regression/2026-08-28-blind-unanswerable/report.md) — and is the first control used to adjudicate anything; the placebo and the sealed subset remain **never run**. 🔴 **The run found a defect in the control's own procedure**: [`BLIND-AUTHOR-BRIEF.md`](../tools/quality-controls/BLIND-AUTHOR-BRIEF.md)'s validation loop graded submitted questions by the engine's own `answerable` — **using the system under test as the arbiter of its own test** — and followed as written would have discarded a perfect 20-question set as 100% defective. Corrected in place; ground truth now comes from a second blind reader. **2026-08-27: created — two of SR-RS decision 15's three controls**, owed since W-78. A **content-free matched-length placebo** (one shared sentence pool, so every placebo has identical vocabulary and cannot discriminate; deterministic from the source sha, no model, installs nothing) and a **15-query decoy set** (⚠ the one kind of evaluation material an agent may author — no correct answer, nothing to fit). 🔴 **The decoys found a defect on their first run**: one of fifteen is reported `grounded` because `coverage` is corpus-wide. ⚠ **Decision 15 KEEPS `NOT BUILT`** — it names three, and the **sealed subset** is a judgement about the power tension, not a build.|
| [`../tools/quality/`](../tools/quality/README.md) | ⚠ **`[cost]` must not change at all** — it is frozen, and changing it after a filed score voids SR-WORK-QUALITY decision 6. `[mix]` changes only by **bumping `version`**, never by a silent edit; **or the golden schema changes** | 2026-09-12 | **2026-09-12 (W-138):** `recall@k`'s blocker got WORSE, not better — the annotation exists but the 50 goldens it was made over are retired with the playground, and the ladder's questions carry no relevance claim, so there is no set to compute it over. `mix.toml` is frozen, so its now-moot WARNING comment is superseded here rather than edited. **2026-08-28 (Arpit ruled option B): +[`goldens.py`](../tools/quality/goldens.py)** — the schema that keeps the **rank contract** (`doc` + `max_rank`) and the **relevance set** (`relevant` + `relevance`) apart, SR-WORK-QUALITY **decision 12**. It refuses an undeclared relevance list (the original defect in a new field), a `doc` outside its own relevance set (annotator 1 did exactly that on `q027`), and an unknown declaration; `recall_slice` returns the eligible queries **and** the excluded count, because a recall number with an unstated denominator is what rule b exists to stop. **+12 tests**; the un-migrated playground file stays valid and reports `recall@k` as *not computable*. ⚠ **`tools/quality/` is no longer a purely frozen instrument** — it now holds one executable module, and the frozen half (`mix.toml`) is unchanged. **2026-08-27: created.** `mix.toml` — the declared query prior (uniform, and *declared* is the whole point) and the published cost of an error (`t = 0.75` → `c = 2`). **Nothing reads it, deliberately**: the cost model is only honest if it predates the first score, and `recall@k` is still uncomputed. ⚠ The class list is marked `provisional = true`; the `unanswerable` class **does not exist yet and must be authored blind**|
| [`../tools/pruning-eval/`](../tools/pruning-eval/README.md) | The gate's definitions change (they must not) or a selector is ported | 2026-09-12 | **2026-09-12 (W-138):** named in SETUP-LAB as frozen history — it hard-codes `acme`/`orbit`, both lost in the 2026-08-20 wipe, and P1 closed FAIL permanently, so nothing is owed; it is named so a reader who opens it knows why it cannot run. Two **frozen** pre-registrations (v1, v2), both committed before their first number; 50 tests; corpus acquisition + 3 diagnostics|
| [`DOC-REGISTRY.md`](DOC-REGISTRY.md) | **A doc is added, retired, or its trigger changes** | 2026-09-15 | **L11 — the sealed answer key is a law.** **2026-09-15 (L11):** the sealed-key prohibition became law `L11` ([SR-LAW-11](../records/0012_LAW-11-sealed-answer-key.md)); SR-WORK-GOLDEN now carries only the process, and every law count, range and `L0..L11` diagram moved with it. · **2026-09-13 (SR-WORK-BACKLOG):** the backlog is the second work file — `work/BACKLOG.md`, 241 named-but-unclaimed items in five classes, governed by [SR-WORK-BACKLOG](../records/0055_WORK-backlog.md) at `0055`. **2026-09-13:** table-in-`flen` gained option **(d) lower `b`**; W-155 closes the *table IS the answer* gap; W-156 carries the environments-vs-single-corpus conflict.  **2026-09-13:** W-143 CLOSED — all four priors ruled; W-151/W-152 close three, W-153 covers the date gap, W-154 restates `rerank_weight` as a cost question. Ratified, not built.  **2026-09-13:** W-97 CLOSED (descoped) — both rows deleted, file archived, residue moved to W-143.  **2026-09-13:** `superseded_weight` ruled closed (W-143 → W-151); **ratified, not built** — the knob goes, the `superseded` fact stays.  **2026-09-13:** SR-OWNERSHIP became **SR-WORK-OWNERSHIP** (`0146` → `0054`, the WORK range); the hole at `0146` closed by shifting `0147`–`0156` down one — a file number is an ordinal.  **2026-09-13:** L9 retired — the environment rule is now [SR-WORK-ENVIRONMENTS](../records/0052_WORK-environments.md) (`0052`, `kind: process`), not a law. The handle and `records/0011` are never reused.  **2026-09-13 (SR-WORK-OPEN-QUEUE):** the queue's 53 rules are stated once in [`records/0051_WORK-open-queue.md`](../records/0051_WORK-open-queue.md); this file is the LIST plus the declared ball legend, and states no other rule.  **2026-09-13 (`kind`):** every record declares `kind: law | component | process` — the gate it answers to. 12 · 52 · 4. SR-WORK-OWNERSHIP decision 11; register §The three kinds.  **2026-09-13 (SR rename):** `ADR-<NAME>` became `SR-<NAME>` and `docs/adr/` moved to `records/` at the repo root — one mechanical change, every reference repointed. Archived `ADR-NNNN` citations and `archive/v0.26-docs/adr/` are unchanged.  **2026-09-12 (W-149):** eleven rows bumped for the bundle migration. **2026-09-12 (positioning §6b):** rows bumped for every doc the positioning change touched; +`type: Registry` frontmatter. **2026-09-12 (W-141):** rows bumped. **2026-09-11 (`[index]`):** rows bumped for the `[index]` session. **2026-09-01: six rows bumped** for W-98 Phase 1. **2026-08-27 (W-89): ten rows bumped for the `L8` change** — CLAUDE.md, `records/`, GLOSSARY, handbook, the architecture pair, OPEN-WORK, IMPLEMENTATION, WORKLOG, INTERVIEW, NOW, and this row. **No row added or retired**: L8 is a change to an existing document, not a new one. **2026-08-26 (post-build): the register states what SHIPPED** — ten rows annotated with their W-82 phase; the detailed diagram redrawn (it still showed `vectors[]`, sign codes and dense fusion, all deleted 2026-08-25); `docs/guide.html` written. **New row — this file tracks itself.** It is maintained like anything else here, and a registry that omits itself is exactly the blind spot it exists to remove. **2026-08-18: restated as live-documents-only** and five tombstone rows deleted (the retired `handoff/` row, the archive maps, the demo-corpus row, and a duplicate `INTERVIEW.md` row) |
| [`README.md`](README.md) | The `work/` contract changes — a file is added, retired, or its rule changes | 2026-09-13 | **2026-09-13 (SR-WORK-BACKLOG):** the backlog is the second work file — `work/BACKLOG.md`, 241 named-but-unclaimed items in five classes, governed by [SR-WORK-BACKLOG](../records/0055_WORK-backlog.md) at `0055`. **2026-09-12 (W-138):** the `setup/` row said *the two things fux needs* — three, and each one's job is SR-WORK-ENVIRONMENTS's. **2026-09-12 (positioning §6b):** +`type: Index` frontmatter. **2026-08-21: `PRIORITY.md`'s row removed (archived); `OPEN-WORK.md` restored as "read first."** The `Cost:` line reference in the session-start checklist dropped (P7). **New row.** The directory's own how-to-use doc; also the home of the three cross-cutting rules (archive-is-not-evidence, concurrent-session hazard, ground-truth-over-prose)|
| [`IMPLEMENTATION.md`](IMPLEMENTATION.md) | **A milestone or release lands** | 2026-09-15 | **2026-09-15 (W-178, Claude Code):** the W-178 section — the typed attribute, the header defect caught before it shipped, the doctor row, and the whole-run failure a missing fetcher turns out to be. **2026-09-15 (W-177, Claude Code):** the W-177 section — the verb deleted, the surface moved, the fence relocated onto `--no-fetch` with a negative control, and the exit-2 finding handed to Arpit. **2026-09-15 (arms settle, 16a):** what the trim costs and what it buys. · **2026-09-15 (Node column BUILT, W-188):** code built, numbers not measured — stated as such. · **2026-09-15 (every report on the spine):** five reports, one of them new, and no engine run. · **2026-09-15 (W-187, the CAP slide spine):** twelve slides, a CAP-1 slide that never existed, and an emitter that no longer writes titles. · **2026-09-15 (W-182 · W-185):** a flake driven on purpose, diagnosed, and fixed. · **2026-09-15 (W-184 · W-183):** a dead proof obligation repaired, and an instrument that can fail. · **L11 — the sealed answer key is a law.** **2026-09-15 (L11):** the sealed-key prohibition became law `L11` ([SR-LAW-11](../records/0012_LAW-11-sealed-answer-key.md)); SR-WORK-GOLDEN now carries only the process, and every law count, range and `L0..L11` diagram moved with it. · **2026-09-15:** the W-146 entry, and two links repointed into `archive/open/`. **2026-09-14 (fux.toml audit, Cowork):** a row for the seven changes, incl. the flat-config-table defect and the stale `agents` default. **2026-09-14 (three fux.toml rulings, Cowork):** two rows — the template move, and the closed-domain-vs-tuning-number rule the live-keys ruling establishes. **2026-09-14 (W-174, Cowork):** a row for what landed, incl. the two wrong sentences the build found. **2026-09-14 (W-159 + W-163 + W-164, Claude Code):** a row for the three — a wrong diagnosis corrected, eight doctor rows kept after a real keep-call, and four gates every one of which was red on the live tree first. **2026-09-14 (W-165 + W-166, Claude Code):** a row for the two items — five surfaces the records already owed, green on both suites, verified from a clean checkout of the commit after an amend produced a broken one. **2026-09-13 (W-147 SHIPPED):** the journal's two consent surfaces are BOUND by a test (all four states) and the specimen line now opens `⚠ NOT a rendering key`. Found in the build: `output.toml` is a COMPLETE declaration, and `provenance.py` named the REJECTED `tune.toml` home. **2026-09-13 (W-154 Part A):** proximity reranking PRICED — +15 to +19 ms p50, **flat in corpus size**, **same on `ask` and `answer`**, 0/124 queries faster. Surface capture, no verdict. W-154 stays open on Part B. **2026-09-13 (W-155 SHIPPED):** the table-is-the-answer gap is MEASURED and the answer is YES — `dump` 30/30 → 0/30, `content` 0/30 → 30/30, both controls hold. (b) is right for two document shapes and wrong for a third. Pre-registration committed alone at `a414012`. **2026-09-13 (W-150 SHIPPED):** the benchmark harness files all SEVEN captures — planted key (no corpus byte moved), CAP-2 rows, CAP-3 hit@k, CAP-4 answers, CAP-5 size, CAP-6 phases, CAP-7 HTML. First run filed; 10/10 planted unanswerables ANSWERED by both versions. **2026-09-13 (W-153 SHIPPED):** `mtime` is on every `--json` hit — five readers, `required: always`, `null` a CLAIM. First amendment SR-API's frozen surface has taken. Prose output unchanged; sorting by it is NOT authorised. **2026-09-13 (W-152 SHIPPED):** `archived_weight` and `recency_half_life_days` are REMOVED, with `recency_multiplier` and `stats.json`'s `newest_mtime` — all three DOCUMENT priors are now gone. No ranking change: every one shipped as a no-op. SR-ARCHIVED-CONTENT decision 6 REVERSED; the W-73 bound test re-aimed at `[priority]`. **2026-09-13 (W-151 SHIPPED):** `[ranking] superseded_weight` is REMOVED — the knob, its key, its Node twin and its four records. A committed key is refused BY NAME. No ranking change: it shipped at `1.0`. The `superseded` FACT and the declared tie-break are untouched. **2026-09-13:** W-143 CLOSED — all four priors ruled; W-151/W-152 close three, W-153 covers the date gap, W-154 restates `rerank_weight` as a cost question. Ratified, not built.  **2026-09-13:** W-97 CLOSED (descoped) — both rows deleted, file archived, residue moved to W-143.  **2026-09-13:** SR-OWNERSHIP became **SR-WORK-OWNERSHIP** (`0146` → `0054`, the WORK range); the hole at `0146` closed by shifting `0147`–`0156` down one — a file number is an ordinal.  **2026-09-13 (SR-WORK-OPEN-QUEUE):** the queue's 53 rules are stated once in [`records/0051_WORK-open-queue.md`](../records/0051_WORK-open-queue.md); this file is the LIST plus the declared ball legend, and states no other rule.  **2026-09-12 (W-149):** a row for the close — L10 satisfied, SR-NODE-SEARCH decisions 13-16 BUILT, both suites whole (3 994 + 85). **2026-09-12 (W-107 CLOSED):** the Node reader's config, and the four uncompared surfaces. **2026-09-12 (W-107):** npm publication, the R1a bin amendment, the `doctor` PATH row, the twin map and the version-parity gate. **2026-09-12 (W-138):** the SR-WORK-ENVIRONMENTS reconciliation section, including the DoD clause that was deliberately not met and why. **2026-09-12 (positioning §6b):** +`type: Milestone Log` frontmatter. **2026-09-12: W-141 closed — `.agents/skills/` shared by Codex and Copilot.** **2026-09-11: W-140's twelve closed rows, with the commit for each and what is left.** **2026-09-11 (correction):** the W-115 chunking entry's closing claim — *"W-116 is NOT closed by this… W-116 stays open behind W-56"* — was **true when written and false as current state**: W-56 was already archived and W-116 ran and closed 2026-09-11. The historical argument is kept; an ⚠ block beneath it names what overtook it and what survives (W-115 remains unmeasured for quality). **2026-09-11 (W-137):** the SR renumber — laws own `0001`–`0100`, every other record starts at `0101`. **2026-09-11 (W-130 rename):** `.fux/types.toml` -> `.fux/formats.toml`. **2026-09-11: +1 section** - W-130, `.fux/types.toml`, with the byte-identity evidence and what did not run on the Mac. **2026-09-11: +1 row** — W-115/W-117/W-119/W-120/W-121, the chunking change, recorded under their own ids so their queue rows could legally be deleted. **2026-09-11: +1 row** — W-126 + W-127: `archived=` on URL lines, `fux doctor`'s `ranking priors` (391 archived documents against a `1.0` weight), and the freshness gate found unable to name the record the change was about — 27 of 63 records are in that position, now pinned. **2026-09-11:** +3 rows — W-128 checksum validators, W-129 `pii.toml` required, W-131 US/Canadian starter rules. **2026-09-11: +1 row** — `[index]` lands, the `pii-digest` ordering defect closed, the queue's environment blockers re-derived to nothing, and the freshness gate diagnosed as over-firing. Plus the **`W110-DOC2QUERY` register row**, which was a filed verdict with no registration (the R9 failure). **2026-09-06 (later):** the W-114 row consolidated to cover both halves. **2026-09-06:** row for W-114's code half (Codex vendor, 173 tests green). **2026-09-05: W-101's row** — two vetoes become runnable. **2026-09-05: W-111's row.** **2026-09-05: W-110's row.** **2026-09-05: W-109's row.** **2026-09-05: W-108's row** — `answer` refers the top 3; 13 fixed / 0 broken; three measured costs. **2026-09-02: +1 row** — W-102/103/104/105, the four the wedged-shell session could not close. **2026-09-01: +W-98 phases 2–4 and +W-103 (PII)** — the two rows that had to exist before their queue rows could be deleted. **2026-09-01: +the W-100 · W-102 red-suite sweep row** — 32 failing unit tests → 2 648 pass, and the two live defects behind them. **2026-09-01 (W-98 Phase 1): +1 row** — the browser fetcher returns the resource. ⚠ **The row states what it does NOT claim**: spike step 4 (a signed-in tenant, a real `.xlsx` end to end) has not been run, so the SharePoint acceptance criterion is unverified and is not asserted. **2026-08-28 (W-93): the version benchmark landed** — `~/my_programs/fux-benchmark` stood up with both engines resident, the corpus generator extended with supersession chains / unanswerables / decoys, per-query rows emitted, and seven verdicts filed. **2026-08-27 (third pass): P3's PASS, W-82's forks re-derived to zero, and the decoy defect.** P3 added to the prediction register. **2026-08-27 (later): the real-URL daemon run, R10's measurement, and three error-message defects.** R10 added to the prediction register as `INCONCLUSIVE`. **2026-08-27 (W-91): row added** — the provenance plane, its two found defects, and the law reversal. **2026-08-27 (W-89): +1 row — `L8`, the first law about *use*.** Evidence is [SR-LAWS](../records/0001_LAWS.md) decision 8 plus `CLAUDE.md`; **no measured run, because this is a decision and not a measurement**, and the row says so rather than leaving the evidence column to imply one. **2026-08-26 (W-86 closed): +1 row** covering P0, P6 and P8, written BEFORE the OPEN-WORK row was deleted - that ordering is the precondition, not a courtesy. **2026-08-27 (W-88): +1 row** — the skip notice; the property table names what is held and what is only asserted, and the verification caveat is written into the row rather than left to the WORKLOG. **2026-08-26 (W-86 P7): +1 row** - setup exports every decoder; the copy runs; imports made absolute because a path-loaded file has no parent package. 8 new tests, 1 631 green. **2026-08-26 (W-86 P2-P5): +1 row** - sixteen decoders, thirty extensions, all stdlib. Four defects recorded that decode to plausible garbage rather than an error, incl. **an entire format (ODF) decoding to nothing silently**. 59 decoder tests, 1 623 green. **2026-08-26 (W-86 P1): +1 row** — the decoder plane. Four copies of the converter, two of them shipped in wheel templates; SR-DECODE accepted; 20 new tests, 1 566 green. ⚠ Not committed, concurrent session live. **2026-08-26 (W-84): +1 row** — `ask` at heading level, closed the day it was filed. **2026-08-26 (W-85 landed): `max_parallel` required, `[sources.url]` live, this repo's own fux.toml fixed by hand.** ⚠ `fux add <URL>` now fetches in a newly scaffolded repo. **2026-08-26 (W-83 landed): the unconfigured fetch ceiling, `max_parallel` written into `fux.toml`, and the policy reported by `fux doctor`.** ⚠ Not committed — a concurrent session was live in the tree. **2026-08-26 (W-82 built): five phases landed** — the URL health report, the detector, parallel fetch and the cap, the changed/unchanged line, and the agent surface. Nine records amended in the same change; 1 433 unit tests green. ⚠ `tests_e2e/` unverified in the build environment (fails identically on a clean tree). ⚠ 3.0 and 3.5 did not land — neither is a code task. **2026-08-25 (latest): W-78's closure row added BEFORE the item was archived** — the precondition missed when W-80 closed. Plus a **P-SUPERSEDE** row in the feature-gate register. **2026-08-25 (cleanup): the two MISSING rows added** — the model removal, and **W-80's closure, which was the governance defect**: `work/open/README.md` and `OPEN-WORK.md` both make closing an item legal only once its outcome is recorded here, and W-80 was archived without that. ⚠ **Five build-log rows annotated as SUPERSEDED** rather than rewritten — they describe a dense lane that was true when written; the DENSE-CHUNK register row's *"the committed vectors stay, because they cost nothing at rest"* was wrong on its own terms (**23.0 % of the index**, measured). **2026-08-26 (W-79 finished): the W-79 row CORRECTED in place** — it recorded `query/fuse.py` as **kept**; the file was deleted later the same day and the row now carries the reversal, the reason it failed review, and the two `proposals/ideal/` docs whose *"code is ported already"* premise died with it. **2026-08-24 (latest): +the DENSE-CHUNK row** in the feature-gate table — the first time the dense lane's bar was run, and it failed |
| [`NOW.md`](NOW.md) | **Every session, at every transition** — it is overwritten, never appended | 2026-09-15 | **2026-09-15 (golden unblock, Cowork):** overwritten — the void date gates, W-191/W-192, and what this session did and did not read under L11. **2026-09-15 (arms settle, 16a):** prepended. · **2026-09-15 (Node column BUILT, W-188):** prepended — what is built, what is unmeasured, what to check first. · **2026-09-15 (Node as a column, W-188):** prepended — the ruling and why it is nearly free. · **2026-09-15 (every report on the spine):** overwritten — what gained a number, what says it has none. · **2026-09-15 (W-187, the CAP slide spine):** overwritten — the ruling, the build and what was NOT run. · **2026-09-15 (CAP slide spine, Cowork):** prepended — decision 14 and W-187. · **2026-09-15:** W-185 closed the day it was filed; W-140 and W-154 both 🟢. · **2026-09-15:** W-184 · W-183 · W-182 closed; W-154 and W-140 unblocked; W-185 filed. · **2026-09-15 (🟡 needs an item, Cowork):** overwritten — SR-WORK-OPEN-QUEUE 23a/23b/23c, W-179–W-183, and the two that may honestly end in a null. **2026-09-15 (W-168 step 1 RULED (c), Cowork):** overwritten again — the inbox is empty and the ruling's two build traps (retrieval-not-scoring; one fold in the shared read path) carried as the next step. **2026-09-15:** rewritten for this session. **2026-09-14 (fux.toml audit, Cowork):** overwritten. **2026-09-14 (three fux.toml rulings, Cowork):** overwritten. **2026-09-14 (W-174, Cowork):** overwritten — W-174 built and archived. **2026-09-14 (session end, Claude Code):** every 🟢 item but W-158 closed; names the two BLOCKED actions and that a clean checkout of HEAD is red until the concurrent session commits. **2026-09-14 (W-174, Cowork):** overwritten — W-174 ratified, not built. **2026-09-14 (W-165 + W-166, Claude Code):** points at W-163 next, and carries the uncommitted SEARCH-SKILL correction as a standing warning. **2026-09-14 (backlog sweep, Cowork):** 21 rows left `BACKLOG.md` — 18 promoted into W-163/W-164/W-165/W-166, 3 resolved by correcting their stale source sentences (B-032 SR-T1-ACCELERATOR, B-239 SR-REFUSAL, B-240 SR-CONFIG); B-242/B-243 added for the two 3.0 proposals; B-129/B-133/B-140/B-175 repointed at the 3.0 docs. **2026-09-13 (3.0.0-alpha.0 branch, Cowork):** five work documents filed from the session's rulings — compare docs `ask-graph-expansion` (accepted), `fux-correct` (accepted), `abstention-gates` (proposed; two rulings recorded); proposals `search-improvements-v3`, `fux-inspect`; items W-160 / W-161 / W-162. Nothing built. **2026-09-12 (W-149 CLOSED):** overwritten — what landed, the two record defects, and that nothing agent-side is unblocked. **2026-09-12 (W-142/W-115/W-144/W-136/W-140):** three instruments, three verdicts; W-142 retired because its endpoint sits at the corpus base rate, W-115 measured at last, W-144 answered and sent to a compare doc, the golden ladder completed to 10 000, and W-140 row 22 (a `⚠` that crashed a Windows console from inside a `FuxError`) fixed while row 21 was deliberately left alone after 11 failed reproductions. **2026-09-12 (W-138…W-140):** the session line; ⚠ this session's first write DROPPED the frontmatter `tests/test_okf_bundle.py` requires — restored in the same session, and it is the second time this file has lost it. **2026-09-12 (W-141):** overwritten. **2026-09-11 (balls primary):** overwritten. **2026-09-11 (marks final):** overwritten. **2026-09-11 (blocks):** overwritten. **2026-09-11 (balls):** overwritten. **2026-09-11 (rule 10):** overwritten. **2026-09-11 (`[index]`):** the `[index]` move. **2026-09-06 (later):** carries both concurrent sessions' markers. **2026-09-06:** rewritten for W-114. **2026-09-05: overwritten** — the unblock proposal filed, uncommitted. **2026-09-02: overwritten** — the verification pass. **2026-09-01: W-102–W-105 implemented, unverified.** **2026-09-01: 🔴 Phase 2 blocked.** **2026-09-01: W-98 Phase 1.** **2026-09-01 (W-98): overwritten** — the spike blocker. **2026-08-28 (W-93): overwritten** — the benchmark run. **2026-08-27 (W-91): overwritten** — the plane is built, the law was reverted, nothing is committed. **2026-08-27: overwritten** — W-89 ruled, `L8` written, and the line carries the one instruction that cannot be inferred from the diff: **`CLAUDE.md` and `records/0001_LAWS.md` must be committed TOGETHER.** **2026-08-26 (W-86): overwritten** — the decoder plane filed, P0/P1 startable, P6 blocked on the lock fork. W-84's two live warnings (**nothing committed**, **`tests_e2e/` unverified**) were **carried forward, not dropped** — an overwrite that loses a still-true warning is the failure this file's rule invites. **2026-08-26 (W-84): overwritten** — heading-level `ask` built and closed; not committed, concurrent rename in flight. **2026-08-26 (W-85): rewritten again — the required key and the write-if-missing rule.** **2026-08-26 (W-83): rewritten to the closed ceiling item and the concurrent-session warning.** **2026-08-26 (W-82 built): five phases landed** — the URL health report, the detector, parallel fetch and the cap, the changed/unchanged line, and the agent surface. Nine records amended in the same change; 1 433 unit tests green. ⚠ `tests_e2e/` unverified in the build environment (fails identically on a clean tree). ⚠ 3.0 and 3.5 did not land — neither is a code task. **2026-08-26 (later): W-82 gains §3.6 — the agent surface and the venv invocation ladder** (Arpit). It fixes a **live** defect: `fux.agent.md` tells an agent to fall back to ordinary search when `fux` is not on `PATH`, so an unactivated `.venv` reads as *not installed* and the agent silently uses grep. Four-rung ladder, probed with `--version`, never activating anything; ⚠ rung 4 (`python -m fux`) **does not exist** — no `__main__.py`. Four new forks. **2026-08-26 (the consolidation): W-74, W-75, W-77 and W-81 merged into [`open/W-82-the-consolidated-build.md`](../archive/open/W-82-the-consolidated-build.md)** — a merge, not a close; every fork stays open. Nine documents archived (4 open detail files, 3 proposals, 2 compare docs), each with a successor row in [`archive/README.md`](../archive/README.md). ⚠ **Both compare verdicts were folded into W-82 §4 VERBATIM before the move**, because archiving a compare doc makes its verdict uncitable. Four calls made the same day; `update --warm` and `answer --memo` **withdrawn**. **2026-08-25 (later): overwritten** — the model and the dense lane are gone; W-80 closed; the queue is five. **2026-08-25: overwritten** — W-78 ruling 2 accepted and built; W-81 filed; the queue is five. **2026-08-24 (latest): overwritten** — W-80 filed; the queue is five |
| [`MACHINE.md`](MACHINE.md) | A surface breaks in a way the code did not cause | 2026-09-12 | **2026-09-12 (the collision, corrected):** contention does not merely inflate a number — it MANUFACTURED a clean tier-localised ingest anomaly (0.77 against ~0.46) that read as an engine regression and was one session's last unresolved item. Caught by a disclosure, not a mechanism. ⚠ An owner lock sees a neighbour in the same run directory; nothing sees a neighbour indexing 10 000 documents next door. **2026-09-12 (the benchmark collision):** two sessions ran `bench.py` under one run id on one Mac; `cmd_latency`'s `open("w")` truncated the other's rows silently, and **two hours of concurrent corpus generation contaminated the timings that survived**. New MACHINE.md section: the two damage kinds, `pgrep -f bench.py` first, delete the rows and never the corpus, and ranked lists survive what latency does not. 🔴 **2026-09-12: the silent `device_bash` failure is a FULL `/sessions` DISK, and the reinstall advice this row carried was wrong.** `cowork_vm_node.log` names it — `listen ENOSPC … /sessions/rcw-<session>/tmp/srt-mux-….sock`, then `Failed to create bridge sockets after 5 attempts` — while the VM reports `guest_ready`, `API: REACHABLE` and a 1 971 ms boot. **Fix: park `claudevm.bundle/sessiondata.img`; the app recreates it.** ⚠ `scratchFolder`'s absence proves only that `device_bash` never *succeeded*, never that the VM failed to boot — the 2026-09-06 conclusion below is corrected in place, its observations kept. ⚠ **`vm_bundles.broken` held 16 GB** beside 14 GB live: those two directories were all 32 GB of `Application Support/Claude`, and a reinstall "worked" three times only by handing over an empty session disk. ⚠ **A fresh disk breaks the current session** (`EACCES` on its carried-over user dir, two relaunches no help) — **budget a new Cowork task as part of the fix.** **Third occurrence of the wedged-shell class; the gate is a procedure — read the log first — for the unchanged 2026-09-06 reason.** **2026-09-06: the device VM can fail to BOOT, not just wedge — and `scratchFolder` is the tell.** Every `device_bash` call including `echo ok` failed with **no underlying error text**; `get_device_info` reports `scratchFolder` only after `device_bash` has succeeded once in a session, so its absence proves the VM never started and separates that from a command-level failure — **make it the first move**. ⚠ **The bridge itself was fine**: `device_list_dir`/`device_stage_files`/`device_commit_files` worked throughout, so a **docs-only session still completes** via stage → rewrite in the container → commit back (no in-place edit, no append). App restart, laptop restart and CLI scripts all changed nothing — the VM image is per-**install** — and on macOS trashing the `.app` keeps `~/Library/Application Support`, so an ordinary reinstall likely preserves the broken image *and* fixes nothing; the lever that rebuilds it also deletes local app data. **Second occurrence of the wedged-shell class (two-strikes trigger); no check written and the entry says why** — the fault is in a VM this repo does not ship. **2026-08-27: the shell can be wedged for a WHOLE session while the file tools keep working** — every `mcp__workspace__bash` call failed identically (`ensure user: useradd failed: exit status 12`), five times, no recovery; `Read`/`Write`/`Edit`/`Grep` were unaffected. ⚠ **It removes the `_to_delete/` workaround this same section documents** — that needs `mv`, and the file tools have no rename and no unlink, so a session in this state **cannot remove a file by any means**. Also no `git` (no ground-truth check) and no `pytest`. The recorded hazard is the *documentation* one: a session in this state must write docs as **ruled, pending a command Arpit runs**, never as a completed cleanup. **2026-08-26 (W-84): `git mv` strands an `index.lock` too, and has no read-only form** — use a plain `mv` for an untracked detail file being retired into `archive/open/`; git records the move at `git add` time from the paths. **2026-08-22 (Cowork): the remote bridge leaves an undeletable `.git/index.lock`.** `git status` refreshes the index, creates the lock, and cannot unlink it because the bridge blocks `unlink` — so every later git *write* fails. `git --no-optional-locks status` is the fix; a stranded lock is `mv`'d aside, never deleted. Cost one stranded lock before it was diagnosed. **New row.** Local vs Cowork device VM vs cloud container vs CI: the device bridge cannot delete (so `git checkout`/`stash`/`reset` fail there), has no network and Python 3.10; macOS case-insensitivity hides broken links; the lab persists and is never rebuilt |
| [`LESSONS.md`](LESSONS.md) | **A session learns something the hard way** — a failure whose remedy is judgement rather than a rule. A lesson whose remedy IS a rule goes to the record that owns it and is named here only as the failure that produced it | 2026-09-15 | **2026-09-15 (W-177's own error, Claude Code):** +§*"commit with explicit pathspecs is not a rule you can follow mechanically"* — second occurrence of the shared-checkout commit class, with [`scripts/commit-paths.py`](../scripts/commit-paths.py) as its two-strikes gate. **2026-09-15:** a flake fired in `tests_e2e` and its traceback was discarded by `| tail -3` — the SECOND such loss on this test family, and the habit is the session's, not the code's. · **2026-09-14 (new, W-173 item 5):** `CLAUDE.md` §Hard-won build knowledge became this file. **A log, not a record** — a record states what is true now and carries no history, and here the date is part of the lesson. Carries 2026-08-09 (M1's pruning gate), 2026-09-12 (two sessions on one machine) and 2026-09-14 (five items in one session). ⚠ **Nothing in it is normative**: where an entry and a record disagree the record is the rule. The two v0.19-0.26 lessons that still bind are listed as lessons and their RULES stay in [SR-RANKING](../records/0111_ranking.md) and [SR-LAW-3](../records/0005_LAW-3-deterministic.md). |
| [`../benchmark/reports/TEMPLATE.html`](../work/benchmark/reports/TEMPLATE.html) | **A `fux:fill:NAME` marker is added, renamed or removed** (the emitter refuses on a mismatch), a capture's direction of goodness changes, or a slide is added | 2026-09-15 | **2026-09-15 (arms settle, 16a):** the parity slide is one row, and names the three arms. · **2026-09-15 (Node column BUILT, W-188):** a **thirteenth slide**: reader parity, `fux:fill:parity`, with the opposite-reading warning outside the marker. · **2026-09-15 (every report on the spine):** unchanged by the rebuild — the legacy reports were re-titled to it, not regenerated from it. · **2026-09-15 (W-187, the CAP slide spine):** **twelve slides**: CAP-1…CAP-6 each titled `CAP-n — <capture>` outside their markers, `.finding` for the emitter's one-line finding, and a new `fux:fill:ranked` region. CAP-7 has no slide. · **2026-09-14 (W-158):** gained the ten `fux:fill:` / `fux:end:` marker pairs that `fux-benchmark/bin/report.py` fills, and the paragraph stating that everything OUTSIDE a marker — including every numeric header's direction marker — is copied through verbatim. 🔴 **A literal `-->` written inside the authoring comment closed it early**, and the first generated report carried the template's own how-to instructions as visible page text; the two marker names are now spelled without their delimiters in that paragraph, with the reason. ⚠ The CAP-2 figure stays a human's inline SVG — a generated report says so in the figure's place rather than omitting it. |
| [`../records/0156_inspect.md`](../records/0156_inspect.md) | **A lens is added, removed or redefined**, a provisional floor is re-measured or dropped, the golden ladder grows a rung (the floors are measured on it), or `inspect` gains a write | 2026-09-14 | **2026-09-14 (new):** `fux inspect` — six lenses over the committed index, descriptive by default. 🔴 **`findable share` ships DESCRIPTIVE, not floored** — 1.000 on every golden rung *and* on forty copies of one runbook, and the cause is structural (a fingerprint is the document's own rarest terms, and a document's path is unique indexed vocabulary). The flag went to the exhaustive *unreachable share*. ⚠ **`boilerplate share` is floored at 0.60 and is the weakest of the three**: every rung is 0.47 against this repo's 0.072. ⚠ **The floors are re-measured by nothing** when the ladder changes — stated as owed. Words come from a gitignored local dictionary built from REDACTED text, like the index. [The floors run](regression/2026-09-14-inspect-floors/report.md). |
| [`../records/0155_agent-surfaces.md`](../records/0155_agent-surfaces.md) | **A new agent-surface KIND becomes reachable** (a vendor ships hooks/commands/subagents), a shipped surface changes class, or the advisory hook is measured | 2026-09-14 | **2026-09-14 (new):** names the category *agent surface* and classifies by what a surface DOES. Ships five **acting** surfaces, Claude-only: an advisory `PreToolUse` hook (exits 0 always — decision 4 bars enforcement on an inexact rule), a seeded `settings.json`, three commands, a `fux-researcher` subagent, an `fux-cited` output style. 🔴 **`settings.json` is co-owned** — seeded never overwritten, and exempt from the drift test because this repo's own copy carries the golden-answer deny rules. ⚠ Two surfaces are named and unpinned: MCP descriptions (no test compares them to the skills) and CLI output (carries none of the guidance yet). |
| [`../records/TEMPLATE.md`](../records/TEMPLATE.md) | The SR convention changes | 2026-09-13 | **2026-09-13:** SR-OWNERSHIP became **SR-WORK-OWNERSHIP** (`0146` → `0054`, the WORK range); the hole at `0146` closed by shifting `0147`–`0156` down one — a file number is an ordinal.  **2026-09-13 (`kind`):** every record declares `kind: law | component | process` — the gate it answers to. 12 · 52 · 4. SR-WORK-OWNERSHIP decision 11; register §The three kinds.  **2026-09-13 (SR rename):** `ADR-<NAME>` became `SR-<NAME>` and `docs/adr/` moved to `records/` at the repo root — one mechanical change, every reference repointed. Archived `ADR-NNNN` citations and `archive/v0.26-docs/adr/` are unchanged.  **2026-09-11 (W-137):** the *How to use this file* block names the two ranges — an ordinary record numbers from `0101`, a Law record from `0011` — and the placeholder path goes `000N_` -> `NNNN_`. **2026-08-27 (Arpit, the record-set rewrite): the template defines the TEN-KEY frontmatter and forbids the two things a body may not contain.** Keys in a fixed order — `type · name · title · description · status · date · feature · owns · laws · timestamp`, with `supersedes`/`ratifies` optional — and **the body opens at §1 and restates none of them**; the `- **Name:** …` bullet block every record carried is deleted, because two hand-written copies of one fact drifted. ⚠ **`Amended` sections are abolished and the word may not appear as a heading** — a correction appended below a false sentence leaves the false sentence in place, and an agent reads top-down and acts on the first answer it finds. The one thing that survives is an argument that still binds: a rejected alternative belongs in *Alternatives considered*, because **it is the reason the current shape is the current shape** — the failure is the argument, the date it happened is not. Enforced by a rewritten [`tests/test_sr_frontmatter.py`](../tests/test_sr_frontmatter.py) (key set, order, quoting, title, no restatement, no amendment, H1 agreement) and a now-bidirectional [`tests/test_sr_owns_consistency.py`](../tests/test_sr_owns_consistency.py). **2026-08-22 (Arpit): the template gains `## References`** — the last section of every record: every source it cites, gathered into **Records · Code · Measured evidence · Project docs · Papers and specifications**, empty groups deleted. Three rules in its instruction block — nothing appears there that is not cited in the body; **never an archived document**; every link resolves. §2's `Reference (required)` is unchanged, and stays the *argument's* grounding. ⚠ **This row had never recorded the 2026-08-22 worked-output block** (optional `**Output —**` in every §2 section); noted here rather than backdated. **2026-08-18: §1 gains two optional sections** — **Examples** (real, capture-copied, two or three at most) and **Charts** (default: none; one measure per chart, every number measured or computed with its source named, same collapsed-ASCII-twin rule as the diagram). Both are *deleted* rather than left empty when they do not apply — SR-LAWS is the worked example. Earlier: **Rewritten** into the §1-humans / §2-agents shape: one-screen §1 with a Mermaid block **and** its hand-paired ASCII twin, §2 with context · decision · consequences · alternatives · reference · **veto condition written as a condition to check** |
| [`../tests/test_regression_runs.py`](../tests/test_regression_runs.py) | **The per-run contract changes** | 2026-08-28 | **2026-08-28 (W-93): two gates added.** `verdicts()` now globs `VERDICT*.md`, not the bare name — a run ruling on seven thresholds files seven verdicts and the siblings were unchecked, which is the worst state for a verdict: filed, cited, guarded by nothing. And `test_measured_run_files_its_per_query_rows` enforces Arpit's 2026-08-28 ruling. ⚠ **Baselined at 2026-08-29, not 2026-08-28**: five runs were filed on the ruling's own day before any harness could emit rows, their reports are frozen, and turning a rule on by editing the evidence it governs is the failure `CLASSIFY_SINCE` exists to avoid — so a fixture-level test guards the baseline from drifting past the runs it exempts. **2026-08-27: a run directory holding a frozen `PRE-REGISTRATION.md` and NO report is legal** — `pre-registered, not yet measured` (SR-RS decision 17). Legal ONLY while there is no report; it must still be listed in the index. **2026-08-25 (later): a `pre_registration:` path may resolve to a MIRROR** at `<run>/evidence/pre-registration/<original path>` when the live file has been deleted — SR-RS decision 16. A second check refuses a mirror sitting BESIDE a live file, because two frozen thresholds for one verdict is the ambiguity the freeze exists to prevent. Verified by watching it go red while both existed. **2026-08-25 (W-78 ruling 2): the classification gate.** A measured run dated ≥ `2026-08-25` must carry `classification: blind|informed` and an `## Authorship` section — SR-RS decisions 11 and 13. Surface captures exempt, read from the **same** declaration the evidence rule reads so the two exemptions cannot drift. Six `tmp_path` fixture tests guard the *rule* as well as the runs, so a refactor that empties the parametrisation cannot silently disable the gate. Verified red against a planted violating run before shipping. **New row.** Every filed run carries a report, an ANALYSIS and its evidence — `evidence/` waived only for a run whose report declares itself a *surface capture* — and is listed in the index. Verdict docs must carry `type: Verdict`, a prediction id, a ruling, and a **pre-registration path that exists** |
| [`../tests/test_doc_registry.py`](../tests/test_doc_registry.py) | **The registry's own rules change** | 2026-08-18 | **New row.** Enforces live-documents-only: no row points into `archive/`, every row's target exists, one row per document, every row carries an ISO date, and — the inverse blind spot — **every live `work/*.md` has a row** |
| [`../tests/test_archive_law.py`](../tests/test_archive_law.py) | The one-archive rule changes | 2026-08-18 | **New row.** Fails when a directory named `archive` exists anywhere but the repo root, and when a live doc still points into a retired second archive. Exempts `archive/` itself (frozen), `work/WORKLOG.md` (append-only history) and the frozen run docs under `work/regression/` |
| [`../tests/test_sr_frontmatter.py`](../tests/test_sr_frontmatter.py) | **The frontmatter schema changes** | 2026-09-13 | **2026-09-13 (`kind`):** every record declares `kind: law | component | process` — the gate it answers to. 12 · 52 · 4. SR-WORK-OWNERSHIP decision 11; register §The three kinds.  **2026-09-13 (SR rename):** `ADR-<NAME>` became `SR-<NAME>` and `docs/adr/` moved to `records/` at the repo root — one mechanical change, every reference repointed. Archived `ADR-NNNN` citations and `archive/v0.26-docs/adr/` are unchanged.  **New row.** Six keys, fixed order, values containing `: ` quoted, `name`/`status` matching the body, and the title carrying both the name and the number. Written after frontmatter broke twice in one day — an unquoted colon only strict YAML rejected, and a global rename that put a name where eight titles' numbers belonged |
| [`../tests/test_sr_freshness.py`](../tests/test_sr_freshness.py) | **The SR-currency rule changes**, or the ownership table changes | 2026-09-13 | **2026-09-13 (SR rename):** `ADR-<NAME>` became `SR-<NAME>` and `docs/adr/` moved to `records/` at the repo root — one mechanical change, every reference repointed. Archived `ADR-NNNN` citations and `archive/v0.26-docs/adr/` are unchanged.  **2026-08-27: it RAN for the first time, and failed on itself.** Eight commits were flagged for not updating records that did not exist when they landed. Every commit is now judged against `git show <sha>:records/README.md` — the register AS IT STOOD AT THAT COMMIT (SR-WORK-OWNERSHIP decision 9). **Three tests pin it**, including one proving the gate still bites. **2026-08-21 (PRIORITY P1): now requires the OWNING record specifically** — touching some other, unrelated record no longer satisfies it (was: any record touched at all). Escape-hatch regex anchored to a whole line (`^no SR affected$` / `[no-adr]`), not a substring match. Shares its table/owner parsing with `test_sr_ownership.py` via `tests/sr_lib.py`, so the two can no longer disagree about who owns what. Runs in CI via `pytest -q tests`, now with `fetch-depth: 0` (a shallow checkout audited zero commits — silent pass, not a check). Escape hatch is `no SR affected` in the commit message — a claim in git history, not a silent skip. Baseline self-bootstraps from the commit that adds it; `records/RULE-SINCE` moves it forward |
| [`../scripts/sr-owns.py`](../scripts/sr-owns.py) | The hash definition or the skip list changes | 2026-09-13 | **2026-09-13 (new):** stamps every `owns:` claim with a 12-hex content hash of the file or directory it names. Run it BEFORE `sr-hash.py`. |
| [`../tests/test_sr_owns_hash.py`](../tests/test_sr_owns_hash.py) | The hash definition changes | 2026-09-13 | **2026-09-13 (new):** every claim carries a hash and every hash matches the bytes. SR-WORK-OWNERSHIP decision 13. ⚠ Fires on a typo as loudly as on a redesign — read the record before re-stamping. |
| [`../scripts/sr-hash.py`](../scripts/sr-hash.py) | The hash definition changes | 2026-09-13 | **2026-09-13 (new):** stamps each record's `content_sha` — SHA-256 over the whole file with its own `content_sha:` line removed. `--write` to stamp, no flag to check. Run it in the SAME change that amends a record. |
| [`../tests/test_sr_content_hash.py`](../tests/test_sr_content_hash.py) | The hash definition changes | 2026-09-13 | **2026-09-13 (new):** every record's `content_sha` is current, stamping is idempotent, and the digest moves on a frontmatter change (hashing the body alone was rejected). SR-WORK-OWNERSHIP decision 12. |
| [`../scripts/sr-guard.sh`](../scripts/sr-guard.sh) | The SR-currency rule changes | 2026-09-13 | **2026-09-13 (SR rename):** `ADR-<NAME>` became `SR-<NAME>` and `docs/adr/` moved to `records/` at the repo root — one mechanical change, every reference repointed. Archived `ADR-NNNN` citations and `archive/v0.26-docs/adr/` are unchanged.  **2026-08-21 (PRIORITY P1): moved from a `pre-commit` hook to `commit-msg`** — `pre-commit` runs before the commit message exists, so reading `.git/COMMIT_EDITMSG` there read the *previous* commit's leftover message. `commit-msg` receives the message path as `$1` and runs after it is written. Now requires the OWNING record (resolved from the ownership table + the register's record-listing table, portable grep/sed — not `awk`'s 3-arg `match()`, a gawk extension macOS's bwk `awk` lacks) — `ln -sf ../../scripts/sr-guard.sh .git/hooks/commit-msg` |
| [`../tests/sr_lib.py`](../tests/sr_lib.py) | The ownership table's shape, or how records are cited, changes | 2026-09-13 | **2026-09-13:** SR-OWNERSHIP became **SR-WORK-OWNERSHIP** (`0146` → `0054`, the WORK range); the hole at `0146` closed by shifting `0147`–`0156` down one — a file number is an ordinal.  **2026-09-13 (SR rename):** `ADR-<NAME>` became `SR-<NAME>` and `docs/adr/` moved to `records/` at the repo root — one mechanical change, every reference repointed. Archived `ADR-NNNN` citations and `archive/v0.26-docs/adr/` are unchanged.  **2026-08-27: `ownership_table`, `describes_table`, `register_names` and `record_path_for` take an optional register `text`**, so the freshness gate can parse an old revision. A register predating the `DESCRIBES` markers returns an EMPTY relation, not a parse error. **New row (PRIORITY P1).** The parsing both `test_sr_ownership.py` and `test_sr_freshness.py` import — `ownership_table()`, `owner_of()`, `register_names()`, `record_path_for()` — one implementation, so the freshness gate and the ownership twin cannot compute two different answers for who owns a component |
| [`../tests/test_sr_ownership.py`](../tests/test_sr_ownership.py) | **The ownership table in `records/README.md` changes — same change, always** | 2026-09-13 | **2026-09-13:** SR-OWNERSHIP became **SR-WORK-OWNERSHIP** (`0146` → `0054`, the WORK range); the hole at `0146` closed by shifting `0147`–`0156` down one — a file number is an ordinal.  **2026-09-13 (SR rename):** `ADR-<NAME>` became `SR-<NAME>` and `docs/adr/` moved to `records/` at the repo root — one mechanical change, every reference repointed. Archived `ADR-NNNN` citations and `archive/v0.26-docs/adr/` are unchanged.  **2026-08-27 (W-89): `test_records_do_not_restate_the_laws` gains L8's handle** — `"hashed, bounded, and local"`, added in the same change as the law. ⚠ **It caught a real paraphrase the same day, which is why it is here and not left to review:** SR-WORK-QUALITY decision 11 was first written as *"bound by L8 — hashed, bounded, local, off every committed and networked path"*, i.e. the law restated inside an accepted record, where a drifted copy reads as authority. Rewritten to cite the number and send the reader to the one home. ⚠ **The check still only greps three handles out of eight** — it finds *copies*, never a narrower claim standing in for a law, which is the failure SR-LAWS' veto note says cannot obviously be automated. **2026-08-21 (PRIORITY P1): now also checks each record's own `**Owns:**` line** (`test_owns_line_is_a_subset_of_the_register` and `test_no_path_is_claimed_by_two_records_owns_lines`, in `test_sr_owns_consistency.py`) — an unqualified `Owns:` line is an active claim, checked against the register; an `Owns (on acceptance/build):` line is a future claim, not checked until the record's status flips. Shares its table/name parsing with `test_sr_freshness.py` via `tests/sr_lib.py`. Otherwise unchanged: fails when a `src/`/`tools/` component is claimed by no record, when a claim points at a missing file or a closed `W-nn`, when a record's declared name disagrees with the register, or when a record restates a law |
| `../.github/` | Required checks, release path change |  2026-09-05 | **2026-09-05 (W-107 Phase 0): `log-probe.yml` is NEW** — the glibc half of the `log()` evidence, `workflow_dispatch` only. Deliberately not on push: it is evidence for one frozen pre-registration, not a gate, and it measures the runner's libm rather than anything in a diff. It declares no dispatch input, because an input nothing reads is the knob-that-cannot-work defect. Workflow YAML already targeted the new tree; restored `scripts/ai-review.sh` + `apply-branch-protection.sh`, which the live CI referenced but were missing from the tree since the v0.26 archive move |

## How agents use this file

1. **At task end:** scan the trigger column; if your change fired a
   trigger, update that doc and bump its row. CLAUDE.md binds you to this.
2. **Adding a doc:** new maintained doc → new row, in the same change that
   creates it. Archiving a doc → retire its row, same change.
3. **No ⚠ rows remain** — the two stale rows (CLAUDE.md, GLOSSARY.md) were
   cleared by W-03 on 2026-08-09; GLOSSARY was rewritten and CLAUDE.md's
   rewrite was proposed for review. **Correction (2026-08-10):** CLAUDE.md's
   row previously said the rewrite was "adopted" — the file itself still
   carries a "PROPOSED — not in force" header, so it has not been; fixed
   above. Adoption is Arpit's call and unchanged by this session.
