# DOC-REGISTRY — the freshness tracker for **live** documents

**How to use this file.** One row per maintained doc: what triggers an update
to it, and when it was last verified true. **Bump the row in the same change
that touches the doc** — the row's date is the only thing that makes staleness
visible at a glance. A row older than ~30 days is a review prompt, not a
failure.

### The rule: this registry lists live documents only

**An archived document loses its row in the same change that archives it.**
Not struck through, not annotated "retired" — **deleted**, the same discipline
[`OPEN-WORK.md`](OPEN-WORK.md) applies to closed items, and for the same
reason: the file's length should mean something. A registry padded with
tombstones stops answering *"what do I have to keep true?"*, which is the only
question it exists to answer.

Three consequences, all mechanical:

1. **No row may point into [`../archive/`](../archive/README.md).** An archived
   doc is frozen and never updates, so a trigger and a last-verified date are
   meaningless on it. Where it went is the archive map's job, not this file's.
2. **Every row's target must exist.** A row for a deleted file is the same
   defect wearing different clothes.
3. **One row per document.** Two rows means two last-verified dates for one
   file, which is the confusion this file exists to prevent.

Enforced by [`../tests/test_doc_registry.py`](../tests/test_doc_registry.py).

**The archive map is deliberately not listed here.** It lives under `archive/`,
so rule 1 excludes it — and it does not need a row: the archive law in
`CLAUDE.md` and `tests/test_archive_law.py` already require it to exist and to
name a successor for everything in it.

**Contract:** every maintained doc has an update **trigger** and a
**last-verified** date. A task that fires a trigger updates the doc *and* its
row, in the same change. Docs large enough to carry §humans + §agents sections
update both.

| Document | Update trigger | Last verified | Notes |
|----------|---------------|---------------|-------|
| [`PRIORITY.md`](PRIORITY.md) | Arpit reorders; a row flips to DONE (with its commit sha); Arpit rewrites a row's content | 2026-08-21 | **2026-08-21 (later still): P6 flipped DONE.** Refer wired into `answer` (default-on, `--no-refer` opts out); ADR-REFER and ADR-ANSWER both accepted, Arpit ruling directly on the one tension found (ADR-REFER's own text tied acceptance to the still-unmeasured W-59 budget sweep — accepted anyway, with that gate kept as a named veto condition, not hidden). **2026-08-21 (later still): P5 flipped DONE.** Materialise-first built; both forks and two of three sub-questions ruled by Arpit directly (L2: outside entirely; delta path: force re-fetch; salt: not built; `code`: kept); the third (`loc`/`id`) resolved by architecture, not a live choice — see `meta-privacy.compare.md`. One done-when clause corrected on contact: "reveals no URL slug" was never achievable — `loc` is the refer plane's only fetch address and is separately committed in plaintext via the URL source list, so this was never a real leak to close. **2026-08-21 (later): P5 rewritten on Arpit's instruction — "materialise first, then index".** Hashing by default is unchanged; what is added is the sequencing: a non-git document's bytes go to the local gitignored cache **before** its record is written, and every reader-facing surface takes display text from that cache rather than from the committed record, so `hashed` no longer means unreadable. P5's three original sub-questions (salt / `loc`-`id` / `code`) are kept as written. Three research findings recorded in the why-column as findings, not rulings: a repo-committed salt is not a salt (a cloner has it too, so per-index salting only bites if the salt is per-machine and out-of-band); embedding inversion against `code` is demonstrated at 92% exact-match on 32-token inputs (Morris et al., EMNLP 2023); tf+`wlen` are volume leakage that published attacks reconstruct from regardless of term hashing. Two forks explicitly reserved for Arpit: whether a mandatory cache needs `hashed` to imply `snapshot` or L2 to gain a named exception, and what the delta path (`_reusable()`) does on a cache miss. The P5 parenthetical claiming per-index salting "kills cross-index dictionary attacks, breaks nothing" is flagged as overstated rather than deleted. **2026-08-21: P1 and P2 flipped DONE**, evidence `1fc51a7` for both plus `docs/adr/RULE-SINCE`; P4's `FetchError` sub-item struck as done incidentally (P2 also named it). **2026-08-20: created on Arpit's instruction** from the independent audit — the one ranked order of work (P1–P8) and the gate: an agent works the first non-DONE row or blocks via `BLOCKED.json`. Read before OPEN-WORK. |
| [`../CLAUDE.md`](../CLAUDE.md) | Scope, constraints, lifecycle, layout changes | 2026-08-19 | **2026-08-19 (later): version facts bumped to `0.33.0`** — a statement of fact, fixed on contact, not a change to what the file instructs. **2026-08-19: ADOPTED — the PROPOSED header is deleted; the M0a rewrite is in force** (W-32, Arpit). Five factual passages fixed in the same change: "no package on `main` yet" → **0.32.0 released**; version `0.30.0.dev0` → **0.32.0**; "§Error contract (applies once `src/` exists)"; an archived-ADR citation for the ingest modes → [ADR-EXTRACTED](../docs/adr/0016_extracted-mode.md)/[ADR-ENRICHED](../docs/adr/0017_enriched-mode.md); and §Scope's "**No M2+ work while P1 is unmeasured or failed**" — P1 closed FAIL and M2 *shipped*, so the file forbade a released milestone. §Documentation discipline gains the preserved rule the header used to carry — **agent-steering files are proposed, never auto-applied** — with an explicit carve-out that **statements of fact about the repo are fixed on contact**, which is why five of them had rotted. **2026-08-18: §Documentation discipline added** (three-file session discipline · OPEN-WORK rules · archive-is-not-evidence · concurrent-session + ground-truth hazards · ADR standing rules), §Layout rewritten to the `work/` + `docs/` tree, the laws named **L1–L7**, and two stale passages fixed ("numbering continues at 0016"; "`src/fux/` does not exist yet"). Still carries its **PROPOSED** header — W-32 is Arpit's call. Earlier: **Still proposed, not adopted** — the file's own header says so. **Correction (2026-08-12): there is no `CLAUDE.md.proposed`.** The rewrite was written straight into `CLAUDE.md` at `3892c55` and every session since 2026-08-09 has followed it, so "adopt" = delete the header and "reject" = revert ~800 lines. Three passages are stale (`src/fux/` "does not exist", §Layout's `archive/v0.26-docs/`, the dead `OPEN-WORK §2` pointer) and are **deliberately unfixed** under law 7. Packaged for decision in [the ratification package](../archive/handoff/v0.32.0-ratification-package.md) §4. **2026-08-12: new *Triage first* section added on Arpit's explicit instruction** (a human-blocked queue stops the session, blocked-list first) — a deliberate edit to the live file, separate from the W-32 adopt/reject call. **2026-08-12: Triage-first section extended on Arpit's explicit instruction** — the inbox rule (5-day naming), two-strikes-→-gate, and the worklog `Cost:` line. |
| [`../README.md`](../README.md) | Status, guarantees, reading order change | 2026-08-21 | **2026-08-21 (PRIORITY P2 reconciliation):** "No graph query lane yet (M3)" was stale — M3 landed 2026-08-20 (unreleased); replaced with what shipped and its two open acceptance gaps (W-57). Reading-order item 4's link text said `work/adr/0004_index-format.md` while pointing at `archive/adr/0004_index-format.md` — text corrected to match the target, and a note added that the archive may be named for orientation but never backs a live claim. **2026-08-19 (W-54, the sources rewrite, released as `0.33.0`):** the status block gains `fux setup` and `fux url`, the two committed source lists are named as the corpus, and the **two scaffolding moments** are stated where a reader would otherwise assume ingest writes code. **Status rewritten for M2**: the accelerator, the measured R3 number, the differential law, the five verbs, and hybrid-off. Previously: "T0 slice: ingest + ask work on this repo"; gained a **`.fux/` layout table** and the new URL paths (ADR-DOTFUX). **Reading-order item 5 repointed at the **`fux-playground` sibling repo** (SETUP-PLAYGROUND); `examples/` deleted from this tree.** |
| [`../.fux/README.md`](../.fux/README.md) | The `.fux/` layout table changes (a plane is added, renamed, or flips committed/derived) | 2026-08-19 | **2026-08-19 (W-54, the sources rewrite, released as `0.33.0`):** `sources/` holds two lists; the fetcher section names `fux setup` as the writer and `http.py` as the second file. New row. **Generated** by `store/fuxdir.py` at ingest, write-if-missing — edit the generator, not the file; a consumer's copy is never overwritten (ADR-DOTFUX) |
| [`../CHANGELOG.md`](../CHANGELOG.md) | A release-worthy change lands | 2026-08-21 | **2026-08-21 (PRIORITY P6): `[Unreleased]` gains a Changed — breaking entry** — `answer` fetches/verifies/re-scores by default now, `"source"` gains a `"refer"` value. **2026-08-21 (PRIORITY P5): `[Unreleased]` gains the materialise-first display cache** under Added, plus the per-asset session-lock hook. **2026-08-21 (PRIORITY P4): `[Unreleased]` gains a `### Fixed` section** — the six reproduced defects (merge-driver ancestor check, BOM, path NFC, df precision, CRLF ×3 files, fetch-cache size cap). **2026-08-20 (W-60): the TTL fetch cache and the fourth `cached` verdict, under Added.** **2026-08-20 (M5): `fux hooks` and `fux-merge-index` under Added; L5-at-write-time under Changed—breaking** (breaking only for a caller writing records directly; every existing record complied). **2026-08-20 (W-45+W-55): a `Changed — breaking` section opens `[Unreleased]`** — only prose files are indexed now, and `dirs` accepts `!` exclusions. The entry states plainly that the type filter **changes rankings and is unmeasured**. **2026-08-20 (M4): `[Unreleased]` gains the refer plane core** — fetch through the consumer contract, freshness by content sha, ARC, byte budget. Flagged in the entry as **not exposed by any verb** and its record not accepted. **2026-08-20 (later): `[Unreleased]` gains the M3 graph lane** under Added — three verbs, unseeded communities, and the derived `graph.json` plane. **2026-08-20: `[Unreleased]` opened** — W-46 under Fixed (the `--hybrid` crash on a source install), W-48 under Changed (`"path"` in `ask --json --explain`, `"source"` on both `answer --json` branches). **Released as `0.33.0`** on 2026-08-19 — the sources rewrite, with **two breaking retired keys** (`[sources] dirs`, `[sources.url] middleware`) and their migrations shown as diffs, `fux setup` and `fux url`, per-URL attributes, and three fixes. Prior: **Released as `0.32.0`** on PyPI 2026-08-13 (M2 — verified black-box from the published wheel; 0.31.x never published). Prior: `0.30.0` (M0 + M1). `[Unreleased]` carries ADR-URL-INGEST, ADR-DOTFUX, and **ADR-T1-ACCELERATOR — the M2 accelerator, `fux build`/`find`/`answer`, the differential law, R3 PASS, and hybrid default-off**; plus the Windows doctor-crash fix |
| [`../docs/index.md`](../docs/index.md) | Bundle contents change | 2026-08-09 | OKF bundle root |
| [`architecture.svg`](architecture.svg) + [`architecture-overview.svg`](architecture-overview.svg) | Any tier/record-shape/query-path change (detailed) · any component add/remove (overview) | 2026-08-18 | **Moved into `work/` 2026-08-18.** Prior: Rev-2 pair: overview (5 components) + detailed; rev-1 pair archived. Build-order caption no longer names the deleted AcmePay fixture. |
| [the ADR register](../docs/adr/README.md) | Any design/scope/status change | 2026-08-21 | **2026-08-21 (PRIORITY P6): ADR-REFER and ADR-ANSWER both flip ⏳ proposed → ✅ accepted.** ADR-REFER's status/Feature lines updated to name the acceptance and keep the still-open budget-sweep veto condition explicit rather than closing it. ADR-ANSWER substantially rewritten (§1, decisions, examples, veto condition) since the verb's own default output shape changed, not just its status — the old M2-only content is now what `--no-refer` produces, not the default. ADR-ASK and ADR-CLI (both directory-level owners of touched components) gain Consequences bullets pointing at ADR-ANSWER for the full decision. **2026-08-21: +ADR-CACHE (0035, ⏳ proposed, built: yes)** — the refer plane's two caches carved out of ADR-REFER on Arpit's call, taking `src/fux/refer/arc.py` and `src/fux/refer/fetchcache.py` with them (most specific wins; `tools/refer-bench/` is **not** split — one harness runs R4 for the whole plane). ADR-REFER keeps the fetch contract, verification, chunking, rescoring and the byte budget; its decisions **5a/5b/5c and 9 are pointers now, and those numbers are retired, not reused**, so every doc citing "ADR-REFER decision 9" stays true. The ownership paragraph is rewritten as **two** live carve-outs. Nothing was re-decided — the record states two pre-existing open weaknesses in their own veto conditions: ARC-vs-LRU was measured post-hoc at R4, and `no_cache` is advisory rather than enforced. **2026-08-21: +ADR-MERGE-DRIVER (0034, ⏳ proposed, built: yes)** — decisions 6–9 of ADR-MAINTENANCE carved out on Arpit's instruction, taking `src/fux/maintain/mergedriver.py` with them (most specific wins; the harness is **not** split — one file runs R5 and R6). ADR-MAINTENANCE keeps the hooks, the installer that registers the driver, and L5 at write time; its decision numbers 6–9 and veto conditions 2 and 4 are **retired, not reused**, so decision 10 and every doc citing it stay true. The ownership paragraph now says why this carve-out is in the table when the six companion records are not. `tools/maintenance-bench/`'s row read "written, not run" — fixed on contact, both gates ran 2026-08-20. **2026-08-19 (W-54, the sources rewrite, released as `0.33.0`):** `built` flipped to **yes** for ADR-URL-LIST and ADR-HTTP-FETCHER and to **partial** for ADR-DIR-LIST (the file and the declaration are built; the archived *signal* is gated on W-44's instrument). Ownership gains `setup.py` and `sources.py`, `templates/` and `ingest/sourcelist.py`, with its executable twin in the same change. The build of record; agent quick-ref section at top. **M2 marked SHIPPED with its measured results; R3 row now carries PASS (27.2 ms).** Corrected §M2's class-3 acceptance list — it named `q008`/`q017`, which are not `known_failure` queries |
| [`open/`](open/README.md) | **An item opens or closes** — its file is created with its index row and **deleted with it** | 2026-08-20 | **2026-08-20 (W-60): closed and archived.** **2026-08-20 (M5): W-25 closed and archived; +W-61** for its unrun gates. The stale W-56 row was also removed — its detail file had already moved to `archive/open/`, so the row pointed at nothing. **2026-08-20 (later): W-27 closed and archived** — Arpit ratified it directly rather than through the retired two-week gate; see the IMPLEMENTATION.md ratified-decisions row. **2026-08-20 (W-56): W-56 closed and archived** — both environments rebuilt and under git. **W-57 re-scoped in the same change**: its named query ids (`q005`/`q009`/`q011`/`q015`) belonged to the lost golden set and cannot be recovered, so its targets are now *phenomena*, and it moves to the `arpit` lane because the goldens are human work. **2026-08-20 (later): +W-60** (a TTL-bounded local fetch cache for `url:` sources, compare doc awaiting Arpit; does not reopen W-58). **2026-08-20 (W-45+W-55): both closed and archived.** The queue's ADR-DIR-LIST/ADR-INGEST group is gone with them. **2026-08-20 (M4): W-24 closed and archived** (its spec written into it first, then archived with it); **+W-58** (no recorded ingest time) and **+W-59** (the refer plane is built and unmeasured). **2026-08-20 (later): W-23 closed and archived; +W-56 and +W-57.** W-56 is the finding that both sibling environments are missing; W-57 carries the two DoD items M3 did not meet. **2026-08-20 (later): +`W-55-no-file-type-filter.md`**; `W-45-source-exclusion.md` moved to OPEN·human with its compare doc written, and two stale facts in it fixed on contact (an options table naming the retired `[sources] exclude` key, and a DoD line claiming this repo writes `.evidence/` — it writes both). **2026-08-20: W-46 and W-48 closed** — both detail files moved to `archive/open/`, both index rows deleted. **2026-08-20:** `W-45-source-exclusion.md` repointed off the retired `[sources] dirs` key onto `.fux/sources/dirs`; the finding itself is unchanged. **2026-08-19 (W-54, the sources rewrite, released as `0.33.0`):** `W-54-sources-rewrite.md` archived; W-44 narrowed to the half decision 10 still gates. **2026-08-19: closing changed on Arpit's instruction** — a closed item's detail file now moves to `archive/open/` instead of being deleted; the index row is still deleted, so the queue's length keeps its meaning. W-30 and W-32 were the first two. One file per open `W-nn`; holds open work only, which is the property that makes `OPEN-WORK.md` trustworthy. Closed items leave no tombstone here. **Phase 0:** W-42 and W-43 deleted on close; **W-44 added**; W-32 and W-33 amended (W-32's `git mv` adoption step was impossible as written). **2026-08-20 (queue review): W-61's status corrected on contact** — it read `held — Arpit's word required`, but the hold was lifted the same day (`d98874d`) and the pre-registration it required is committed, so the row and the detail file's `Blocked by:` line both said the opposite of the repo. R5/R6 also read *running*; they are pre-registered and **unrun**. |
| [`OPEN-WORK.md`](OPEN-WORK.md) | **Any work item or prediction changes state** | 2026-08-21 | **2026-08-21 (later): the `## Predictions` section deleted** — it recapped R1–R7, duplicating `IMPLEMENTATION.md`'s milestone table and stale since R7 closed unmeasured. **Every live item re-derived against rule 4 (not read as evidence)** before this change: W-26, W-38, W-44, W-52, W-57, W-59, W-61 each still carry an unchecked DoD box, so none closed and no row was removed. **2026-08-21 (PRIORITY P3): R7 closed unmeasured**, on Arpit's call — no pre-registration, no fux-lab run; a preliminary check against this repo's own committed index extrapolates ~2× over the 250 MB@100k budget, but against today's unbuilt-encoding placeholder format, not the design the threshold was sized for. "Predictions still unmeasured" table emptied and retitled; W-26's row updated with what it inherits. **2026-08-20 (W-60): closed.** **2026-08-20 (M5): W-25 closed**, W-26 becomes next-and-unblocked, **+W-61**. R5/R6 reassigned from W-25 to W-61. W-59 marked unblocked-but-held. A **stale W-56 row** reappeared from a concurrent session after the item was archived and was removed again. **2026-08-20 (later): W-27 row deleted** — ratified closed by Arpit, not built through W-26. **2026-08-20 (W-56): W-56 closed**; W-59 unblocked (the lab is back); W-57 re-scoped and moved to the inbox. The predictions table now records that the lab is runnable again **but every prior baseline is gone**, and that **Arpit has held prediction runs** until he says otherwise. **2026-08-20 (later): the inbox is two** — **+W-60** (refer-plane fetch cache, `arpit` lane, compare doc awaiting verdict; grouped under ADR-GRAPH/ADR-REFER/ADR-RECORD alongside W-58, which it does not reopen). **2026-08-20 (W-45+W-55): both closed**, and their group heading removed. **2026-08-20 (M4): W-24 closed**, W-25 becomes next-and-unblocked, **+W-58** to the inbox and **+W-59** behind W-56. R4 reassigned from W-24 to W-59, because the plane is built and only the gate is outstanding. **2026-08-20 (later): W-23 closed** (M3 shipped); **+W-56** (both sibling environments missing — the lab the standing obligation says never goes, and the playground with 50 graded goldens and no remote) and **+W-57** (the graph lane acceptance measurement, blocked on it). The predictions table now says plainly that **all four remaining predictions run in a lab that does not exist**. **2026-08-20 (later): the inbox is no longer empty** — W-45 moved to the `arpit` lane with its compare doc written, and **W-55 filed** (no file-type filter; 14 % of the index is not prose). Both filed today, so neither is aging. **2026-08-20 (later): W-46 and W-48 closed and deleted** — the two defect rows and their ADR-ASK/FIND/ANSWER group heading are gone; the queue is eight rows, of which four are parked and three blocked upstream. **2026-08-20: reconciled against the code, nothing removable.** All ten rows re-derived (rule 3) and every one is genuinely pending — W-46's `None` guard is still absent from `hybrid.py`, W-48's two `--json` gaps are still in `query/__init__.py`, `refer/` is still the 7-line stub, no graph verb exists, and no live-vs-archived pre-registration exists so W-44/W-52 stay parked. **Two stale facts fixed:** the footer said `v0.32.0` is on PyPI (it is `0.33.0`), and W-45 was grouped under ADR-CONFIG and led with the retired `[sources] dirs` key — regrouped under **ADR-DIR-LIST**, which is the record its change will now have to update. **2026-08-19 (W-54, the sources rewrite, released as `0.33.0`):** **W-54 closed and deleted**, and its five merged defects with it. W-45's schema question now has a file to hang on. **2026-08-19: W-30 closed and deleted** — the inbox is four. **2026-08-18: restructured — items first, grouped by the record each one will have to update; the rules and standing obligations moved to the foot.** One line per item with a `agent`/`arpit` lane tag, replacing the two lane tables. Earlier: **2026-08-18: six explicit rules + split into two concurrent lanes** (A agent-executable · B needs Arpit), ordered independently; reconcile-don't-read made explicit. Earlier: **Restructured 2026-08-12 into an index**: one line per open item, detail in [`open/`](open/README.md), **every DONE item purged** (W-20/21/40/41 rows deleted — their record is the ADR + WORKLOG). **2026-08-12, Phase 0:** W-42 and W-43 closed (rows + detail files deleted); **W-44 opened** (retired-content signalling); **R2 moved to 3/3 PASS** and left the unmeasured-predictions table. Open set is W-22 (next) · W-23…W-27 · W-30/31/32/33/44 (human) · W-38 parked. **2026-08-12: Blocked-on-Arpit inbox block added to the header + `filed` column on every row** (5-day rule: an aging OPEN·human row is named, with its age, first). **2026-08-20 (queue review): W-61's status corrected on contact** — it read `held — Arpit's word required`, but the hold was lifted the same day (`d98874d`) and the pre-registration it required is committed, so the row and the detail file's `Blocked by:` line both said the opposite of the repo. R5/R6 also read *running*; they are pre-registered and **unrun**. |
| [`paper/the-fux-index-paper.md`](paper/the-fux-index-paper.md) | Architecture changes; a P-prediction gets measured (M7 updates §5–6 to measured) | 2026-08-18 | **Moved `docs/paper/` → `work/paper/` 2026-08-18** (it is mid-rewrite: §4–§6 are rewritten to measured at M6). Prior: Architecture of record + figures |
| [`INTERVIEW.md`](INTERVIEW.md) | Direction/strategy/major decision changes — **kept current during the session** | 2026-08-20 | **2026-08-20 (later): §1 rewritten — the agent lane is empty.** A table of the seven things that shipped, with a **measured?** column that says *no* four times, and the standing instruction not to promote ADR-REFER or ADR-MAINTENANCE out of ⏳ proposed without their gates. §2 makes all three remaining paths Arpit's, and records that **W-26 looks available and is not**. Two lessons added: *run it, do not read it* (four defects in one session came from executing code that read correctly) and *a control is what makes a test mean something*. **2026-08-20: §1 and §2 rewritten for M3 + M4, and five lessons added.** §1 leads with the fact that two milestones landed and **neither is measured**, because the measuring environments are gone (W-56). §2 makes the next step Arpit's rather than an agent's. The new lessons: port-then-measure (the archived PPR ranks by parity); a knob that cannot work is worse than a missing knob (max_age had no input); removing randomness beats seeding it; a differential test earns its keep while being written; and an invisible convention's decay is countable (2 of 7). **2026-08-19 (W-54, the sources rewrite, released as `0.33.0`):** §1 and §2 rewritten to the current state (Lane B inbox empty, the release is the next step), and **four lessons added** — chief among them *a law enforced over the wrong corpus is not enforced* and *fix the shape, not the check*. **2026-08-18: four maintained sections added** (state of play · in flight + next step · standing constraints · lessons learned) ahead of the succession history. Earlier: Succession record; the reset is a mandatory entry. **+Phase 0 entry (2026-08-12)**: R2 3/3 PASS; an unindexed source is not a ranking failure; the retired-content finding was filed (W-44), not solved; a recorded rank is a snapshot of a corpus. **+ADR-DOTFUX entry** (the ignore-rule failure mode; the opaque-config-table line to hold) |
| [`WORKLOG.md`](WORKLOG.md) | **Every substantive exchange** (append) | 2026-08-21 | **2026-08-21: two entries appended** — P5 and P6, plus the session-lock hook fix, plus the pre-existing ADR-CACHE entry from a prior Cowork session, reconciled across four commits. **2026-08-20 (later): second entry appended** — W-45+W-55, W-56, M5 and W-60 closed in five commits; the hold on prediction runs honoured and named as the reason two records stay ⏳ proposed. **2026-08-20 (latest): entry appended** — the refer-plane fetch-cache idea researched and filed as W-60, compare doc written, proposed verdict F. Earlier **2026-08-20: entry appended** — W-45 and W-55 asked for again and found **already closed** in `9ba02b1`; verified by re-derivation (archive listing, IMPLEMENTATION row, 719 passing) rather than rebuilt, and the entry says so plainly. Earlier **2026-08-20: entry appended** — W-46, W-48, M3 and M4 core closed in four commits; W-55/56/57/58/59 filed; the three things deliberately refused, and the two deviations from a written DoD, both stated in the entry. **2026-08-18: header rewritten** — chat-only sessions count, the `Cost:` line is mandatory, past entries are never edited (one stated exception: a repo-wide mechanical rename). Earlier: Rolling session handoff |
| [`../docs/GLOSSARY.md`](../docs/GLOSSARY.md) | New recurring term, or a term changes meaning | 2026-08-21 | **2026-08-21 (ADR-CACHE): +`TTL fetch cache` and +`` `cached` (the fourth verdict)`` — two v0.30 recurring terms that had NO entry**, a month after W-60 shipped them: `TTL` was live in a policy field, a verdict label, a compare doc and an ADR without ever being defined. The `ARC` entry, which predated ADR-CACHE and cited only the compare doc, now explains the ghost lists and the scan-resistance argument, says it is in-memory and per-process as against the TTL store's on-disk, and cites [ADR-CACHE](../docs/adr/0035_cache.md). **2026-08-19: ingest-mode entries re-grounded** — three citations pointed at `archive/adr/0001_ingest-mode-naming.md`, an archived doc backing a live claim; repointed to [ADR-EXTRACTED](../docs/adr/0016_extracted-mode.md)/[ADR-ENRICHED](../docs/adr/0017_enriched-mode.md), and *extracted mode*'s "⏳ name proposed" is now ratified. **+6 M2 terms** (`accelerator (T1)`, `block / block line`, `differential law`, `mx (block max)`, `offset table`, `skipping`). **+4 terms** (`.fux directory`, `derived plane`, `middleware (URL)`, `url source`) — paying ADR-URL-INGEST's recorded debt; fixed a stale "numbering continues at 0016" line. Rewritten for v0.30 (index vs db, ledger, keyspace, wire/runtime, refer/snapshot, inferred/enriched, pruning, ARC); archived v0.26 terms marked not-current. **+3 terms** (`playground`, `golden query`, `known failure / xfail`) from SETUP-PLAYGROUND. |
| [`compare/README.md`](compare/README.md) | A compare doc opens, closes, changes status | 2026-08-21 | **2026-08-21: meta-privacy row updated** — reopened 2026-08-21 for materialise-first display; both halves ✅ accepted. **2026-08-20 (R5): +hook-at-scale row** — the twelfth fork, ⏳ awaiting Arpit. **2026-08-20 (later): +maintenance-trigger row** — the eleventh fork, ✅ accepted (git hooks + delta ingest, per W-25). **2026-08-20 (earlier): +refer-fetch-cache row** — the tenth fork, ⏳ awaiting Arpit. **2026-08-20: +source-exclusion row** — the ninth fork, ⏳ awaiting Arpit. Seven v0.30 forks; verdict-first convention |
| [`compare/*.compare.md`](compare/) | New evidence, verdict change, reopen-trigger fires | 2026-08-21 | **2026-08-21: `meta-privacy.compare.md` reopened (PRIORITY P5)** — option D (materialise first, then index) added; both forks PRIORITY.md reserved for Arpit ruled directly (L2: outside entirely, citing ADR-CACHE's identical precedent; delta path: force a re-fetch, though grounding found `_reusable()` already never carries a hashed record forward without one, so the ruling changed nothing live); two of three sub-questions ruled (salt: not built; `code`: kept); the third (`loc`/`id`) resolved by architecture — `loc` is the refer plane's only fetch address and is separately committed in plaintext already, so hashing it costs function for no privacy gained. New reopen-trigger naming what would revisit each of the five. **2026-08-20 (R5): +`hook-at-scale.compare.md`** — opened by R5's failure, not by an idea. Four options (documented ceiling · **defer, proposed** · pre-push · make the corpus-wide passes incremental), each judged against the measured attribution rather than against intuition. **§4 is the part that decides it**: two O(corpus) passes are the whole 44 s, so even 10× faster misses the 1 s bound by 4.5× — only removing the work from the commit path reaches it, which makes this an architectural call and not an optimisation. It explicitly does **not** reopen `maintenance-trigger.compare.md`: hooks are still the right trigger; what is in question is what the hook *does when it fires*. **2026-08-20 (later): +`maintenance-trigger.compare.md`** — the trigger mechanism for keeping the committed index and the derived graph plane fresh; ✅ accepted (git hooks + delta ingest, per W-25); one open sub-decision (whether the hook also calls `fux build`) flagged for `ADR-MAINTENANCE`. **2026-08-20 (earlier): `source-exclusion` and `file-type-filter` are both DECIDED and BUILT** — their verdict blocks now record Arpit's call rather than a proposal. They stay live (not archived): a compare doc is the reasoning behind a decision and outlives the item that carried it. **2026-08-20: +`source-exclusion.compare.md`** — W-45's fork, written from a fresh measurement of the committed index rather than from the item's prior write-up. Options C (`.gitignore`) and D (the dot-prefix convention) are **eliminated by measurement**: every contaminating file is git-*tracked*, and the dot-prefix convention was followed by 2 of 7 filed runs and dropped by the other 5. Proposed verdict is an exclusion **entry**, not the attribute ADR-DIR-LIST anticipated — the attribute grammar describes properties of the thing on the line, and values carry no whitespace or quoting, so two exclusions would need a comma sub-grammar the format has never had. Surfaced a larger unrecorded cause underneath it, filed as **W-55**. **2026-08-19: `ingest-mode-naming` DECIDED** — ratified by Arpit, verdict block rewritten with its reopen trigger. `ingest-mode-naming` ⏳ (ADR-INGEST) · `pruning-criterion` ❌ **falsified** by P1-RERUN · `storage-architecture` carries a **size amendment** |
| [`../docs/adr/`](../docs/adr/) | A feature completes (one ADR per feature); **any behaviour change, in the same change** | 2026-08-20 | **2026-08-20 (R5+R6): ADR-MAINTENANCE amended and deliberately still `proposed`** — its consequences said *"R5 AND R6 ARE NOT MEASURED"*; they now say what the measurements found, and both veto conditions name the run and its reproduce command instead of saying *held*. **Veto 1 has fired**, and the record does not absorb it: what replaces the automatic hook is an open fork ([`hook-at-scale.compare.md`](compare/hook-at-scale.compare.md)), so acceptance waits on a decision rather than on another measurement. **2026-08-20 (R4): ADR-REFER's veto condition 1 becomes checkable** — it read *"held pending Arpit's word"* and now names the run and its reproduce command, with the serial-fetch boundary written into the check itself. Condition 2 (the budget sweep) is explicitly still unmeasured, and the record stays **`proposed`**: one gate passing is not the whole DoD, and the record's own sentence — *an accepted record for an unmeasured plane is how an unproven thing becomes load-bearing* — does not retire on a partial. ADR-REFER and ADR-MAINTENANCE each take their harness directory in the ownership table. **2026-08-20 (delta ingest): ADR-INGEST's veto condition fired and was honoured** — a filed [cost profile](regression/2026-08-20-ingest-cost-profile/report.md) puts **92 % of a full ingest in `_fuxvec_code`**, so decision 1 splits: edges re-resolve every run, extraction is **carried forward** for an unchanged `sha` (decision **1b**), gated on three conditions together. The record predicted this fix by name in its own Alternatives — *"the natural answer if the veto condition below ever fires"* — and the veto text, the check block, the diagram, the §1 capture and the flag table in **ADR-CLI** all move with it. Two consequences are recorded rather than hidden: **term-collision detection is now complete only under `--full`**, and a newly available embedding bundle does not retro-fit `code`. The §2 capture is left **verbatim and annotated**, not rewritten to match today's output. **2026-08-20 (W-60): ADR-REFER amended in the change that built it** — decisions 5a-5c (the TTL cache, its separation from ARC, and wall clock confined to it) and decision 6 becomes four-state with `cached`. Two veto conditions added, one of them an honest gap: **a permission revoked at the source is not observed until the entry expires, and nothing detects it.** Decision 4 and W-58 explicitly untouched, so a later reader cannot conflate a local "when we last looked" with a committed "when it was ingested". **2026-08-20 (M5): +ADR-MAINTENANCE (0033, ⏳ proposed, built: partial)** — hooks, the index merge driver, and L5 moved into `write_index`. Proposed rather than accepted **because R5 and R6 have not run**. It answers the sub-decision `maintenance-trigger.compare.md` left open (the hook does refresh the derived plane, and it comes free with `fux ingest`) and cites that verdict rather than re-arguing it. Also amends **ADR-CLI** (a sixth group, `hooks`, and decision 1c — the first verb that writes outside `.fux/`) and **ADR-INDEX-LIFECYCLE** (L5 at write time; the index now merges through a driver). **2026-08-20 (W-45+W-55): +ADR-TYPES (0032, accepted)**, and **ADR-DIR-LIST gains decisions 2a-2c** (the `!` exclusion entry, order-independence and no un-exclude, and `*` not crossing a `/`). ADR-DIR-LIST's own anticipation was **corrected rather than fulfilled**: it expected an exclusion attribute, and the verdict took an entry. ADR-CONFIG and ADR-DOTFUX each gain one consequence — a third path constant with **no `fux.toml` key**, and a fourth file `fux setup` writes. **2026-08-20 (M4): +ADR-REFER (0031, ⏳ proposed, built: partial)** — the refer plane core. Proposed rather than accepted **because R4 has not run**, and an accepted record for an unmeasured plane is how an unproven thing becomes load-bearing. Two decisions were forced by facts found while building: `max_age_seconds` is **refused** (no ingest time exists in the record — W-58) and the assembler has a **floor** (greedy score-per-byte is systematically biased toward short passages). Also amends **ADR-FETCHER**: `_sanitize` promoted to `sanitize` and now shared with verify-time, because a one-character divergence between two copies would mark every URL document permanently stale. **2026-08-20 (later): +ADR-GRAPH (0030, accepted)** — M3. Also amends **ADR-CLI** (a fifth verb group, `graph`; decision 1b; eleven flat verbs is still not a tree) and **ADR-T1-ACCELERATOR** (`fux build` is a two-lane build; `DETERMINISTIC_FILES` gains `graph.json`). ADR-GRAPH records two things as decisions rather than implementation: communities are **unseeded** (the randomness is removed, not pinned — asserted by parsing the module AST) and **derived, not committed** (a label is global, so committing it turns a one-file commit into a corpus-wide diff). It also records a **deliberate correction to the port** — the PPR walk is lazy, because the archived walk truncated at 3 iterations ranks by parity. **2026-08-20: three records amended for the two closed defects** — ADR-CLI's known-defect note on `ask --hybrid` becomes a fixed-in reference and its "we now owe a regression test" consequence is discharged; ADR-ASK decision 6 and its flag table now say `--explain` reads the same in text and `--json`; ADR-ANSWER decision 7, its worked no-match example and its consequences now carry `"source"` on **both** `--json` branches — which also makes its own veto check true for the first time. **2026-08-19 (W-54, the sources rewrite, released as `0.33.0`):** twelve records amended across five commits — **ADR-INDEX-LIFECYCLE gains decision 9** (a value-encoding change bumps neither `_format` nor `analyzer`: the property set is unchanged, `title_h` is not a term, and the old shape is already refused per record with the migration named). ADR-URL-LIST's grammar is built and it now owns the parser and the writer; ADR-DIR-LIST decision 10 is split file-from-signal; ADR-CONFIG has no required keys and three retired keys; ADR-DOTFUX decision 6 is the two-moment table; ADR-HTTP-FETCHER decision 2 **was wrong and says so** — `fux setup` writes the fetcher, not `ensure_layout`; ADR-CLI's mental model is four groups because the *count* was never the model. **2026-08-19 (later): +six single-file companion records, all ⏳ proposed, none owning a module** — ADR-CACHEDIR-TAG (0024), ADR-DOCS-TABLE (0025), ADR-CODES-TABLE (0026), ADR-RUNTIME-MANIFEST (0027), ADR-RUNTIME-STAMP (0028), ADR-RUNTIME-STATS (0029), each documenting one file `derive/build.py`/`dense.py`/`store/fuxdir.py` already generates under [ADR-T1-ACCELERATOR](../docs/adr/0011_accelerator.md)'s and [ADR-DOTFUX](../docs/adr/0003_fux-directory.md)'s existing ownership — requested by Arpit, after being told this fragments a single already-accepted feature into six records and explicitly confirming he wanted that anyway. ADR-POSTINGS (0013) now cites all six from its Reference section. **2026-08-19: the register gains a `built` column** — `status` is whether Arpit ratified the decision, `built` is whether the engine does it, and four records were accepted-and-unbuilt with nothing in the index saying so. A `no` or `partial` row must be claimed by an open item. Earlier: **+ADR-DIR-LIST (0023, accepted, unbuilt); ADR-ARCHIVED-SIGNAL (0022) superseded and archived hours after it was written** — source directories move out of `fux.toml` into `.fux/sources/dirs`, sharing ADR-URL-LIST's grammar **by reference, not restated**, with one attribute: `archived` (absent means false). **`archived` is now declared, never derived** — the path heuristic was exact for this repo, where `test_archive_law.py` enforces one archive, and a silent convention for any consumer whose retired docs sit in `old/` or `deprecated/`. W-53 filed for the build; W-45 gains an obvious home (an exclusion attribute on a directory line) and is **not** decided. The superseded record's own content — annotate never reorder, the per-record property, all three verbs agreeing, `df` excluded — is carried in unchanged — W-44 closed as option **B**, annotate never reorder: a record under `archive/` carries `archived: true`, every verb surfaces it, the ranking is byte-identical, and it needs **no config key** because the one-archive law makes the test complete. The `df` half is explicitly excluded and filed as **W-52** (26.6% of records archived; **42.1% of live terms carry an inflated `df`**), behind a pre-registration. **The Arpit inbox is now empty.** **ADR-URL-LIST decisions 12–13** — a fux-written line states every attribute explicitly (no implicit state in a generated file); reader lenient, writer strict, and a line missing an attribute is evidence of hand-editing that `fux doctor` can report. **L5 is untouched** — it now means what a *missing* attribute means, which a correct file never exercises. **W-33 closed** — the numbering convention confirmed (`docs/adr/` live, starts at 0001; `archive/` is archived), and four milestone items swept from reserving numbers to reserving **names** (`ADR-GRAPH`, `ADR-REFER`, `ADR-MAINTENANCE`, `ADR-T2-SEGMENTS`) — they had been holding `0006`–`0009`, which accepted records already own. **W-31 ratified** — ADR-DOTFUX, ADR-URL-INGEST and ADR-CONFIG accepted by Arpit as-is, closing the item; `.fux/README.md` stays generated at ingest time. **(eleventh change): the attribute set is defined and closed** — ADR-URL-LIST §The attribute set names both attributes (`fetch`, `meta`) with their values, defaults, defining record, and **whether each changes committed bytes** (`fetch` no, it routes; `meta` yes). Two classes are excluded on principle — **fetcher tunables** (`wait=`, `port=`) belong to `[sources.url.config]`, and putting them here is the adapter cap breached through the back door; **content overrides** (`title=`) because the document owns its content. Three candidates the grammar *could* hold are named and explicitly not decided (`snapshot`, `tag`, `max_age`), each with the record that would own it. Adding an attribute is now a change to ADR-URL-LIST, which is what makes the unknown-key error safe to be strict about. **ADR-CDP-FETCHER and ADR-HTTP-FETCHER gain full example sets**: declaration, tuning, the contract, a run, and the resulting record — including that a browser-fetched record is still `mode: extracted`, because **rendering is not enrichment**; and, for HTTP, the whole generated fetcher and the shell-page failure decision 3 deliberately refuses to paper over (`wlen: 3` is the signal a human reads once). **(tenth change): `middleware` is renamed `fetcher` throughout, and +3 records** — **ADR-FETCHER (0019)**, **ADR-CDP-FETCHER (0020)**, **ADR-HTTP-FETCHER (0021)**, all accepted. The name was wrong because middleware *composes* and nothing here does — one file, one `fetch(url)`, exactly one running per URL — and `adapter` was unavailable because ADR-RECORD already spends it on `src`. ADR-FETCHER takes the contract (and `ingest/urlsrc.py`) out of ADR-URL-INGEST decisions 1/2/7, which now point at it rather than paraphrase it. **ADR-HTTP-FETCHER decides W-50's core question**: a plain stdlib GET is the default, **generated write-if-missing into the consumer's repo** rather than placed in `src/`, so core keeps zero network lines — and there is **no automatic escalation** to the browser, ever. Code, tests, `fux.toml`, `.fux/` and every live doc renamed; `[sources.url] middleware` is now a retired key that errors with instructions. **Found writing it: URL ingestion has never worked out of the box** — `DEFAULT_FETCHER` names a file fux neither generates nor ships, and two docstrings said otherwise (**W-51**). **(ninth change): ADR-URL-LIST gains decisions 7–11** — the per-URL attribute grammar (`<url> key=value …`), **decided and unbuilt** on Arpit's instruction: one form only, unknown key is a loud error, a line beats the source-wide setting, a duplicate URL with conflicting attributes errors rather than last-wins, `fetch=` reserved for W-50. Every list valid today stays valid. **(eighth change): +ADR-URL-LIST (0018, accepted)** — the committed URL list split out of ADR-URL-INGEST decisions 5–6: one URL per line so the file merges at 5 000 entries, loader-sorted so config order can never change committed bytes, non-`http(s)` a loud `file:lineno` error, and the list is *intent* not state. It owns no `src/` component — a file format, not a module. Its §Consequences records a **silent-truncation defect found writing it** (`#` starts a comment anywhere, so a URL fragment is stripped and two anchored URLs collapse into one), filed as **W-49**; and its veto fires the moment a second fetch mechanism exists, which is **W-50**, Arpit's change request against the shipped fetch contract. **(seventh change): +ADR-EXTRACTED (0016) and +ADR-ENRICHED (0017), both accepted** — ADR-ENRICHED accepted on Arpit's instruction the same day, and both records carry a worked `.fux/index/*.jsonl` example: ADR-EXTRACTED a verbatim capture from this repo's committed index, ADR-ENRICHED a **before/after specimen** (labelled as hand-written, the only non-captured example in the record set) showing `terms` untouched, enriched vocabulary in a separate `terms_e`, an `INFERRED`-grade-6 edge in the slot `edges.py` already reserves, an `enrich.at_sha` provenance block that makes staleness detectable, no prose key, and the `_format` v1→v2 bump the shape costs. **An accepted record that owns nothing** is deliberate — it decides a contract, not a component, and acceptance is explicitly not permission to build. — the ingest-mode naming ratified by Arpit, one record per mode, closing W-30. ADR-EXTRACTED takes `ingest/extract.py` from ADR-INGEST's claim (most specific wins) and states what extraction *promises*; ADR-ENRICHED names the model-assisted mode and **fences it out of the maintenance path** — its own command, output pinned then ingested, graded below deterministic signal, prose summaries excluded under L2 — and explicitly does **not** authorize the build, which stays behind W-38's M8 gate. The register's status column was **stale for eight records**: it printed ⏳ proposed while the frontmatter on disk said accepted, contradicting this file's own prose two paragraphs below. Corrected. **2026-08-18 (sixth change): the v0.30 set is archived** — all five records to `archive/adr/`, in one change, on Arpit's instruction; `work/adr/` no longer exists. Their eight successors moved `proposed` → **accepted** and took the components, because a record cannot own the engine and be a proposal at once. Records that supersede nothing stay ⏳ proposed. **(fifth change): +five records** — **ADR-RECORD** (0010), **ADR-T1-ACCELERATOR** (0011), **ADR-RANKING** (0012), **ADR-POSTINGS** (0013), **ADR-CONFIG** (0014), all ⏳ proposed; and **all nine earlier records retrofitted with §1 Examples**. Two carry Charts: the R3 latency comparison and the BM25F saturation curve, the latter computed from the code's own constants. **(fourth change): +three verb records at Arpit's chosen numbers** — **ADR-ASK** (0004), **ADR-FIND** (0005), **ADR-ANSWER** (0006), all ⏳ proposed and written from a captured session; the earlier three re-indexed to 0007/0008/0009 to seat them, which moved no prose because records are cited by name. Earlier: **+four records (0003, 0007–0009), all ⏳ proposed:** **ADR-DOTFUX** (the `.fux/` layout), **ADR-INGEST** (the pipeline and what "incremental" means), **ADR-URL-INGEST** (the middleware contract), **ADR-INDEX-LIFECYCLE** (generation, update, staleness). Each names what it supersedes and **retires nothing yet** — the predecessors are unratified (W-30/W-31), so ratification comes first and each carries `Owns (on acceptance)` rather than `Owns`. **+ADR-CLI (0002)** — the command-line surface, the first record of the new set: six verbs, the boundary error contract, the `--json` shape, and every command captured verbatim; it took `src/fux/cli.py` from ADR-T1-ACCELERATOR in the ownership table. **(third change): ADR-LAWS renumbered `0013` → `0001`**, and the **ASCII twin is now collapsed** in a `<details>` block (Mermaid is what renders; the twin is the fallback, and collapsing it keeps §1 to one screen) — enforced by `test_adr_ownership.py`. The renumber, opening the new sequence — the register is now two tables, the new set in `docs/adr/` and the retiring v0.30 set in `work/adr/`. Numbers are ordinals scoped to a directory and a generation, so `0001` exists in both at once; `test_adr_ownership.py` enforces uniqueness *within* a directory. **(second move): a record's directory is now its state.** The eight v0.30 records moved to `work/adr/` as **superseded-pending** — live and citable, replacement planned; only **ADR-LAWS** stays in `docs/adr/`, which keeps the register, `TEMPLATE.md`, and is where new records are written. Earlier the same day: **2026-08-18: the register was rebuilt.** Every record now carries a `**Name:**` and is **cited by name** in prose, never by number; files renamed `000N_name.md`; `**Owns:**` + `**Laws:**` lines added; **+ADR-LAWS** (0013) — the non-negotiable constraints have exactly one home and no record restates them; an **ownership table** with an executable twin at `tests/test_adr_ownership.py`. Earlier: **+ADR-T1-ACCELERATOR (M2 accelerator + differential law, ⏳ proposed)** — R3 PASS, hybrid default-off. **ADR-RECORD's Consequences updated: R2 closed 3/3 PASS**, and its recorded Q2 rank now carries its date (a rank is a snapshot of a corpus, not a property of the engine). The numbering contradiction is now a **prepared diff** awaiting Arpit rather than an open flag — see [`handoff/v0.32.0-adr-numbering.diff`](../archive/handoff/v0.32.0-adr-numbering.diff). **Renumbered from 0001 for v0.30** (README has the policy): 0001 naming (proposed) · 0002 P1 INCONCLUSIVE · 0003 **P1 FAIL / option E** · **0004 index format & committed store, accepted** · **0010 URL source via consumer middleware, ⏳ proposed — amended in place by 0011** · **0011 `.fux/` directory layout, ⏳ proposed** (0005–0009 remain reserved for M2–M6 per OPEN-WORK DoDs). Archived v0.26 ADRs cited as "archived ADR-NNNN". **+SETUP-PLAYGROUND** (playground leaves the repo, accepted). README now flags the unresolved 0012-vs-0016 numbering contradiction with CLAUDE.md. Broken relative link to the archived ADR path fixed (`../` → `../../`). |
| [`proposals/`](proposals/README.md) | An idea is parked, graduates, or is rejected | 2026-08-20 | **2026-08-20: both refer-plane proposals graduated and were archived** — `caller-set-freshness-policy` (implemented **with its central knob refused**; see W-58) and `token-budget-retrieval` (implemented, plus a floor the proposal did not anticipate). Live successor for both is ADR-REFER, which is still ⏳ proposed, and the README says so rather than letting archival read as ratification. **`caller-set-freshness-policy` and `token-budget-retrieval` have GRADUATED** into the v0.33.0 M4 handoff §4 (their recorded trigger — "when W-24 starts"); they archive with ADR-0007, not before. | **3 filed this session** from the agent-search-API landscape research: `agent-search-landscape` (research note + evidence base), `caller-set-freshness-policy` and `token-budget-retrieval` (both graduate into the M4/W-24 handoff). Prior set unchanged: 4 filed 2026-08-09 (mcp-adapters, knowledge-ci, wavelet-self-index, query-log-pruning) + 3 carried; v0.26-era moved to archive |
| [`setup/`](setup/README.md) | **An external dependency is set up differently, or a new one appears** | 2026-08-20 | **2026-08-20 (later): both docs now describe environments that exist**, are under git, and name what could not be restored. **2026-08-20 (W-56): both setup docs gained a `Rebuilt` section, and both describe environments that now exist again.** SETUP-LAB's environment table is relabelled *existed before 2026-08-20* — every baseline and corpus in it is gone, including `rfc`, the 8 872-document corpus **R3's 27.2 ms was measured on**. SETUP-PLAYGROUND records that the corpus and harness were rebuilt and the **~50 goldens deliberately were not**, and that this invalidates W-57's named query ids. **New row.** `fux-playground` (`~/my_programs/fux-playground`, grades) and `fux-lab` (`~/my_programs/fux-lab`, measures) — neither is in this repo and neither is reconstructible from it. `SETUP-PLAYGROUND` was ADR-PLAYGROUND until 2026-08-18; `SETUP-LAB` was written the same day, the lab having gone weeks without being written up anywhere |
| [`../tests/test_setup_docs.py`](../tests/test_setup_docs.py) | **The setup-doc rules change** | 2026-08-18 | **New row.** Every doc in `work/setup/` declares `type: Setup`, a name, and a **`location` that resolves outside this repository** — if it were inside, it would not need a setup document |
| [`regression/`](regression/README.md) | Every measurement run — report + ANALYSIS + evidence, **plus `VERDICT.md` when it rules on a pre-registered threshold** | 2026-08-21 | **2026-08-21: +`2026-08-21-r7-preliminary-analysis`** — **no `VERDICT.md`, deliberately**: no pre-registration exists for R7, so there is nothing to rule against. Report + ANALYSIS + reproducible evidence only, measuring this repo's own committed index (real git-pack compression, 2.429×) to extrapolate density at 100k docs. Filed as the record of Arpit's call to close R7 without running the full pre-registered bench. **2026-08-20 (R5+R6): +`2026-08-20-r5-hook-latency` and +`2026-08-20-r6-merge-driver`** — **two runs, because one harness ruled on two predictions and a verdict is per prediction.** **R5 FAIL** at the judged 100 000 documents (44.4 s vs a 1 s bound; **passes at 1 000**), with an attribution that closes off the obvious response: two O(corpus) passes are the whole cost, so **10× faster still misses by 4.5×**. **R6 INCONCLUSIVE** — every tier matched, but tier 1 matched with the driver *removed*, so it is uninformative and the frozen table does not cover the result; the control arm was added while writing the pre-registration and justified itself on the first run. Both verdicts state what was **not** done: the hook was not tuned, and the instrument was not edited in the change that files the verdict. **2026-08-20 (R4): +`2026-08-20-refer-plane-r4`** — **R4 PASS** and the first run of the reopened prediction series. Cold k=10 p95 **1.113 s** against a 3 s bound, warm **0.016 s** against 300 ms, through the *shipped* consumer fetcher against a real loopback HTTP server ([R4-REFER](regression/2026-08-20-refer-plane-r4/VERDICT.md)). **The verdict states its own boundary**: the plane fetches serially, so cold cost is `k ×` the source's latency — the 500 ms arm breaches the bound at 5.069 s, and paper §8's "(k=10, parallel)" is not built. The warm bound tested less than it appears to, and the pre-registration said so **before** the run. Two harness defects are recorded rather than quietly fixed: a fetcher passed as a module (every fetch degraded to `unverified`, 1.9 ms, a triumphant-looking pass) and markdown served as `text/plain` to an HTML→markdown fetcher (whitespace collapsed, zero citations). ARC-vs-LRU is measured and reported **post-hoc** — the metric changed after seeing a number it reversed — so the cache compare doc's trigger stays open. **2026-08-20 (delta ingest): +`2026-08-20-ingest-cost-profile`** — a **cost measurement, not a gate**: 92 % of a full ingest is the dense embedding at both 1 k and 5 k documents, and carrying unchanged extraction forward is **22.7× / 26.4×** faster and **byte-identical**. It is the evidence ADR-INGEST's veto condition required, and it says in its own header that it is **explicitly not R5** — prediction runs are held ([W-61](open/W-61-maintenance-measurement.md)), and a cost profile is not a threshold call. **Nine entries.** **2026-08-20 (W-56): the M2 accelerator report's Reproduce block is ANNOTATED, not edited** — three of its five commands cannot run because `~/my_programs/fux-lab` and the `rfc` corpus behind R3's 27.2 ms no longer exist. The numbers stand; what is withdrawn is the claim that they can be regenerated. Annotated rather than rewritten because editing a filed run's evidence is forbidden: a measurement is superseded by a newer measurement, never by a rewrite.  **+`2026-08-19-w54`** — a *surface capture*: the whole URL path, which this repo's corpus never touches, built from nothing by `fux setup` and run offline. Closes W-47 (hashed meta ingests **and builds**, exit 0), W-49 (a fragment survives; two fragment-differing URLs are two records), W-51 and W-53. The differential holds over a corpus containing hashed records, which the harness had never seen. Its fixture supersedes `2026-08-18-ingest-and-index`'s, which reproduces the pre-W-54 surface and was **not** edited. **+`2026-08-18-query-verbs`** — every flag of the three verbs, both output modes, the empty case, and the differential law demonstrated byte-identical; found **W-48**. **+`2026-08-18-ingest-and-index`** — the `.fux/` tree, a committed record, the runtime manifest, determinism/change/deletion, and the URL-middleware contract via a no-network fixture; it found **W-47**. **+`2026-08-18-cli-surface`** — a *surface capture*, not a measurement: every `fux` command and its verbatim output, behind ADR-CLI, with a reproduce fixture. It found the W-46 hybrid crash. Prior: four runs filed. **+`2026-08-12-m2-accelerator`** — **R3 PASS** (worst-case p95 27.2 ms), the differential law over 5 536 comparisons, and the hybrid net-−6 measurement that set its default. **+`2026-08-12-r2-close`** — R2 closes **3/3 PASS** on this repo's own corpus (report + ANALYSIS + before/after/probe evidence); its post-hoc finding opened [W-44](open/W-44-archived-content-signalling.md). Prior: `2026-08-09-pruning-eval` (INCONCLUSIVE), `2026-08-09-pruning-rerun` (**FAIL**) |
| [`../tools/pruning-eval/`](../tools/pruning-eval/README.md) | The gate's definitions change (they must not) or a selector is ported | 2026-08-09 | Two **frozen** pre-registrations (v1, v2), both committed before their first number; 50 tests; corpus acquisition + 3 diagnostics |
| [`DOC-REGISTRY.md`](DOC-REGISTRY.md) | **A doc is added, retired, or its trigger changes** | 2026-08-18 | **New row — this file tracks itself.** It is maintained like anything else here, and a registry that omits itself is exactly the blind spot it exists to remove. **2026-08-18: restated as live-documents-only** and five tombstone rows deleted (the retired `handoff/` row, the archive maps, the demo-corpus row, and a duplicate `INTERVIEW.md` row) |
| [`README.md`](README.md) | The `work/` contract changes — a file is added, retired, or its rule changes | 2026-08-18 | **New row.** The directory's own how-to-use doc; also the home of the three cross-cutting rules (archive-is-not-evidence, concurrent-session hazard, ground-truth-over-prose) |
| [`governance.md`](governance.md) | A steering/process file is added, retired, moves, or its enforcement changes | 2026-08-21 | **New row (filed by a concurrent Cowork session; indexed here to close the registry contract).** Maps ~90 governance files across `CLAUDE.md`/`docs/`/`work/`/`tests/` — what each governs, who reads it, what enforces it, and where the weight comes from. Not reviewed for accuracy in this change |
| [`IMPLEMENTATION.md`](IMPLEMENTATION.md) | **A milestone or release lands** | 2026-08-21 | **2026-08-21 (PRIORITY P6): +a row** — the refer plane wired into `answer`, landed `9f8366e`, ADR-REFER and ADR-ANSWER both accepted. **2026-08-21 (PRIORITY P5): +a row** — materialise-first display for `hashed` records, landed `1ba9be1`, with the five rulings and the two unrelated same-session commits named so they are not mistaken for part of it. **2026-08-20 (delta ingest): +a row under §Defects closed outside a milestone** — the cost profile, decision 1b, and the two narrowed guarantees it buys. **2026-08-20 (W-60): +a row** — verdict F built, with both named hazards honoured and the ACL-staleness gap recorded rather than glossed. **2026-08-20 (M5): +M5 row** — built, both gates unrun, with the `post-commit`-not-`pre-commit` argument and the add/add limitation recorded in the row. **2026-08-20 (later): +W-27 ratified-decision row** — Arpit closed the M7 dogfood gate directly, replacing the two-week logged-use DoD with an ongoing `DOGFOOD.md`-per-version-upgrade obligation; no regression evidence backs it, by design (a human call, not a measurement). **2026-08-20 (W-56): +a row** — both measuring environments rebuilt, the three bugs found by running rather than reading, and the explicit statement that every prior baseline is unrecoverable. **2026-08-20 (W-45+W-55): +a row under §Defects closed outside a milestone** — both verdicts built as one grammar change, with the pre-build measurements and the explicit note that this repo was **not** re-ingested. **2026-08-20 (M4): +M4 row** — the refer plane core, landed and unmeasured, with the two design changes that facts forced and the defect the ARC differential caught recorded in the row. **2026-08-20 (later): +M3 row** — the graph lane, landed and unreleased, with its two unmeasured gaps named in the row rather than omitted from it. **2026-08-20: new §Defects closed outside a milestone** — W-46 and W-48 recorded there. A defect fixed on its own has no milestone row to live in, but OPEN-WORK rule 2 still forbids deleting its item until the outcome is here. **2026-08-19 (W-54, the sources rewrite, released as `0.33.0`):** milestone row added; the W-44, W-50 and W-31 ratification rows given their outcomes. **New row.** The evidence store `OPEN-WORK` reconciles against before an item may be deleted. Seeded 2026-08-18 from `git tag` + CHANGELOG + the filed runs — P1 (both), M0, M1, `.fux` layout, URL middleware, playground, R2 close, M2 |
| [`NOW.md`](NOW.md) | **Every session, at every transition** — it is overwritten, never appended | 2026-08-20 | **New row (2026-08-20).** One line naming what is in flight right now, written by the agent and read back by the `UserPromptSubmit` hook so the next session knows what the last one was doing when it stopped. **Distinct from every other tracker here**: WORKLOG is the append-only trail, INTERVIEW is the state of play, this is the single volatile line. It carries no frontmatter and is exempt from the `type` requirement for the same reason the other ALL-CAPS trackers are. |
| [`MACHINE.md`](MACHINE.md) | A surface breaks in a way the code did not cause | 2026-08-18 | **New row.** Local vs Cowork device VM vs cloud container vs CI: the device bridge cannot delete (so `git checkout`/`stash`/`reset` fail there), has no network and Python 3.10; macOS case-insensitivity hides broken links; the lab persists and is never rebuilt |
| [`../docs/adr/TEMPLATE.md`](../docs/adr/TEMPLATE.md) | The ADR convention changes | 2026-08-18 | **2026-08-18: §1 gains two optional sections** — **Examples** (real, capture-copied, two or three at most) and **Charts** (default: none; one measure per chart, every number measured or computed with its source named, same collapsed-ASCII-twin rule as the diagram). Both are *deleted* rather than left empty when they do not apply — ADR-LAWS is the worked example. Earlier: **Rewritten** into the §1-humans / §2-agents shape: one-screen §1 with a Mermaid block **and** its hand-paired ASCII twin, §2 with context · decision · consequences · alternatives · reference · **veto condition written as a condition to check** |
| [`../tests/test_regression_runs.py`](../tests/test_regression_runs.py) | **The per-run contract changes** | 2026-08-18 | **New row.** Every filed run carries a report, an ANALYSIS and its evidence — `evidence/` waived only for a run whose report declares itself a *surface capture* — and is listed in the index. Verdict docs must carry `type: Verdict`, a prediction id, a ruling, and a **pre-registration path that exists** |
| [`../tests/test_doc_registry.py`](../tests/test_doc_registry.py) | **The registry's own rules change** | 2026-08-18 | **New row.** Enforces live-documents-only: no row points into `archive/`, every row's target exists, one row per document, every row carries an ISO date, and — the inverse blind spot — **every live `work/*.md` has a row** |
| [`../tests/test_archive_law.py`](../tests/test_archive_law.py) | The one-archive rule changes | 2026-08-18 | **New row.** Fails when a directory named `archive` exists anywhere but the repo root, and when a live doc still points into a retired second archive. Exempts `archive/` itself (frozen), `work/WORKLOG.md` (append-only history) and the frozen run docs under `work/regression/` |
| [`../tests/test_adr_frontmatter.py`](../tests/test_adr_frontmatter.py) | **The frontmatter schema changes** | 2026-08-18 | **New row.** Six keys, fixed order, values containing `: ` quoted, `name`/`status` matching the body, and the title carrying both the name and the number. Written after frontmatter broke twice in one day — an unquoted colon only strict YAML rejected, and a global rename that put a name where eight titles' numbers belonged |
| [`../tests/test_adr_freshness.py`](../tests/test_adr_freshness.py) | **The ADR-currency rule changes**, or the ownership table changes | 2026-08-21 | **2026-08-21 (PRIORITY P1): now requires the OWNING record specifically** — touching some other, unrelated record no longer satisfies it (was: any record touched at all). Escape-hatch regex anchored to a whole line (`^no ADR affected$` / `[no-adr]`), not a substring match. Shares its table/owner parsing with `test_adr_ownership.py` via `tests/adr_lib.py`, so the two can no longer disagree about who owns what. Runs in CI via `pytest -q tests`, now with `fetch-depth: 0` (a shallow checkout audited zero commits — silent pass, not a check). Escape hatch is `no ADR affected` in the commit message — a claim in git history, not a silent skip. Baseline self-bootstraps from the commit that adds it; `docs/adr/RULE-SINCE` moves it forward |
| [`../scripts/adr-guard.sh`](../scripts/adr-guard.sh) | The ADR-currency rule changes | 2026-08-21 | **2026-08-21 (PRIORITY P1): moved from a `pre-commit` hook to `commit-msg`** — `pre-commit` runs before the commit message exists, so reading `.git/COMMIT_EDITMSG` there read the *previous* commit's leftover message. `commit-msg` receives the message path as `$1` and runs after it is written. Now requires the OWNING record (resolved from the ownership table + the register's record-listing table, portable grep/sed — not `awk`'s 3-arg `match()`, a gawk extension macOS's bwk `awk` lacks) — `ln -sf ../../scripts/adr-guard.sh .git/hooks/commit-msg` |
| [`../tests/adr_lib.py`](../tests/adr_lib.py) | The ownership table's shape, or how records are cited, changes | 2026-08-21 | **New row (PRIORITY P1).** The parsing both `test_adr_ownership.py` and `test_adr_freshness.py` import — `ownership_table()`, `owner_of()`, `register_names()`, `record_path_for()` — one implementation, so the freshness gate and the ownership twin cannot compute two different answers for who owns a component |
| [`../tests/test_adr_ownership.py`](../tests/test_adr_ownership.py) | **The ownership table in `docs/adr/README.md` changes — same change, always** | 2026-08-21 | **2026-08-21 (PRIORITY P1): now also checks each record's own `**Owns:**` line** (`test_owns_line_is_a_subset_of_the_register` and `test_no_path_is_claimed_by_two_records_owns_lines`, in `test_adr_owns_consistency.py`) — an unqualified `Owns:` line is an active claim, checked against the register; an `Owns (on acceptance/build):` line is a future claim, not checked until the record's status flips. Shares its table/name parsing with `test_adr_freshness.py` via `tests/adr_lib.py`. Otherwise unchanged: fails when a `src/`/`tools/` component is claimed by no record, when a claim points at a missing file or a closed `W-nn`, when a record's declared name disagrees with the register, or when a record restates a law |
| `../.github/` | Required checks, release path change | 2026-08-10 | Workflow YAML already targeted the new tree; restored `scripts/ai-review.sh` + `apply-branch-protection.sh`, which the live CI referenced but were missing from the tree since the v0.26 archive move |
| [`../DOGFOOD.md`](../DOGFOOD.md) | Every fux version upgrade | 2026-08-20 | **New row.** Created closing W-27 (Arpit ratified 2026-08-20, replacing the retired two-week logged-use gate). Standing obligation: refresh on every version bump — currently `0.33.0`, log empty pending the next bump |

## How agents use this file

1. **At task end:** scan the trigger column; if your change fired a
   trigger, update that doc and bump its row. CLAUDE.md binds you to this.
2. **Adding a doc:** new maintained doc → new row, in the same change that
   creates it. Archiving a doc → retire its row, same change.
3. **No ⚠ rows remain** — the two stale rows (CLAUDE.md, GLOSSARY.md) were
   cleared by W-03 on 2026-08-09; GLOSSARY was rewritten and CLAUDE.md's
   rewrite was proposed for review. **Correction (2026-08-10):** CLAUDE.md's
   row previously said the rewrite was "adopted" — the file itself still
   carries a "PROPOSED — not in force" header, so it has not been; fixed
   above. Adoption is Arpit's call and unchanged by this session.
