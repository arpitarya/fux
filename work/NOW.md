---
type: Pointer
description: "One line: the current state and the immediate next step. Overwritten every session."
---

⚠ 2026-09-18 Claude Code: **the queue's inbox grew a recap again, and the guard built for the first one had never run.** Arpit named the two paragraphs under *Blocked on Arpit* directly — the *"first three are a HAND"* recap and the *"W-196 and W-197 were both ruled"* one. Both **deleted**; the inbox is its table and closes, and no row's text, ball or age moved. 🔴 **The finding is strike two, not the prose:** `scripts/check-open-work-inbox.py` (written 2026-09-15 for the same class) **detects this case correctly and never ran** — `.claude/settings.json` matched `Write|Edit|MultiEdit` and the prose arrived through `Bash`, which is how a session edits files whenever it is told to prefer the shell. **The gate, in the same change** ([SR-WORK-SESSION](../records/0060_WORK-session.md) decision 13): `Bash` added to the matcher, and **`test_the_inbox_carries_only_its_table`** in `tests/test_open_work_is_not_stale.py` — it **imports** the script rather than reimplementing it, so hook and suite cannot drift, and it runs where the writing tool cannot matter. Proved to bite on a reintroduced paragraph. **Record first (L0):** rule 45a's scope was wrong — it spoke only of the *emptied* table while the script always checked the populated one too; it now reads *"the inbox section carries its table and nothing else — populated or empty"*, `owns:` and `content_sha` re-stamped. ⚠ **One same-class hole is named, not fixed:** `session-lock.sh` is PreToolUse on the edit tools only, so a `sed` edit takes no lock. **4 935 unit tests green.** → **Next: nothing here changes the queue — 🟢 W-194 is buildable now, with W-198, W-199 and W-200 behind it; W-136 / W-175 / W-168 stay Arpit's.**
