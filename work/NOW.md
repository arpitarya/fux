✓ **W-82 IS FIVE-SIXTHS BUILT (2026-08-26).** §3.1 the URL health report · §3.2 the detector · §3.3 parallel fetch + the cap · §3.4 the changed/unchanged line · §3.6 the agent surface. **Nine records amended in the same change** (ADR-DOTFUX, ADR-INGEST, ADR-REFER, ADR-MAINTENANCE, ADR-ANSWER, ADR-ASK, ADR-AGENT-POLICY, ADR-FETCHER, ADR-CONFIG). **1 433 unit tests green**, up from 1 335.

→ **THE LOOP IS CLOSED, and that is the headline.** The refer plane used to compute `fetched_sha != indexed_sha`, render `stale`, and **throw the fact away** — so a changed URL kept its old terms, stopped ranking, was never cited, and nothing noticed. It now records the doc id in the dirty list, where `ingest.run(only_urls=…)` consumes it. **Recall, not correctness** — a changed document could never be mis-answered, only fail to surface.

⚠ **VERIFICATION, STATED HONESTLY — read this before claiming anything is green.** `tests/` is 1 433 green; the 2 failures are a known artefact of the harness shim below (`fux doctor` correctly reporting `3.10 < 3.11`). **`tests_e2e/` IS UNVERIFIED.** It spawns the real CLI and fails **identically — 55 failed / 11 errors — on a CLEAN TREE** in this sandbox. That means **no regression**, and it does **not** mean green. **Someone must run it on a real 3.11+ install before release.**

⚠ **How the suite ran at all, disclosed because it is a deviation.** The sandbox has only Python 3.10 and github.com is blocked, so no 3.11+ interpreter could be fetched. **`tomllib` is the only 3.11+ dependency in `src/`**, and `tomli` is that exact module backported, so a harness-only `tomllib.py` shim on `PYTHONPATH` made the suite runnable. **The shim never enters the repo.** It tests 3.10, not the supported floor.

⚠ **THREE CORRECTIONS THE BUILD MADE TO W-82's OWN PLAN**, each found by reading the code rather than the spec. **(1)** `url-state.json` may carry **no timestamp** — `refer/fetchcache.py` states the invariant ADR-REFER rests on, *wall clock lives in the TTL store and nowhere else*, so freshness is counted in **networked runs**. W-75 had specified `validated_at`/`changed_at`; shipping them would have quietly contradicted an accepted record. **(2)** **Kiro was already in `KNOWN_AGENTS`** with a `.kiro/steering/` rendering — §3.6 said *add*, the truth was *extend*. **(3)** Rung 4 is **`python -m fux.cli`**, which already works, so the ladder is complete without `src/fux/__main__.py` — **fork B is downgraded, not closed**.

⚠ **§3.0 AND §3.5 DID NOT LAND, AND NEITHER IS A CODE TASK.** §3.0 needs a real URL corpus to run `fux update` twice against; §3.5 needs `fux-playground`. Neither exists on this machine, and producing a number for either without one is the failure ADR-RS exists to prevent.

⚠ **Twenty-seven forks are still Arpit's and none was picked.** The two that bite first: **ADR-RS decision 12's scope** (§3.0 is an `informed` run made entirely of cost deltas, which the rule *as written* forbids — the next task collides with it on day one), and **fork 7, the narrowed-refresh flag name** (`--dirty` / `--stale` / `--changed`), free today and a deprecation cycle once anything ships on it.

→ **Next: §3.0.** Run `fux update` twice on a real URL corpus, count the fraction whose sanitized sha was unchanged. **≥ 80 % → the `validate` fork is yes; ≤ ~40 % → the four-function contract stands.** No new code. It must **disclose the decision-12 collision rather than self-exempt**.

⚠ **Nothing is committed.** Working tree only.
