---
type: Pointer
description: "One line: the current state and the immediate next step. Overwritten every session."
---

✓ 2026-09-15 Claude Code: **both golden question sets are in — W-189 and W-145 are CLOSED and the lane is agent-open again.** Arpit ran **prompt 2** (Codex, set 1 — 125 questions, `s1-001…s1-125`) and **prompt 3** (Claude, set 2 — 124, `s2-001…s2-124`); both [`questions/set-N.jsonl`](golden/questions/README.md) are in the tree (**staged — committing them is his**, by the process) and were verified **questions-only without reading one question** — fields `id` and `question` and nothing else, 0 duplicate ids, 0 cross-set collisions. **W-145 closed as overtaken** (the key it would have regenerated no longer exists, and set 1 is Codex-authored), so ✅ **W-87's Part B blocker is gone** and both its halves now wait on W-136 phase 5 alone. 🔴 **Next is prompt 4 — verify the eight rungs — and it needs a session that has NEVER read `questions/`; this one read the ids, so it may not run it.** ⚠ **A LAW record was amended and needs Arpit's ratification**: SR-LAW-11 decision 11 asserted the set 1 key was Claude-authored and provisional, which Codex's authoring made false — the normative sentence is verbatim, only the fact is marked overtaken. ⚠ **W-136's 🟣 2026-09-30 gate now rests on a false premise** (*Codex limit exhausted*, but Codex authored set 1 today) — only he lifts a date gate, so it stands.

✓ 2026-09-15 Cowork: **the golden benchmark is RESET and its pipeline renumbered.** Arpit deleted the key, `golden-answer/` and the 124 released questions; **the seed corpus and all eight rungs survive**. **Six prompts now, in the order he runs them** — 1 seed (documents only) · 2 Codex writes **set 1** · 3 Claude writes **set 2** · 4 corpus, blind · 5 run + **hand-off** · 6 Codex scores. 🔴 **Sets are NUMBERED, not named after their author** — the author is a fact about a set, not its identity. 🔴 **Prompt 5's hand-off is the design**: it carries what fux **answered and cited**, not only what it ranked, so **the only place a golden answer and a fux answer ever meet is a chat Arpit is sitting in**. ⚠ **Prompt 5 may not say whether an answer is right** — *correct* first appears in prompt 6's output, from Codex. ⚠ **Every id from the deleted key is orphaned** — same corpus, different instrument, so no old number may be compared with a new one. The release prompt is **retired, not deleted** (his instruction). → **He runs prompt 2, then 3; nothing in the golden lane is agent-closable until he does.**

✓ 2026-09-15 Cowork: **two golden question sets, and the answer key stops being a file.** Arpit ruled a **Claude-authored set A** over Codex's seed documents alongside **Codex's set B**, and that **no agent may reach either set's answers** — **law [L11](../records/0012_LAW-11-sealed-answer-key.md) amended** from *closed to Claude* to **Arpit's custody, no key file, pastes only, and that route is Codex's alone**. 🔴 **The carve-out is one handoff wide**: the session that authors set A writes no file, hands the answers over in the chat, and never runs a rung or returns — authorship buys no access. **W-189** (closed 2026-09-15) and **[W-190](open/W-190-question-difficulty.md)** filed; **W-190 BUILT** — [`tools/golden-difficulty/`](../tools/golden-difficulty/) makes difficulty a **count of the discriminations a question forces** (`d≤1` easy / `2` medium / `≥3` hard, unanswerable **floored at hard**) plus a per-rung distractor count, and **refuses a key path inside the repo**. ⚠ **Set A can never claim `blind`** — author and runner are one model family; that is an **assumption**, not his ruling, filed as SR-LAW-11 decision 7. ⚠ **The bands are movable only until a number is scored against them** (SR-RS 10b). ⚠ **`work/golden/golden-answer/` is Arpit's to delete** — no agent may, and it blocks nothing.

✓ 2026-09-15 Cowork: **the benchmark's arms are `A` · `B` · `B-node` — the tier-off arm is OUT** (Arpit; SR-WORK-BENCHMARK **decision 16a**). A benchmark measures what ships, and `[graph] ask_boost` defaults to `true`. Two pairs: `A|B` the version delta, `B|B-node` reader parity where **0 discordant is expected and a difference is a defect**. 🔴 **What the graph tier is WORTH stays unmeasured and is W-161's**, not a benchmark's — decision 12a's one-off attribution stands as filed history. Harness, report and template updated and re-exercised on the fixture; **still no run, still no number.**

✓ 2026-09-15 Cowork: **the Node column is wired into CAP-1/2/3/4 and the report has a reader-parity slide — BUILT, but no run has executed** (SR-WORK-BENCHMARK decision 16; W-188 stays open for the run). 🔴 **Three pair kinds, three readings:** `version` (the finding), `reader` (a defect — two readers of one index), `tier` (the graph tier doing something, W-161's unmeasured value) — conflating the last two would file a finding as a bug. New `parity.jsonl`: identical / first differing rank / **max |Δscore|**, the number `PRE-REGISTRATION-NODE`'s `log()` cell left unmeasured. ⚠ **`answer_node` has never run once** — if Node's `--band` payload has no `confidence` block, every Node row will read `answered`; check one question by hand first. ⚠ `cmd_file` had been writing CAP-4 to `answers.jsonl`, a name `.gitignore` bans tree-wide — now `answer-layer.jsonl`.

---
type: Pointer
description: "One line: the current state and the immediate next step. Overwritten every session."
---

✓ 2026-09-15 Cowork: **W-188 filed — the Node reader becomes a column in CAP-1/2/3/4, not only CAP-6** (SR-WORK-BENCHMARK decision 16; ratified, NOT built). 🔴 **Node vs Python reads the opposite way from A vs B**: 0 discordant is expected and any difference is a defect in one reader — reported, never gated (Arpit's call). ⚠ **Nearly free**: `bench.py::ask_node` already returns the ranked list beside the timing and throws it away; **CAP-4 is the one that costs a pass**, and its parity claim covers locators and the band, not just answered/declined. CAP-5 and ingest/build get **no** Node column by construction — the reader writes no index.

✓ 2026-09-15 Cowork: **all five filed benchmark reports are on the capture spine, and `2026-09-15-node-column` has a report at all for the first time** — SR-WORK-BENCHMARK decision 15 (Arpit: *“if a number exists great add it … if it doesnt say that it doesnt exist … in future capture that number”*). 🔴 **No number changed and no engine ran**: CAP-1 gained a slide with 120 filed lists on the 2026-09-13 deck and 600 on l9, the two 2026-08-28 decks had their combined slides split, and every absent capture now says which one it is and that the next run carries it. ⚠ **One defect found by doing it:** an absent `ingest`/`build` printed **0.0 s** — a fabricated zero — and prints `—` now.

✓ 2026-09-15 Cowork: **the benchmark report's spine is RULED — one slide per capture, titled `CAP-n — <name>`, each comparing arm A against arm B; CAP-7 gets none because it *is* the report.** [SR-WORK-BENCHMARK](../records/0053_WORK-benchmark.md) decision 14 amended; **[W-187](../archive/open/W-187-report-cap-slide-spine.md) filed and BUILT the same session** — twelve slides, a **CAP-1 slide that never existed**, `_fill_ranked` and the `ranked` marker, and the emitter's 13 `<h2>`s turned into findings under the template's own titles. ⚠ **Template and emitter must be committed together** — the emitter refuses on a marker mismatch (decision 11), and `fux-benchmark` is uncommitted (decision 13). 🔴 **The suites were not run** — no interpreter on the bridge can import `fux`. The three hand-written reports stay frozen.

✓ 2026-09-15 Claude Code: **the green queue worked continuously — W-184, W-183 and W-182 CLOSED, W-154 and W-140 unblocked, W-185 filed.** The session opened on an uncommitted pile (W-168 step 1 + law L11, built by the previous session and committed by neither) with `tests/test_sr_ownership.py` red on top; that landed first, and **`.fux/README.md`'s verb table turned out to have drifted three verbs from the template that generates it** — every test read the file, none read the template, and a fresh `fux setup` was handing out a short list (SR-DOTFUX 6b). **W-184:** the differential harness was dead in **three** ways — a PNG killed the query set, `bench_r3` decoded the same bytes with `errors="replace"` and **ran**, and behind the crash `compare()` still passed `archived_weight=`, removed by W-152 two days earlier. **22 144 byte-identical comparisons** now, through `run.py` itself. **W-183:** the contribution is a test a candidate can fail — `agreement` **0.4141** over 524 cited-decision contests against a chance rate of **0.0748**, inside a band frozen in its own commit beforehand. **W-182:** the stranding did **not** reproduce (0 of 104, with 67 trials inside a live runner), and a **different** race did — `git add -A` dies on `.fux/index/<shard>.jsonl.tmp`, 2 of 8 at the early-delay step.

✓ 2026-09-15 Claude Code: 🔴 **the golden ladder has NO LINKS, and three queued items were waiting on the wrong thing.** Asked to proceed with the 124 released questions rather than wait for Codex, the [anchor mechanism probe](regression/2026-09-15-anchor-mechanism/report.md) counted the ladder's edges instead of assuming: **0 anchor-bearing edges of 1 002 on all eight rungs**, every edge `supersedes`, **not one `ref`**, and zero link syntax in `work/golden/seed/`. **0 of 124 flips at every anchor weight** on four rungs including `rung-10000`, with a **positive control** that moves a target from non-candidate to top-1 — so the zero is the corpus, not the harness. **W-168 step 8, BOTH of W-161's arms** (`ask_kinds` follows `ref` alone; `fux graph --seed` returns the seed and nothing else) **and W-176 step 10 are inert on this corpus** — they need **link-bearing documents**, which no question can substitute for. A **data defect (23b), not a null**: no verdict, both pre-registrations annotated and **unmoved**.

→ **Next:** 🔴 **Arpit runs golden prompt 2 (Codex, set 1), then prompt 3 (Claude, set 2, fresh session)**, then 🔴 **his corpus call** — link the existing golden seed (breaks comparability with every filed ladder number), grow it with new linked documents, or build a sibling corpus. It decides what Codex is asked for on 2026-09-30. Then 🟢 **W-154**, the last green one. **W-179 measured Node's latency for the first time and the SPLIT is the result:** the Node reader is **flat** in corpus size (26.3 → 26.4 ms across a 10× corpus, a ninth of Python's at 1 000 documents) and **all of its growth is W-161's in-memory graph rebuild** — 36.9 ms at 100, 243.1 ms at 1 000, ~2.4 s at 10 000. ⚠ Read without the fourth arm the column says the opposite, and that wrong answer would have been believed. 🔴 **The load-bearing number is W-161's**: its tiers ship **on and unmeasured**, and one is now priced on one reader with its value still unmeasured on either. 🔴 **W-144 is in the inbox**: the frozen `b` sweep FAILED — no pre-registered value clears — **and the lever works below the range** (`content` at `b ≤ 0.3`, `main` at `b ≤ 0.15`), with nothing regressing down to `b = 0`, including the family option (b) destroyed. Three options, none taken here: picking `0.15` because a probe found it is the moving-threshold failure the rule exists to prevent. 🔴 **W-186 was the find of the session: every one of the eight golden rungs was unreadable by HEAD** — a `fux.index.v2` index, the three `[ranking]` priors W-152 removed, and the `fux.toml` key W-164 moved, each a good decision whose blast radius nobody swept. All eight are re-ingested and answering, no document moved, and the stamp gained **`engine_commit:`** because `fux 2.0.1` named two engines that cannot read each other's index. ⚠ **Nothing detects the next one** — named, deliberately not half-built. **W-181 closed W-170 with it:** the observer seam costs `+0.7 ms` p50 on this repo and `−0.3 ms` on rung-10000, and a **2 000 ms** observer adds **+63 ms** against a 50 ms cap. **W-185 closed the day it was filed** — `index/*.jsonl.tmp`, settled by a two-arm probe (**0 `git add -A` failures in 3 871 runs against 2 335 of 3 933**) rather than by argument, because 🔴 **both candidates the capture pass proposed were wrong**. ⚠ `.fux/.gitignore` is write-if-missing, so the fix reaches no existing repo and a `fux doctor` warn row is all that does — **write-if-missing hid a fix twice today**. ⚠ **Two things stated rather than closed:** *0 of 104 stranded is unreproduced, never closed* — the surviving ordering needs a delay injected inside `run_once`, which a capture pass may not add; and **W-183's screen cannot see circularity through a third variable**, so its two mitigations are mitigations, not proofs. 🔴 **W-140 row 21's stranding is *unreproduced*, never *closed*** — the surviving ordering needs a delay inside `run_once`, which a capture pass may not add, so what is left of W-140 is a call rather than work.
