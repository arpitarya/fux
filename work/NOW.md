---
type: Pointer
description: "One line: the current state and the immediate next step. Overwritten every session."
---

⚠ 2026-09-20 Claude Code: **the queue's three green rows are built and closed — W-198, W-194, W-200 — and the queue now has none left.** **W-198**: L11 permits a key at one address, `work/golden/golden-answers/`, gitignored and closed to every agent on both spellings; the record says out loud that this is a **retreat** and its §Context keeps all three arguments against what it now permits; W-197 closed with it. **W-194**: `meta = "hashed"` deleted outright, **law L5 retired** (ten live laws, twelve numbers), `fux.index` **v3 → v4**; the ACL-mismatch leak is an **accepted, documented exposure**. **W-200**: `.fux/runtime/ingest-log.jsonl` plus a `fux doctor` row — one line per consumed document naming its decoder, its fetcher and its outcome. 🔴 **Three specs were wrong about the tree and each cost something**: W-200's file path was already **L8's consent-gated answer journal** (every ingest created a consent-gated file — the e2e suite caught it, "a journal appeared with no consent of any kind"); W-194's `_reusable` gate silently took `reused_count` to **0**, turning every ingest into a full re-extract **with a byte-identical index and no error** — caught only because `test_delta.py` asserts the COUNT; and W-198's `.gitignore` pattern ended in a slash, so the singular key path was **not ignored at all** once the directory was deleted. ⚠ **Two guards cannot be amended by any Claude session** (`guard-golden-answer.sh` and W-198's own item file both trip the hook's substring TARGET check) — a sixth guard was added beside the first rather than routing around it, and the false positives are asserted as measured facts. **5 011 unit tests, 144 e2e, 36 node, green on a clean tree.** → **Next: nothing is agent-closable. Every row waits on Arpit — W-136 (4d), W-175 (4d), W-168 (4d), W-201 (2d) and W-199 (0d, newly filed: its 🟢 row contradicted its own `ball: arpit` frontmatter and three unruled decisions).**
