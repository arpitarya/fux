✓ **W-78 ruling 2 is RULED — the run-classification rule is ACCEPTED (Arpit, 2026-08-25), in the rewritten form.** Every *measured* run is **`blind`** or **`informed`** and says which; an informed run is **reclassified, never banned** — filed, listed, cited, allowed to inform the corpus, and **never compared with a blind run or used to state a delta**. That is **TREC's manual/automatic split, in force there since 1994**, and it is the one thing the wording drafted on 2026-08-24 got wrong. **That drafted wording was REFUSED** and is kept struck-through in W-78 so the ruling stays legible against what it ruled on.

✓ **Where it lives, and it is checked.** [ADR-RS](../docs/adr/0036_predictions.md) decisions **11-15** (+ a §1 human section, four new veto conditions, and the literature: TREC, CONSORT 2025 item 20a, ARRIVE 2.0 item 5, Kaufman & Rosset, Kriegeskorte, KDD 2024 source bias, doc2query, the BIG-bench canary) · [`CLAUDE.md`](../CLAUDE.md) §Conformance runs, which stays the normative home · [`work/regression/README.md`](regression/README.md) per-run contract **row 7** · [ADR-ENRICH](../docs/adr/0040_enrich.md) gains a pointer saying why its silence on authorship was deliberate. **The gate is [`tests/test_regression_runs.py`](../tests/test_regression_runs.py)** — `classification:` plus an `## Authorship` section, **baselined on the run directory's own date** because a filed report is frozen and editing evidence to satisfy a later rule is the exact act the rule prevents. **Verified red** against a planted violating run, then removed.

⚠ **The ruling costs fux's own evidence something, and the cost is filed rather than absorbed.** Decision 14's resolution floor puts the blind arms' **`+1` and `-1` below detection**: the honest reading is **no detected effect of enrichment**, not `+1`. What survives is the **concordance** — both blind authors broke the *same two* queries, the informed author preserved exactly those two, **≈ 0.028**, ~**17x** the weight of the *"broke nothing"* sentence actually filed. ⚠ **And the floor is a PLACEHOLDER** — two author samples — labelled provisional everywhere, with veto 10 firing if anyone quotes it as measured.

✓ **W-81 filed (`agent`) — two of the accepted rule's six parts did NOT take effect.** Classification, reporting, the comparison bar and the floor are **protocol** and are live. The **sealed query subset** and the **decoy / content-free-placebo controls** are **apparatus**; ADR-RS decision 15 is marked `NOT BUILT` rather than written as in force. ⚠ **Sealing makes the power problem worse before better** — 50 queries is already under-powered and splitting it shrinks the half that carries the claim; W-81 names the three honest options and forbids picking silently. ⚠ **And "sealed" must be mechanical**: BIG-bench's canary was embedded *so that* it would be excluded and a model reproduced it anyway, so a directory an agent is asked not to read is not sealed.

→ **W-78 is still open, on ONE ruling, and it is yours.** [Reopen ADR-RERANK veto 1, or confirm it standing on a rewritten reason](compare/cross-encoder-reopen.compare.md) — **recommend CONFIRM**. ⚠ Leg 2 (determinism) is independent and holds whatever you rule, so reopening licenses an argument, not a build. ⚠ Leg 1 is **weakened by the Ettin correction** — §2's tables are MiniLM-L6, a weaker model than the record specifies. The cheapest thing that would settle it is **one offline Ettin-17M run on the playground's 50 goldens**, ignoring determinism, purely to learn the number.

→ **Queue is five.** W-78 (ruling 1 only) · **W-81** · W-77 · W-74 · W-75.

⚠ **Owed on Arpit's machine (carried forward, unchanged by this session):** `fux ingest --full && fux build` on fux; the same on **fux-playground**, whose committed index is still `fux.index.v1`. Its `[ranking]` -> `.fux/tune.toml` migration is applied there but uncommitted. **The `supersedes:` declaration is applied only in the cloud copy** — a fixture change, proposed, not committed.
