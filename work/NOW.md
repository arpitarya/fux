→ **W-86 FILED (2026-08-26): the decoder plane.** Arpit: *"I want individual decoders to be built out."* [Detail](open/W-86-the-decoder-plane.md) — seven phases, seven forks. Plus [`compare/index-lock.compare.md`](compare/index-lock.compare.md), written on his instruction to *"do the research and make a call."* **No code changed this session.**

✅ **FORK E RULED (2026-08-26): the consumer-owned decoder — and L1 DID NOT MOVE.** Arpit: *"let the consumer add the dependencies — unless the consumer adds the dependencies, that feature won't be available"*, missing ones **erroring loudly**. The session expected to propose an L1 amendment plus a `CLAUDE.md` + ADR-LAWS edit; **neither is needed.** ADR-ENRICH decision 1 already states the pattern as a table — network I/O → `.fux/fetchers/`, model calls → the consumer's agent — and **this is its third row: third-party parsing libraries → `.fux/decoders/<name>.py`.** ADR-FETCHER decision 1 grounds it: *"no dependency for any of [them] — a design choice rather than a dependency budget."* **If a later session proposes amending L1 for "optional dependencies", W-86 §12 is the fence it is crossing.**

⚠ **THE REAL OBJECTION WAS L3, NOT L1, and a future session will get this backwards.** A decoder that ran whenever its library happened to import makes **two developers with identical sources produce different root hashes** — the index becomes a function of the environment. **Declared, not detected** is what closes it, and the repo already had the mechanism three times: ADR-FETCHER is literally *"declared not detected"*, W-83's ceiling, W-85's required key.

⚠ **The honest cost: a consumer decoder can break L4 and NO GATE CAN STOP IT.** An import fence cannot reach code loaded by path. Same asymmetry ADR-ENRICH decision 3 owns about `model:`. **Do not claim a test covers it.** New **P7**; new sub-fork — bytes or path?

→ **P0 AND P1 ARE STARTABLE NOW.** They depend on no fork. P0 is the heading grammar; P1 is `src/fux/decode/` plus lifting HTML out of the two fetchers. **P6 is blocked** until Arpit rules the lock.

⚠ **THE DECODER PLANE ALREADY EXISTS, IN THE WRONG PLACE, TWICE.** `.fux/fetchers/http.py:69` — *"HTML -> Markdown - stdlib html.parser, deterministic"* — and `cdp.py:282` carries the same `_MdParser` marked *"Kept identical to…"*. **Nothing tests that they agree.** Neither is reachable from the git-dir walker, so **a local `.html` on disk is never decoded**. If a session starts by writing an HTML decoder, it has already gone wrong.

⚠ **A LIVE DEFECT, FREE TO FIX, AND IT IS P0.** `DEFAULT_TYPES` allows `.rst`, `.adoc`, `.org`; `extract.py` derives headings with `^(#{1,6})\s+` **alone**. reStructuredText underlines, AsciiDoc `== Section` and Org `* Heading` match none of it — **three of the six allowed types have had every heading land in the body field**, with an empty `phrases` list feeding the `§` lines W-84 shipped the same day. Fixing it **changes rankings**, so ADR-EXTRACTED is amended in the same change and the `tests_e2e/` goldens are re-blessed deliberately.

⚠ **THE LOCK EXISTS — THE GAP IS ITS SCOPE, and a future session will misread this.** `runner.py::acquire` is `O_CREAT|O_EXCL` with a pid inside, and its docstring already rejects `fcntl`/`msvcrt` correctly. **It has exactly one caller: the background runner.** A foreground `fux ingest` calls `request_stop` to *evict* a runner, then writes **holding nothing** — so two foreground ingests race. **Asserted from call-site reading, NOT reproduced**; P6 must falsify it before building on it. Also `except OSError: return False  # degrade, never block` is right for a runner and **inverted for a writer**.

→ **The lock call, proposed not decided: B — two files, neither new.** A mutex **must** be gitignored; a queue **must** be committed; no `.gitignore` can express half a file. **Neither may be named `fux.lock`** — that name is the archived engine's manifest and is out-of-scope in CLAUDE.md pending its own ADR. Arpit ruled the split himself: **committed queue, gitignored progress.**

→ **Two refusals worth keeping.** `.json` does **not** re-enter the allowlist by argument — ADR-TYPES verdict G was **measured**, and only a new pre-registration at 10 000 documents may replace it. And **full YAML is refused on correctness, not cost**: expanding anchors duplicates terms and inflates `tf`, so the *conformant* parser is the wrong one here.

→ **A race was observed and disproved inside this session.** `open/W-85…` briefly read as an open item with no OPEN-WORK row; re-derived against `archive/open/`, `archive/README.md`, `IMPLEMENTATION.md` and `git status`, **W-85 was filed, built, closed and correctly archived** by the concurrent session, which moved the file mid-write. `work/open/` holds W-86 and the README. **Re-deriving is the only reason a false defect did not land in three documents** — OPEN-WORK rule 3 applies to other sessions' work in flight, not just to your own.

⚠ **CARRIED FORWARD FROM W-84, STILL TRUE:** nothing from W-84 or W-83 is committed — a concurrent session was mid-rename (`store/recordshape.py` → `recordschema.py`). Commit explicit paths, never `-A`. And **`tests_e2e/` remains unverified** — the sandbox has Python 3.10 and a harness-only `tomllib` shim; the two `test_doctor.py` failures are that shim and predate every recent change. Someone must run it on a real 3.11+ install before release.
