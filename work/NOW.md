---
type: Pointer
description: "One line: the current state and the immediate next step. Overwritten every session."
---

✓ 2026-09-15 Claude Code: **the green queue worked continuously — W-184, W-183 and W-182 CLOSED, W-154 and W-140 unblocked, W-185 filed.** The session opened on an uncommitted pile (W-168 step 1 + law L11, built by the previous session and committed by neither) with `tests/test_sr_ownership.py` red on top; that landed first, and **`.fux/README.md`'s verb table turned out to have drifted three verbs from the template that generates it** — every test read the file, none read the template, and a fresh `fux setup` was handing out a short list (SR-DOTFUX 6b). **W-184:** the differential harness was dead in **three** ways — a PNG killed the query set, `bench_r3` decoded the same bytes with `errors="replace"` and **ran**, and behind the crash `compare()` still passed `archived_weight=`, removed by W-152 two days earlier. **22 144 byte-identical comparisons** now, through `run.py` itself. **W-183:** the contribution is a test a candidate can fail — `agreement` **0.4141** over 524 cited-decision contests against a chance rate of **0.0748**, inside a band frozen in its own commit beforehand. **W-182:** the stranding did **not** reproduce (0 of 104, with 67 trials inside a live runner), and a **different** race did — `git add -A` dies on `.fux/index/<shard>.jsonl.tmp`, 2 of 8 at the early-delay step.

→ **Next:** 🟢 **W-154**, the last one. **W-179 measured Node's latency for the first time and the SPLIT is the result:** the Node reader is **flat** in corpus size (26.3 → 26.4 ms across a 10× corpus, a ninth of Python's at 1 000 documents) and **all of its growth is W-161's in-memory graph rebuild** — 36.9 ms at 100, 243.1 ms at 1 000, ~2.4 s at 10 000. ⚠ Read without the fourth arm the column says the opposite, and that wrong answer would have been believed. 🔴 **The load-bearing number is W-161's**: its tiers ship **on and unmeasured**, and one is now priced on one reader with its value still unmeasured on either. 🔴 **W-144 is in the inbox**: the frozen `b` sweep FAILED — no pre-registered value clears — **and the lever works below the range** (`content` at `b ≤ 0.3`, `main` at `b ≤ 0.15`), with nothing regressing down to `b = 0`, including the family option (b) destroyed. Three options, none taken here: picking `0.15` because a probe found it is the moving-threshold failure the rule exists to prevent. 🔴 **W-186 was the find of the session: every one of the eight golden rungs was unreadable by HEAD** — a `fux.index.v2` index, the three `[ranking]` priors W-152 removed, and the `fux.toml` key W-164 moved, each a good decision whose blast radius nobody swept. All eight are re-ingested and answering, no document moved, and the stamp gained **`engine_commit:`** because `fux 2.0.1` named two engines that cannot read each other's index. ⚠ **Nothing detects the next one** — named, deliberately not half-built. **W-181 closed W-170 with it:** the observer seam costs `+0.7 ms` p50 on this repo and `−0.3 ms` on rung-10000, and a **2 000 ms** observer adds **+63 ms** against a 50 ms cap. **W-185 closed the day it was filed** — `index/*.jsonl.tmp`, settled by a two-arm probe (**0 `git add -A` failures in 3 871 runs against 2 335 of 3 933**) rather than by argument, because 🔴 **both candidates the capture pass proposed were wrong**. ⚠ `.fux/.gitignore` is write-if-missing, so the fix reaches no existing repo and a `fux doctor` warn row is all that does — **write-if-missing hid a fix twice today**. ⚠ **Two things stated rather than closed:** *0 of 104 stranded is unreproduced, never closed* — the surviving ordering needs a delay injected inside `run_once`, which a capture pass may not add; and **W-183's screen cannot see circularity through a third variable**, so its two mitigations are mitigations, not proofs. 🔴 **W-140 row 21's stranding is *unreproduced*, never *closed*** — the surviving ordering needs a delay inside `run_once`, which a capture pass may not add, so what is left of W-140 is a call rather than work.
