---
type: Pointer
description: "One line: the current state and the immediate next step. Overwritten every session."
---

✓ **W-149 CLOSED — L10 is satisfied: a consumer gets ONE generated `fux.mjs`, built at publish, in both registries.** The bundler ([`nodebundle.py`](../src/fux/store/nodebundle.py), stdlib, deterministic, **refuses** on any form it does not understand), the **prune** (47 stale `.mjs` files deleted, `node_modules/` spared), the monorepo **auto-detect + format-preserving wire**, the three-rung `.fux/fux`, `publish.yml`'s one-build-two-registries, the wheel's `hatch_build.py` hook, and the arm's **sixth surface** (the bundle vs the module tree, **0 discordant**) all landed together. Decisions 13-16 are BUILT; the RULED/NOT-BUILT markers are flipped. Verified end to end from an installed wheel: 4 files, no `src/`, shim runs, bundle byte-identical to a local build — and **this repo's own `.fux/node/` is migrated**, so L10's veto command now returns `.fux/node/fux.mjs` and nothing else. 🔴 **Running that migration is what found the last defect**: the prune was gated on the VERSION, so a tree written by the same version before the shape changed was never repaired — fux itself was that case. Trigger is now version **or shape or layout**.
🔴 **Two record defects found by BUILDING, not by reading.** Decision 15's detection table put *"`workspaces` array → C"* above *"Berry → A"* and therefore gave every Yarn Berry repo shape C — **an accepted record contradicting its own warning inside one file, the W-83 class, which no check here can see.** Corrected in the same change. And the Berry fallback was broader than the evidence: [probe 2](regression/2026-09-12-yarn-berry-probe/report.md) shows `.fux/node` links in Berry too and the boundary is the **linker** — `node-modules` hoists the bin to the root (shape C works), PnP has none (shape A, and PnP is Berry's default).
⚠ Both suites whole after the change: **3 994 unit + 85 e2e pass**, `node --test node/test/*.test.mjs` green (⚠ `node --test node/test` without the glob is `MODULE_NOT_FOUND`, not a failure). `test_working_tree_is_not_mid_violation` green — seven records amended in the same change (NODE-SEARCH · LAW-10 · DOTFUX · DOCTOR · T1-ACCELERATOR · ACQUIRED · PII).
🔴 **Nothing agent-side is unblocked by this.** Nine 🔴 rows, all filed 2026-09-12: W-143 (four priors answered NO) and the `weak`/`answerable` ruling unblock the most. Next: `work/OPEN-WORK.md` — the 🟢 lane is W-140's one flake row and W-87's phase-5 half, both waiting on someone else.
⚠ Arpit, 2026-09-12: the built corpora in fux-lab and fux-benchmark are KEPT and reused — never wiped.
