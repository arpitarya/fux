✓ **W-79 closed — the dead fusion code is deleted.** `query/hybrid.py` gone, `[fuse]` out of ADR-TUNE's schema, `explain --no-tune` removed, `playground_grade.py` repointed at `run_query`. ADR-TUNE, ADR-CLI, ADR-ASK, ADR-T1-ACCELERATOR amended in the same change. Full suite green (1355 passed).

✓ **Both of W-78's rulings now have a compare doc, a literature review and a recommendation** — nothing ruled. **[Reopen or confirm](compare/cross-encoder-reopen.compare.md): recommend CONFIRM, on a REWRITTEN reason.** The canonical *BM25+CE = +4.2 nDCG* is a **2021** number nobody has re-run for a small cross-encoder; gains concentrate where the first stage is **weak** (MS MARCO .220→.388) and nearly vanish where it is strong (**SciFact .658→.676**) — fux is a SciFact; **~40 % of queries gain nothing**; rerankers fall **below retriever-only in ~half** of measured configurations. The metadata alternative has **+32 points** on versioned technical documentation. ⚠ The honest counter: most retrievers score **below random** on negation and **cross-encoders are the only sub-LLM class that clears it**. **[The blind-authorship rule](compare/blind-authorship-rule.compare.md): recommend ACCEPT, REWRITTEN** — **TREC has had it since 1994** and *reclassify, don't ban* beats the drafted *"is not evidence"*.

⚠ **A fifth correction, caught by Arpit within the hour: the compare doc argued against the wrong model.** *"22M MiniLM"* was my gloss — the record says **17–32M**, which is **Ettin**'s reranker line (17.6M / 32.8M), chosen *because* it beats MiniLM: **+0.051 nDCG@10 over `ms-marco-MiniLM-L12-v2` at half the parameters**, ~1.9x MiniLM-L6's CPU throughput. So §2's tables are evidence about a **weaker** model than the one specified. **The distributional and determinism legs survive; the value leg is restated at the correct strength.** And the latency is worse than it looked: 20 candidates at 267 pairs/s is **~75 ms** against a 33.5 ms current p95 and a 150 ms bar — **about half the remaining headroom.**

⚠ **Three corrections to fux's OWN filed evidence, recorded in W-78.** The *"zero broken"* argument is **p ≈ 0.49** as a marginal comparison — the force was always the **concordance** (both blind authors broke the same two; ≈ 0.028). **Fifty queries is under-powered**, so `+1` and `-1` are noise. The enrichment runs have **no source-bias control**. ⚠ **And to yesterday's determinism finding:** the cause is **per-ISA kernel selection, FMA contraction and libm** — *not* that IEEE-754 differs; Arm's own docs say basic ops are identical. onnxruntime's own suggested tolerance is **~1e-5**, looser than our measured 1.9e-6.

→ **Queue is four.** W-78 (both rulings now have evidence they lacked) · W-77 · W-74 · W-75.

⚠ **Owed on Arpit's machine (carried forward, unrelated to W-79):** `fux ingest --full && fux build` on fux; the same on **fux-playground**, whose committed index is still `fux.index.v1`. Its `[ranking]` -> `.fux/tune.toml` migration is applied there but uncommitted. **The `supersedes:` declaration is applied only in the cloud copy** — it is a fixture change and is proposed, not committed.
