✓ **ADR-TUNE built** and released as `v2.0.0-alpha.1` — `.fux/tune.toml`, per-source priority, `--no-tune`, `fux tune`; `[ranking]`/`[dense]` retired from `fux.toml` with an error naming the new home. 1 352 unit + 73 e2e green. · ✓ W-77's audit committed earlier the same day (register renumbered `0001`-`0041`, 71 broken links repointed, `tests/test_doc_links.py` added as the gate) · ✓ W-73 and W-76 closed and retired to `archive/open/`.

**Two defects this build surfaced, both fixed here:** the runtime stats plane stored a **pre-weighted** `total_wlen`, which would have made a field weight move `avg_wlen` on the scan path and not the accelerator path (`RUNTIME_SCHEMA` -> `fux.runtime.v4`); and `fux doctor` warned about files fux itself writes. **The finding to carry:** BM25 saturates, so an unweighted pruning bound is nearly indistinguishable at large `tf` — the obvious differential sweep passes while proving nothing, and the shipped fixture is verified by mutation.

→ **Next: the blind enrichment re-grade** in fux-playground — 2026-08-24's `38/50` and `41/50` were written by an author who had seen the failing queries.

Queue is three, all `arpit`: **W-77** (four rulings + the governance gap + the new archived-link fork), **W-74**, **W-75**. ⚠ **Owed on Arpit's machine: `fux ingest --full` then `fux build`** — three docs moved to `archive/open/` and the runtime schema moved to v4. Nothing is broken meanwhile; a stale plane degrades to the scan path.
