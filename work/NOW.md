✓ **The blind enrichment re-grade ran, and it inverted the comparison it was checking.** Enrichment written by an author who had **not** seen the goldens is worth **+1** (`32 -> 33`); the enrichment as committed, whose author had, is worth **+9** (`32 -> 41`). Both previously-recorded numbers reproduced exactly. **The diagnostic is not the score — the contaminated arm breaks ZERO of fifty rankings** while adding vocabulary to nine of ten documents; the blind arm fixes 3 and **breaks 2**. An error profile that clean has been fitted to the evaluation. Filed: [`regression/2026-08-24-blind-enrichment-regrade/`](regression/2026-08-24-blind-enrichment-regrade/report.md), **a report and not a verdict** — nothing was pre-registered.

Earlier the same day: ✓ **ADR-TUNE built** (`v2.0.0-alpha.1`) · ✓ W-77's audit committed · ✓ W-73 and W-76 closed.

→ **Queue is four, and every one is `arpit`.** New: **W-78** — ADR-RERANK's veto 1 deferred the cross-encoder on *"enrichment is worth 10, reranking 4"*, which blind reads **+1 against +4**. The record is **amended and deliberately not reopened**; reopening a ruling made on a comparison is the ruler's call. Second ruling owed: a **blind-authorship rule** for ADR-RS — fux forbids `--update-goldens` for exactly this failure in the other direction. Then W-77, W-74, W-75.

⚠ **Owed on Arpit's machine:** `fux ingest --full` then `fux build` — three docs moved to `archive/open/` and `RUNTIME_SCHEMA` moved to `fux.runtime.v4`. Nothing is broken meanwhile; a stale plane degrades to the scan path. ⚠ **The playground's `fux.toml` needs the same `[ranking]` -> `.fux/tune.toml` migration** the engine repo just made; it was done in the cloud copy for the re-grade and is **not** committed there.
