---
type: Pointer
description: "One line: the current state and the immediate next step. Overwritten every session."
---

✓ W-174 shipped + the `fux.toml` audit: `[sources] urls_file`, live keys, per-fetcher `[sources.url.config.<name>]` derived from the fetchers, `.env` overrides for `cdp.py` · → commit with explicit pathspecs, then W-169. ⚠ one red row is a concurrent session's (`test_doc_links`, cage proposal)
