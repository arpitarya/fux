✓ **The blind enrichment confound is closed, and it closed against enrichment.** A second blind author scored **31/50** (net `-1`) against the first's 33 (`+1`) and the contaminated 41 (`+9`). **Two blind authors, mean zero.** The decisive evidence is not the score: **both broke the same two queries** and the contaminated author broke neither — identical casualties from two independent agents is a property of the task, not of craft.

**The mechanism is one word.** `q015` asks *"what is the **current** decision for east west traffic"* and wants the current ADR. Both blind authors truthfully wrote *"no-longer-current"* / *"replaced by the current decision"* on the **superseded** ADR; the contaminated author wrote *"retired and replaced"* and never used the token. **BM25F cannot see negation** — honest metadata about a retired document ranks it as a live one. The blind authors wrote the better documentation and were punished for a token collision. ⚠ Fux's own answer, `[ranking] superseded_weight`, is **inert on the fixture**: `superseded_ids` reads a frontmatter `supersedes:` key and the playground declares supersession in prose only, so the prior has never been graded by anything.

Earlier the same day: ✓ **ADR-TUNE built** (`v2.0.0-alpha.1`) · ✓ W-77's audit committed · ✓ W-73 and W-76 closed · ✓ the first blind re-grade.

→ **Queue is four, and every one is `arpit`.** **W-78** now carries `n = 2` and a demonstrated mechanism rather than one sample and a caveat: does ADR-RERANK's veto 1 reopen, and does ADR-RS gain a blind-authorship rule. Then W-77, W-74, W-75.

⚠ **Owed on your machine:** `fux ingest --full` then `fux build` — three docs moved to `archive/open/` and `RUNTIME_SCHEMA` moved to `fux.runtime.v4`. Nothing is broken meanwhile; a stale plane degrades to the scan path. ⚠ **`fux-playground` needs the `[ranking]` -> `.fux/tune.toml` migration** and a `fux ingest --full` (its committed index is still `fux.index.v1`); both were done in the cloud copy for the re-grades and are **not** committed there.
