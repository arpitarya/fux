✓ **W-78 IS CLOSED. Both rulings made, and neither built anything.** **Ruling 1, on your delegation:** ADR-RERANK veto 1 — **condition 1 VACATED, not rewritten.** The compare doc's own §6 recommendation was **not** taken: its lead leg argued value from other people's corpora about a **weaker model than the record specifies**, and substituting a second unmeasured claim for the first is W-78's own error. **A condition with no evidence behind it should be absent, not restated.** ⚠ **Consequence, stated: this record now holds NO position on the cross-encoder's value.** Correct — there is none. **Condition 2 RESTATED** as *score-level drift below the target corpus's adjacent-gap floor*, replacing a `5e-10` bar that measurement showed demanded **~200 000x** more precision than the corpus can resolve. **The cross-encoder is still refused and still unbuilt** — the refusal now rests on **one measurable unmet condition** instead of a dead argument and a mis-specified bar. Reopening is **two named experiments**, not an argument.

✓ **`superseded_weight` was measured for the first time since it shipped — and FAILED.** [P-SUPERSEDE](regression/2026-08-25-supersession-and-reranker-default/VERDICT.md), pre-registered in its **own commit before the run**. The prior needs a frontmatter `supersedes:` key and the playground declared supersession in prose only, so **the flag had never set and the knob was untested code.** Declared it. **The control is clean** — the edit alone fixes 0, breaks 0 — so every movement is the demotion.

⚠ **It fires. It fixes `q015` — the canonical failure a cross-encoder was wanted for — and breaks four queries whose correct answer IS the superseded document:** *"can I start new work against helix mesh"* · *"why keep a superseded record"* · *"why did we adopt a service mesh in the first place"* · *"how do we stop a slow dependency taking down checkout"*.

→ **The finding, and it is the transferable one: SUPERSESSION IS A PROPERTY OF THE QUERY'S INTENT, NOT OF THE DOCUMENT.** A superseded document is fully relevant to *what did we used to do*, *why*, and *may I still use it* — and wrong for exactly one intent, *what is true now*. **`q015` contains the word "current". The four it breaks do not. The signal was in the query the whole time.** A per-document multiplier cannot express that, because the difference is not about the document. **`superseded_weight` keeps its neutral default.**

✓ **The reranker reproduces `28 -> 32`, `+4`, zero broken — exactly the filed number. The default still does not flip, and that call goes AGAINST the number.** `rerank.py`'s constants were chosen from *"the 4x5 sweep over the 50 goldens"* — **retriever settings authored with the evaluation in hand**, which is ADR-RS decision 11's own example of an `informed` artifact, and decision 12 says an informed run never supplies a delta. ⚠ **The rule bites the session that helped write it, and it is right to.** This also **corrects a claim in ADR-RERANK's own W-78 amendment** — that the `+4` was clean because *"the author of the arithmetic could not target a query"*. True of the algorithm, **false of its constants.** ⚠ Honest counter: those constants were taken **from the middle of a plateau, not a peak**, precisely to survive an untuned corpus. That is why this is a **hold, not a rejection**.

⚠ **A defect in my own pre-registration, recorded.** P-RERANK-DEFAULT was **not a prediction** — its own frozen rule said a pass *"yields a recommendation, not a changed default"*, so no outcome could change anything. **A threshold that cannot fire is a measurement wearing a gate's clothes.** One `VERDICT.md` filed, for P-SUPERSEDE. **The rule worth keeping: if you cannot name what a PASS would change, it is not a prediction.**

⚠ **And a correction to something I told you three times.** I claimed fux-playground's `tune.toml` still had `[dense]` and that **every command there errors**. **It does not contain `[dense]`** — I inferred it from fux's own config and never checked. What is actually owed is real and different: **its index is `fux.index.v1` with a two-field analyzer**, which current fux refuses outright. `fux ingest --full && fux build` before it answers anything.

→ **From a concurrent session, carried forward so it is not lost:** a read-only design review of the end-to-end flow against the CLI found two gaps — `fux update` warms index statistics but **not** the refer plane, so the first question still pays a full render per cited URL; and there is **no answer memo**, so *"nothing changed → the same answer"* is not a behaviour fux has. Proposed `update --warm` and `answer --memo` as **flags, not verbs**. Nothing built; both land inside **W-75**.

→ **Queue is FOUR.** W-81 (`agent`) · W-77 · W-74 · W-75.

→ **Everything now points at one thing.** The reranker default, the cross-encoder's value, and any generalisation from ten documents are all blocked on **a second corpus with goldens nobody has tuned on.** It does not exist. **That is the bottleneck, and W-74 — which defines what "right" even means — has been open the whole time.**
