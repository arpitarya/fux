---
type: Pointer
description: "One line: the current state and the immediate next step. Overwritten every session."
---

✓ 2026-09-14 Claude Code: **`2.0.1` RELEASED.** PyPI has `fux-engine` 2.0.1 (wheel + sdist, OIDC); **npm is STAGED with tag `latest` and waits for Arpit to approve it at npmjs.com**. Two shipped bugs, one in each consumer extension point: `[sources.url.config]` went **verbatim to every fetcher**, so a repo could configure at most ONE of the two shipped fetchers ([SR-FETCHER](../records/0117_fetcher.md) decision 8); and an `.xlsx` row budget counted **XML elements, not records** — a phantom blank row spent budget and the loss left no trace ([SR-TABULAR](../records/0150_tabular.md) decision 7). 🔴 **The fixes arrived with six red tests and no coverage, and writing the tests found a THIRD bug** — a sheet that exactly filled the budget claimed a truncation that never happened. CI 8/8 and node-arm green on `444e3bb6` before the tag. ⚠ **Check after approving npm:** the `bin[fux]` warning fired again — **second release running**, so it is a defect now, not a one-off. ⚠ **`main` and `release/3.0.0-alpha.0` carry the SAME fetcher fix in TWO shapes** (`urlsrc.config_for()` vs `3709d40f`'s `UrlSource.config_for`); **that merge will conflict and the call is Arpit's.** 🔴 `work/BLOCKED.json` is still open and was worked over on his instruction. Five 🔴 on Arpit: W-156, W-146, W-112, W-144, W-148.

✓ 2026-09-13 Cowork: **every benchmark HTML report now lives at ONE dated path** — `work/benchmark/reports/<yyyy-mm-dd>-<run>.html`, the run directory's own name with `.html` on the end. **[SR-WORK-BENCHMARK](../records/0053_WORK-benchmark.md) decisions 5 and 7 amended, 8–10 added** (Arpit): CAP-7 leaves the run directory, is built from `reports/TEMPLATE.html`, **a capture with no number keeps its section and says so**, and **every number carries its direction of goodness** (↑ higher is better / ↓ lower is better / — neither). All three decks **rebuilt into the template from filed rows — no run re-executed**; the two 2026-08-28 runs now state the four/three captures they never took instead of hiding them. Originals in `archive/benchmark-reports/`. ⚠ **I edited `tests/test_benchmark_capture.py`** (Cowork touching `tests/`) — it asserted CAP-7 at the old path and would have gone red; 14 tests pass, incl. a new one that fails the pre-amendment layout. `sr-hash` and `sr-owns` both clean, stamped by hand not `--write`. 🔴 **NOTHING COMMITTED.** → **W-158 🟢**: the harness still emits the old path and shape, and `2026-09-12-benchmark-l9` has no report.

✓ 2026-09-13 Claude Code: **`2.0.0` RELEASED.** PyPI has `fux-engine` 2.0.0 (sdist + wheel, OIDC); **npm is STAGED and waits for Arpit to approve it at npmjs.com** (id `b5303aa3-f75b-479a-a696-95ee742b1d64`). CI 8/8 and node-arm 7/7 green on the release commit — Windows green for the first time, which is what found four SHIPPED bugs: fux's stdout was cp1252, Node's `fux_passage` refused every path there, the engine read git with the platform code page, and a background re-index could strand work. ⚠ **Check after approving npm:** the stage warned it removed `bin[fux]`; a tarball built here keeps it. → W-159 open (doctor's shadowing row cannot fire on Windows). Five 🔴 still on Arpit: W-156, W-146, W-112, W-144, W-148.
