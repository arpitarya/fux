✓ **The flip rate is MEASURED, and the determinism veto quoted the wrong quantity.** ADR-RERANK veto 1 condition 2 cites **element** drift (`1.907e-06`, one tensor element after one encoder block) against `round(score, 9)`. **A ranking only moves when drift beats the gap to the NEXT DOCUMENT** — median **0.27** here, five orders of magnitude above it. **At `1.907e-06`: 0.00 % order flips, 0.00 % membership flips, 0.00 % at-risk**, in both arms, 297 queries x 50 trials — no adjacent top-5 pair is even within 2x it. Knee at **~1e-4 (52x)**; real damage only at **1e-2 (~5 200x)**. **The `5e-10` bar asks for ~200 000x more precision than this corpus can resolve.** [The run](regression/2026-08-25-rank-flip-susceptibility/report.md).

⚠ **Condition 2 is NOT reopened, and no build is licensed.** Three reasons, all recorded: the **score-level** drift of a real reranker has **never been measured**; a cross-encoder reranks ~20 already-similar documents so its score gaps are probably **tighter** than BM25F's, which makes this a **lower bound not an estimate**; and it is one corpus at 495 documents, three orders below the design point. **What changed is that condition 2 is now falsifiable** rather than rounding-derived — and the prerequisite experiment is named: run any small ONNX reranker on two architectures and diff the **final scores**.

⚠ **The first run of the harness was wrong, and that is what found the real result.** Including exact ties gave a **flat 6.42 % floor across seven decades** — too flat to be real. A top-5 containing two identical scores flips under **any** nonzero nudge, including `1e-12`, because `docidx` breaks the tie. Separating them gave the clean curve **and exposed this: 4.38 % of queries contain an EXACT top-5 tie, so fux's own tie-breaking is a bigger source of arbitrary ordering than the cross-ISA drift would be — 4.38 % against a measured 0 %.** Deterministic, so it breaks no law; **arbitrary, which is what matters when the objective is the right answer rather than the same answer.** Fork filed unruled: leave it · break ties on a declared signal · surface the tie to the caller.

✓ **A full review and cleanup — code, areas, open work — and the headline is that the queue did NOT shrink.** All five open items are live and **four of the five are `arpit`-lane rulings**. Nothing in `work/open/` was archived, and that is the correct outcome, not an omission: an item awaiting your ruling is not an item an agent may retire.

⚠ **The review found a bug I had shipped four hours earlier.** `tools/differential/playground_grade.py` called `run_query(..., use_hybrid=True)` — a keyword the function no longer accepts — so **the differential harness raised `TypeError` before grading anything**. Found by a sweep, not by a test: **no test imports that harness**, which is how a live tool sat broken. It now grades `scan` vs `accelerator`, which is exactly the pair the differential law binds. Also fixed: two stale docstrings citing deleted modules, and `tests/data/embed_reference.json`, whose only readers live under `archive/v0.26/` — **a fourth orphan, left behind by the very change that filed the orphan-check item.**

✓ **Archived — 14 documents, each with a stated reason.** Four compare docs to [`archive/compare/`](../archive/compare/README.md): a decided fork **stays** in `work/compare/` because the doc carries its **reopen-trigger**, so the bar for archiving is that the trigger can no longer fire. `wire-format` and `keyspace-unification` both hang off **T2, which was declined on measurement**; `pruning-criterion` is **forbidden** by ADR-POSTINGS decision 8; `r7-size-budget` forks over the shape of a budget **that was retired with no successor**. Plus `query-log-pruning` and **the whole nine-file `ideal/` design review** — every one of its build-order steps is resolved: five built, two **refused by you** (ruling 1, *"a fresh clone not having an index… is a big no"*), one **decided-not-built on a measurement** (delta hooks, the R5 re-run), one moot.

⚠ **A governance defect of mine, found and fixed.** I closed **W-80 without recording its outcome in `IMPLEMENTATION.md`** — and both `work/open/README.md` and `OPEN-WORK.md` make closing legal **only once that row exists**. The row is now there, along with the model-removal row that was also missing.

⚠ **`governance.md` — the file whose entire job is to be the index — was the stalest file in the repo.** `open/` said 7 (is **5**), `compare/` 13 (**18**), `proposals/` 8 (**13**), `regression/` 13 (**29**), the ADR register 33 (**41**). Every count recounted, verified against the tree, and now dated.

⚠ **`work/compare/README.md` was wrong in both directions**: three rows still read *"awaiting Arpit"* for forks you ruled on 08-19 and 08-20, and **four docs had no row at all**. Both fixed; 18 rows for 18 docs.

⚠ **Two live docs are standing on premises that are dead.** [`record-freshness`](compare/record-freshness.compare.md) was decided *"no age bound"* because *"no committed field is temporal"* — **every record now carries `mtime`**; that is W-77 ruling 1 and it is **yours**. And [`meta-privacy`](compare/meta-privacy.compare.md) still records *"ruled: keep `code`"* with a justification naming three deleted things — the only place in `work/compare/` the model deletion left a false claim.

⚠ **The paper got a banner, not a rewrite.** Three of its committed-index components were never built or are deleted — **pruned** term statistics, **dense binary codes**, the **six-prefix MST keyspace**. §5's size model and §6's latency model rest on them. **I did not re-derive the models**, because that would mean inventing numbers nobody measured. §4 stays untouched under your standing `BLOCKED.json` ruling.

→ **Two new governance items filed into W-77, both yours.** (1) The ADR status vocabulary — `accepted | proposed | superseded` — **has no value for a record whose SUBJECT ceased to exist**; ADR-CODES-TABLE sits at `proposed` because that is the only literally-true option, and deleting it instead is what put **two records on number `0022`** last time. (2) A frozen report's pre-registration link points at a path that never held it, and **neither gate catches it** — decision 16 covers *deleted* pointers, not *wrong* ones.

→ **Queue is five, unchanged.** W-78 (**ruling 1 only** — recommend CONFIRM) · W-81 (`agent`) · W-77 (**now six items**) · W-74 · W-75.

⚠ **Owed on your machine, and now filed where it survives** (`work/setup/fux-playground.md`, not just here): **fux-playground's `.fux/tune.toml` still has `[dense]` and every `fux` command there now ERRORS.** Delete the table — ranking does not move. Its index is still `fux.index.v1` and wants `fux ingest --full && fux build`. This repo's own index also wants a rebuild to drop the `vectors` field.
