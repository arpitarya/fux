✓ v0.35.0 released and live on PyPI — the source verbs (`fux add`/`remove`/`update`), the progress plane, scan-by-default; verified black-box from the published wheel. 947 unit / 64 e2e green on nine CI arms · a Windows console crash caught by CI on the release push became a permanent check (two-strikes rule) · next: W-61's two calls, blocked on Arpit, 1 day old
