<!-- One line. Overwritten, never appended. The agent writes this; the
     UserPromptSubmit hook reads it back so the next session knows what was in
     flight when the last one stopped. -->
idle — nothing in flight
