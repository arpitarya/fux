---
type: Pointer
description: "One line: the current state and the immediate next step. Overwritten every session."
---

✅ 2026-09-20 Claude Code: **[W-206](../archive/open/W-206-compare-and-proposals-sweep.md) executed and closed — compare 24 → 8, proposals 17 → 10.** Sixteen compare docs and six proposals archived with a row each; **nine of the sixteen merged into their owning record** under a **second permitted form** of the archive sentence (*"the trigger lives in `<record>` `<decision>`"*), which Arpit's default added because those nine could not satisfy the first form **precisely because their triggers were checkable**. 🔴 **Two triggers existed only in a doc about to archive and were ported first** — [SR-LOCKS](../records/0140_locks.md) veto 4 (NFS/SMB) and [SR-MAINTENANCE](../records/0129_hooks.md) veto 10 (the half-written shard); **port, then move, in that order.** ⚠ **Three things the table did not predict:** a frozen verdict's pointer broke for the **second time in two sessions** (and `test_doc_links` structurally cannot see that class — `work/regression/**` is exempt, so only the whole suite does); `src/fux/decode/csv.py` is a **versioned decoder**, so a docstring repoint there would invalidate carry-forward for every `.csv` document in every repo; and the DOC-REGISTRY row the item asked for is **forbidden by that file's own rule 1**, recorded beside the rule rather than worked around. → **Next: W-205 part 1 — STILL NOT STARTED**, the named next step for two sessions now. Its records are written (SR-DECODE 20, SR-TYPES 13, SR-INGEST 23); it owes a pre-registration, the build on both readers, and a measured run on rung-00100 and rung-10000 with a VERDICT. Also open: **W-199 DoD 10** (the `decoder=` half of the pipe ruling). **For Arpit: GitHub About/topics is your `gh`** — the one line left from `positioning-documents-not-code`, which archived without it. Inbox: **empty**.
