✓ **The embedding model and the entire dense lane are DELETED** (Arpit, 2026-08-25: *"Remove the model."*). `src/fux/embed/` — `model.py`, `fuxvec.py`, `chunkvec.py` and the 7.9 MB `model.bin` — plus `query/dense.py`, `derive/dense.py`, `.fux/runtime/codes.jsonl`, the committed per-chunk `vectors` field, the `[dense]` tune table and **`ask --hybrid`**. **13 ADRs amended in the same change.**

✓ **What it cost, measured A/B on the same corpus** ([run](regression/2026-08-25-model-removal/report.md)) — **the wheel went 6.84 MB -> 233 KB, 30.1x, because the download was 97 % model.** Committed index **-22.6 %** (4 290 chunk vectors -> 0; corroborated by a direct count of the `vectors` key at **23.0 %**). Full ingest **6.8x faster**, 33-36 s -> ~4.9 s. **The differential law holds** — six queries, `--scan` == `--fast`, byte-identical. **Ranking does not move**, and that is an argument from the code (`mode` defaulted to `off`; `should_fuse` returned before any dense work), **not a measurement** — 13 ADRs changed in the same commit, so a cross-tree ranking diff was never possible.

⚠ **The lane's failure was structural and is the transferable part.** The bundled model **mean-pools static token vectors** — no layers, no attention — so *"A supersedes B"* and *"B supersedes A"* embed identically. **It was as order-blind as BM25F**: it duplicated the lexical lane's blind spot at 23 % of the committed plane and 85 % of every ingest. *An added lane earns its cost only where it fails DIFFERENTLY from the lane it is added to*, and nothing in the original review asked what the new lane's failures would look like.

⚠ **Three findings nobody was looking for.** (1) **`fuxvec.py` had been dead since 2026-08-23 and its tests passed the whole time** — a tested module looks alive; three orphans have now been deleted by hand in two days and **nothing would catch the fourth**. (2) **`git rm` on the files did not delete the package** — an empty directory is an importable namespace package, so `import fux.embed` still succeeded. (3) **The first differential check passed VACUOUSLY**: `[dense]` was still in `tune.toml`, every call errored, and two empty strings compared equal. The harness now asserts a non-empty result count first. ⚠ **That is the second vacuous pass caught in two days.**

✓ **W-80 closed by DISSOLUTION, not fixed** — no bundle, so no missing provenance. **Neither fork it offered was taken.** ⚠ **And one of its claims was wrong**: it cited *"ADR 0006's ≤10 MB bundle budget"*, and **no such budget exists in any live record.** The 7.9 MB bundle was never governed by a written size rule. Two proposals retired with it: [the retrieval-tuned swap](../archive/proposals/retrieval-tuned-static-embedding.md) and the agent hybrid policy.

⚠ **W-81 grew a scope defect in the rule ruled yesterday.** ADR-RS decision 12 says an informed run **never supplies a delta** — and the first run filed under the rule is informed by construction and made entirely of deltas. **As written it forbids reporting a file size.** Contamination needs an evaluation set to exist; bytes on disk have none. ⚠ Latency sits awkwardly between: the metric cannot be fitted, but the query *sample* can. **Narrowing it is yours** — the run applied the rule as written and disclosed the conflict rather than exempting itself.

⚠ **A gap in the ADR status vocabulary, also yours.** `accepted | proposed | superseded` has **no value for a record whose SUBJECT ceased to exist**. ADR-CODES-TABLE stays `proposed` — literally true, never accepted — rather than inventing a fourth status or repeating the ADR-T2-SEGMENTS deletion that put two records on number `0022`.

→ **Queue is five.** W-78 (**ruling 1 only** — reopen ADR-RERANK veto 1 or confirm it; recommend CONFIRM) · W-81 · W-77 · W-74 · W-75.

⚠ **Owed on your machine.** **fux-playground's `.fux/tune.toml` still has `[dense]` and will now ERROR** — delete the table there. Its committed index is still `fux.index.v1` and wants `fux ingest --full && fux build`. ⚠ **This repo's own `tune.toml` also still carried `[fuse]`**, orphaned by the concurrent session's W-79 change and never removed; fixed here in passing.
