---
type: Pointer
description: "One line: the current state and the immediate next step. Overwritten every session."
---

✓ 2026-09-17 Claude Code: **`3.0.0-alpha.1` is released — and it carries nothing.** Tag `v3.0.0-alpha.1`, commit `e25338a5`, [publish run 35254145949](https://github.com/arpitarya/fux/actions/runs/35254145949) green; **PyPI `3.0.0a1`** and **npm `3.0.0-alpha.1` under `alpha`**, both smoke-tested from the registry. ⚠ **`git diff v3.0.0-alpha.0..v3.0.0-alpha.1 -- src/ node/ pyproject.toml hatch_build.py` is EMPTY** — only the four version sites and the CHANGELOG moved, so **no consumer on `3.0.0-alpha.0` has a reason to upgrade**. Cut on Arpit's instruction with the emptiness stated first and reaffirmed; he declined both alternatives (wait for **W-194**'s index-v4 break, or fold in the `node/package.json` `bin` fix). **The number is spent either way** — PyPI never permits a version to be reused. 🔴 **Worth knowing before the next bump:** moving the four version sites turns the tree RED — `SR-LAWS` owns `src/fux/__init__.py` and `SR-NODE-SEARCH` owns `node/`, so `test_sr_owns_hash.py` and `test_sr_freshness.py` fail until `scripts/sr-owns.py --write && scripts/sr-hash.py --write` runs. **Run the stamper dry first**: it writes *every* stale component, and a concurrent session's work was in this tree. ⚠ **That session is live** — W-194 (ratified, `meta=hashed` deleted, `fux.index` → v4, **L5 retires**), prompt 6E and W-175 were being filed throughout; **this session touched none of it** and committed with explicit pathspecs. → **Next: W-194 is the queue's one green row**, and it is buildable now.
